# Book of Deeds: packet progress

Update this table at the end of every session. Cadence: implement file, then its
QA file, then the next implement file. Never skip a QA row. Every session's final
response names the next file by full path.

| # | File | Status | Completed | Notes |
|---|---|---|---|---|
| 0 | overview.md + catalog/ | DRAFTED | 2026-07-08 | Awaiting maintainer packet review |
| 1 | phase-01-sim-core.md | DONE | 2026-07-08 | 186 deeds + evaluator + persisted state + retro-on-join; all 7 Fiesta deeds shipped (bot-roster gate); goldens deliberately regenerated (draw digests unchanged); deferred list in the commit body |
| 1Q | phase-01-qa.md | DONE | 2026-07-08 | Acceptance + full gate green. Catalog fidelity clean: 197 catalog blocks, 186 transcribed (11 deferred stay in catalog, none leaked), 0 invented / 0 silently dropped, 2285 renown, 19 titles, 3 borders, name/desc/renown/hidden all match; 42 manual deeds all have a grant site; wayfarer POI marks match zone labels exactly. Both mutations (threshold flip, renown-increment break) redded a test. architecture-reviewer: 0 blocking / 0 should-fix (3 benign notes). test-coverage-auditor findings applied: added 2v2-bracket, lifetimeXp-boundary, quest and gathering negatives, plural-quests branch coverage. Adversarial: 85-event join burst drains and converges; dual-write byte-equal on second save; renown recomputed on load; grantDeed not client-reachable; no-op set re-adds do not dirty; removed-content sanitize never touches deed state |
| 2 | phase-02-iworld-wire.md | DONE | 2026-07-08 | IWorldDeeds facet (5 members) in both worlds; deed_set_title command + entity title identity-wire key; deeds/dstats heavy-gated + renown/atitle per-tick self keys; deedUnlocked in HEAVY_SELF_EVENTS; load re-applies the saved title through the setter validator (stale id loads untitled; one 01 fixture amended to earn-then-select). Gates re-pinned: W0c 209/58/151 + 26 facets, W0a 40 delta keys + 3 renames + renown-absence pin, W0b 132/141, facet-tag block appended. Goldens stayed green (no regen needed). Reviewers: cross-platform-sync 0 blocking (dstats <=2s freshness floor documented as deliberate), qa-checklist + test-coverage-auditor should-fixes all applied (client-frame/server-dispatch lockstep round-trip, server null-clear, mid-session unlock re-emit). Full gate PASS twice; adversarial answers in session output |
| 2Q | phase-02-qa.md | DONE | 2026-07-08 | Acceptance re-run green (5 files, 397 tests) + full gate PASS. cross-platform-sync: all 8 checklist items PASS, 0 critical/warning (parity + wire tables clean; facet purity confirmed under the architecture walk; title clear propagates as a full re-wire); one tracked follow-up: deedUnlocked has no HUD handler yet, owned by the window session by scope (toasts are named out of scope for the wire slice). test-coverage-auditor: 0 blocking / 0 should-fix, 5 nits; all actionable ones landed in 2c9f57440 (entity-field asserts on the rewardless/border reject arms, exact renown-propagation pin toBe(5), fresh-player empty-wire value fidelity, legacy save without an activeTitle key, plus the cross-client CLEAR direction and a content-drifted earned-id negative from the adversarial pass; the two defensive-dead-code notes recorded, no change needed). Adversarial probes (temp file, run then deleted): drifted title id via raw dispatch is a silent no-op with zero error frames; resume-path reconnect resets lastSent/sentEnts/selfHeavyDirty so all four self keys repopulate with current values; two-client set-then-clear converges end to end. No coordinator growth (sim.ts +29 thin delegation only; hud/main/renderer untouched); no player-visible English introduced |
| 3 | phase-03-deeds-window.md | DONE | 2026-07-08 | 9483dd8fb: window (deeds_view pure core + cold painter, bank shape) + watchlist tracker (writer facet, one skeleton innerHTML allowance, cap 5, per-character woc_deed_watch) + earned moment drain-batched in the pure core (banners coalesce to the LAST unlock, one sound per drain, retro folds to ONE summary line, all unit-pinned) + KeyZ chain (input union + dispatchEdge + keyboard/gamepad + rebind label; test free-key fixtures reworked) + 10 category and 21 bespoke marquee crests (new bell primitive) + nameplate/inspect/char-sheet titles (borders join the ms-badge row; nameplate borders stay a v1 cut) + chronicler interact diverts through sim.interact() then opens Chronicles. Resolved judgment calls: earned hidden deeds shelf on Feats (content has no home category; they stay Renown-bearing non-feats in completion); refresh sig adds a deedStats digest beyond the packet list so open-window bars stay live; mobile sheet pin shipped now (mobile_window_coverage guard demands it; full mobile stays the final slice). M16 forced 49 wordy hudChrome.deeds keys x 5 non-Latin locales: real fills shipped + glossary deedsSystem terms locked. Browser E2E green end to end incl a natural-play earn (Saul x9 -> hid_saul_footnote) and title equip; CDP key delivery is dead in this headless env for ALL binds, so the real-KeyZ check dispatches the keydown on window (full in-app chain exercised). Reviewers: qa-checklist 1 blocking (literal dashes inside the guard-test regex, now unicode escapes) + nits applied; test-coverage-auditor 2 should-fix (entry display-field pins) + nits applied (new tests/deed_i18n.test.ts). Deferred: orphaned game.milestone.unlocked key (18 live translations, project-end sweep), dead 'milestoneUnlocked' string in server HEAVY_SELF_EVENTS (server slice), inspect BORDERS (not wire-backed; would need a facet/wire change). Full gate PASS (11392 tests + tsc + all builds) |
| 3Q | phase-03-qa.md | DONE | 2026-07-08 | PASS thru f8f395b1a. Scope clean (sim/types.ts = sanctioned milestoneUnlocked deletion only; world_api untouched; perf baseline untouched). Acceptance re-run + full gate PASS twice (fresh at 8a65069af, again at f8f395b1a). Auditor findings applied in f8f395b1a: deedsRefreshSig + deedStatsDigest extracted to the pure core with per-dimension movers pinned (dropping any of the 8 repaint dimensions now reds a test), watch-cap fifth-add arm, nearest same-fraction tie-break; retro-banner runtime-spy nit recorded no-change (plan bannerId:null pins + source guard, auditor-accepted). Browser E2E all green, CDP keys ALIVE this session (the window-session deadness did not reproduce; real KeyZ/F/Esc/Tab/x used): fresh zeros, hidden never leaks via search in all 10 cats, unlock banner/gold line/fireworks, tracker drop + 1/1,000, live open-window refresh, cap-6 disabled note, retro x3 and x1 = ONE line zero banners via the real drain, Esc close + focus return, Tab trap, collapse + watchlist survive reload, Saul x9 Footnote + title equip/revert, own-plate nameplate subtitle (titles are player-kind by design; NPC probe invalid), char-sheet link opens Book, ja round trip while open (filterAll stays English = pending nonwordy key, by design), rebind Z-X works/persists/resets via real Key Bindings panel, /play clean, death + cross-map travel with window open coherent, same-tick double unlock coalesces banner to LAST. Zero page errors. Esc-in-search does not close: platform-wide input guard (input.ts), bank-identical, not a deeds bug. Packet conflict flagged: this QA file says no locale overlay edits; overview M16 rule wins (fills = exactly the 49 flagged keys x5, same commit). Screenshots tmp/deeds_qa/ (no PR yet, gist deferred) |
| 4 | phase-04-server-persistence.md | DONE | 2026-07-08 | character_deeds (realm no-default, UNIQUE(character_id,deed_id), 3 indexes; account index forward-placed for the roll-up, commented) + accounts.deed_broadcasts; deeds_db.ts boundary (5 parameterized queries, pool-only import so NEITHER bot-detector copy needs factory edits, verified against the private clone) + deeds_records.ts FIFO observer wired into detectActivity; marquee broadcast (isMarqueeDeed: renown>=25 OR reward; retro NEVER; opt-out read per unlock) via SocialService.broadcastDeedUnlock (guildmates + whoFriended followers, isIgnoring, dedupe, earner excluded) delivered as id-based deedBroadcast (SimEvent union entry per the calendarResult precedent, flagged: the no-sim-edit boundary vs the one client event switch, reviewers accepted) + hud chat arm; GET /api/deeds/rarity (registry-only, publicReadRateLimited, 5-min TTL cache in main.ts, stale-on-error) + POST /api/deeds/broadcasts (requireAccount active + withBody, deeds.invalid_input); deedsRarity() facet in BOTH worlds (Sim null pinned; ClientWorld soft-fail fetch, 4 failure arms pinned) + W0c re-pins 210/58/152 + FACET_DEEDS; window rarity line via the pure deedRarityFraction gate (PACKET CONFLICT flagged: 03 only RESERVED the slot with a comment, no render existed; implemented here per the verified-fed step); sheet deeds block { renown, earnedCount, activeTitle, recent<=5 } on ALL FOUR arms (owner arms included so the owner sheet stays a superset; recent fetch dual-edited into the legacy main.ts arms); registry-only routes added to the 3 surface ledgers (completeness REGISTRY_ONLY_PATHS arm, inventory rows, API_CONTENT_TYPE). M16: 3 wordy keys x5 non-Latin fills (glossary terms) + resolved regen + sha256 re-baseline. Live E2E vs :8797 + psql: 17/17 (rows, one-per-pair, realm stamp, guildmate broadcast, no-text-field, rarity map, sheet, second-login no-op, opt-out trio). Reviewers: migration-safety 0 blocking (account-index + snapshot-semantics + eventual-consistency notes: comments added); privacy-security CLEAN (2 INFO for maintainer sign-off: public recent earnedAt timestamps; opt-OUT default TRUE); test-coverage 0 blocking, 4 should-fix ALL applied (renown-24 boundary, ClientWorld fetch suite + null-earned guard tightened, deedRarityFraction pure-core extraction, Sim null pin). Gate PASS (11440 tests + tsc + all builds) |
| 4Q | phase-04-qa.md | DONE | 2026-07-08 | PASS. Pre-QA merge of origin release/v0.23.0 (c20507e07: /metrics aggregates + Vale Cup reward fix) with release-merge-audit CLEAN (4 overlap files byte-faithful both sides; no ledger, injection, or route loss; db-mock gaps latent only). Scope clean: the one boundary item beyond the sanctioned deedsRarity getter is the deedBroadcast SimEvent union entry, recorded as the accepted calendarResult-precedent deviation. Acceptance green; full gate PASS twice; the first run's single red was a PRE-EXISTING cross-test race (new_endpoint's whole-tree porcelain snapshot vs i18n_scan's non-atomic 18.7 MB rewrite of i18n.status.json, which release still TRACKS while .gitignore and main say untracked), fixed with atomic write-then-rename in i18n_scan.mjs; the de-track itself is left to the maintainer (upstream-owned). Findings applied: rarity numerator JOINed to the same eligible population as the denominator (migration-safety should-fix; mutation-verified literal pins) + deedRarityFraction clamps at 1; hidden-deed existence stripped from anonymous/third-party surfaces per the DeedDef secrecy contract (publicRarityPayload + isHiddenDeedId in deeds_records feeding the main.ts cache; sheet recent strips on the PUBLIC arm only, the owner keeps their own earns; flag for maintainer sign-off alongside row 4's two INFO items); deedBroadcastLine extracted to deed_i18n (composition Node-pinned, hud arm source-pinned, arm verified live in the 04 E2E); SocialEvent/SimEvent deedBroadcast declaration-parity tsc pin; canonical db-mock shape mirrored into game_state_metrics + vale_cup_online and insertBankLedgerRow added to the canonical character_lease_game shape; drifted-id observer arm and string earned_at branch now tested. No-change findings with evidence: broadcasts-toggle rate limit (parity holds: 57 authenticated mutations incl account/marketing ship none), main.ts rarity-cache extraction (the implement spec mandates main.ts placement next to the board caches; noted as the third TTL-cache copy, a rule-of-three chore candidate), hud outer-switch assertNever (calendarResult precedent, tsc clean). Reviewer verdicts: migration-safety 1 should-fix (fixed) + forward note (the 05 roll-up MUST dedupe per account by deed id, never SUM raw rows: two characters on one account double-count); privacy-security CLEAN, all 8 proofs; test-coverage-auditor all 5 required confirmations PASS; cross-platform-sync both checks PASS. Bot-detector: zero edits needed in either copy (deeds_db is pool-only); the private clone ran green MOUNTED on the merged tree (29 files, 269 tests). Adversarial: db-down never blocks the loop or autosave (FIFO catch, fire-and-forget broadcast, rarity serves stale cache); retro floods insert-only and never broadcast nor read the opt-out; the one owner toggle gates BOTH audiences before fan-out; no game.ts English (dev-channel console.error only); diff hygiene clean (no dashes, emojis, or packet vocabulary) |
| 5 | phase-05-leaderboard.md | DONE | 2026-07-08 | Renown board (?board=deeds, GLOBAL-ONLY, scope accepted-and-ignored) + pure scoring core server/deeds_board.ts (counted set = distinct renown>0 deed ids per ACCOUNT, floor 50, score desc then earliest completionTime = max of per-deed EARLIEST earns then accountId; display char = highest-renown char, tie lowest id; buildDeedsBoardEntries skips a mid-refresh-deleted display char, rank gap preserved) + main.ts cache (30s TTL, stale-on-error, boot warm, accountId internal-only) + self row (rank + ceil percentile vs pre-cap total, authed ranked callers). Route: in-handler optionalReadAccount composition (route middleware would 401 invalid tokens on the OTHER boards, so the shared route stays middleware-free, pinned); legacy main.ts arm dual-edited byte-identical for anon + valid-token (lenient bearer = authz-gap-close class); parity corpus + characterization golden leaderboard_deeds added. Board integrity: ELIGIBLE_ACCOUNT_SQL retrofitted into players both arms, arena, guilds (member-level, sum deflation not guild delisting), ALL FIVE daily-rewards ranked reads (one population for page/total/self/top10), deeds read; dev board deliberately exempt (comment); setOnAccountModerated hook in moderateAccount (covers both admin arms AND game.ts GM sanctions; no other banned_at write exists) busts players+guilds+deeds caches; arena uncached, daily per-request. Client: IWorldDeeds.deedsLeaderboard both worlds (W0c 211/58/153 + FACET_DEEDS), deeds_leaderboard_view pure core (UI_PURE_CORES), window tab between Guilds and Devs (devs-tab pattern incl aria-selected/roving), title stays a deed id resolved via deedTitleText, self line server-resolved only. M16: 5 wordy hudChrome.deeds.lb* keys x5 non-Latin fills (locked glossary terms) + resolved regen + sha256 re-baseline same commit. Live db E2E 6/6 (order/floor/fields/no-accountId, self row, invalid bearer 401, ban delists deeds+players, expired suspension stays, unban relists). deedCount = counted-set size (judgment call: feats do not inflate the public count) |
| 5Q | phase-05-qa.md | DONE | 2026-07-08 | PASS. Pre-QA merge of origin release/v0.23.0 (62f1f2f24: fishing pole vendors + mobile UI fixes #1684); release-merge-audit CLEAN (16 genuine overlap files verified both sides: chroniclers + fishing vendors coexist in zone2/zone3, deeds hud arms + chrome keys + 5 non-Latin locale fills intact; delta has zero server files so no legacy-arm or injected-seam work; i18n regen + sha256 re-baseline inside the merge commit, equivalence test green; no new db-mock sites; the release delta's own 9 test files green on the merged tree, 327 tests). Scope reconciled: diff matches the row-5 summary exactly (2 new modules, 3 new test files + golden fixture, 7 extended test files, M16 fills same-commit). Acceptance re-run green (10 files, 521 tests) + full gate PASS twice (pre- and post-findings). Reviewers all clean: privacy-security 4/4 CONFIRMED (no accountId or email in any public body on either arm, active RouteDef arm 401s an invalid bearer with the legacy lenient divergence documented + pinned + prod dispatch defaulting to the new arm, bust seam boot-injected module-private + post-commit-only + every moderateAccount caller admin/GM-gated, zero SQL interpolation); migration-safety 3/3 CONFIRMED (zero DDL in the diff, double-boot idempotency verified live on :8799, NULL moderation columns eligible by truth table and the joins provably cannot drop legacy rows, activeTitle JSONB read null-safe); test-coverage-auditor: all 6 hunt orders COVERED, 0 blocking / 0 should-fix, 2 nits (APPLIED: name-escape assertion added to the renown escape case; NO-CHANGE with evidence: row-level exclusion stays mechanism-pinned because the suite has no live-db vitest class, every db test mocks pg, and the row-level behavior was verified live in both sessions). Live dev-server spot checks all green: REST 13/13 (order, floor, renown/deedCount values, scope 'global' under ?scope=realm, 0-indexed paging, self rows 1/50 and 2/100, below-floor and anonymous get no self row, invalid bearer 401, ban through the real admin route delists on the NEXT request and unban relists, inside one TTL, proving the bust hook; players/guilds/devs arms 200) and browser E2E via real login/realm/dblclick/KeyK/tab clicks (Renown tab renders rows + self standing line, players/guilds/developers/daily tabs unchanged, zero page errors; title travels as a deed id and renders localized Champion). BONUS validator proof: a psql-forged activeTitle without the matching earned deed was cleared by the join validator through the full server + persistence stack. Load sanity by code read: every deeds read behind the 30s TTL cache, posture byte-similar to the players board (no in-flight dedupe on either, pre-existing). Adversarial: shared route middleware untouched (RouteDef bare, auth composed in-handler, parity + characterization goldens pin legacy anonymous behavior); banned_at/suspended_until writes single-sited in moderateAccount (4 SET arms, 4 callers, all through it); deleted-character tested (deeds_board.test.ts:243) + ON DELETE CASCADE in DDL; i18n artifacts same-commit (e12611eb3, commit graph checked); zero packet vocabulary or dashes in the diff; named gap for the polish sweep: the window header subtitle stays "Lifetime XP" on every tab including Renown (pre-existing shared chrome, identical on the release parent, the Daily tab has the same quirk). Packet conflict flagged: this QA file's exit step says push; the branch is LOCAL ONLY per the session ground rules, so no push. Next = phase-06-wiki-guide.md |
| 6 | phase-06-wiki-guide.md | DONE | 2026-07-09 | Spoiler-safe /wiki/deeds page + generator GUIDE_DEEDS (177 public of 186; 9 hidden filtered STRUCTURALLY by the def hidden flag, never reach content.generated.ts). CONFLICT flagged + resolved: the generator spec calls desc "the only criteria surface", but deed descs name boss:true mobs (Morthen, Vael, Korzul, Nythraxis, Ysolei, Thunzharr, Deacon Varric, Sister Nhalia) and per-encounter mechanics; baking desc would trip the existing "never bakes a boss display name" guard AND leak instanced spoilers, so per overview-wins I emit NO desc and NO trigger (only id/name/category/renown/feat/rewardTitle/rewardBorder). Catalog roll shows Name/Renown/Reward; criteria stay in the in-game Book. Route+page+registry+sitemap; deeds page reachable via the route-derived search (no per-deed indexing, deliberate). M16: 28 wordy keys x5 non-Latin fills (glossary deedsSystem terms; Saul stays Latin) + sha256 re-baseline same commit; 3 non-wordy keys (catHeading/colName/featTag) English-pending for the maintainer release fill. Tests: SIM-table-driven spoiler block (hidden id/name/desc absent, no-trigger/no-desc for every deed, exact non-hidden set equality, reward value pins, render smoke, empty-catalog survival). Reviewers: qa-checklist READY (0 blocking/0 should-fix, 3 nits ALL applied), test-coverage-auditor (all 5 behaviors DECISIVE, 1 should-fix + nits ALL applied). Gate PASS; browser E2E green (light/dark/mobile portrait, zero hidden/boss leaks, /api/site-presence 502 is backend-absent noise) |
| 6Q | phase-06-qa.md | DONE | 2026-07-09 | PASS thru 1ca089d36. Pre-QA merge of origin main d22382e58 (main absorbed release/v0.23.0 via #1605 + deb-author fix) with release-merge-audit CLEAN: both overlap files (vale_cup.ts, hud.ts) hold both sides, and the release possession gate composes correctly with the cup deed hooks (gates return before the touch attribution, so touch deeds now count only genuine at-the-feet strikes, matching the personal-outcome rule); delta suites + deeds.test.ts green (167). Acceptance re-run all green from the clean merged tree (freshness, sitemap, guide 38/38, i18n build+scan diffless) + full gate PASS twice (pre- and post-findings, unpiped in the worktree; full suite behaves here, no symlink limitation). Spoiler audit by hand: 9 hidden defs enumerated from the sim source, zero id/name/desc hits in content.generated.ts, the rendered DOM, AND the built guide entry chunks (dist graph checked; deed_i18n refs in sim/world_api/catalog are comments only); the one grep hit was the PUBLIC deed soc_gilded_strongbox sharing the bank proper noun; the single trigger mention is the interface doc comment. Mutation test: blanking the generator hidden filter + regenerating redded 5 tests (freshness + 4 spoiler pins naming hid_saul_footnote), one-path git-checkout restore back to green. Browser: cold deep-path + SPA sidebar nav (window-flag probe, no reload), locked copy verified (Saul of Eastbrook Vale; total only ever climbs; all for show / never stronger), 4-deed sim spot-check across categories, portrait 390px no horizontal scroll + no landscape gate, guide is a FIXED dark design (color-scheme pinned dark in guide.html, no toggle) verified identical under both OS scheme emulations; the only console noise is the known /api/site-presence 502 (backend absent) and a Season substring false positive (deed name Seasoned Boarballer). i18n: 28 identical fills across exactly the 5 non-Latin overlays match the M16 wordiness rule ([a-z]{4,} after placeholder strip) precisely; the 3 pending keys (catHeading/colName/featTag) provably non-wordy; no dashes/emojis anywhere in the diff (perl scan); sha256 same-commit. Reviewers: qa-checklist READY 0 blocking (1 should-fix + 2 nits), test-coverage-auditor all 3 mandates CONFIRMED (4 nits); ALL findings applied in 1ca089d36: catalog tables wrapped in guide-table-scroll + guide-keytable with deed column rules (was unstyled browser-default), category label keys now a compile-checked literal map, route wiring pinned to literals (sub/navKey/group), Border + Feat cell labels literal-pinned, emitted-field exact allowlist (closes the border-slug lane generically); the render-arg nit landed as comment + wiring pin because PageContext is REQUIRED by GuidePage (the suggested no-arg call fails tsc). Flag for maintainer: three public deed names echo withheld boss epithets (Fogbinder Unbound, Scourge No More + the Deathless, Drowning the Moon); guards catch full names/desc/trigger, not name fragments; reviewer and QA judge it acceptable-by-design (players see these names in game) but it is a deliberate call to confirm. Next = phase-07-steam.md |
| 7 | phase-07-steam.md | DONE | 2026-07-09 | 7d3a499d9, shipped DARK behind STEAM_ENABLED. Pre-work: origin/release/v0.24.0 merged (c279048dc: queued-cast ability + warlock pet resummon; the only conflicts were 2 parity goldens, deliberately regenerated from the merged tree, only those 2 changed) with release-merge-audit CLEAN (7 overlap files verified BOTH_SIDES_INTACT by parallel fan-out; deed hooks compose correctly against the release early-returns; Fiesta practice gate holds; zero server files in the delta so no legacy-arm, ledger, db-mock, or i18n-hash work). Slice: steam_links DDL (wallet_links shape, both uniques, CASCADE; additive leaf, rollback-clean) + pool-only steam_db (insert classifies the 23505 by re-read; misattribution race accepted, both arms terminal 409s); 3 registry-only routes with steamDisabledGuard FIRST (steam.disabled 503 pre-auth on all three, zero db traffic while dark) + STEAM_LINK_POLICY (fused ip+account 5/min, tunables row) + withBody; ticket.ts pure (wocc-link identity, 40-2048 hex clamp, verdict parse incl vacbanned/publisherbanned; replay residual documented: equals Steam's own linking model) + web_api shell (5s timeout, malformed 2xx = upstream, logs nothing); achievement_map 68/100 in DEED_ORDER (deferred ACH_NINEFOLD + ACH_RINGWRIGHT excluded and pinned absent, ACH_GOLDEN_GOAL confirmed deleted with ACH_VCUP_GOLDEN_GOAL live, 4 hidden deeds mapped with active consistency pin, 3 cross-file spot pins); mirror observer hooked INSIDE deeds_records after the upsert resolves (game.ts untouched): detached FIFO, in-flight dedupe, 4-attempt capped backoff, one no-secret warn per drop, 60s promise-valued link cache with SYNCHRONOUS onLinkChanged invalidation so relink never pushes to a stale id, reconcile-on-link = earned-and-mapped set (earnedDeedIdsForAccount served by character_deeds_account); /api/status steam:{enabled} dual-arm same-commit + status_get golden deliberately re-minted (name-list trim stays the only status deviation); electron steam.cjs (steamworks.js 0.4.0 lazy, steam distribution or UNPACKAGED WOC_STEAM_DEV only per the hatch-closure pattern, init retried per click, never throws across IPC, ticket via getAuthTicketForWebApi) + trustedSender-gated channel + ipcMain.handle guard-scan test + optional DesktopBridge.steamLinkTicket (trio detection unchanged) + steam-depot-only packaging (files re-include after !node_modules + dist asarUnpack + steamAppId stamp; website artifacts byte-identical); client card in BOTH entries (byte-parity pinned) gated on api.steamAdvert() before any authed call, Link desktop-only, web = status + unlink; 11 wordy keys x5 non-Latin fills + steamLink glossary category + hudChrome.steam.title BRAND_ALLOW + resolved regen + sha256 same commit. Steam Web API shapes verified against live partner docs (AuthenticateUserTicket v1 GET + REQUIRED identity; SetUserStatsForGame v1 form name[i]/value[i], achievement flags idempotent). GATE LESSON (first run redded 31 files): the ./steam barrel dragged routes.ts load-time requireAccount into game.ts's graph and mirror read db-boundary exports at load, breaking fixed-list vi.mock overlays; fixed by concrete-module imports (deeds_records -> steam/mirror, both status arms -> steam/config, barrel = routes only) + call-time arrows in REAL_DEPS. Reviewers all CLEAN, 0 blocking / 0 should-fix: privacy-security (3 INFO, all applied), migration-safety (all 6 points PASS, 3 INFO reflected), cross-platform-sync (all 6 points PASS, 3 nits: advert pre-gate deliberate, web chrome intended, markup parity pin ADDED). Manual appid-480 note: real steamworks.js exercised through steam.cjs with Steam installed but NOT running (init fails clean: one warn, null ticket, no throw); happy-path mint needs a logged-in Steam client, left for the maintainer. Full gate PASS (final run on the exact committed tree; 3 runs total, first caught the mock-graph lesson) |
| 7Q | phase-07-qa.md | DONE | 2026-07-09 | PASS thru 698fab2bc. Pre-work check: origin/release/v0.24.0 tip 3ad630434 already an ancestor (the implement session's merge c279048dc holds the latest), no new merge. What shipped confirmed: icon script SKIPPED (optional stretch, absent), appid-480 loop RUN (Steam installed, not running: clean init failure). Acceptance re-run fresh all green (7 steam files 105 tests + parity/architecture 30). One real gate red vs the implement session's recorded PASS: changed-files biome errored on shell.css (the cs-steam-group addition pushed a selector past lineWidth 100); fixed formatting-only, style-reading suites green, gate then PASS all 9 steps (twice: post-fix and final on the committed tree, 11684 tests). Flag-off boot by hand: new arm answers 503 steam.disabled on all three routes PRE-AUTH with zero db traffic (bogus bearer still gets steam.disabled), advert {enabled:false} on BOTH arms; legacy arm 404s the steam routes BY DESIGN (registry-only convention; the advert is the dual-armed surface). Flag-on with fake key: advert true, authed status {enabled:true,linked:false}, non-hex ticket 400 steam.invalid_ticket, hex garbage hits the real upstream and maps to 503 steam.upstream, no crash, key in ZERO log lines or bodies. Dev-client browser pass via real login both states: flag off card hidden zero page errors; flag on renders the web posture (label + hint only). Reviewers: privacy-security CLEAN all 6 items (2 INFO: replay residual documented in ticket.ts, equals Steam's own linking model; status read unlimited accepted, matches the authenticated-read parity); migration-safety all 8 PASS (1 INFO applied: the character_deeds_account comment now names earnedDeedIdsForAccount instead of claiming the index unused); cross-platform-sync all 6 PASS zero findings (dual-arm advert literals verified byte-equal at main.ts:1500 + leaderboard.ts:562, IWorld/wire/SimEvents untouched); test-coverage-auditor all 5 named suspicions COVERED, 4 should-fix ALL applied in 698fab2bc and each mutation-verified decisive: recorder-to-mirror call-site pin (deleting the hook reds 2 tests), limiter MOUNTED on POST link via the real chain 429 (dropping rateLimit from the route table reds it; partial db mock keeps SCHEMA real), enabled-but-unprovisioned drop arm (one warn, queue drains after), legacy-arm flag-on advert named parity assertion (hardcoding false reds it); nits: Set-Cookie absence added beside the body-key pin; backoff/TTL self-comparisons recorded NO-CHANGE per the auditor's own conditional (timing tunables, not wire contracts; the attempt count is already literal-pinned). Adversarial all 6 PASS: no steam_appid.txt exists anywhere (init passes the id directly); mirror hook lives in deeds_records only, game.ts has zero steam refs (neither bot-detector copy needs edits); the relink test pins the old id never receives a write after unlink+relink; no await reaches the world loop (void recorder + promise tail + synchronous enqueue with a detached link lookup); no stale advert render (steamAdvert re-fetches per refresh, env read live per call, verified live across a flag flip); exactly 11 wordy steam keys x5 non-Latin locales and nothing else translated. Map pins mutation-checked (one bad entry reds 3 tests). Hygiene: zero dashes, emojis, or packet vocabulary in the shipped diff and commit messages |
| 8 | phase-08-mobile-polish.md | DONE | 2026-07-09 | Pre-work: origin/release/v0.24.0 merged (a875c65b4, PR #1708 professions onboarding) with release-merge-audit CLEAN; the sha256-conflict-marker trap FIRED and was fixed by amend (i18n:gen does NOT rewrite the baseline); gathering.ts hand-resolve keeps the release quest-item grant AND the branch gather mark. Slice (6 commits): mobile standalone full-screen #deeds-window (bank convention: safe-area pinned, chip-row rail, max-height 480 compensation with BOTH floors relaxed to 44px), tracker compact cap 88px + hud-mobile-compact chip fold whose tap opens the Book, mobile-deeds More-tray button in BOTH entries behind a new hand-authored book glyph + onDeeds chain, long-press peek on deed cards (consumePeek on both card actions), a11y closes (unlock + retro lines through the polite combat announcer since banner/chatlog carry no live semantics, watch aria-pressed, recent-crest alt, reduced-motion gates the unlock firework), options deed-broadcast row behind an online-only OptionsHooks seam over a NEW read-tier GET /api/deeds/broadcasts (registry-only, ledgered; jsdom round-trip incl echo-differs + failed-write revert), fairness source pin on both deed painters (no governor/tier read), 2 M16 keys x5 non-Latin fills + sha256 same-commit, css corpus manifest 67/65 with the new ten-dash section. Verification passes: fairness PASS (structural oracle 6/6 by fresh agent), perf floor PASS (tracker classified in ARM 1, budget green NO baseline edits, window stays cold), balance PASS with ZERO catalog edits (186 deeds / 2285 Renown / 19 titles / 3 borders / 68 Steam entries match the assembly resolutions exactly; values only 0/5/10/25/50; no dup titles/borders/ACH names). MAINTAINER FLAGS: paragon+mythic deeds reward TITLES while the legacy MILESTONES table had them as borders (catalog authored it deliberately with a maintainer-call note; changing the kind now would invalidate persisted activeTitle picks, and the legacy ms-border badges still render from the dual-written unlockedMilestones); character_deeds login-time reconcile would close the dropped-insert drift edge (records header now states the real guarantee); leaderboard header subtitle stays Lifetime XP on every tab (pre-existing shared chrome); the packet-authoring docs commit message contains the word packet (squash-time cleanup). Reviewers: qa-checklist session READY 0 blocking/0 should-fix, qa-checklist whole-feature READY 1 should-fix + 2 nits ALL resolved (comment correction + dead milestoneUnlocked entry removed + muted-broadcast recorded no-change), test-coverage-auditor 1 should-fix + 2 nits ALL applied (echo-differs case, peek-guard association pins, GET read-scope source pin), privacy-security CLEAN (read/active scope split intentional). Online smoke tour 35/35 green (tmp/deeds_8_tour.mjs vs :5173/:8787 + live db: real KeyZ, natural Saul earn + banner + title equip, keyboard trap 30/30, toggle persisted to accounts.deed_broadcasts=f, phone 844x390 full walkthrough incl real More-tray button + long-press peek + tracker chip, 640x360 both strip arms); screenshots tmp/deeds_8/ (gist deliberately NOT pushed: unannounced feature). mobile_input_zoom_check 28/28. Full gate PASS (twice; final on the committed tree). PR draft in the session summary |
| 8Q | phase-08-qa.md | DONE | 2026-07-09 | PASS thru 5f373ddca (row amended after the fact: the 8Q exit step deleted the whole packet before this table could record it; the full verdict lives in the session record). Feature complete; 11-row online walkthrough green; sites coverage gap closed + 40px filter-chip floor fix landed; packet dir deleted by design in 5f373ddca |
| P0 | polish round: merge + research + files 09 to 14 | DONE | 2026-07-09 | Packet restored from 5f373ddca^ (overview, catalog, progress; policy revised: durable parts now KEPT, see overview intro). origin/release/v0.24.0 merged db71dad6d (PRs #1730 moderation history + #1728 bot-detector); release-merge-audit CLEAN (5 overlaps all additive both sides; new /admin/api/moderation/history brought its own inventory row; setOnAccountModerated bust hook unaffected, #1730 is read-only; regenerated i18n status scans, sha256 baseline verified matching; deeds db functions live in deeds_db.ts so the linkdead db-mock trap is not in play; 13 test files 326 green + tsc clean). Eight-agent research fan-out digested into the six new implement+QA pairs and overview section 9. Key research anchors: 197 icons at 512 RGBA named exactly by deed id, 186/186 covered, 11 orphans = the deferred+cut set, ingest = sharp resize 128 WebP to public/ui/deeds (raw unhashed path); titles already on 5 surfaces, chat needs a fromTitle deed id on the chat event, target frame is client-only via entity.title, social/XP-board need db state->>'activeTitle' fills; rename target = chronicler_edda_hartwell display name only (id persisted in saves), Saul is pinned by tests+guide prose, 5 non-Latin overlays go stale on rename (14 fixes); deeds documented NOWHERE outside the packet (zero-hit grep); i18n scope = 1560 deeds pending rows + DEED_LOCALES full catalog fill invisible to the counter; deferrals still hold (0 enchanting/jewelcrafting/inscription recipes, no account-level lane) |
| 9 | phase-09-catalog-refresh.md | DONE | 2026-07-09 | thru b7f687350 (c80ee08af implement + 2 test-pin commits). Pre-work: origin/release/v0.24.0 tip already an ancestor of db71dad6d, no new merge. 8 blocks authored docs-first, 6 transcribed: prog_crown_below (25, quests x5 nythraxis chain, ACH_CROWN_BELOW), prog_mere_at_rest (25, quests x4 temple back half, Sethrael via q_palecoil per the preferred route, ACH_MERE_AT_REST), prog_callused_hands (5, q_prof_intro; the "first profession pick" arm rejected: no pick state exists), prog_tools_of_the_trade (10, NEW hubCraftsPerformed counter bumped in crafting.ts craftItem on ok + requiresHubStation, ACH_TOOLS_OF_THE_TRADE), dgn_nythraxis_crypt (10, quest q_nythraxis_sealed_crypt because the crypt has no final boss so dungeonClears can never record it, ACH_NYTHRAXIS_CRYPT), chr_marsh_first_cast (5, existing fish:mirefen_marsh mark, zero new plumbing). NEW DEFERRAL (maintainer flag): both salvage deeds authored but NOT transcribed, salvage has zero player-facing wiring on any host (no IWorld/UI/wire/server path; ringwright class); salvagesPerformed counter ships with the transcription; ACH_FIRST_SALVAGE held unregistered + absence-pinned. Live set 192 deeds / 2365 Renown / 19 titles / 3 borders / 72 Steam; +80 Renown transcribed (window 60-110); scripted recount matches README resolutions 14-19 exactly; no existing trigger/renown/DEED_ORDER position edited (first 186 byte-identical, index-185 + tail pinned). Deferral rechecks re-verified: 15 recipes with zero enchanting/jewelcrafting/inscription; deeds_records still observer-only. Icon brief at catalog/new-deeds-icon-brief.md (all 8 ids incl the 2 deferred). Wiki regen 183 public (9 hidden filtered); i18n untouched (no catalog keys, no sha256 re-baseline needed); deed_i18n manifest auto-covers (403 rows pinned). One full-suite catch: deeds_view fresh-character denominator pins 174/177 to 180/183. Reviewers: architecture-reviewer 0 blocking / 0 should-fix (2 notes recorded no-change: recipeById re-lookup deliberate, CraftResult carries no hub flag; new-counter no-retro is locked decision 3); test-coverage-auditor 1 should-fix APPLIED in b7f687350 (all six new triggers literal-pinned incl both chain questIds arrays) + 1 nit recorded no-change per its own conditional (interior launch-order snapshot: pre-existing pattern, appends-only diff adds no risk). Gate PASS (11774 tests + tsc + all builds; final run on the committed tree). Adversarial: all six answered in the session summary |
| 9Q | phase-09-qa.md | DONE | 2026-07-09 | PASS thru c8ad8b481 (zero code defects; the one finding is docs-only, applied in this commit as README resolution 20). Pre-work: origin/release/v0.24.0 tip already an ancestor, no new merge. Acceptance re-run verbatim from the clean tree, all exit 0 (deeds quartet 120 tests, architecture+S3 53, guide 39, steam map 9) + npm run gate UNPIPED PASS all 9 steps on the committed tree. Fidelity: scripted recount (tsx, executed module) matches resolutions 14 to 19 EXACTLY (192 deeds / 2365 Renown / 19 titles / 3 borders / 72 Steam, per-category AND per-prefix counts, renown values scale-only); all 6 new blocks transcribed 1:1 (name/desc/renown/trigger/hidden/Steam string-identical); deeds.ts diff pure addition (zero deleted lines); first 186 DEED_ORDER entries runtime-identical to the pre-09 table, tail exactly the 6 new ids; pre-09 renown still 2285; the 5 deferred ids absent, ACH_FIRST_SALVAGE/NINEFOLD/RINGWRIGHT/GOLDEN_GOAL unregistered; catalog block count 205 = 197+8 (the README format template is the 206th heading). Deferral rechecks live: recipes.ts 15 recipes with zero enchanting/jewelcrafting/inscription mentions; deeds_records observer-only; salvage zero player-facing wiring re-proven (the only non-sim salvage hit is an unrelated caravan string in sim_i18n). By-hand sites (temp probe, run then deleted): hub craft through the PUBLIC Sim.craftItem grants exactly once at counter 1 (+10 Renown once, plus the legitimate nested prog_first_craft +5 when the hub craft is the first-ever craft) and a second craft never re-grants or re-pays; REAL completeFishing at genuine Marsh and Vale z coordinates (zone derived from live position) grants each debut with zero cross-grant; serialize-then-rejoin retro round trip grants all four quest deeds retro:true with renown recomputed = catalog sum over the earned set. Mutations x3 all decisive, all restored by reverse edit to a byte-clean tree: threshold 1 to 2 redded 4 tests across 3 files, bump-to-zero redded both hub site tests, dropped chain quest redded behavioral + literal pin. i18n/wiki: only i18n-adjacent diff file is the deed_i18n manifest pin (403 rows derive from DEEDS, new ids auto-covered); no overlay or sha256 movement; wiki:content diffless on the committed tree; 6 names in the generated guide, no desc leak, hidden stays 9. Reviewers: 09's architecture (0/0) and test-coverage (should-fix applied in b7f687350) verdicts re-read, nothing unresolved; fresh qa-checklist READY 0 blocking / 0 should-fix / 1 optional nit (recipeById re-lookup, already recorded no-change), with its dstats-wire and restoreDeedStats-additivity traces covering the cross-platform and migration questions for this established counter class. Adversarial: the coverage sweep confirms every in-range content record has a deed or written rejection EXCEPT two inert profession systems with no written record, now closed as resolution 20 (recipe acquisition: recipes all grandfathered known, no learn surface on any host, the ringwright class; archetype refinement helpers: empty client stubs, selection predates the baseline); double credit is subset nesting only (first-ever hub craft pays first_craft+tools, the 5th chain quest can pay crypt+crown, the established chapter-meta pattern, probe-confirmed); icon brief ids = exactly the 6 DEED_ORDER tail ids plus the 2 marked deferred; zero dashes, emojis, or packet vocabulary in the shipped diff and commit messages |
| 10 | phase-10-icon-art.md | DONE | 2026-07-09 | thru 1847d626f (f7e8d4801 assets + 1847d626f logic/tests). Pre-work: origin/release/v0.24.0 tip 5c4628dc2 already an ancestor, merge is a no-op. Ingested the maintainer's 512px icon set to 128px WebP under public/ui/deeds (raw unhashed /ui path, NO media-manifest change) via new scripts/convert_deed_icons_webp.mjs: sharp lanczos 512->128, WebP q82/alphaQuality100/smartSubsample/effort6 with a 15KB per-file cap re-encoding to q75, DEED_ORDER loaded through esbuild the build_content way, orphan skip, regenerated src/ui/deed_image_ids.ts (sorted literal Set, exact Biome shape so re-runs are a no-op diff), stale-webp cleanup + zero-match abort guard. Converted 186; skipped 11 orphans (the 9 account-level + prog_ringwright + pvp_vcup_bet_flex deferred/cut set); missing art for 6 live deeds (the 09 additions prog_crown_below/mere_at_rest/callused_hands/tools_of_the_trade, dgn_nythraxis_crypt, chr_marsh_first_cast) which keep the procedural category crest. Weight ~1000 KiB across 186 files, heaviest prog_full_build 7480 B (under cap, no q75 fallback fired). Wiring: icons.ts deedImageUrl(crestId) + a kind:crest image branch in iconDataUrl (mirrors itemImageUrl; URL-only because iconCanvas(crest) is class-portrait only per unit_portrait_painter, never a deed crest); deeds_view.ts deedCrestId returns deed_<id> when art OR bespoke, else deed_cat_<category> (image branch outranks the 21 bespoke procedural recipes, kept as forward-compat fallback). deeds_window.ts UNCHANGED (both img sites pick up art through iconDataUrl). DEED_IMAGE_IDS is a zero-import DOM-free module both icons.ts and the pure core deeds_view.ts import (purity holds, architecture.test.ts green). Gate tests/deed_icons.test.ts: exact bijection both directions (mutation-verified), valid RIFF/WEBP header, webp-only dir, art-backed ids all live, art->/ui/deeds URL never data:, artless->null (procedural), 11 orphan ids literal-pinned + never committed, non-deed_ prefix guard, bespoke+art outranks via iconDataUrl; deeds_view.test.ts crest resolution updated to the 3 tiers. CREDITS.md maintainer-commissioned row; package.json assets:deeds. Browser desktop pass (dsf2, offline): 177 art crests loaded / 0 broken / 6 procedural (exactly the 6 artless: 4 progression + 1 dungeon + 1 chronicle) / 173 desat; recent strip 5/5; desat reads clearly over painted art so NO filter change (no before/after shots needed); screenshots tmp/deeds_10 (not committed, unannounced feature). Phone-landscape puppeteer boot timed out (mobile-E2E infra, orthogonal: the img-src change is viewport-independent, mobile sheet verified in 08). Reviewers: qa-checklist READY 0 blocking / 0 should-fix (traced the URL-vs-dataURL swap end to end: no synchronous iconCanvas consumer ever receives a deed_ crest, guide renders only class/family crests); test-coverage-auditor 1 should-fix + 2 nits ALL applied (bespoke-arm subsumption documented, non-deed_ prefix negative, art-outranks-bespoke via iconDataUrl). Adversarial: neither-art-nor-recipe id -> category crest (never UNKNOWN_RECIPE, never a broken img); a deleted webp with the id kept reds the bijection gate; the pure core passes the DOM-global and forbidden-import scans; urlCache never sees image URLs (the image branch early-returns before the cache, so procedural and image URLs cannot collide on one crest id); weight and heaviest file recorded (heaviest under the 15KB cap). Full gate PASS twice (final on the committed tree, all 9 steps). |
| 10Q | phase-10-qa.md | DONE | 2026-07-09 | PASS thru fb199cae4, zero code defects (this row is the only change). Pre-work: origin/release/v0.24.0 tip 5c4628dc2 already an ancestor, merge no-op. Acceptance re-run verbatim from the clean tree, all exit 0: deeds quartet 83, architecture 24 (pure-core scan holds with the id-set import), icon gate 8, hud_perf_budget 28 no baseline edits; git ls-files public/ui/deeds = 186 = DEED_IMAGE_IDS size = converted count; npm run gate UNPIPED PASS all 9 steps (11782 tests + tsc + all builds). Asset audit: set equality three ways (committed webps vs DEED_IMAGE_IDS vs DEED_ORDER-with-art) 186=186=186 zero diffs; artless = exactly the six 09 additions; the 11 orphan ids absent from tree, wired set, and DEED_ORDER; sips x5 across categories all webp 128x128 hasAlpha; weight 1000.3 KiB, heaviest prog_full_build 7480 B, zero over-cap; zero committed PNG/512 sources; CREDITS + assets:deeds rows present; ingest idempotence proven against the v2 source (re-run = byte-clean porcelain, summary identical 186/11/6). Mutations x5 all decisive, each restored byte-clean: deleted webp reds A+A2, unwired id reds A the other direction, dead static branch reds the art-URL pin, /ui/deed URL drift reds the literal pin, artless-to-deed id (the UNKNOWN_RECIPE lane) reds 3 deeds_view pins. Purity: deeds_view imports = 2 type-only + the id set (no DOM, no icons.ts); the only iconCanvas consumer in the tree is unit_portrait_painter class crests, zero deed consumers. Browser pass GREEN on BOTH viewports, zero page errors (desktop 1600x900 dsf2; phone landscape 844x390 raw CDP override): all 10 rails walked on both, 177 art loaded / 0 broken / 6 procedural (progression 4, dungeon 1, chronicle 1), fresh desat 183/183, staged-earn desat drops exactly to 173, recent strip 5/5/5 art minis at dpr 2, search stable with prog_crown_below staying procedural, earned card renders full-color art beside desat neighbors; screenshots tmp/deeds_10qa for the 13 session (renown 0 in the staged shots is the raw-map staging artifact; the real grant path recomputes, pinned in 1/9Q). PHONE BOOT RECIPE, the reason the 10 session's phone run timed out: the offline quick-start on phone hardware awaits the install preflight, so click mobile-preflight-continue after btn-start-offline; and the mobile deeds sheet is position fixed so offsetParent is null, probe visibility via rect. Long-press peek shows the tooltip and the grid survives release; the More-tray button opens the Book. Cold open time IMPROVED, not just unchanged: KeyZ-to-paint median 198ms with art vs 585ms pre-art on the same probe (static URLs replace about 30 synchronous procedural canvas composites; the 1.3s first-page sample is browser warmup, present on both sides). Corrupt-PNG probe: sharp decode throw exits 1 before any write or stale cleanup, tree untouched, never ships garbage. Hygiene: zero dashes, emoji, or packet vocabulary in the shipped diff and commit messages. Reviewers: 10's verdicts re-read and resolved; fresh qa-checklist over the final amended diff READY 0 blocking / 0 should-fix (2 informational notes recorded: assets:deeds requires the external source-dir arg by design; the bespoke arm is behaviorally subsumed today and kept as the forward-compat tier) |
| 11 | phase-11-title-surfaces.md | DONE | 2026-07-09 | thru e8e705dfc (42118dc0b sim chat + ae1100203 server fills + 9f253c670 ui renders/i18n + e8e705dfc review fixes). Pre-work: origin/release/v0.24.0 tip 5c4628dc2 already an ancestor, merge no-op. Chat: optional fromTitle deed id on the sim chat event, stamped by one speakerTitle helper at all 11 player emit sites (incl the dev-bot reply; mob/boss yells never); SocialEvent chat mirrors the field (type-parity pinned), guild+officer stamped from SocialActor.activeTitle filled by actorFor from the LIVE sim meta so both dispatch arms share one stamp; client compose = titledDisplayName inside .chat-player-name with the context menu closing over the RAW name, and the toWhisper echo never decorated (it displays the recipient). ONE pattern key hudChrome.deeds.titledName '{name} [{title}]' owns bracket text AND placement (non-wordy, English-pending, no M16 fills; resolved regen + sha256 same commit); deed_i18n gains titledDisplayName + titledNameDecoration (NUL-sentinel split into pre/post so a prefix-placing locale works, space-titles proven). Target frame: #tf-name split into pre/name/post children at Hud construction (no HTML edits, both entries); descriptor/view/painter gain OPTIONAL titlePre/titlePost as family members (player/party pass none = zero writes, pinned); resolve elided on a (language|title id) sig at the paint site; muted-gold .uf-title INLINE in the nowrap ellipsized line (frame height constancy kept, h=18 live; long combos ellipsize = the accepted classic behavior); hud_perf_budget baseline UNTOUCHED. Player card: PACKET CONFLICT flagged: the spec's under-the-name 540px spot is fully allocated (subtitle+flex y109-135, realm y158, dev-badge band y168-190, stats y196), so titleText draws goldDim on the realm baseline after the measured realm text, clamped left of the shared header right-edge (constants shared with drawHeader); untitled cards byte-identical via the pure cardTitleLayout null gate (draw call single + guarded, source-pinned); the localized OG card page DEFERRED exactly as the packet anticipated (per-locale stored copy has no deed-title locale surface; English into 21 localized OG strings rejected). Roster/friends: state->>'activeTitle' joined into listFriends/guildMembers (zero extra queries, charactersForDeedsBoard normalization); FriendInfo + server entries typed nullable-required; the wholesale social mirror needed no client change; .soc-title INSIDE the ellipsized .soc-name, guild placement AFTER the rank chip so the chip never moves. XP board: LeaderboardEntry.title (deed id, null untitled); the ONE refreshLeaderboard cache fill feeds both dispatch arms = parity by construction (REST parity corpus + characterization goldens byte-unchanged); offline Sim leaderboard fills meta.activeTitle (grant-then-assert pinned); players tab renders the Renown-tab title cell on a new .lb-row-players 6-col grid, header reuses lbTitleCol, sticky standing keeps an empty title cell for alignment. Profile: /c/ renders the new character_sheet sheetTitleText under the h1 (English by design; stale/non-title renders NOTHING, raw ids never leak); tests/profile_page.test.ts drives the REAL handler over a partial db mock. Guards: zero W0a/W0b/W0c edits (211/58/153 hold); parity goldens, REST fixtures, and the perf baseline all byte-untouched; S3 green by construction with the grep proof that the ONLY server English resolution (sheetTitleText) feeds the English-by-design SSR page. Layout matrix GREEN at all four widths (1600x900, 1280x720, 1024x768, 844x390 mobile-touch) against the live server+db with the two longest titles (the Resplendent / Boarball Legend, 15 chars) on a 16-char name: bracketed say+guild+own lines, target frame inline, both rosters, board column, card line, profile page; no horizontal overflow or pushed buttons anywhere; screenshots tmp/deeds_11 for the 13 session. E2E recipes: the server's sticky rememberedChat routes BARE text to the last channel (force /s); the charselect take-over button sits under a fixed ancestor (offsetParent null, click blindly); KeyH = target-nearest-friendly beats nameplate clicks; a linkdead resume keeps stale in-memory titles AND autosave clobbers a psql forge (restart the server after forging). Reviewers: architecture CLEAN 0/0 (its one note, board-title runtime parity, resolved by sync); cross-platform-sync CLEAN, all 6 comparison rows MATCH (2 INFO recorded: card/OG asymmetry deliberate, titledName English-pending); qa-checklist READY 0 blocking / 0 should-fix, 1 nit APPLIED (card header x/right-edge lifted to shared constants); test-coverage-auditor 4 should-fix + 3 nits ALL applied in e8e705dfc (friends+roster render source pins, players-board cell + grid pins, real-SQL title_reads suite with literal state->>'activeTitle' pins on every arm + empty-to-null normalization + the cache-fill source pin, offline-board title case, pattern-order suite, officer untitled arm), except the brittleness nit recorded no-change per its own text (pins match repo style; a key rename cannot pass silently because t() throws on untracked keys and values are pinned by output literals). Deferred + recorded: party/raid frames (change-minimal dense payload), mail sender lines (stamp-at-send needs a letter-shape change), chat bubbles (name-free by design), the OG card page (above). Full gate PASS (final run on the committed tree, all 9 steps) |
| 11Q | phase-11-qa.md | DONE | 2026-07-09 | PASS, zero code defects (this row is the only change). Pre-work: origin/release/v0.24.0 tip a0fb22fb4 merged as ab0d9745c (10 commits: codex-ai-architecture tooling, mailbox parcel quantity, sim stale-movement-intent fix); only the 4 generated i18n artifacts conflicted, resolved by regen with the sha256 re-baselined SAME commit (both sides added catalog keys: mailbox + deeds) and the resolved-equivalence test green; release-merge-audit CLEAN (6-group overlap fan-out all BOTH_SIDES_INTACT: the release intent-clear in spirit.ts and delves/runs.ts composes with all three deed hooks on live paths; the 5 locale-overlay unions insertion-only with zero duplicate keys; ZERO server files in the delta so no legacy-arm, inventory, or injection work; no new db-mock sites; the delta's own 8 suites 184 green + tsc clean on the merged tree). Acceptance re-run verbatim from the clean merged tree, all exit 0: deeds/deed_i18n/snapshots 164, architecture+S3 53, hud_perf_budget 28 with the baseline byte-untouched, the 17 surface suites 324, parity corpus + W0 pins 388; npm run gate UNPIPED PASS all 9 steps (949 files, 11858 tests, tsc, all builds), re-run PASS on the final committed tree. Browser walkthrough 10/10 recorded PASS (server freshly rebuilt on the merged branch + dev, two REAL clients, A equipping and clearing through the actual Book titles tab): bracketed say/whisper/yell/general/guild on both clients; ja via the real options picker re-renders the pattern around the RAW name; officer chat correctly NOT delivered to a member viewer (the titled officer arm is sim-pinned); the party invite dialog is not automatable headless (channel sim-pinned); context-menu title AND whisper prefill are the RAW name; target frame renders the title inline with frame height 68=68 against a mob; a clear propagates to chat and re-target within one round trip with zero empty brackets; the titled card draws its line and the UNTITLED CARD IS BYTE-IDENTICAL TO A PRE-11 BUILD served from a base-sha worktree (canvas dataURL string equality, offline deterministic char both sides); guild roster + friends titles inside 348px; players AND Renown tabs both render titles in-game; anonymous board fetches byte-identical between API_DISPATCH=new and =legacy servers for all 4 query shapes; /c/ renders the title and a psql-forged content-drifted id renders NOTHING (element absent; the single grep hit is the static CSS rule) with the restore rendering again; layout matrix green at all four widths (the mid-size runs first died to a machine-load software-GL freeze at load average 11; the probe-pattern entry and an idle rerun are green, no product issue). Parity/S3: fromTitle pins cover every channel plus the SocialEvent/SimEvent type-parity pin; W0a/W0b/W0c untouched (211/58/153 hold); the ONE refreshLeaderboard fill feeds both dispatch arms (the byte proof above); the only server English title resolution is sheetTitleText into the English-by-design pages (tree-wide grep; the card OG consumer never landed, deferred by 11). Perf: painter writes all ride the elided setText seam; the target-frame resolve elision proven LIVE by dev-module instrumentation (zero resolves across 4s of held-target painting, exactly +1 per title change in both directions). Mutations x2 decisive, each restored byte-clean: a mob-yell fromTitle stamp reds exactly the mob negative; a setActiveTitle null no-op reds 7 tests across 3 files (chat clear stamp, meta+wire clear, dispatch null echo, second-client re-wire, offline board). Titled/untitled/stale assertions present per surface (stale degradation centralized in deed_i18n plus the profile stale arm). Reviewers: 11's four verdicts re-read with nothing open; a fresh qa-checklist over the final amended diff on the merged tree returned READY 0 blocking / 0 should-fix (its four VERIFY items are exactly this session's browser walk, all green). Adversarial: no surface can show an unchosen title (validated setter + join validator + stale-id collapse, forge-proven); zero tier or governor reads; a 40-char de_DE-length title DOM-probed on the three tight surfaces: the social row ellipsizes inside the 348px window at constant row height 41, the target frame stays single-line h=18, the board cell clips inside its grid column, chat wraps by design, so NO surface breaks first; zero dashes, emoji, or packet vocabulary in the shipped diff (perl scan). E2E recipes for later sessions: the static #deeds-window shell exists before first open so gate on display equals flex, never on display equals none; equipping from the titles tab leaves a window-open state, Escape until the body class clears before typing chat; puppeteer waitForFunction stalls against this software-GL page under machine load, hand-rolled poll loops with raced retried evaluates are the reliable entry |
| 12 | phase-12-chroniclers.md | DONE | 2026-07-10 | thru 6b28b07f4 (f857ebea6 rename + f9c6961f0 look + 26481dfa5 spread + 6b28b07f4 merge-audit fix). Pre-work: origin/release/v0.24.0 tip c9b316dbb merged as c6c3fa634 (28 commits: jail, heroic loot flair + soulbound + shared-personal marks, native Discord auth, password reset + SES, buff no-stack, min-range, low-tier FCT). Conflicts: the 4 generated i18n artifacts (regen + sha256 --write SAME commit, both sides moved the baseline) plus ONE real conflict in awardHeroicMarks, resolved by keeping the release earners/sharedPersonal structure and re-seating the branch ctx.markDeedsDirty beside heroicDaily.marked.add (the heroicMarkCircuit predicate reads that set). npm ci required before tsc went green (new release deps: aws sdk sesv2, capacitor app/browser). release-merge-audit via 6-group fan-out: server/sim-core/client/ui/http CLEAN; sim-loot caught a real cross-side gap (heroic instances drop heroic_ variant ids, base-keyed col_set_* deeds never credit) fixed in markItemDiscovered via def.heroicOf credit (bounded depth 1, rng-free; drop + rejoin test arms; join seed funnels through it so held variants retro-heal); 2 release-inherited INFO notes recorded no-action (password routes have no db-free parity fixture by principled delegate-only choice; a parity.test.ts comment overstates the discord deviation mask); the recurring db-mock trap CLEARED (moderation_game mock keys traced, deeds_db/steam mirror import only pool, 185 targeted tests green on the merged tree). Implement: rename (zone3 name + id-retention comment, 2 chapter descs, catalog names + Home lines; DELIBERATE sha256 rebaseline same commit; equivalence 5/5; wiki regen diffless), look (npc_chronicler VisualDef: mage.glb, Mage_Hat, staff, spellbook_open offhand via the warlock Spellbook_open gripRef, tint entity 0.55; NPC_KEYS x3; colors Saul 0xd08a2e amber, Osric 0x3fa66b teal, Zenzie 0x5a6fd6 indigo; unit pins incl distinct-and-off-reserved), spread (Saul {15,-16} f2.4 nn 14.9, Osric {-14,303} f-1.4 nn 9.2, Zenzie {2,643} f3.1 nn 13.2; all ZERO findSafePos drift at seed 20061; PACKET DEVIATION: the specced Osric spot {-14,306} sits inside the west-building collider band z305-309 and silently relocates ~4 units, the documented Petra Vell hazard, so he stands 3 south of spec). Verification: offline browser tour tmp/deeds_12_tour.mjs ALL GREEN (each hub: NPC click opens the Book at the Chronicles category, visited mark lands live; nine CONSECUTIVE Saul talks earn hid_saul_footnote, talks at other chroniclers reset the streak; recipe: left-click talks only within INTERACT_RANGE+2=7, out of range it silently targets); screenshots tmp/deeds_12 for session 13 (shared scholar silhouette unmistakable vs villager crowds; hues read amber/blue/violet; hats stay dark by multiplicative-tint design, identity rides bands/robes). PACKET PREMISE FLAGGED: no overworld night exists (fixed SUN_DIR + static day HDRI; only the Yumi maze override is night-lit), so the day-and-night silhouette check reduces to day. Acceptance: packet vitest list 318 green; Edda Hartwell grep ZERO in src/server/tests (the guide's only Edda is Edda Reedhand the delve companion); npm run gate UNPIPED PASS exit 0. Adversarial 5/5: pre-rename fixture probe round-trips the visited mark and the earned footnote with its date (the Saul streak counter is session-scoped, never persisted); the five non-Latin overlays stay stale BY DESIGN, session 14 spec owns the fix (re-verified); ctor ids def-order stable, parity goldens green WITHOUT regeneration; walkability proven in-browser plus findPlayerPath 0.00 gap to all three; wiki emits no chronicler name. Reviewers: architecture-reviewer CLEAN 0/0 (ran parity itself, 188 green), qa-checklist READY 0/0 (its 4 VERIFY items are the gate/browser runs this session executed; heroic-fix over-credit checked and refuted: a variant keeps its base quality and markVisited is idempotent) |
| 12Q | phase-12-qa.md | DONE | 2026-07-10 | PASS thru d57522a26 (one code change: the missing rename pins, test-only). Pre-work: origin/release/v0.24.0 tip 083856d8c merged as 318b11b0f (6 commits: mobile joystick autorun lock #1724); conflicts only the generated i18n artifacts; the sha256-conflict-marker trap FIRED AGAIN and was caught post-commit (i18n:gen does not rewrite the baseline; resolved_hash --write + equivalence 5/5, amended into the merge). release-merge-audit via 5-agent fan-out CLEAN: all overlaps BOTH SIDES intact (release autorun latch + branch deeds keybind and mobile button; the removed onAutorun seam fully re-bound through the death_input_reset injection at main.ts; zero server/wire surfaces; db-mock trap clear); inherited upstream staleness recorded (scripts still poking the removed mobile-autorun button, byte-identical to the release parent, upstream-owned). Acceptance re-run verbatim from the clean tree all exit 0 (deeds quintet 253, architecture+S3 53, equivalence 5, visual_manifest 7, Edda Hartwell grep ZERO in src/server/tests); npm run gate UNPIPED PASS all 9 steps (11949 tests + tsc + all builds). Blast-radius: whole-tree grep hits only packet docs (chronicles.md keeps ONE deliberate historical rename note) + the five stale overlays, and the 14 file names exactly those five (ja_JP, ko_KR, ru_RU, zh_CN, zh_TW); template id keyed unchanged at NPC_KEYS, the zone3 def, CHRONICLER_TEMPLATE_IDS, NPC_IDS; resolved English = Chronicler Zenzie; commit graph confirms f857ebea6 carried resolved regen + status + sha256 SAME commit. Pre-12 fixture probe (temp, run then deleted): visited mark + earned footnote/chapter dates survive load AND serialize, the join drain re-fires NOTHING, renown recomputed; the Saul streak counter is session-scoped by design so the persisted surface is exactly mark + earned record. FINDING FIXED: the QA file premise (the rename has a pin) was FALSE, zero tests pinned Zenzie; added the catalog pin (both chr_peaks chapter descs contain Zenzie's chronicle + an all-deed negative sweep on the FULL old name so Edda Reedhand cannot false-match) and the NPC-name pin (NPCS.chronicler_edda_hartwell.name toBe Chronicler Zenzie). Mutations x3 all decisive, each restored byte-clean: NPC_KEYS row drop reds visualKeyFor, a name revert reds the name pin, a desc revert reds the desc pin. Browser re-run on a fresh character (offline :5199, tour + probe): all three hubs interact, Book opens at Chronicles, visited marks land live; Saul nine consecutive talks earn hid_saul_footnote; positions EXACT at spec with zero findSafePos drift (Saul {15,-16} f2.4 nn14.9, Osric {-14,303} f-1.4 nn9.2 with the documented z306-collider deviation standing, Zenzie {2,643} f3.1 nn13.2), all grounded; target frame renders Chronicler Zenzie (out-of-range left-click silently targets, the range-7 recipe); ja via ensureLocaleLoaded renders the STALE old transliteration then English round-trips (expected until 14, recorded); day-only screenshots tmp/deeds_12qa (the no-overworld-night premise re-confirmed). No other NPC moved, proven two ways: the authored zone diff vs c6c3fa634 is exactly the three chronicler defs, and all 35 non-chronicler resolved placements are byte-identical base-vs-HEAD at seed 20061 (detached-worktree probe, stronger than the two-per-hub spot check). Determinism: two fresh worlds identical at seeds 20061 and 7 (3 chroniclers + 24 neighbor rows each); parity 164 green, goldens untouched by 12 (verified against the commit range). Adversarial: zero prose, quest, letter, gossip, or guide references to the old name or old spots; zero chronicler references in scripts/ or tests/browser so no scripted walk paths through the moved spots; the one-line .glb swap note present at manifest.ts; hygiene scan clean on added lines and commit messages. Reviewers: 12's two verdicts re-read with nothing open; fresh qa-checklist over the post-12 delta READY 0 blocking / 0 should-fix / 1 VERIFY (the auto-merged hud.mobile.css is byte-identical to release HEAD and covered by the release branch's own CSS gates; its informational note = the known 14-owned stale fills) |
| 13 | phase-13-docs-screenshots.md | DONE | 2026-07-10 | thru 8682bf78d + review fixes (e5930ee94 docs layer + 46fbc83bc harness + 5121f2cf1 evidence + 8682bf78d and the follow-up findings commit). Pre-work: origin/release/v0.24.0 tip eae9bb63b merged as 77cde32b4 (5 commits: native Apple sign-in #1753, /playtime lifetime readout #1739); conflicts = the 4 generated i18n artifacts + the chat_social parity golden, regenerated on the merged tree (i18n:gen + deliberate UPDATE_PARITY, only that golden moved); the sha256-marker trap fired a THIRD time and was fixed by resolved_hash --write + equivalence 5/5, amended into the merge; 5-agent release-merge-audit CLEAN (all overlaps both-sides intact; /playtime replies via ctx.error so the 11 speakerTitle emit-site pins hold; totalPlayedSeconds is a distinct JSONB key beside the deeds fields; the release ledgered its own apple-auth routes; tsc + completeness + inventory suites green on the merged tree; zero packet-premise damage). Slice: docs/design/deeds.md (vocabulary, architecture, 10 binding rules, add-a-deed recipe, deferred set + why) + docs/CLAUDE.md index; root CLAUDE.md every-conquerable-content rule + repo-map row; README Highlights/World-and-systems/HUD parenthetical/layout row at marketing altitude; the five area CLAUDE.md touchpoints; scripts/deeds_screenshots.mjs (offline REAL-PLAY capture: soc_meet_bursar + nine-talk Saul streak earns on camera, title equipped through the actual Titles pane, no dev commands; per-tier raw-CDP metric flips with clip capture, because a puppeteer setViewport mobile flip RELOADS the page and drops the live world) + 28 frames x 5 viewports + index README committed under docs/achievements/screenshots/ (force-added past the blanket screenshots/ ignore rule, the docs/screenshots precedent; PNGs stamp game tree 77cde32b4, later commits are docs-only). Wiki sweep clean: zero chronicler names baked, six 09 deeds rowed, hidden ids absent, guide test green (regen diffless). Acceptance: guide + architecture 63 green; npm run gate UNPIPED PASS all 9 steps (11965 tests + tsc + all builds), rerun after the review fixes. Reviewers, ALL findings applied: qa-checklist READY 1 should-fix + 2 nits (the deeds bullet had DISPLACED the live player_context_menu.ts bullet, restored; broadcast fan-out reattributed to game.ts with deeds_records keeping upsert + isMarqueeDeed + Steam hook; evaluator phrasing now names the 1 Hz proximity sweep beside dirty-only grant evaluation); fresh drift reviewer 2 should-fix + 2 nits (DEED_ORDER is DERIVED from table order so the recipe now says append at the table END, dropping the false QUEST_ORDER analogy; the counter callback is bumpDeedStat, markDeedsDirty only flags; border wording; sweep dup). Adversarial answers in the session summary |
| 13Q | phase-13-qa.md | DONE | 2026-07-10 | PASS thru 3aebcfcf0. Pre-work: origin/release/v0.24.0 merged ab04c265b (19 commits: enchanting profession #1712 + wire-aura alloc fix #1746); conflicts = items.ts (hand-resolve keeps the release equipmentInstance bookkeeping AND the branch markDeedsDirty in equip/unequip) + the generated i18n artifacts + 49 parity goldens (deliberate UPDATE_PARITY regen on the merged tree); the sha256 trap fired a FOURTH time (npm run i18n:hash only PRINTS, resolved_hash --write + equivalence 5/5, amended into the merge). 7-agent release-merge-audit: every overlap BOTH-SIDES-INTACT (tsc rules out recalcPlayerStats 5th-param drift across all 24 call sites; db-mock trap CLEAR: the release tests mock no server/db and deeds persistence imports only pool; no new routes or IWorld members; goldens pin the empty equipmentInstance shape; the release addItemInstance routes through markItemDiscovered so enchanted copies feed discovery). TWO REAL FINDINGS FIXED test-first (f4fe8ffad): (1) retroFallbackGrants read ANY positive craftSkill as proof of first craft, and enchanting skill (flat +1 per disenchant/apply-enchant, zero recipes) would permanently misgrant prog_first_craft on join to a disenchant-only character; the inference now excludes the enchanting key (negative + mixed-arm pins). (2) Both enchanting skill-gain sites lacked ctx.markDeedsDirty (the crafting.ts contract) so the craftSkill deeds would not evaluate on the crossing tick; both sites fixed, and the site tests stage PRE-DISCOVERED reagents because first-discovery dirty marks mask the gap (naive staging passed on the broken code). Merge-staled docs fixed (07734649a): deeds.md ringwright deferral re-grounded (enchanting ships ENCHANTS + sim skill gain but zero player-facing wiring; jewelcrafting/inscription keep zero recipes), catalog README resolution 21 appended (all deferrals STAND; collection.md rejection premise superseded, conclusion stands), icon-brief mechanism named (PR-body line). Acceptance re-run from the clean merged tree all green (guide+architecture 63; deed suites + enchanting + parity 218; equivalence 5); npm run gate UNPIPED PASS twice, 11993 tests, final on the committed tree. Doc audit: the 5 named checks PASS and the ten-claim audit ALL MATCH (trigger vocab = the 17-kind union both directions; deferral ids absent from DEED_ORDER; root rule literal-scope complete; README marketing altitude, classic only; server/CLAUDE.md persistence keys real; keybind Z; six UI modules + UI_PURE_CORES; evaluator tick-tail dirty-only + 1 Hz sweep; character_deeds DDL + FIFO observer; deeds_board floor 50 + tie-break; TTL caches; IWorldDeeds facet; structural wiki hidden filter; icon pipeline; DEEDS_ERA + Steam cap 100). Routing walkthrough (fresh agent, root CLAUDE.md only, packet dir forbidden): deeds-in-same-change appears 4x on the routed path, wiki regen 4x, icon flag routed; all three required elements present; two pre-existing non-deeds routing gaps recorded, not fixed (content CLAUDE.md never routes parity goldens for content adds; dungeon-interior render routing implicit). Screenshot audit: ALL 28 opened by fresh viewers, index 1:1 + sha rows verified, script re-run = identical set no orphans, raw-CDP confirmed in source. REAL EVIDENCE DEFECT FOUND: puppeteer page.screenshot re-asserts its managed defaultViewport, silently reverting the raw-CDP tier override mid-run, so 18 of 28 originally committed frames were top-left crops of a re-asserted stale layout (tracker/chat/player entirely outside the clip on narrow tiers); root-caused with before/after probes bracketing the screenshot call. Harness hardened (c56b70e09, 0e8b323af, 3a8fd350e, 7b29455de, a9c9198e3): defaultViewport null + ALL tiers via raw CDP + re-assert after entry (the entry flow drops the override once per run) + cursor parking (hover-tooltip photobomb) + own-plate separation from Saul + showOwnNameplate seeded (a real Options toggle, default false: the 07 frames had NEVER shown the own plate, including the original set) + phone chat opened through its REAL pointer-bound button (touchscreen tap; synthetic click is a no-op; in-run check added) + bursar talk retry (one run flaked on the single projected click). Full set recaptured with real earns on the merged tree; EVERY frame re-verified 28/28 PASS on all six checks (soft caveats recorded: the earned social row sits below the fold, carried by the recent-strip crests + counter; a cosmetic internal chat scroll half-line at 1024/1280). Hygiene: dash/emoji scans clean on every touched shipped file; the step-5 phase grep hits blame entirely to upstream release commits (tick-phase order, perf phases, an api-pipeline doc path), zero from this branch; AGENTS/GEMINI thin pointers unchanged. Reviewers: 13's verdicts re-read, nothing open; FRESH qa-checklist over the amended diff (10+ claims spot-verified) returned exactly 1 should-fix, the same enchanting staleness, applied; its biome VERIFY clean. Adversarial: doc-surface omissions judged correct (server/http/CLAUDE.md is pipeline-level, no deeds PRD by design, the PR template carries no per-feature checklist since the root rule + pinning test enforce); no cosmetic-only or fairness contradiction; screenshots honest (real earns, teleports position-only, the one seeded setting named in the harness comment) |
| 14 | phase-14-catalog-recheck.md | DONE | 2026-07-10 | thru 3ce0a320f (docs-only, 1 file: catalog/README.md resolutions 22-25). Pre-work merge no-op: release tip 69150d670 already an ancestor at session start (ab04c265b had brought it), so the audited range is exactly the five round merges (ab0d9745c, c6c3fa634, 318b11b0f, 77cde32b4, ab04c265b) = release span 5c4628dc2..69150d670, 15 PRs + 2 direct commits, all named in resolution 22; cf57f4b0a (#1689 mobile HUD) landed upstream MID-SESSION after the audited tip and is recorded as deferred to the next pre-work merge (post-tip tail has zero sim/server files). ZERO-ADDITIONS verdict: live set unchanged at 192 deeds / 2,365 Renown / 19 titles / 3 borders / 72 Steam (scripted recount matches resolution 14 exactly); DEED_ORDER untouched; no icon-brief change (zero new ids). Resolution 23 rejections: jail #1735 (R6: moderation.act-gated punishment; brawl has no match object, kills credit nothing, deaths revive full-HP), /playtime #1739 (R6: input-blind attendance, lastActiveTick never consulted), heroic epics/variants #1705 (R2 luck; soulbound writes no counter; marks = delivery-shape for outcomes dgn_ already awards), auth cluster #1748/#1753/667227aa0 (zero sim footprint), QoL cluster x10 (heroic-equalization precedent; damage.ts deed hooks untouched by #1707/#1711; the mail deed counts sends not quantities so #1695 is inert), the three arcane materials (only source is unwired disenchant, col_discovery_250 pool unchanged at 263), and the worklist's queued-cast + warlock resummon recorded NOT IN RANGE (both sweeps empty). Resolution 24: jail-brawl combat-ledger interaction ACCEPTED with duel-parity reasoning (ledger counts consensual PvP by design; jail farming strictly dominated by free duels; deaths feeds only cmb_first_fall count 1; jail revive is reviveAt sickness=false at spirit.ts so hid_keepers_toll_twice unfarmable; MAINTAINER FLAG: any future zero-stakes-PvP ledger exclusion must include jailed-vs-jailed). Resolution 25 deferral rechecks all STAND: ringwright (recipes.ts 15 recipes, jewelcrafting/inscription 0; ENCHANTS is reagent data, not a recipe source), salvage pair (salvageItem zero callers; salvagesPerformed compliantly absent), nine account ids (deeds_records.ts observer-only), enchanting (resolution 21 re-verified; both 13Q evaluator fixes hold), Steam 72/100; forward note: enchanting skill eases prog_craft_specialist/prog_around_the_ring when wired, easing-only. Method: 9 parallel verifiers + adversarial coverage critic (the critic surfaced the jail-ledger interaction and confirmed the six resolution-14 deeds kept live producers through the merges). Acceptance green: deeds quartet + steam map pins (140), architecture + S3 + guide freshness (92+3 skipped), gate PASS unpiped (11993, all 9 steps). qa-checklist over the docs diff: READY, 0 blocking, 1 should-fix (#1689 mis-listed in range, FIXED pre-commit) + 1 VERIFY (jail revive sickness flag) confirmed in code same session. Adversarial: range re-derived at end (five merges, unchanged), no new deed so no attempt/punishment/RNG/attendance exposure, DEED_ORDER byte-identical, every rejection lives in the dated resolutions, diff clean of dashes/emojis/packet vocabulary |
| 14Q | phase-14-qa.md | DONE | 2026-07-10 | PASS. Pre-work: origin/release/v0.24.0 tip cf57f4b0a (PR #1689 mobile HUD, the one merge resolution 22 deferred to this session) merged as b08b1bebd; no source conflicts, but the sha256 trap fired a FIFTH time (i18n:gen leaves the baseline stale after both sides move catalog keys; resolved_hash --write + equivalence 5/5, amended into the merge). release-merge-audit via 6-agent fan-out: four overlap groups clean, TWO REAL INTERACTIONS FIXED test-first: (1) b88576f95 hud.ts: the release keeps the closed More tray display:flex (opacity/visibility carry the transition) but excluded it only in syncAnyWindowOpenState, so isWindowVisible read the closed tray as open, topmostOpenWindow picked it at z 100, and closeAll stamped an inline display:none no class toggle ever clears: tray dead until reload (the ONLY mobile entry to the Book of Deeds), every mobile Escape swallowed before the options menu, isWindowOpen pinned true for gamepads; visibility now keys on the mobile-more-open body class (the social-window arm pattern) and closeManagedWindow closes the tray through its class mechanism like its own X button; client_shell slice-pins added, red-then-green. (2) 832ba54de hud.mobile.css: the release's new generic bottom safe-area padding double-counted inside the inset-pinned #deeds-window, squeezing the max-height 480 filter-bar budget; padding restored to var(--window-pad) with a deeds_window pin. Zero new routes, zero db-mock sites, signature audit clean (pinchZoomDelta reshape has no branch caller on the old shape). Acceptance re-run verbatim from the clean merged tree all exit 0 (deeds quartet 131, architecture+S3 53+3 skipped, guide 39, steam map 9); scripted recount matches resolution 22 EXACTLY (192 deeds with the full category split, 2365 Renown, scale-only values, 19 titles, 3 distinct borders, 72 Steam); deeds.ts untouched since 13Q so DEED_ORDER byte-identity holds by construction; wiki:content diffless on the committed tree (183 public, 9 hidden filtered); deed_i18n manifest 403 = 192x2+19 pinned; no overlay edits anywhere in the 14 diff (docs-only, verified). Range completeness proven independently: the merge list re-derived from scratch (first-parent merges + per-merge second-parent enumeration) matches resolution 22 exactly, and #1689 is absorbed here as resolution 26 (zero sim/server/world_api files; QoL rejection class; zero additions re-affirmed by recount). 10-agent verification fan-out over every rejection, deferral, and producer, all CONFIRMED: jail R6 (moderation.act gated on every entry command, brawl kills credit nothing, jail revive sickness=false, JAILED_BLOCKED_COMMANDS covers arena/vcup/dungeon/crypt/delve/duel; lockout is server-side, noted in resolution 28), playtime R6 (accrual input-blind, lastActiveTick never consulted, no deed reads totalPlayedSeconds), heroic R2 (variants only enter via Rng-rolled drops, soulbound writes no counter, the four damage.ts deed hooks byte-untouched by #1707/#1711, marks circuit intact), auth cluster diff-tree clean, QoL cluster x10 (mail deed bumps once per send at post_office.ts regardless of #1695 parcel quantity), deferrals all STAND live at HEAD (recipes.ts 15 with jewelcrafting/inscription 0, salvageItem zero callers, deeds_records observer-only, enchanting wiring absent on every host with both 13Q evaluator fixes holding), all six 09 deeds have live producers and sit at DEED_ORDER tail 187-192. Adversarial critic: wayfarer triggers are PINNED markIds so no dynamic widening is possible, the only release zone3 change is quietMechanics, no unjudged PlayerMeta/CharacterState/SimContext surface in the range, jail.ts ships no mobs/POIs/items/quests. ONE standing-record correction, recorded as resolution 27 with MAINTAINER FLAG: resolution 13's col_discovery_250 luck-free construction (253+10=263) overcounts attainability (12 counted defs are source-less at this tree, 8 more are swag-only; the strictly deterministic floor lands at about the 250 threshold with little or no headroom); predates the audited range, the no-shrink verdict stands, the deed stays comfortably earnable (rares/epics/variants/market all feed the meter). Minor register = resolution 28 (keepers-toll import nuance, dev_teleport cage body, mailAttachmentsSent naming hazard, server-side lockout location). Reviewers: 14's qa-checklist verdicts re-read with nothing open; FRESH qa-checklist over the 14 docs diff returned READY 0 blocking / 0 should-fix / 1 VERIFY (the exact 263 pool figure, resolved by this session's recount into resolution 27); its 9 factual spot-checks all PASS and the README append verified pure-append with zero deletion lines. npm run gate UNPIPED PASS on the final committed tree. Hygiene: zero dashes, emojis, or packet vocabulary in the shipped diff and commit messages |
| 15 | phase-15-wiki-audit.md | DONE | 2026-07-10 | thru 8442c5e4d + the closing nit/docs commit. Pre-work merge NO-OP (origin/release/v0.24.0 tip cf57f4b0a already an ancestor via b08b1bebd, fetch brought nothing; no release-merge-audit needed). Audit matrix all 8 items run. Set equality PASS by executed-module script: 183 guide rows = the 183 public of 192 in DEED_ORDER order, per-category split matches resolution 22 exactly, name/category/renown/feat/rewardTitle/rewardBorder field-exact, 9 hidden absent, field allowlist holds. Freshness: wiki:content diffless pre and post. Spoiler safety re-proven from scratch: 9 hidden defs enumerated from sim source, zero id/name/desc hits in content.generated.ts, the rendered DOM on both viewports, and the guide-OWNED built chunk; every deed desc unbaked; 13 boss-flagged names enumerated across ALL content tables (zone1/2/3, dungeons, temple, delves; the MOBS-only test guard sees just 7 of them), zero baked; chronicler names Osric Fenn/Zenzie/Edda Hartwell zero in every baked artifact, Saul prose-only sanctioned. Echo re-inspection: ZERO new epithet echoes from the 09/14 additions (the six new names carry no boss or mechanic token); the standing three re-affirmed; the aggressive token scan classified the rest benign (public zone geography, public dungeon/delve instance names, Boundstone-class substring noise); 15 public deed ids carry boss given-name fragments at ID level, baked-never-rendered since v1, recorded. FINDING RECORDED, MAINTAINER FLAG, no code change: dist/guide.html modulepreloads the shared chunk (icons-*.js) carrying the game client's full English content tables (all deed descs, hidden deed names, boss display names) because the i18n catalog's entity-English assembly (world_entity_i18n.ts) value-imports sim/data; proven PRE-EXISTING by a detached-worktree build at the 6Q tip 1ca089d36 (identical exposure in its icons-50jUSzXd.js), so 6Q's built-chunk-clean claim held only for the guide-owned chunk (clean then and now); the wiki renders none of it and identical bytes ship to every game client (hidden-deed secrecy is presentation-level everywhere by design: the in-game Book also ships hidden defs and filters at render); full severance means splitting entity-English out of the runtime catalog graph, an i18n-architecture change out of guide-layer scope (a one-line concrete-import swap of ITEM_SETS was tried and reverted: world_entity_i18n's own barrel import makes it a no-op); flag: a follow-up chore making the entries consume the catalog at build time only would close it and shrink the wiki bundle substantially. Fixes landed (8442c5e4d): the controls reference gains the missing Z row (its Interface table listed every other window bind) with new key guide.controls.deeds x5 non-Latin fills (the locked Book of Deeds glossary terms) and a test pinning the rendered row against the real BIND_ACTIONS deeds default KeyZ so a shipped-default rebind reds the guide; the dungeons page related links cross-link the deeds catalog (pinned); howBody reword teaches the watch tracker and rewardsBody broadens title display to nameplate/chat/boards (now matching README and docs/design/deeds.md); resolved regen + sha256 re-baseline SAME commit (registry accounting verified: +15 pending Latin locales, +6 translated). STALE FILLS FOR 16: the howBody and rewardsBody rewords stale their five non-Latin fills and every other locale, and the registry re-stamps srcHash keeping them translated so NO gate flags it (the reword-staleness lane); 16 must re-translate guide.deedsPage.howBody and guide.deedsPage.rewardsBody. Judgments recorded, 16 and future contributors inherit: icon-art omission RIGHT (the wiki teaches the system, never window presentation; no guide page describes window chrome); deeds-page prose names no keybinds (the controls page owns key glyphs); the leaderboard was already covered by standingsNote; the home page correctly carries no deeds card (its three pillars are marketing-level with no system enumeration); cross-links: dungeons backlink ADDED (clear/heroic deeds are the strongest overlap), world and professions DECLINED (world already at five links with a thinner tie; the professions tie is a handful of prog_ craft deeds); the 06 precedents stand (no per-deed listings on other pages, no per-deed search indexing, criteria stay in-game); renownBody's luck sentence verified live (16 public 0-renown non-feat deeds exist); featsBody's world-firsts wording is the shelf definition per the design doc (live feats today are 3 legacy/era entries). Consistency: the wiki, docs/design/deeds.md, and README share the six-term vocabulary with zero baked totals anywhere. Browser pass on the committed tree, desktop 1600x900 + phone portrait 390x844, ALL PASS: h1, 183 rows, 10 sections, both new prose lines render, hidden/boss/chronicler names absent from the DOM, no horizontal scroll, controls Z row, dungeons cross-link, nav link, route-derived search resolves /wiki/deeds, SPA nav without reload, zero console errors beyond the backend-absent /api noise. Acceptance: guide+architecture+S3 vitest green (94+3 skipped), equivalence + status-registry guards green post-commit (they git-diff HEAD so they red on an uncommitted-but-correct tree, the staged-artifacts lesson), npm run gate UNPIPED PASS all 9 steps (12005 tests) with the final re-run on the exact committed tree. Reviewers: qa-checklist READY 0 blocking / 0 should-fix, 3 VERIFY (full gate + browser pass, both executed this session; the reword-staleness, recorded above) + 2 nits (redundant as-never cast APPLIED; the stale root-doc claim that i18n.status.json is gitignored recorded as the known upstream-owned 4Q item); fresh independent spoiler auditor PASS with ZERO new findings (its own enumeration matched: 9 hidden ids, 13 boss names; every wiki-owned artifact clean incl the viewer closure chunks, css, sitemaps, llms.txt, robots, stills, and the runtime search; the shared icons-*.js confinement proven two ways, manifest closure + full-build sweep showing the other dirty chunks are game-only and unreachable from /wiki; no public deed name contains a full boss display name; Edda Hartwell fully retired from src, dist, and public). Hygiene: zero dashes, emojis, or packet vocabulary in the shipped diff and commit messages |
| 15Q | phase-15-qa.md | NOT STARTED | | |
| 16 | phase-16-translations.md | NOT STARTED | | |
| 16Q | phase-16-qa.md | NOT STARTED | | |

Cleanup policy (revised 2026-07-09): the durable parts of this directory
(overview.md, catalog/, progress.md, screenshots/) are KEPT permanently; the
polish round's phase files are deleted in the phase-16-qa exit step. The
memory file `achievements-system-design.md` remains the record of maintainer
calls.
