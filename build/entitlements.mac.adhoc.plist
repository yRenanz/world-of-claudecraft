<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for LOCAL AD-HOC-SIGNED test builds only (mac.identity "-",
  selected by scripts/electron-build.mjs when no CSC_LINK/CSC_NAME is set;
  production Developer ID builds use entitlements.mac.plist, which drops
  disable-library-validation). The extra entitlement is required here because
  ad-hoc signatures carry no team ID, so with hardened runtime + library
  validation the nested Electron frameworks would fail to load and the app
  would not launch (electron-builder warns about exactly this combination).
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
  </dict>
</plist>
