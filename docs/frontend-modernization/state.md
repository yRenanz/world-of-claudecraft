# Frontend Modernization v0.16.0: State (cross-phase cheat sheet)

The single reference every phase loads first. Holds locked decisions, the canonical workflow
blocks, the validation matrix, and the running ledger of what each phase adds. Update this at the
end of every phase (Step 6).

Branch: `feature/frontend-modernization-v016` (worktree `/Users/fernando/Documents/wocc-v0.16.0`),
branched off `origin/release/v0.16.0` (`e31eb05d`).
Current phase: P17b (bundle + lazy-load + cross-engine + close) DONE 2026-06-26 as a MEASURE-AND-DECLINE.
The play-entry initial (eager) JS bundle was measured precisely (3,327,191 B / 3.173 MiB raw, ~952 KB gzip;
~1.5 MiB of it is i18n DATA, not deferrable code), and a fully-proven selective lazy-load prototype (options +
market behind an aria-busy/role=status loading state) shrank it by only -50,688 B (~49.5 KiB raw / ~14 KiB
gzipped / ~1.5% of the play bundle) with ZERO runtime/FPS impact (sim/renderer/per-frame HUD untouched; this is
load-timing only). The maintainer judged the saving too small to justify the lazy-load complexity + the per-window
first-open fetch, so decision 13's selective lazy-load + the JS bundle-budget CI gate were DECLINED and the
prototype REVERTED IN FULL (tree clean, no source/test/config committed; the measurement is the deliverable, see
the progress.md P17b row + the [[frontend-v016-phase17b-bundle-lazy-declined]] memory). Decision 14's cross-engine + axe CI matrix
(Chromium/Firefox/WebKit + mobile WebKit as a first-class instance, 140 browser tests green, browsers provisioned
via npx playwright install, bare vitest run still browser-free) was prototyped + verified but reverted with it; it
remains an OPTIONAL small standalone re-land (a BROWSER_MATRIX gate in vitest.browser.config.ts + one ci.yml job).
PACKET STATUS: the extraction + per-frame + a11y + standing-harness work (P0-P17a) is IMPLEMENTATION-COMPLETE; of
the two P17b close-out infra items, decision 13 (bundle lazy-load) is DECLINED-ON-EVIDENCE and decision 14
(cross-engine WebKit-in-CI) is OPEN/optional. The packet-authoring history is kept below for provenance.

PACKET AUTHORED + AMENDED + fidelity-reviewed + DEEP-REVIEWED-AND-RESTRUCTURED
(2026-06-24). A 23-reviewer + synthesis Workflow ground-truthed every phase against live source and
found the architecture sound and the ordering correct, but flagged: two BLOCKING spec errors (P5, P6),
a set of stale line-refs (P0/P1/P3/P4/P11/P13), the WCAG-2.2-AA + no-magic-values rows missing from the
cold-window and per-frame phases, and several cross-cutting gaps (ClientWorld-vs-Sim painter parity, a
responsive rendered-layout gate, the write-elision writer coverage gap, admin/guide CSS scope,
`user-scalable=no`). To retire the 40% context risk up front (user directive: "rather take longer and
get it perfect than degrade by context usage"), the 18-phase packet was RESTRUCTURED into 30 phases via
sub-letter splits (see the OLD->NEW map below). No product code has moved. P0 (foundation gates) is
DONE 2026-06-24: the css_corpus + UI-purity guards landed (tests/css_corpus.test.ts,
tests/architecture.test.ts) and the three non-regression floors are recorded
(perf-/visual-/mobile-baseline-v016.md). Surfaced for later phases: perf_tour's mobile profile cannot
boot (portrait viewport + undismissed #mobile-preflight, landscape-only d16a) so mobile perf is deferred
to P17a; mobile_button_size + mobile_joystick_size are RED (pre-existing preflight-dismissal gap) and only
2 of 6 mobile E2E scripts truly assert, all for P4b. NEXT: phase-01-css-lightning-tokens-base.md.

## Provenance (read these once for the why)

This packet is a RESTART of the completed `feature/frontend-modernization` (FB) refactor onto the
much larger `release/v0.16.0`. The decision and its evidence:
- `feasibility-v0.16.0.md`: why we restart (Option B) instead of merging FB forward.
- `v016-restart-direction.md`: the expanded scope (per-frame extraction + per-element perf) and the
  process learnings (smaller phases for the 40% rule; perf-gated acceptance for hot-path work).
- `v016-recon-and-packet.md`: the deep line-number RECON of v0.16.0's frontend. It is the
  line-number source, NOT the authoritative plan: its 16-phase table is SUPERSEDED by the 30-phase
  ledger in THIS file wherever they differ. The phase files elaborate THIS state.md.

FB itself is a read-only SOURCE: ~70% of its artifacts port forward file-for-file (build config,
`src/styles/*.css` shape, the cold-window cores/painters V16 lacks, the pure cores, the guards).
"Restart" = re-run the extraction on the bigger base, reusing FB's files where they fit; it is NOT a
retype-from-scratch. CAUTION (from the deep review): several phase docs were written against FB source
and not fully re-grounded on V16, which is the root of the stale-ref cluster below; trust LIVE source
over an FB carryover whenever they disagree.

---

## OLD -> NEW phase map (the 18 -> 30 restructure, 2026-06-24)

Sub-letter suffixes only; kept-whole phases keep their id 1:1. Splits exist because each half plus its
mandatory QA pass plus in-session remediation of findings exceeds the ~40% Opus-degradation ceiling.

| Old id | New id(s) | Why split |
|---|---|---|
| P0 | P0 | kept whole |
| P1 | P1 | kept whole |
| P2 | P2 | kept whole |
| P3 | P3 | kept whole |
| P4 | P4a (shell) + P4b (mobile + per-entry .extra) | largest single CSS move + two-entry diff; mobile gotchas are a distinct risk set |
| P5 | P5 | kept whole (correctness rewrite, not a budget split) |
| P6 | P6 | kept whole (correctness rewrite, not a budget split) |
| P7 | P7a (talents) + P7b (social + bags) | talents is an interactive mutable-buffer window; +WCAG/+no-magic/+QA pushes the trio over 40% |
| P8 | P8a (options) + P8b (market + char) | options is ~1180 lines / 9 sub-panels; char carries a Three.js preview + a skin-event Math.random |
| P9 | P9a (canvas: map + arena) + P9b (DOM: questlog + spellbook + leaderboard) | canvas painters collide with no-magic-values; the async leaderboard is its own state machine |
| P10 | P10a (xp + swing leak-fix + the PainterHost elided-writer extension) + P10b (unit_frame FAMILY) | the load-bearing family + first perf gate + a11y + the writer extension is the full novelty load |
| P11 | P11a (cast bars) + P11b (target frame) + P11c (party keyed pool) | 2 BLOCKING core-design decisions serialize the slices; the party pool is a high-churn rewrite |
| P12 | P12a (action bar + the allocation-budget spike) + P12b (auras + minimap) | 3 named top-risks + the unresolved alloc spike + a heavy canvas no-magic-values surface |
| P13 | P13a (FCT core + driver wiring) + P13b (FCT pooled painter + migration + perf gate) | net-new infra; split at the unwired-painter boundary |
| P14 | P14a (per-element tier knobs) + P14b (nameplate_view extraction) | the nameplate extraction is a real Three/DOM-entangled extraction, not a thin add-on |
| P15 | P15a (shared a11y infra) + P15b (chrome-wide audit + axe/keyboard tooling) | 5 infra slices + 5 audit-and-FIX slices across ~17 windows + the tooling slice |
| P16 | P16 | kept whole |
| P17 | P17a (harness floor, test-only) + P17b (bundle + lazy-load + cross-engine + close) | the only behavior-affecting source change + 3 new CI gates + the first all-together perf run |

Execution order (the linear "Next:" chain): P0, P1, P2, P3, P4a, P4b, P5, P6, P7a, P7b, P8a, P8b, P9a,
P9b, P10a, P10b, P11a, P11b, P11c, P12a, P12b, P13a, P13b, P14a, P14b, P15a, P15b, P16, P17a, P17b, then
the packet's final QA.

P18 cleanup wave (the leftover nice-to-have / should-have / nit items from P0 to P17b, see
`phase-18-cleanup-overview.md`): P18a, P18b, P18c, P18d, P18e, P18f, then a short whole-wave QA. This is
a separate post-packet wave, NOT part of the 18 to 30 restructure above; it closes the deferrals the
main packet itself recorded, behavior-preserving and presentation-first.

---

## Locked decisions (record once, never re-litigate)

1. Vanilla HTML/CSS/TS. No Svelte/React/Tailwind/Lit/signals. One build-time dependency added:
   Lightning CSS (devDependency). The per-frame HUD stays framework-free.
2. This packet supersedes the FB packet for v0.16.0. It is the only frontend plan going forward.
3. The per-frame HUD stays framework-free with a hard perf gate; imperative DOM writes go through
   the existing write-elision cache (`hotWriteCache` + `setText`/`setDisplay`/`setTransform`/
   `setWidth`, `hud.ts:1322-1372`) reading `IWorld`. No reactivity, no Shadow DOM, no signals.
4. HUD cold-window extraction is PRESENTATION-ONLY: consume V16's already-extended `IWorld`; the
   only signature consumed that changed is `leaderboard(): Promise<LeaderboardPage>` (one painter,
   P9b). Do not extend `IWorld` or touch `src/sim`/`server`/`src/net`/`headless`. If a phase finds
   it needs to, STOP and surface it (scope change).
5. Per-frame extraction uses the same Humble Object pattern (pure core from `IWorld` + thin painter)
   but HOT path: the pure core is allocation-light (no per-frame garbage); the painter preserves
   write-elision (DOM write only on change, routed through the host's elided writers); every
   per-frame phase carries a frame-budget perf gate, not just tsc + tests.
   - 5a WRITE-ELISION WRITER COVERAGE (added by the deep review). The four existing elided writers
     (`setText`/`setDisplay`/`setTransform`/`setWidth`) cache one string per element and CANNOT
     express `setProperty('--var', v)`, `classList.toggle`, or `style.color` writes that the hot
     paths need (xp `--xp-fill`, `.rested`, target `style.color`, elite/channel/party class toggles).
     P6 exposes the four existing writers as the write-elision facet; P10a EXTENDS the facet with
     elided `setStyleProp(el, prop, val)` + `toggleClass(el, cls, on)` keyed per `(element, prop)`.
     Until that lands, the "all writes elided" rule and the "no raw style/textContent on the hot path"
     routing test are mutually unsatisfiable, so the routing test asserts only the writers that exist
     and documents any allowed raw write.
6. Graphics-tier UI is driven from the STATIC preset (`graphicsPresetLabel`), NEVER the FPS
   governor (two-controller hazard; the `ui` gfx bucket stays `governable:false`). `data-fx-level`
   + `--fx-*` are INTERNAL (no `t()`). The resolver module lives in `src/game/` (a render-importable
   leaf), NOT `src/ui/`, because `src/render/gfx.ts` imports the shared `EFFECTS_QUALITY_LOW_CUTOFF`
   from it and render must not import `ui` (see decision 8 and the P5 fix).
7. Encapsulation is a CSS problem: `@layer` + `#id`-prefix isolation (the `@scope` future-layer is
   deferred on the browser floor, as in FB). `src/styles/shell.css` is NEW in V16 (FB had no single
   `shell.css`; its shell rules were distributed); it gets a layer assignment in P1, not a port of a
   non-existent FB file.
8. The design decisions settled as defaults (revisit only at the named phase):
   - 'advanced' graphics preset -> HUD fx: HONOR its `effectsQuality` slider for a distinct HUD-fx
     level (not collapse to 'high'), so the expert path sheds HUD cost independently. (P5)
   - ui_effects_profile API is the FULL 5-axis FB contract (`tier` / `motion` / `heavyShadows` /
     `ambientAnim` / `allowFctCrit`) + `uiEffectsTokens` (with the 0.001-not-0 motion-scale floor) +
     `uiEffectsProfilesEqual` + `uiEffectsAllowFctCrit`, NOT the `{fxLevel, tokens}` shorthand. The
     resolver DEFINES `EFFECTS_QUALITY_LOW_CUTOFF = 0.5` and `gfx.ts:308` is refactored to import it
     (the constant does NOT exist in V16 today; `gfx.ts:308` is a bare `0.5`). (P5)
   - The resolver also reads `reducedMotion = OS matchMedia('(prefers-reduced-motion)') OR
     settings('reduceMotion')` with a change listener, debounces the `effectsQuality` apply ~180ms,
     and gates the apply on `uiEffectsProfilesEqual` so a no-op never re-stamps `data-fx-level`. (P5)
   - PainterHost: a THIN shared host the already-tested bespoke windows COMPOSE into, factored into
     TWO facets: a presentation dep-bag (icon/money/tooltip) for cold windows, and a write-elision
     facet (the four `private` Hud writers bound as closures, per the vendor template, no visibility
     change) for hot painters. The delve pilot proves the core-to-painter split + dep-bag, NOT the
     DOM write path (it is Canvas-2D); a tiny unit test exercises the elided writers against a fake
     cache. (P6)
   - FCT per-frame driver: fold into `hud.update()` so the existing `hud` perf bucket covers it,
     not a second rAF. (P13a)
   - Every tier knob reads the static preset, never `governor.state().levels`. (P5/P14a)
9. COMPONENT CONTRACT (every extracted component). Pure view-core (DOM/Three-free, Node-tested,
   allocation-light if hot) + thin write-elided painter + INSTANCE-PARAMETERIZED for reuse and
   multiplicity (no hardcoded element ids, no single-instance assumptions). Build reusable FAMILIES,
   not bespoke per-instance modules, on the rule of three: ONE `unit_frame` core+painter reused
   across player/target/party (ready for focus/raid/boss); the action bar instance-parameterized so
   a second/third bar is `new ActionBarPainter(barDescriptor)`. Actually ADDING the extra bars or
   raid frames is a follow-on FEATURE that inherits this seam, NOT part of this refactor. The
   `unit_frame` descriptor (P10b) MUST be validated against the FULL target/party field set, not a
   token stub, so P11a/b/c reuse it with no core change.
10. ACCESSIBILITY (WCAG 2.2 AA on the HUD CHROME). Windows, buttons, forms, menus, chat, tooltips:
    semantic roles + aria, focus management (trap + return on window open/close), visible
    `:focus-visible` never animated/blurred/transitioned away, skip links, live regions for chat +
    combat text, target-size minimums (SC 2.5.8, >=24px absolute floor; prefer the existing 40x40px
    touch floor on mobile controls, do NOT weaken it). The 3D world/canvas is OUT of scope (not
    screen-readable); state the boundary honestly. A11y is built IN per window/element phase (the
    WINDOW/CONTROL validation row is MANDATORY, not deferred to P15) and consolidated + audited in
    the dedicated Accessibility phases (P15a infra, P15b audit). Also drop the `user-scalable=no` /
    `maximum-scale=1.0` viewport lock (live at `index.html:5` and `play.html:5`); it fails SC 1.4.4 /
    1.4.10, and the 16px input-font floor is the anti-zoom guard. Keep that the only contrast
    adaptation: see decision 11.
11. THEMING. ONE dark MMORPG aesthetic (theme.ts runtime `--color-*` accent theming stays). NO
    light / `prefers-color-scheme` theme. DO support `forced-colors: active` (Windows high-contrast):
    borders/focus survive, meaning is never carried by a background-image alone.
12. NO MAGIC VALUES IN PAINTERS. DOM painters drive CSS custom properties / tokens, never a literal
    hex/px/color in TS; thresholds + cadences (the 100/250/500ms frame-divider, breakpoints, the
    combat-announce cadence) are named constants. A guard enforces it. CANVAS painters
    (map/arena/minimap/delve/nameplate) cannot read CSS vars directly: they resolve the `--color-*`
    tokens via `getComputedStyle` ONCE per redraw (cached), never per-marker/per-frame, and every
    other literal is a named constant.
13. BUNDLE DISCIPLINE. A JS bundle-budget CI gate (sibling to `asset:budget`). Measure the
    cold-window cost first (the eager module graph of the play entry via build chunk metadata), then
    SELECTIVELY lazy-load (dynamic import) only the genuinely heavy + rarely-opened cold windows
    (options/market/leaderboard are candidates) while keeping frequently-opened ones (bags/char)
    eager. Each lazy window's loading state carries an a11y contract (aria-busy/role=status +
    focus-return across the async swap). Evidence-driven, never blanket splitting. (P17b)
    - OUTCOME (P17b, 2026-06-26): MEASURED then DECLINED. The eager play bundle is 3,327,191 B
      (3.173 MiB raw / ~952 KB gzip), ~1.5 MiB of which is non-deferrable i18n DATA. A fully-proven
      options+market lazy-load prototype shrank it by only 50,688 B (~14 KiB gzipped, ~1.5%) with ZERO
      runtime/FPS impact (load-timing only). The maintainer judged it not worth the complexity, so the
      lazy-load + the JS bundle-budget gate were NOT adopted (prototype reverted in full). leaderboard
      (~7.5 KB) was the candidate the measurement refuted as too cheap; bags/char stay eager. The
      measurement is the deliverable; this decision is settled (do not re-attempt without new evidence
      that the i18n-data-dominated bundle has materially changed).
14. BROWSER MATRIX. Big-3 desktop PLUS mobile Safari/WebKit as a first-class target (the game is
    mobile-playable); a `forced-colors` pass; a MINIMAL `@media print` reset. Cross-engine E2E incl
    WebKit is wired into CI in the close phase (P17b), closing FB's open webkit-in-CI item.
    - OUTCOME (P17b, 2026-06-26): PROTOTYPED + VERIFIED but NOT landed. The opt-in browser suite ran
      green across Chromium/Firefox/WebKit + a first-class mobile-WebKit instance (140 tests; engines
      provisioned by `npx playwright install`; bare `vitest run` stayed browser-free via the existing
      vite.config exclude). It was reverted together with the decision-13 prototype, so this remains
      OPEN/optional: a small standalone re-land (a `BROWSER_MATRIX` env gate in vitest.browser.config.ts
      + one standing ci.yml job) would close FB's webkit-in-CI item independently of the bundle work.
15. CLIENTWORLD-vs-SIM PAINTER PARITY (added by the deep review). Every painter consumes `IWorld`,
    which BOTH the offline `Sim` and the online `ClientWorld` mirror satisfy, but the perf harness
    exercises only the offline `Sim`. A core that assumes a Sim-only field shape or cadence (target
    cast remaining, combo pips, party out-of-range, async leaderboard/market) passes every offline
    gate and silently misrenders online. Every `*_view` core test MUST feed BOTH a Sim-shaped and a
    ClientWorld-mirror-shaped `IWorld` stub.
16. RESPONSIVE IS GATED, NOT JUST PRESERVED (added by the deep review). The CSS extraction preserves
    rules verbatim, but `css_corpus` is CSS-TEXT completeness only and cannot catch a `dvh`->`100vh`
    swap, a dropped `safe-area-inset`, or a lost `@media` breakpoint. P0 records a mobile-layout
    baseline and P4a/P4b wire the EXISTING V16 mobile E2E scripts (`mobile_input_zoom_check`,
    `mobile_button_size`, `mobile_joystick_size`, `mobile_chat_safe_area`, `mobile_minimap_safe_area`,
    `mobile_community_hud_safe_area`) as a blocking RESPONSIVE row.
    - 16a ORIENTATION: the IN-GAME view is LANDSCAPE-ONLY on web mobile, NEVER portrait. V16 already
      implements this and the extraction MUST preserve it intact: the `#rotate-device` overlay shows
      under `body.mobile-touch.game-active` + `@media (orientation: portrait)` (play.html ~4920 +
      5934-5953, index.html ~5761 + 6837-6866), backed by `requestMobileFullscreenLandscape()`
      (`screen.orientation.lock('landscape')` + fullscreen, `main.ts:482`), the `orientationchange`
      listener (`main.ts:474`), and the `mobilePreflight.baseLandscape` "Rotate your device to
      landscape" copy. There is NO in-game portrait layout by design; portrait shows the rotate
      overlay. The PRE-GAME SHELL (start/login/char-select), the `/wiki` guide, and the admin
      dashboard stay PORTRAIT-CAPABLE / normally responsive (the overlay is gated on `game-active`).
      This is exactly why P4a (shell, portrait-OK) and P4b (in-game mobile, landscape-only) are split.
      HAZARD: the `#rotate-device` orientation rules DIFFER between index.html and play.html (index
      sets `display:none` under portrait in one block; play sets `display:flex`); P4b must
      preserve-both-exactly into the per-entry `.extra`, never merge them. The mobile E2E row (16)
      runs the IN-GAME profile in LANDSCAPE, and adds a portrait-in-game assertion that `#rotate-device`
      is shown (not a broken portrait HUD); the shell profile is tested in both orientations.
17. PERSISTENT-MONOLITH OUTCOME (owned, not a defect). `hud.ts` stays the per-frame wiring hub and
    cold-window dispatcher after the packet; the win is that every behavior now lives in a tested
    core + thin painter that `hud.ts` composes, not that `hud.ts` shrinks to nothing. Reviewers
    should not expect the file to disappear; the line count drops as inline blocks become painter
    calls, but the seam, not the size, is the deliverable.
18. ADMIN / GUIDE CSS SCOPE (decided by the deep review). The Lightning flip (P1) becomes the
    project-wide transformer and WILL reprocess `admin.html` (a 254-line inline `<style>`) and
    `guide.html` (a 1621-line `styles.css` with backdrop-filters). This packet's EXTRACTION scope is
    the game HUD (`index.html` + `play.html`) only; `admin.html`/`guide.html` keep their current CSS
    shape. The obligation P1/P4 carry is a SURVIVAL gate (their CSS still builds and their
    `backdrop-filter`s survive minification `-webkit`-first), not an extraction. `admin`/`guide` a11y
    beyond what they ship today is out of this packet's scope; state the boundary, do not silently
    claim "both inline blocks empty" for all entries.

## Non-negotiable constraints (carry every phase)

- Determinism: pure cores stay DOM/Three-free; no `Math.random`/`Date.now`/`performance.now` in any
  registered pure core. (The FCT painter MAY use `Math.random` for jitter; the FCT CORE may not. The
  char skin-event `Math.random` at `hud.ts:9596` stays on the painter, like FCT, or is out of P8b's
  core scope.)
- Server authority untouched; do not move any outcome to the client.
- i18n: every NEW player-visible string is a `t()` key; new control labels go in
  `src/ui/i18n.catalog/hud_chrome.ts` (English-only). Never edit `i18n.locales/<lang>.ts`. The
  action-bar aria-label elision (P12a) must keep the `t()` call (no concat / `??` fallback). New
  async-failure copy (leaderboard/market) and the cast eat/drink label resolve via `t()` in the
  PAINTER (the i18n-free `src/render/cast_bar.ts` core emits a discriminator, never a `t()` call).
- No generated-file hand-edits; regenerate via the build.
- Shared worktree: commit with EXPLICIT paths, never `git add -A`.
- No em dashes, en dashes, or emojis anywhere. NOTE the byte-for-byte CSS-move phases (P2/P3/P4):
  the no-dash rule applies to NEW text only; relocating an existing comment that already contains a
  dash is allowed, and where a moved comment's dash is gratuitous it may be normalized (comments
  only, never a selector/value).

---

## Canonical workflow (every implementation phase follows this)

EFFORT + VERIFICATION + FAN-OUT are three SEPARATE axes (decided 2026-06-24; quality is the goal,
token cost is not a constraint). Do not conflate them under the `ULTRACODE` tag:
- EFFORT: every phase runs on Opus 4.8 at `xhigh`. Reserve `max` for a genuinely stuck moment (it
  overthinks / oscillates on structured tasks); do not default to it.
- VERIFICATION: every phase, regardless of its `ULTRACODE` tag, ends with an adversarial verification
  pass (a fresh-subagent diff review prompted for COVERAGE, plus a "what is missing" critic) on top of
  the STEP 3 review-dispatch and the QA pass. This is the highest-leverage quality lever; always do it.
- GENERATION FAN-OUT (what the `ULTRACODE: yes` tag actually gates): use a Workflow to fan out parallel
  generation ONLY where the phase decomposes into independent work items (the CSS section cohorts, the
  cold-window batches, the per-frame element slices). For a single-module, sequential, or docs phase
  (P5, P6, P7a, P9a, P16, P17a, P17b), DO NOT force generation fan-out: it fragments a design that one
  mind should hold and collides on shared files. Single coherent author pass + the verification above.
  P16 in particular MUST stay single-author (parallel CLAUDE.md prose self-conflicts).
- CONTEXT HYGIENE: a Workflow SHELL (push heavy reading/editing to a subagent that returns a summary)
  is encouraged on EVERY phase to keep the orchestrator under the 40% ceiling, even when generation is
  not parallelized. This helps the budget independently of fan-out.
The per-phase `ULTRACODE` tag in each phase file marks only the GENERATION-fan-out axis; xhigh effort
and the adversarial verification pass apply to all phases either way.

- STEP 0 Pre-flight: `git status` clean (ask the user if not; concurrent session may share the
  checkout). Memory scan (MEMORY.md + the entries the phase lists). Confirm you are in the
  `feature/frontend-modernization-v016` worktree.
- STEP 1 Load context (do NOT read `hud.ts` (14,377 lines) or the HTML entries whole): spawn an
  Explore agent to read+summarize this `state.md`, the phase's `progress.md` row, the phase file,
  `v016-recon-and-packet.md` (the line numbers), and the specific source ranges the phase lists.
  The orchestrator keeps the summary, not raw dumps. THE 40% RULE: the packet is already pre-split so
  each phase fits well under ~40% context INCLUDING its mandatory QA pass plus in-session remediation
  of every BLOCKING/SHOULD-FIX/NICE-TO-HAVE finding (that remediation load is why the splits exist).
  If a phase's working set STILL approaches the ceiling, split again rather than degrade.
- STEP 2 Choose orchestration + execute: pick the lightest tool. Default to parallel Agent/Workflow
  fan-out, one slice per window/element. Use `isolation: "worktree"` only if agents edit
  overlapping files. Request fan-out EXPLICITLY.
- STEP 3 Validation + review dispatch: run the validation-matrix rows for the change type. Spawn
  review agents only for the surface the diff touches (Review Dispatch Matrix). Prompt each for
  COVERAGE not filtering; do not commit until each reports no BLOCKING. Resume a truncated reviewer
  with: "Stop reading more files. Output the full report now based on what you've already seen. No
  more tool calls. Format: BLOCKING / SHOULD-FIX / NICE-TO-HAVE / VERDICT."
- STEP 4 Commit cadence: 2-5 Conventional Commits with a scope, EXPLICIT paths.
- STEP 5 Acceptance: the phase file's checklist, all items verifiable and green (incl the perf gate
  for per-frame phases). A failed perf gate BLOCKS marking the phase complete (decision 5 / risk 1).
- STEP 6 Docs + memory: update `progress.md` and this `state.md` (new files, tokens, decisions).
  Record surprising rules in memory. Per-frame phases TAG the green-perf-gate commit so a later
  cumulative regression (surfaced first at P17a) bisects to a phase, not to "relax the budget."
- STEP 7 Final response: status, files, validation results, reviewer verdict, deferrals, and the
  one-line handoff naming the next phase file (per the execution order above).

Each implementation phase is followed by a QA pass using `qa-checklist.md` (the shared QA starter:
correctness + test-coverage + dead-code agents, then the dispatch matrix, then fix BLOCKING/
SHOULD-FIX, then update docs). Never skip QA; end each phase by naming the next.

---

## Validation matrix (run the rows that match the change type)

- Baseline (every phase): `npx tsc --noEmit`.
- Pure core added/changed: `npx vitest run tests/<core>.test.ts` + `npx vitest run
  tests/architecture.test.ts` (the UI-purity guard) + a same-input-same-output assertion + the
  ClientWorld-vs-Sim parity assertion (decision 15: drive the core with BOTH a Sim-shaped and a
  ClientWorld-mirror-shaped `IWorld` stub).
- New `.ts` module added (every phase that adds a core/painter): `biome check` on the new/changed
  `.ts` (the V16 ratchet; do not let ~30 new modules accrue lint debt for the close session).
- CSS / HTML entry changed: `npx vitest run tests/css_corpus.test.ts` (the completeness guard, keyed
  on the LIVE 10-dash `/* ---------- name ---------- */` markers, over inline `<style>` UNION
  `src/styles/*.css` so coverage is conserved as P1-P4 migrate) + `npx vitest run
  tests/client_shell.test.ts` + `npm run build` (all 4 entries) + the backdrop-filter survival check
  (built CSS, `-webkit`-first; meaningful from P2 on, a no-op in P1) + `biome check` on the new `.css`
  + a screenshot-diff against the P0 visual baseline for any phase that risks a cascade change.
- RESPONSIVE / mobile changed (P4a/P4b, P15b): run the V16 mobile E2E scripts as a blocking row
  (decision 16: `mobile_input_zoom_check`, `mobile_button_size`, `mobile_joystick_size`,
  `mobile_chat_safe_area`, `mobile_minimap_safe_area`, `mobile_community_hud_safe_area`); a
  real-CDP mobile-inset check, not a CSS-text assertion.
- PER-FRAME phase (P10a-P14b): `npm run` the perf_tour harness and assert frameP95 <= the P0 baseline
  AND hudHotDomSkipRate >= the P0 baseline; for P12a/P12b, the allocation-budget assertion (RESOLVED by
  the P12a spike, open-decision 4: IN-PROCESS REFERENCE-STABILITY via `tests/util/alloc_probe.ts`
  [`probeAllocationStability` / `assertAllocationStable`] - drive the core N times and assert the
  returned container + every directly-contained reference-typed slot stay `===` the first call, since a
  correct in-place core allocates zero new per-frame arrays/objects; the reused-array reference test is
  the floor; heapUsed-delta was measured and rejected as noise; the full-HUD fallback where no single
  core can be isolated stays perf_tour frameP95 + longtasks); for P13b, the bounded-node-count
  AoE-burst assertion. A unit test that the painter routes ALL writes through the host's elided
  writers that EXIST (decision 5a; no raw `style`/`textContent`/`setAttribute` beyond a documented
  allowed write). CANVAS painters (P9a, P12b minimap, P14b nameplate) are gated on cadence + cached
  background + frameP95, NOT the elided-writer routing test (decision 12).
- WINDOW or CONTROL changed (MANDATORY on every cold-window + per-frame + chrome phase, decision 10):
  the WCAG 2.2 AA chrome checks (automated axe-core or equivalent over the built window; keyboard
  reachability + focus-return; a `forced-colors: active` snapshot; visible `:focus-visible`;
  target-size >=24px, and >=40x40 on mobile touch controls). Plus the no-magic-values painter guard
  (decision 12; DOM painters reference tokens/vars; canvas painters resolve tokens once per redraw).
  The full cross-window a11y audit (skip links, global focus management, live regions) runs in
  P15a/P15b; the PER-WINDOW roles/aria/labels/target-size are NOT deferred there.
- BUNDLE changed (P17b): the JS bundle-budget gate; for any window switched to a dynamic import, the
  initial bundle (the eager module graph of the play entry) shrinks by its measured cost and the
  window still opens (with an a11y-correct loading state) on first use.
- Player text changed: `npx vitest run tests/localization_fixes.test.ts`. New label in
  `hud_chrome.ts` (English-only) does not trip the release tier.
- Pre-merge / CI mirror: `npm run i18n:gen && npm test && npx tsc --noEmit && npm run build:env &&
  npm run build:server && npm run build`, then the i18n freshness check; on `release/**` the
  release-tier gate (`I18N_RELEASE_TIER=1`) pending=0; and `biome ci --changed` (the forward
  ratchet that lints the new files).

## Review Dispatch Matrix (spawn ONLY the rows the diff touches)

- `privacy-security-review`: only if the diff touches `server/`, `src/admin/`, `src/net/`, a
  deploy/secret file, or introduces SQL/auth/secret/new `Math.random`|`Date.now`|`performance.now`
  in `src/sim/` or a pure core. Should rarely fire (presentation-only).
- `migration-safety`: only if `server/*_db.ts` DDL or `characters.state` JSONB changed. Never here.
- `cross-platform-sync`: only if `src/world_api.ts` (IWorld), `src/sim/`, `src/net/online.ts`,
  `server/game.ts` wire/dispatch, or the i18n matchers changed. Consuming the already-landed IWorld
  in a painter does NOT change it; this should not fire. (The ClientWorld-vs-Sim parity obligation,
  decision 15, is covered by the per-core parity test, not by spawning this reviewer.)
- `qa-checklist`: every phase that completes a deliverable set (the default reviewer).

If no row matches (docs/test-only), spawn no review agent.

---

## Phase ledger (30 phases; fill in as phases complete)

Full per-phase scope/acceptance is in the `phase-NN-*.md` files; line numbers in
`v016-recon-and-packet.md` (the recon, superseded where it differs from this ledger).

| Phase | Title | Risk | Kind | Status |
|---|---|---|---|---|
| P0 | Foundation gates: CSS-corpus + UI-purity guard + perf/visual/mobile baseline | low | port+extend | done (tests/css_corpus.test.ts + tests/architecture.test.ts UI_PURE_CORES/RENDER_PURE_CORES; baselines perf-/visual-/mobile-baseline-v016.md; mobile perf + 2 mobile E2E scripts surfaced for P17a/P4b) |
| P1 | CSS A: Lightning flip + tokens + base + the CSS-import seam | low | port | done (Lightning flip via browserslistToTargets + zero-dep .browserslistrc parser, no browserslist npm dep; dead css.postcss removed; src/styles/{index,tokens,base}.css; one @layer order declared in index.css, imported once from src/main.ts -> both game entries; --range-fill stays the slider inline fallback; play.html 3 cursor url()s absolutized to survive the flip; biome src/styles override; 3 commits b9fe99b2/0a120e9f/0892c250; tsc + vitest 3906 + build x4 green; qa-checklist no BLOCKING) |
| P2 | CSS B1: in-world HUD chrome (full section map incl Fiesta HUD + tooltip) | medium | port | done (NEW src/styles/hud.css under @layer components, barrel @imports it after tokens+base; runs A+B + tooltip + FCT + Interface/adaptive/perf + Fiesta + center/vignette/death; Fiesta+tooltip ORPHANS CLOSED, tooltip upgraded to a 10-dash marker -> css_corpus 48 index/46 play. CASCADE fixes for the unlayered-beats-@layer intermediate hazard: UI-chrome-icons (.ui-icon/.x-btn .ui-icon/.micro-btn .ui-icon/.pfm-crest) moved to base.css @layer base, closing the P1 base-tier gap so #mm-music .ui-icon component-override wins; and 5 .btn.fiesta-practice/.arena-bracket*.fiesta glue rules DEFERRED inline to P3 with their unlayered .btn/.arena-bracket bases. backdrop -webkit-first on the 2 real rules + PORTED scripts/check_backdrop_survival.mjs build gate (P0 gap, first phase with teeth) + 12-case test. Lossless token-multiset proof; css_corpus/client_shell/backdrop + tsc + full vitest 3931 + build x4 + survival(both twins) + biome + live HUD screenshot all green; commits local) |
| P3 | CSS B2: modal + feature windows (arena/market/options/theme/emote ranges fixed) | medium | port | done (NEW src/styles/layout.css `@layer layout` = the `.window` shell only; NEW src/styles/components.css `@layer components` = all feature-window bodies in source order: delve, lockpick, windows lead chrome (quest dialog/.btn/loot/item-quality), character window, spellbook, quest log, leaderboard, talents, modals and dropdown, vendor, bags, social, map, arena, market, options, theme picker, emote, + the 5 deferred Fiesta glue rules now beside their .btn/.arena-bracket bases. Barrel imports both, order tokens,base,layout,hud,components -> components.css last so window bodies win ties over hud.css (both `@layer components`); both load via the one src/main.ts barrel so index.html AND play.html get them. index.html inline emptied 187-1158; play.html inline windows untouched (P4b reconciles; its unlayered inline still wins, zero visual change). CASCADE safe: only other bare `.window` is hud.css `.panel,.window{opacity}` (disjoint), `body.frosted-panels .window` + base.css `.window .panel-title`/`.window.window-dragging`/`#start-screen :is(.btn,...)` are more specific; no equal-specificity tie flip. NO backdrop in P3 content. css_corpus split the coarse "windows" banner into per-window banners +11 (window shell/character window/spellbook/quest log/leaderboard/talents/modals and dropdown/vendor/bags/social/map) -> 59 index/57 play. 6 moved-comment em dashes normalized. 3 tests repointed (client_shell, mobile_window_transform via inline-UNION-modules, social_status_dots). Lossless: 625 rules identical (biome cosmetics absorbed). tsc + css_corpus + client_shell + full vitest 3931 + build x4 + survival + biome(new .css) all green; adversarial 4-lens review no BLOCKING; commits local) |
| P4a | CSS C-1: pre-game shell + char-select -> shell.css | medium | port | done (NEW `src/styles/shell.css` `@layer shell`, barrel `@import`s it after components.css, @layer order UNCHANGED. Moved 12 of 19 inline banners verbatim: start screen, loading, play console, Skin picker `.skin-*` rows, login form, animated + cinematic backdrops, controls drawer, Clean up styles char-select rows, class details panel, unified char-select layout (whole, incl its 860 block + interspersed body.mobile-touch shell rules), skin-select overlay. DISCOVERY: index.html NOT cleanly partitioned after P2/P3 - chat/party frames/context menu/prompts/trade window/elite target frame + `#tf-debuffs` + paperdoll `.equip-slot` + `#bags.drop-target` are P2/P3 chrome NEVER extracted (absent from src/styles); HELD inline + SURFACED as a P2/P3 cleanup follow-up; the two grab-bag banners (Skin picker, Clean up styles) SPLIT so char-select/shell rows moved (base+override together), only genuine chrome held. CASCADE: whole-section moves into @layer shell, source order preserved; held mobile-touch SECTION + chrome cluster stay inline-unlayered. ONE flip caught+fixed: `#hero-view #login/realm-panel{margin-top:10px}` (2,0,0) in @layer shell lost to held `body.mobile-touch[data-start-panel] #X-panel{margin-top:0}` (1,2,1 unlayered, dead in original) -> relocated that held rule into @layer shell so #hero-view wins (10px preserved). Backdrop -webkit-first: 3 std-first pairs reordered, survival 11 twins. css_corpus NO change (all 59 banners present: 12 in shell.css, 7 inline). 2 tests repointed (client_shell + charselect_sort_parity -> shellCss). Key file paths: src/styles/shell.css NOW EXISTS and carries the desktop shell sections; the inline `<style>` blocks are NOT yet empty (P4b finishes that) - index.html still holds the chrome cluster + #tf-debuffs + paperdoll/bags + the mobile-touch SECTION; play.html untouched. P4b NOTE: 2 bare HUD rules (#community-hud/#quest-tracker) rode along in the char-select 860 block (cascade-neutral, re-home then). Lossless 4526 pairs identical + puppeteer computed-style diff 0 across 6 states. tsc + css_corpus + client_shell + charselect_sort_parity + full vitest 3931 + build x4 + survival + biome(shell.css) all green; adversarial coverage Workflow (cascade/lossless/classification/qa + synthesis) 1 BLOCKING = the margin-top flip now fixed+verified, all else cleared; commits local) |
| P4b | CSS C-2: mobile-touch -> hud.mobile.css + per-entry .extra; empty both inline blocks | medium | port | done (CSS extraction P1->P4b COMPLETE: both inline `<style>` blocks EMPTY, all game CSS under one flat @layer order. NEW src/styles/hud.mobile.css (@layer hud-mobile) = the in-game mobile-touch block. Folded in the P4a-surfaced orphan cleanup: chrome (chat/party frames/context menu/prompts/trade window/elite target frame + #tf-debuffs) -> hud.css; paperdoll + #bags.drop-target -> components.css; pre-start #ui hide -> base.css; re-homed #community-hud/#quest-tracker from shell.css's 860 block -> hud.mobile.css. RECONCILE (Fernando "do what is best for the project"): play.html was PURE stale drift of index (237 rules missing/41 drifted/0 intentional), so instead of preserve-both-exactly it was reconciled to the canonical modules (empty inline -> falls back to the shared barrel both entries load via src/main.ts). Only per-entry diff preserved = #rotate-device orientation gate (decision 16a): NEW src/styles/index.extra.css (@layer index-extra, suppress in-game) + play.extra.css (@layer play-extra, show in portrait), each via a per-entry `<link>`; so the .extra files are TINY (just #rotate-device), not the ~976/~60 recon estimate. LAYER-NAME BUG fixed: dotted `hud.mobile`/`index.extra`/`play.extra` are SUBLAYERS (hud.mobile = mobile under early hud -> lost to shell); renamed FLAT hud-mobile/index-extra/play-extra, ordered tokens,base,layout,components,hud,shell,hud-mobile,index-extra,play-extra (hud-mobile AFTER shell). Lossless multiset (index OLD==NEW; play differs only by #rotate-device) + negative-control-validated computed-style diff = 0 real flips on index (caught+fixed the mobile mode-select/play-console/btn-play flip). tsc + full vitest 3931 pass/8 skip + build x4 + survival(11 twins) + biome(3 new files) green. Mobile E2E (index): input_zoom 28/0, minimap/community/chat safe-area pass; button_size/joystick_size pre-existing RED (stale #offline-select entry-flow harness, screenshot-only/no assertions, not P4b). 6 tests updated (client_shell 41/41, charselect, styles_extraction, + the NEW per_entry_css_wiring.test.ts guarding the per-entry `<link>` + #rotate-device gate; the "3931 / 5 tests" above predates that final commit, true count 3938). RE-AUDIT 2026-06-24 (ultracode): all gates re-green (tsc; full vitest 3939 pass/8 skip after hardening, 377 files; build x4 + survival 11 twins; biome) + mobile E2E row (input_zoom 28/0, chat/minimap/community-hud real-CDP safe-area pass); 6-finder adversarial Workflow + independent selector-set diff re-confirmed the RECONCILE LOSSLESS (2034/2034 play selectors present, 0 unique dropped) and the #rotate-device split cascade-safe. Verdict PASS-WITH-FIXES (0 BLOCKING); fixed: index.html dotted-`hud.mobile` comment trap, hardened styles_extraction barrel @import-completeness+order guard (teeth-proven, closes silent-un-styling), extended the dotted-name guard to both .extra files, tightened the barrel header. Follow-ups: FIXED shell.css .se-preview-hint invalid `color: var(--color-text-muted) b0` -> `color-mix(... 69% ...)` + NEW tests/css_value_validity.test.ts bug-class guard (commit c7fd1364, full vitest now 3949/8 across 378 files); FIXED the cross-cutting mobile harness entry-flow drift (commit d8318b5e): NEW scripts/enter_offline_game.mjs canonical entry (in-page #btn-offline click + name/class/start, dismiss mobile preflight, wait for window.__game boot) routed through all 23 mobile_* scripts; 21/23 fully pass, touch_opacity exits 1 only on the environmental 502 (no local server), joystick_deadzone has a separate pre-existing settings-nav issue. commits local) |
| P5 | ui_effects_profile resolver (src/game, 5-axis, defines the cutoff) + applier | medium | port+extend | done (NEW src/game/ui_effects_profile.ts pure leaf DEFINES EFFECTS_QUALITY_LOW_CUTOFF=0.5 + the full 5-axis FB contract: resolveUiEffectsProfile({presetLabel,effectsQuality,reduceMotion}) / uiEffectsTokens flat --fx-* Record / uiEffectsProfilesEqual / uiEffectsAllowFctCrit(string); imports NOTHING. gfx.ts:308 now imports the constant (render->game leaf; the bare 0.5 replaced, sibling terrainDetail/foliageDensity/shadowQuality 0.5s left). NEW src/ui/ui_effects_applier.ts host: prefers-reduced-motion matchMedia channel + 180ms effectsQuality debounce + uiEffectsProfilesEqual diff-guard, stamps data-fx-level (=tier, internal, no t()) + the --fx-* tokens on documentElement, mirrors theme.ts. main.ts constructs it after applyTheme(), applyNow() at boot, routes reduceMotion(immediate)+graphicsPreset(immediate)+effectsQuality(debounced); reduce-motion is single-source = OS matchMedia OR settings('reduceMotion'), body.reduce-motion stays only the CSS hook (no second flag), em dash at the reduceMotion comment normalized. Registered in UI_PURE_CORES (forbiddenUiCoreImport fits the zero-import leaf) + an import-absence test pins no-governor/no-render/no-ui (two-controller hazard); GFX_BUCKET_BANDS `ui` band still governable:false (untouched). advanced HONORS effectsQuality -> distinct LOW tier below the cutoff (NOT collapsed to high). CSS (tokens.css :root full-Ultra seeds + B1 glass low whole-rule drop; hud.css B2 ambient play-state+motion-scale x5, vignette full-tint hold on reduce-motion + low axes, B4 glow calc x4, B6 fct-crit->fct-rise low gate, all -webkit-first/inert at default). DECISIONS: object resolver input + flat token map (theme.ts-style) adapt FB's positional/structured shapes; uiEffectsAllowFctCrit keeps FB's STRING (dataset.fxLevel) signature, not (profile), since P14 reads the published attribute; B6 is a minimal data-fx-level CSS gate (FB did B6 in JS; the per-element uiEffectsAllowFctCrit JS consumption is P14a/b). Validation: tsc; full vitest 3977 pass/8 skip; css_corpus+client_shell+css_value_validity+architecture+resolver green; build x4 + backdrop survival 12 twins; biome clean on the 3 new files (gfx.ts/main.ts/hud.css carry only pre-existing grandfathered biome debt, 0 new); built-bundle computed-style proof: default=byte-identical, low sheds every bucket, reduce-motion vignette holds full tint while ultra keeps glass+glow. NEW tests/ui_effects_wiring.test.ts pins the load-bearing CSS rules + applier/main.ts wiring (folded from QA, since the computed-style proof is a gitignored tmp script). No WCAG/no-magic/perf rows (no window/control/painter/per-frame). qa-checklist 0 BLOCKING / 3 SHOULD-FIX (doc+test-hardening, folded) / 3 NICE-TO-HAVE. commits local) |
| P6 | PainterHost (two facets) seam + cold-window pilot | medium | port+extend | done (NEW src/ui/painter_host.ts: FACET 1 PainterHostPresentation {itemIcon,moneyHtml,itemTooltip,attachTooltip} the cold windows compose; FACET 2 PainterHostWriters + makeWriterFacet(cache,onWrite,onSkip) binding the 4 elided writers as closures over a SUPPLIED Map (key scheme byte-identical to Hud's private setText/setDisplay/setTransform/setWidth; host-agnostic, no window, Node-importable). NEW src/ui/delve_map_painter.ts (Canvas-2D pilot): pure delveDrawModel(IWorld,size,pad,names) -> {layoutId,schematic,mobs,party,player,arrow,areaLabel} (reads ONLY delveRun/entities/player/partyInfo, no DOM/i18n/color) + class DelveMapPainter with paintMinimapDelve (writes #zone-label through the facet + circular minimap) / paintWorldMapDelve (rectangular map + on-canvas title); absorbed the orphaned private drawSchematicPrimitives; resolves 6 --color-delve-* tokens via getComputedStyle ONCE/redraw (D12, zero literal hex). src/ui/delve_map.ts now EXPORTS delveLocalToCanvas (the former private toCanvas) so the painter's marker math is the core's, not a duplicate (kills the painter's 23/46 magic numbers). ONLY vendor composes FACET 1 (VendorWindowDeps extends PainterHostPresentation, Hud builds presentationBag once + the call site spreads ...presentationBag); lockpick (callbacks getState/tierName/onEngage/... + its own getElementById) and raid_lockout_view (pure HTML-string builder raidLockoutPanelHtml) have nothing to compose -> UNTOUCHED (D8; the FB-carryover "3 windows onto a unified bag" was wrong). hud.ts: writerFacet+delvePainter+presentationBag fields by the portraits field; both delve branches -> painter calls; overworld #zone-label now this.setText (so all #zone-label writes share hotWriteCache = coherent skip-rate); deleted the 4 bg-cache fields + drawSchematicPrimitives + dead delve_map/DELVE_MODULE_LAYOUTS imports (-211 net lines). DISCOVERY (recon/phase-file were WRONG): the world-map delve site is NOT "static schematic only" -- BOTH sites paint mobs+party+arrow and differ ONLY by size(162 vs canvas.width)/pad(8 vs round(S*0.06))/circular-clip(minimap)/marker-size(3 vs 4)/disc-radius(4 vs 5)/lw(1.5 vs 2)/label-sink(#zone-label DOM vs on-canvas title); both preserved via the two parameterized entry points. Validation: tsc clean; biome clean on every new/changed .ts (vendor_window bounded pass: import sort + 2 concat->template, behavior-identical; delve_map_painter has 1 grandfathered noNonNullAssertion warn-level getContext! matching icons.ts/hud.ts/player_card.ts/unit_portrait_painter.ts, non-gating); full vitest 4008 pass/8 skip (381 files); architecture purity (delve_map.ts stays a pure UI core in UI_PURE_CORES, must not + does not import the painter) + no-magic-values canvas guard + write-elision-facet fake-cache unit test (write-once-then-elide, per-element keying, type-prefix namespacing, two-facets-one-cache coherence) + Sim/ClientWorld parity (two stubs, D15) + both-sites-same-output + client_shell + S3 i18n guard green; one source-text guard repointed (#zone-label this.setText). No sim/server/net/headless/world_api touched (D4). REVIEWERS: fork adversarial render-parity PARITY HELD (0 visible deviations, line-by-line vs the deleted inline code: room/mob/aggro/dead/label/outline tokens == old hex, delveLocalToCanvas math identical, the offscreen-bg-build-order + per-member-stroke + redundant-player-skip deviations all RENDER-NEUTRAL/NON-ISSUE); qa-checklist 0 BLOCKING / 3 NICE-TO-HAVE (the getContext! warn, canvas-draw parity code-read-only since a 2D ctx isn't in the Node suite, the outline token short-hex correctly in CSS). WCAG: vendor change is compositional (byte-identical markup) so a11y preserved; the source-scan asserting focusable <button> rows + aria-labels survive composition is the axe-equivalent (no DOM/axe in the Node suite); delve Canvas is OUT of a11y scope, the #zone-label stays a real text node IN scope. commits local) |
| P7a | Cold-window: talents (interactive, mutable edit buffer) | medium | port | done (NEW src/ui/talents_view.ts: pure buildTalentsView(stage,cls,total) -> header counts + tab pips + spec list + class/spec TreeVM {per-node shape/state/disabled/left/top/chosen + arrow x1,y1,x2,y2,filled}; named consts TALENT_CELL_W=86/CELL_H=70/NODE_SIZE=46/TOP_PAD=6; gating reuses cloneAllocation+validateAllocation+dormantNodes+pointsSpent byte-for-byte; DOM/Three-free, in UI_PURE_CORES. NEW src/ui/talents_window.ts: class TalentsWindow (open/close/render + paintSpecTab/paintTree/talentTooltip/openChoicePopup/setSpec/nodeClick/nodeRemove/footerHtml/wireFooter/keyboardActivate); composes PainterHostPresentation (only attachTooltip used) + a Hud deps bag (root/getStage/setStage/playerClass/totalPoints/currentAllocation/activeLoadout/loadouts/currentBar/saveLoadout/switchLoadout/deleteLoadout/applyLoadoutBar/buildDropdown/inputDialog/confirmDialog/showError + captureFocus/restoreFocus). CRUX: the core is NOT IWorld-derived -- it takes the mutable talentStage edit buffer as an INPUT; Hud OWNS the single buffer (talentStage field) and the painter reads/replaces via getStage/setStage + mutates in place, re-derives, repaints (no second buffer). hud.ts: thin toggleTalents delegate + TalentsWindow lazy-closure field by presentationBag; removed the inline talents methods + openChoicePopup + private roleLabel + keyboardActivate + talentTab field; KEPT applyLoadoutBar; pruned dead talents imports; closeManagedWindow 'talents-window' case routes through talentsWindow.close(); net -518 (14172->13654). talent_i18n.ts EXPORTS roleLabel (shared w/ char sheet, same game.talents.role* keys). tokens.css +6 --color-talent-* (exact pre-existing hex), classAccent=--color-text-muted, signature=--gold. DECISION 12: painter has ZERO hex (var(--color-*)); SVG arrow stroke moved attr->inline style (var() needs a style decl, not an SVG presentation attr) = the one DOM-attr-shape delta, visually identical. WCAG: added focus-return (captureFocus/restoreFocus on open + ALL close paths) -- the inline had none; preserved all role/aria/tabindex/keyboard wiring byte-faithfully; live axe/forced-colors/target-size pixel snapshot deferred to manual (no DOM/axe in the Node suite, byte-faithful DOM == inline baseline) per P6/P7 precedent. em-dashes -> ASCII " - ". NEW tests/talents_view.test.ts (gating==validateAllocation + geometry + dormant + same-input-same-output + ClientWorld-vs-Sim seed parity + hasTree=false) + tests/talents_window.test.ts (no-hex/token/no-dash source guards). Validation: tsc clean; full vitest 4022/8 (383 files); architecture purity + client_shell green; biome clean on both new files; no sim/server/net/headless/world_api touched (D4); no i18n.locales edits. REVIEWERS: qa-checklist 0 BLOCKING / 1 SHOULD-FIX FOLDED (Escape close path now routes through talentsWindow.close() for buffer drop + focus-return, matching delve/quest) / 3 NICE-TO-HAVE; independent parity reviewer PARITY HELD (only the 3 intended changes + the required SVG attr->style; no correctness bug, no dangling ref, no wiring hazard). No perf gate (cold/open-on-demand). commits local) |
| P7b | Cold-window: social + bags | medium | port | done (TWO disjoint windows extracted in one ultracode slice. NEW src/ui/social_view.ts (pure core, UI_PURE_CORES): socialStructSig(tab,social,party) the full-rebuild-vs-refresh gate + socialDot + friendRows/ignoreRows/guildView(perms resolved vs viewer rank)/raidView; raidView returns canConvert AND canUnconvert -- the V16-current convert-party (raid->party unconvert) footer the older FB social port had DROPPED, restored here. NEW src/ui/bags_view.ts (pure core, UI_PURE_CORES): bagItemAction (6-way priority trade>marketSell>vendor>petFeed>quest>use, byte-faithful) + bagTooltipHintKey + bagQualityKey (item.quality??'common') + buildBagGrid(inv,lookup,filter)->{state:empty|noMatch|items,visible} REUSING applyBagFilter from bag_filter.ts (no re-derive). NEW src/ui/social_window.ts (class SocialWindow): owns tab/lastStruct/lastContent/notice/suggest state; toggle/close/selectRaidTab/refreshIfChanged/render/refreshList + the friend/guild/raid/ignore Html + footer/addRow/wireChrome + typeahead (wireSuggest/renderSuggest/moveSuggest/highlightSuggest)/resolveAndAct/notice. SocialWindowDeps is STANDALONE (no PainterHostPresentation -- social renders no item rows, uses title=/CSS dots not the floating tooltip): world()/closeOthers/hideTooltip/captureFocus/restoreFocus/showPrompt/startWhisper. CRUX (social is NOT purely cold): hud.update slow-divider calls refreshIfChanged() behind the struct/content diff-gate, so the painter uses ONE delegated click listener on .soc-body (onBodyClick, closest('[data-act],[data-whisper]')) wired once per render(); refreshList() only swaps body.innerHTML so a cadence tick re-attaches NOTHING (vs the inline wireSocialRows re-wiring per refresh = the listener-churn hazard). All dispatch branches byte-identical (unfriend/unblock/gkick/promote/demote/gtransfer/raid-move/convert-raid/convert-party + data-whisper). NEW src/ui/bags_window.ts (class BagsWindow): BagsWindowDeps EXTENDS PainterHostPresentation (icon/money/tooltip/attachTooltip) + bags surface (world/wocBalanceHtml/vendorOpen/tradeOpen/isMarketSell/pendingPetFeed/closeVendor/addItemToTrade/stageMarketSell/showError/setPendingPetFeed/resetPetBarSig/isHotbarItemId/setDragAction/clearActionDropTargets/cancelPetFeed/renderCharIfOpen); render/buildFilterBar/fillGrid(buildBagGrid switch)/refreshGrid(search, keeps focus)/sellBagItem/showDiscardItemPrompt/showSellQuantityPrompt/writeDraggedAction. OWNS the bag-filter state moved off hud (same localStorage key woc_bag_filter, same parse/serialize); .bag-grid scroll-offset captured+reapplied in render() AND refreshGrid(). hud.ts -889 (13654->12765): socialWindow+bagsWindow lazy-closure deps fields by talentsWindow (bags spreads ...presentationBag, social standalone); toggleSocial->socialWindow.toggle, renderBags->bagsWindow.render thin delegators; cadence site (slowHud)->socialWindow.refreshIfChanged(); chat convert-raid/party->sim.convert*()+socialWindow.selectRaidTab(); closeManagedWindow social->socialWindow.close(); deleted ~900 lines inline social+bags methods + the social state fields + bagFilter field + BAG_*_LABEL_KEYS maps + bag_filter import, via an ANCHORED ASSERTED string-splice (mustContain/mustNotContain guards, not sed -- avoids the FB sed-clip neighbour-swallow hazard); startWhisper KEPT in hud (used by chat ctx menu). D12 no-magic: bags qColor '#fff' fallback -> var(--color-quality-default) (tokens.css, exact prior value), QUALITY_COLOR map unchanged; social 500ms = the named shared slow-HUD divider in hud.update (NOT in the painter); typeahead 160/150 named consts. WCAG: social adds capture/restoreFocus on toggle/X/closeManagedWindow close paths (the inline had none; routes through the same Hud helpers as delve/talents); rows/cells stay focusable <button>s w/ aria-labels; live axe/forced-colors/target-size deferred to manual per P6/P7a (CSS lives in P3 components.css, outside this diff). social_status_dots.test.ts source guard repointed hud.ts -> social_window.ts (=== 'off' ? '' :). NEW tests: tests/social_view.test.ts + tests/bags_view.test.ts (struct-sig stability across no-op ticks + tab/permission/raid models + filtered grid states + same-input-same-output + ClientWorld-vs-Sim mirror parity via JSON round-trip) + tests/social_window.test.ts + tests/bags_window.test.ts (no-hex/no-500/no-dash + delegation-not-rewired + bag-grid scroll + token-defined source guards). Validation: tsc clean; full vitest 4056 pass/8 skip (387 files); architecture purity (both cores DOM/Three-free + registered) + client_shell + social_status_dots green; biome clean on the 4 new files (0 errors; 4 grandfathered noNonNullAssertion warns = getElementById('prompt-stack')!/dataTransfer!/view.groups!, same idiom as delve_map_painter). Presentation-only (D4): git diff of world_api.ts/sim/server/net/headless/i18n.locales EMPTY. REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 3 NICE-TO-HAVE (social tabs keep aria-pressed not role=tablist = byte-faithful carry-over, defer to P15; bags closeManagedWindow keeps inline close not a bagsWindow.close() = harmless asymmetry, no painter state to drop; live-axe chrome row = needs a running browser); adversarial parity Workflow (2 slices social+bags, each finding then per-deviation adversarial re-verify) PARITY HELD = 5 claimed deviations 0 confirmed regressions (the focus-return additive, the delegation render-neutral w/ every branch byte-identical incl no element carries both data-act+data-whisper, two comment-only #100->issue-100 edits, the qColor token swap byte-identical key-lookup). No perf gate (cold + 500ms-divider diff-gated, not in the every-frame path). commits local) |
| P8a | Cold-window: options (~1180 lines / 9 sub-panels, full dispatch matrix) | medium | port | done (the densest single cold window in the packet. NEW src/ui/options_view.ts (pure core, UI_PURE_CORES): the four control-descriptor primitives (slider/toggle/boolToggle/choice) + the per-kind pure dispatch-value fns (sliderDispatchValue=Number(raw), toggleNextValue=current>=0.5?0:1, toggleIsOn, boolToggleNextValue=!current) so the painter calls the SAME coercion and the matrix proves no dispatch drift with no DOM; per-panel builders buildGraphicsControls (advanced-preset=5 reveal + useTouchInterface + !isNativeAppShell gating, the interleaved browserEffects/interfaceMode notes inline, the preset+interfaceMode choices flagged rerender) / buildAudioControls (3 volume sliders + a musicToggle position-marker + 3 bool toggles) / buildControllerControls / buildInterfaceControls; buildOptionsMenu (the main-menu routing, online-only bug-report row) + buildBugReportInfo (the ONE IWorld slice -> the decision-15 parity surface). DOM/Three/GAME-free: forbiddenUiCoreImport blocks ../game/* even type-only (IMPORT_RE does not distinguish), so setting keys are plain `string` in the descriptors and the painter narrows them; label keys are a type-only TranslationKey from ./i18n.catalog (a pure types barrel). NEW src/ui/options_window.ts (class OptionsWindow): STANDALONE OptionsWindowDeps -- composes NO PainterHostPresentation (options renders no item rows, the social/decision-6 precedent); imports `type {OptionsHooks,BugReportHooks} from './hud'` type-only (isolatedModules erases it, runtime edge is hud->options_window only, acyclic). Owns the #options-menu DOM + the view/capturingKey/keybindNote/perfSettings state + lifecycle (toggle/close/isOpen/onPerfOverlayMoved) + the 4 control builders (descriptor-driven) + the bespoke controls the 4 primitives do not cover (music toggle reads music.enabled; the PR#730-hardened language picker; theme; chat-timestamps 12/24 seg; keybind capture; gamepad remap; the bug-report form; the perf delegate). hud.ts -1237 (12765->11528): thin delegators + a lazy-closure deps bag; ~1237 inline lines + LANGUAGE_ENDONYMS + BIND_CATEGORY_LABEL_KEYS + BIND_ACTION_LABEL_KEYS + the optionsView/capturingKey/keybindNote/perfSettings fields removed via an anchored, asserted string-splice (mustContain/mustNotContain + a no-dangling-this.<method> sweep, NOT sed); 16 now-dead imports pruned (gamepad_map/mobile_controls whole lines, keybinds->type Keybinds only, theme/perf/i18n members, settings normalizeClickMoveButton). D12 no-magic: the painter carries ZERO hex -- the gold log tint '#ffd100' stays Hud-side in the log() dep closure; BUG_DESC_MAX_LEN/RANGE_FILL_FULL_PCT named; the slider --range-fill percent math uses the named full-scale const. TIER BOUNDARY HELD (decisions 6/8): graphics reads s.num('graphicsPreset') as a plain rounded setting value; NO governor read, NO EFFECTS_QUALITY_LOW_CUTOFF, NO ui_effects_profile import (guard-asserted). WCAG focus-return ADDED (inline had none): captureFocus() BEFORE closeOthers() in toggle(); restoreFocus on close() AND the closeManagedWindow->closeOptions->close() path; native range inputs (role=slider) + aria-label/aria-pressed + role=status/alert live regions preserved byte-faithfully. A re-run senior-quality audit (2026-06-25, ultracode 6-finder adversarial Workflow over the COMMITTED extraction, 18/43 findings confirmed, 0 BLOCKING) then added two a11y fixes the inline original lacked: the gamepad remap listboxes now carry the physical-button aria-label (WCAG 4.1.2; the language picker already did) and sliders set aria-valuetext to the formatted readout (50%/90 degrees) so a screen reader announces the meaningful value, not the raw number. chatTimestamps/chatClock stay Hud fields (the chat renderer reads them); the painter mutates via get/set deps that do field+localStorage Hud-side. A 14th endonym ru_RU:'Русский' is required by Record<SupportedLanguage,string> exhaustiveness (tsc-caught). NEW tests: options_view.test.ts (full per-sub-panel dispatch matrix + same-input-same-output + ClientWorld-vs-Sim bug-report parity via two DISTINCT world shapes -- offline Sim prototyped instance + extra fields + '' realm vs online ClientWorld plain wire object + populated realm, decision 15, NOT a JSON self-clone) + options_window.test.ts (no-color(hex+rgb/hsl)/named-const(2000 pinned to its def line)/no-dash + tier-boundary + WCAG roles+gamepad-listbox-name+slider-aria-valuetext + changeLanguage hardening + bug-report async states + keybind dispatch + per-primitive dispatch-expression + applyControls-routing + cold-window guards); tests/architecture.test.ts registered options_view; tests/client_shell.test.ts (menu-dedup guard repointed inline add() -> options_view labelKey) + tests/localization_fixes.test.ts (S3/R2 localizeBugReportError guard repointed hud.ts -> options_window.ts). Validation: tsc clean; full vitest 4087 pass/8 skip (after the audit hardening); biome clean on the 4 new files; build all 4 entries; architecture purity (teeth proven: an injected ../game import reds options_view, green after revert) + client_shell + S3 green. Presentation-only (D4): git diff of world_api.ts/sim/server/net/headless/i18n.locales EMPTY. REVIEWERS: adversarial parity Workflow (6 sub-panel clusters + per-finding adversarial verify) PARITY HELD = 6/6 clusters, 0 material findings, 0 confirmed regressions; qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 1 NICE-TO-HAVE. HONEST WCAG SCOPE (corrected by the re-run audit): the per-window roles/aria/labels/focus-return ARE proven here by source guards + the two audit a11y fixes above; the live-axe-over-the-built-window, forced-colors snapshot, computed target-size (>=24px desktop, 40x40 mobile), and :focus-visible rows need a running browser/DOM and are DEFERRED to P15b (the opt-in vitest.browser.config.ts), consistent with P6/P7a/P7b. The dispatch matrix is proven at the descriptor + pure-coercion-fn level plus exact-dispatch-expression source guards; full live-DOM event-driving is the same P15b deferral. No perf gate (cold; not in Hud.update). The full per-sub-panel dispatch matrix (a pure descriptor + pure coercion fn per control, source guards for the bespoke controls) is the proven preservation pattern for P8b/P9b. commits local) |
| P8b | Cold-window: market + char (skin-event Math.random stays on painter; 3D preview scoped) | medium | port | done (NEW src/ui/market_view.ts (pure core, UI_PURE_CORES): buildMarketView -> a discriminated MarketView {no-data | browse | sell | collect}. The market is SNAPSHOT-DRIVEN (sim.marketInfo: MarketInfo|null), NOT promise-based, so info===null IS the loading/no-merchant state and there is genuinely NO error channel -- the packet's loading/empty/ERROR triad drops its error variant as never-rendered dead code (the only Promise market-class surface is leaderboard(): Promise<LeaderboardPage>, decision 4 / P9b); HONEST DEVIATION, both reviewers confirmed faithful. browse empty(browse|search|filtered)/list; sell pick-empty|cannot-market|form (coin-split via COPPER_PER_GOLD/SILVER named consts); collect empty|items; reuses market_filters (filterMarketListings/paginateMarketListings) in the CORE, NOT re-derived (guarded). NEW src/ui/market_window.ts (class MarketWindow): owns tab/filters/page/sellItem/search/lastSig + lifecycle (open/close/isOpen/isSellTab/stageSell/refreshIfChanged/render); composes ...presentationBag; tabs + filter dropdowns + 3 bodies + buy/list/cancel/collect dispatch byte-faithful. NEW src/ui/char_view.ts (pure core, UI_PURE_CORES): buildPaperdollView -> {left,right} equip columns (helmet/shoulder/chest/mainhand | gloves/waist/legs/feet) + equipped ItemDef; PAPERDOLL-ONLY, DOM/Three/RNG-free. NEW src/ui/char_window.ts (class CharWindow): paperdoll + HUD-built stat/talent/progression + share/prestige + unequip/drag/keep-focus; the shared 3D preview + skin picker STAY HUD-owned (the single WebGL preview is borrowed by the skin-event overlay + the player card), triggered via renderPreview()/renderSkinPicker() callbacks; the char skin-event Math.random (randomSkinEventLandingAngle) is a SEPARATE overlay never in renderChar's scope, so it never crossed into the core (renderCharPreview/mountCharPreview/renderCharSkinPicker stay byte-identical in hud). hud.ts: renderMarket cluster + renderChar deleted (~712 net) via anchored sed line-deletes after asserting no interleaved foreign methods (the leaderboardPage field sat INSIDE the market field block = the Phase-7 interleaved-keep hazard, preserved); thin delegators + 2 lazy-closure deps fields; cross-window repoints (bags isMarketSell->isSellTab, stageMarketSell->stageSell); slow-divider proximity -> refreshIfChanged/close; locale-repaint -> render/renderIfOpen. WCAG focus-return ADDED to both (inline had none) incl a NEW closeManagedWindow 'char-window' case. D12 no-magic: zero painter hex; QUALITY_COLOR[q] ?? var(--color-quality-default); +2 tokens --color-slot-empty-text:#666 / --color-slot-empty-border:#444 (exact prior values). NEW tests market_view/char_view (state-union/paperdoll + same-input-same-output + two-shape decision-15 parity + char no-Three/no-RNG source assertion) + market_window/char_window (no-hex/named-const/no-dash + WCAG + delegates-not-re-derived guards). architecture.test.ts += market_view + char_view; client_shell market guards repointed hud.ts->market_window.ts; char has no hud-bound client_shell guard. Validation: tsc clean; full vitest 4127 pass/8 skip (393 files); biome clean on the 4 new files (hud.ts biome pre-existing-RED whole-file organizeImports+format = deferred per project policy, identical 2-error count at base 0a6d1e4f; 3 pre-existing dead options imports left as-is, dead at base; P8b adds ZERO new dead imports); build all 4 entries no side-effects; purity green + TEETH PROVEN (injected ../render/characters/assets into char_view reds the guard, green after revert); localization green (NO new player-visible string). Presentation-only (D4): world_api/sim/server/net/headless diff EMPTY. REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 3 NICE-TO-HAVE (painter tests are source-guards per P6/P7/P8a; one stale renderChar comment fixed; pre-existing #bags block/flex inconsistency reproduced faithfully, out of scope); adversarial per-window parity Workflow (2 parallel default agents, branch-by-branch vs 0a6d1e4f) PARITY HELD/HELD, every divergence NICE-TO-HAVE render-neutral or additive (focus-return; market locale lastSig-reset drop render-neutral since render() always full-rebuilds; pager/list fresher-snapshot equal-or-more-correct; char stat-grid whitespace; char ??null=same empty path; char drag op-reorder 4 independent side-effects). First parity Workflow FAILED (agentType cross-platform-sync emits its own report not StructuredOutput) -> re-ran default agent. HONEST WCAG: per-window roles/aria/labels/focus-return proven by source guards + the added focus-return; live-axe/forced-colors/computed target-size/:focus-visible DEFERRED to P15b per P6/P7a/P7b/P8a; the 3D preview canvas is OUT of a11y scope (stated). No perf gate (cold). With P8a + P8b done the PainterHost seam is proven across the full cold-window variety (dense control surface, async/snapshot window, Three-entangled window). commits 83f129e0 + c7545779 local. RE-RUN SENIOR-QUALITY AUDIT 2026-06-25 (ultracode 6-finder adversarial Workflow, 16 agents, over the COMMITTED extraction): 10 findings -> 3 refuted + 7 survived (1 minor + 6 nits), 0 BLOCKING / 0 MAJOR = extraction confirmed faithful + clean. HARDENED 2 test gaps: (1) market_view.test.ts coin-split now exercises the buyValue ?? LEFT arm (healing_potion 170c -> 1s70c) AND the nonzero-gold sellValue*4 RIGHT arm (deathlord_warplate 36000c -> 3g60s); the prior copper-only 40c case left the gold floor-division, the silver modulo, and the ?? left arm untested. (2) architecture.test.ts adds a forbiddenUiCoreImport negative test (flags three/render/game/net/painter, allows sim+sibling cores) so the UI-purity guard's teeth survive a future matcher weakening: the per-phase "guard must FAIL on an injected forbidden import" is now a durable regression test, not a manual ritual. vitest 4130 pass/8 skip. NICE-TO-HAVE tracked for P15b (none a regression): market filter listbox roving-tabindex/arrow-key nav (byte-faithful carry-over of the inline original; advertises listbox semantics it does not fully deliver to AT), an Escape/closeManagedWindow focus-return source-grep, and the live axe / forced-colors / computed target-size rows; the decision-10 "per-window target-size NOT deferred to P15" wording vs the 5-phase live-check-deferral practice is a doc tension to reconcile in P15 (not a P8b defect)) |
| P9a | Cold-window canvas pair: map + arena (preserve mediumHud call site + cadence) | medium | port | done (SPEC-vs-LIVE CORRECTION: the phase doc + recon framed BOTH windows as canvas painters, but renderArenaWindow is DOM/innerHTML -- only updateMapWindow is a true 2D-canvas window. So map -> a Canvas-2D painter with the D12 getComputedStyle-once --color-* token policy; arena -> a DOM Humble-Object painter (token policy N/A, colors in CSS). NEW src/ui/map_window_view.ts (pure core, UI_PURE_CORES): mapWindowMode(delve/overworld discriminator) + buildOverworldMapModel -> flat canvas-px draw model (blit fractions + view rect + cursor + zoneId + POI/portal/npc(?/!)/player-arrow(-facing)/ally(dedup-by-id, friends-first) markers + the >=MAP_DETAIL_ZOOM detail overlay decorations/buildings/props, radii from ppu=S/spanX w/ named consts). NEW src/ui/map_window_painter.ts (Canvas-2D, not registered): resolves 22 --color-map-* tokens via getComputedStyle ONCE/redraw, owns the cached whole-world decorations, faithful draw order; the delve branch stays with P6's delvePainter.paintWorldMapDelve (Hud routes by mapWindowMode -> no duplicated marker drawing). NEW src/ui/arena_window_view.ts (pure core, UI_PURE_CORES): buildArenaView -> {offline}|{live} union (info===null IS the offline note = the decision-15 online-only-shape trap; bracket resolve+commit, canSwitchBracket, 3-branch queueDisabled, party members/warn/none, in-match/queued/idle action, ladder+all-time rows w/ rank+me+knownClass, practice gate, the verbatim raw-row render-skip sig). NEW src/ui/arena_window.ts (class ArenaWindow, DOM painter): owns selectedBracket + all-time cache + per-bracket lbFetchedAt throttle (LEADERBOARD_REFETCH_MS) + lastSig + openerFocus + practiceHook; toggle/close/render/setPracticeHook + fetchLeaderboard (network); offline note + live panel HTML byte-faithful; +WCAG focus-return the inline lacked. hud.ts: updateMapWindow thinned (delve delegate + mapPainter.paintOverworld + this.mapView/cursor from the result + sr-only #map-summary via setText); drawMapDetail/renderArenaWindow/fetchArenaLeaderboard/toggleArena DELETED -> delegators; the mediumHud `display==='block'` guard now drives arenaWindow.render() (SAME call site + 250ms cadence); setFiestaPracticeHook forwards; closeManagedWindow 'arena-window' case for focus-return; the moved arena/map state fields removed; MAP_MAX_ZOOM/MAP_DETAIL_ZOOM moved to the core; dead imports pruned. +22 --color-map-* tokens (exact inline hex). play.html: #map-canvas role=img + data-i18n-aria=hud.core.mapCanvasLabel + sr-only role=status #map-summary live region. i18n: hud.core.mapCanvasLabel 'Map' + mapSummary 'Map of {zone}.' (English-only, NON-WORDY so the M16 non-Latin completeness gate passes with no fills; the delve summary reuses mapSummary w/ the delve name); full resolved + status + sha256 artifacts regenerated + staged. NEW tests: map_window_view (mode/parity/determinism/zoom-blit/detail/player/npc/ally) + map_window_painter (no-hex + getComputedStyle-once + 22 tokens + decorations-cache-once + mediumHud-call-site + cached-bg-not-rebuilt guards) + arena_window_view (offline/live + two-shape parity + bracket resolve/commit + queue gating + ladder/all-time + sig) + arena_window (WCAG focusable controls + focus-return + no-hex + named cadence guards); architecture.test.ts += both cores. Validation: tsc clean; full vitest 4171 pass/8 skip; biome clean on all 8 new files (hud.ts splice = no new violation class, the one getContext('2d')! is grandfathered); build all 4 entries; architecture purity (both cores) + client_shell + i18n completeness/resolved-hash/status-registry green after regen+stage. Presentation-only (D4): world_api/sim/server/net/headless diff EMPTY. WCAG: canvas accessible name + sr-only zone/delve summary; canvas pixel content OUT of SR scope (D10, stated); arena focus-return on toggle/X/Escape; live axe/forced-colors/target-size DEFERRED to P15b per P6/P7/P8. No perf gate (mediumHud cadence-gated like P6's delve pilot). REVIEWERS: qa-checklist 0 BLOCKING / 0 SHOULD-FIX / 4 NICE-TO-HAVE (all faithful-to-inline or intentional: in-match auto-close raw-hides w/o focus-return = faithful + better mid-fight UX; offline branch rebuilds each 250ms = faithful; painter's 3 tEntity wrappers duplicate hud helpers = intentional seam; #map-summary role=status+aria-live redundancy matches lang-select-status). commits local) |
| P9b | Cold-window DOM trio: questlog + spellbook + leaderboard (the one IWorld-consume) | medium | port | done (CLOSES the cold-window seam: P6 pilot through P9b, no inline cold windows remain. NEW leaderboard_view.ts (UI_PURE_CORES; ASYNC-FREE core: buildLeaderboardView maps a resolved LeaderboardPage or a loading/error discriminator -> ranked rows + off-page sticky standing + pager + clamped page) + leaderboard_window.ts: the packet's ONE consumed-new IWorld signature -- the painter awaits the paged `leaderboard(page,size): Promise<LeaderboardPage>` and owns the Promise/await/page-controls/closed-while-in-flight guard/server-clamp mirror; CONSUMED, NEVER CHANGED (D4). The async/paged shape is the textbook decision-15 online-only-shape trap; the parity test feeds BOTH a Sim-shaped and a ClientWorld-mirror page across every state incl a rejecting leaderboard() -> the NEW error state (t('game.leaderboard.retry'), reusing the existing fully-localized key; the inline collapsed rejection into empty). NEW spellbook_view.ts (class kit -> known/rank/onBar/toggleDisabled rows) + spellbook_window.ts (ability icons via iconDataUrl, hotbar toggle/drag, refreshHotbarControls = the one not-cold touch from hud.update() while open). NEW questlog_view.ts (list + selection resolution + detail; the LOG window, not quest_tracker) + questlog_window.ts (composes the presentation bag for the reward row; owns the selected quest id read back by Hud's /share command). hud.ts -266 net: 3 toggle delegators + 3 lazy dep-bags + closeManagedWindow cases + call-site updates (refreshLocalizedDynamicUi, the per-frame + reset-form-bar refresh, the /share selectedQuestId); inline render*/toggle*/pager/refresh + the 3 moved state fields removed. WCAG: focus-return added (inline leaderboard/spellbook had none), role=dialog/aria, leaderboard close <span>-><button>, loading role=status+aria-busy, error role=alert, capture-opener-before-closeOthers; player names esc()'d in the leaderboard painter (matches the questlog sibling). no-magic (D12): .lb-subtitle class + QUALITY_DEFAULT_COLOR token + &mdash;/&starf; entities. i18n: GOTCHA M16 flags "Close" WORDY so the English-only hudChrome.leaderboard.close key alone fails npm test -> filled the 5 non-Latin overlays, Latin English-pending. Tests: 3 view suites (per-state + decision-15 two-stub parity + determinism) + 3 painter guards; architecture +3 cores; client_shell spellbook repointed. Reviewers: qa-checklist 0 BLOCKING (1 MAJOR un-esc'd names FIXED); adversarial 5-finder Workflow 0 confirmed. tsc/biome/vitest 4245+8/build(all)/purity/M16-completeness green; presentation-only diff EMPTY. commits local) |
| P10a | Per-frame: xp + swing leak-fix + the PainterHost elided-writer extension | high | port+extend | done (the FIRST per-frame phase + the first perf gate; LANDS decision 5a. painter_host.ts grew the write-elision facet from 4 to 6 writers: makeWriterFacet(cache,stylePropCache,classCache,onWrite,onSkip) now also binds setStyleProp(el,prop,value) + toggleClass(el,cls,on), MULTI-SLOT keyed per (element,prop)/(element,class) via a per-element inner Map (writeSlot), so one element holds many custom props / toggled classes without clobbering each other or weakening the four single-slot writers (the Top-risk-1 cache-key guard). hud.ts: +hotStylePropCache/+hotClassCache, +private setStyleProp/toggleClass mirroring the four existing single-slot writers over those caches+counters, writerFacet wired with the 2 new caches so HUD-direct + painter writes share ONE skip-rate. NEW src/ui/swing_timer.ts (pure core, UI_PURE_CORES): ZERO imports, i18n-free, parameter-in/next-state-out edge tracking (recovers the swing interval on the reset edge, carries it while counting down; Hud holds swingPeriod/lastSwingTimer + feeds them back) -> {visible,frac,ready,labelKind(ready|seconds),seconds,nextPeriod,nextTimer}; the painter resolves the discriminator through t() (cast_bar pattern). NEW src/ui/swing_timer_painter.ts (caches #swingbar/.fill/.label once -> kills the per-frame $()+querySelector LEAK; routes setDisplay/setWidth/toggleClass/setText). NEW src/ui/xp_bar_painter.ts (consumes the existing xpBarView core; caches #xpbar/.fill/.rested/.label/#player-frame once -> kills the per-frame $() LEAK; routes setWidth + setStyleProp(--xp-fill x2 on bar+player-frame) + setStyleProp(rested left/width) + setText + toggleClass(overflow/rested)). Both inline Hud.update() blocks replaced by painter.paint(); the relocated swing comment em dash normalized to a colon. BYTE-IDENTICAL keys preserved (--xp-fill .toFixed(4); .toFixed(1) percent widths; swing seconds via formatNumber 1-digit) so the skip-rate cannot collapse. INCIDENTAL in-scope leak-fix: updateLowHealthVignette (a per-frame getElementById + raw setProperty/classList, unclaimed by any later phase) rerouted through the cached ref + the new private setStyleProp/toggleClass to exercise the new Hud-private writers. swing_timer registered in UI_PURE_CORES. NEW tests: painter_host.test.ts (the six-writer facet + the multi-slot cache-key regression guard: setStyleProp writes/skips per (el,prop,val) and does NOT collapse two props on one element; toggleClass tracks on AND off; two facets over shared caches keep one skip-rate) + swing_timer.test.ts (gating/edge-recovery/clamp/discriminator + same-input-same-output + decision-15 Sim-vs-ClientWorld-mirror parity feeding two stub shapes carrying host-specific extras + an i18n-free-core source guard) + swing_timer_painter.test.ts + xp_bar_painter.test.ts (recording-facet routing: the painters drive the six writers with byte-identical values + a no-raw-write source scan + the no-magic hex/rgb scan). The makeWriterFacet facet tests MOVED from delve_map_painter.test.ts (now a six-writer concern) -> painter_host.test.ts. architecture.test.ts += swing_timer; client_shell.test.ts mobile-XP-ring guard repointed hud.ts inline -> xp_bar_painter.ts (--xp-fill on bar+player-frame). VALIDATION: tsc clean; biome clean on all 8 new files (hud.ts 2err/31warn IDENTICAL to HEAD = zero new debt, proven by a HEAD-copy biome run); full vitest 4278 pass/8 skip (407 files); architecture purity guard PASSES + TEETH PROVEN (an injected `import ../render/renderer` reds swing_timer, green after revert). A11Y (D10): the xp bar + swing timer are NON-INTERACTIVE indicators conveying state via WIDTH + a TEXT label (swing seconds/ready; xp label), never color alone; they carry NO focusable control so target-size/focus-return DO NOT apply (boundary stated honestly, not claimed audited); 3D canvas out of scope. NO MAGIC VALUES (D12): the painters drive named-constant prop/class names + CSS custom properties; percent VALUE strings (.toFixed) excepted; zero literal hex/rgb (guarded). i18n: the swing label reuses hudChrome.swing.ready/.seconds (NO new keys); xp label already localized by xpBarView; no concat/no ?? fallback. PERF GATE (the first; scripts/perf_tour.mjs desktop, same M4 Max as the P0 baseline, TWO stable runs): frameP95 250ms == baseline 250ms; hudHotDomSkipRate 0.965 >= baseline 0.962 -- IMPROVED, exactly because the formerly-raw xp(--xp-fill/.rested) + swing + vignette writes are now COUNTED through the elided helpers and skipped at steady state; 0 errors. Mobile profile NOT capturable on the untouched tree (deferred to P17a per perf-baseline-v016.md); the renderer-independent skip-rate is covered by the desktop capture. inputIntentToVisibleP95 ~6.4s in both runs tracks a prewarm-budget overrun (15.7s vs 12s, asset-loading boot noise on a busy machine), NOT a per-frame regression (the change only constructs 2 painters + caches refs once at boot). Presentation-only (D4): world_api/sim/server/net/headless/i18n.locales/src/styles diff EMPTY. REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 5 NICE-TO-HAVE (all verified non-issues: the vignette write-order flip is paint-frame-identical; the first-frame active=false no-op; one small SwingTimerState alloc/visible-frame at HUD cadence; the pre-existing #player-frame raw combat toggle touches a different class so it cannot stale the painter's multi-slot cache; hand-built parity stubs per the established pure-core pattern) / 1 VERIFY (mobile perf deferred to P17a, honestly disclosed). TAG the green-perf-gate commit (vitest-4278 / skip-0.965) so a later cumulative regression (first surfaced at P17a) bisects here. commits local. NEXT: phase-10b-perframe-unit-frame-family.md) |
| P10b | Per-frame: unit_frame FAMILY core+painter (player first instance) | high | port+extend | done (the load-bearing reusable FAMILY, decision 9. NEW src/ui/unit_frame.ts pure core in UI_PURE_CORES: UnitFrameDescriptor {present, hpFrac, hpText, resourceKind: ResourceType|'none'|null, resFrac, resText, levelText: string|null, name, portraitKey, absorb: AbsorbBarInput|null, dead, outOfRange} -> UnitFrameView; the core gates present/hidden (shared HIDDEN const), resolves the absorb overlay via the shared absorbBarView (player/target/party never re-derive it), and maps resourceKind -> resClass via unitResourceClass (FOLDS the old inline rage:energy:mana ternary + adds the 'none' case a resource-less target needs); preformatted fracs/text pass through verbatim; NO hardcoded id, NO single-instance assumption, DOM/i18n-free, deterministic. NEW src/ui/unit_frame_painter.ts thin write-elided painter over the six PainterHostWriters: element set REQUIRED frame/level/hpFill/hpText + OPTIONAL name/absorb/resource-group; options shownDisplay (omit for an always-visible frame) / repaintPortrait (the portrait repaint gate lives in the PAINTER not the core, so P11b target's lastPortraitTarget gating is the same code path) / stateClasses (gate dead+oor; party-only). The PLAYER is the first instance: the inline Hud.update block -> playerFramePainter.paint(unitFrameView({...p...})); updateAbsorb('#pf-absorb',p) (raw style.transform + classList.toggle overshield) and the resource className swap FOLD into the elided writers (className -> 3 toggleClass(rage/energy/mana) so it never clobbers the 'low' class updateLowResource owns nor the static 'bar' base). BYTE-FAITHFUL: raw scaleX fracs (no toFixed), String(level), unchanged hp/res text formats; the player painter omits name (static, kept at the login $('#pf-name') set), shownDisplay (frame is always CSS flex), stateClasses, and repaintPortrait, so the player path matches the old inline block exactly (folding name/dead/oor was REJECTED as unfaithful + a tiny skip-rate cost). updateLowHealthVignette + updateLowResource STAY at the Hud call site (player-only side effects, own cores, OUT of the shared family). A11Y (D10): play.html AND index.html (BOTH game entries, see RE-AUDIT) #player-frame role=group + data-i18n-aria + aria-label, hydrated by main.ts translatePage to t(); ONE English-only key hudChrome.unitFrame.playerLabel='Your Hero' (NON-WORDY so the M16 completeness gate does not flag untranslated-English in zh/ja/ko/ru; the phase's 'Player frame'/'Your character' are both wordy and would; user chose non-wordy over filling overlays, staying English-only/no-overlay per the phase); hp/res conveyed as text (#pf-hp-text/#pf-res-text not aria-hidden); no focusable control in the frame so target-size/focus-return N/A; 3D portrait/world out of a11y scope. unit_frame registered in UI_PURE_CORES (architecture purity guard PASSES + TEETH PROVEN: injected document.title reds it, green after revert). TESTS: unit_frame.test.ts (the TWO-DESCRIPTOR contract drives the FULL P11 field set - resClass 'none', levelText null, dead boss, absent target, out-of-range party, null absorb - NOT a stub, proving P11a/b/c reuse needs no core change; + discriminator incl null->mana/'none'; present gate; absorb+overshield; determinism; decision-15 Sim-vs-ClientWorld-mirror parity feeding the entity-shaped absorb with host extras the core ignores; DOM/i18n/id-free guards) + unit_frame_painter.test.ts (recording-facet routing: the player instance writes the byte-exact level/hp/hpText/absorb x2/resType x3/res/resText sequence and NOTHING else; a fuller instance with stateClasses+shownDisplay+name writes display/name/dead/oor; target-like no-resource + party-like no-absorb skip those groups; the portrait gate repaints only on key change; no-raw-write + no-magic-color scans). VALIDATION: tsc clean; biome clean on new files (hud.ts 0 new debt); full vitest 4307 pass/8 skip (409 files); css_corpus + client_shell green after the play.html attribute add; npm run build 4 entries green; localization_fixes 27 pass (key pending, PR-tier permits). PERF GATE (perf_tour desktop, same M4 Max as P0, baseline re-measured via git stash for apples-to-apples): frameP95 250ms == baseline 250ms HELD; hudHotDomSkipRate 0.962/0.965/0.962 (min 0.962 == P0 baseline, touches P10a 0.965); deterministic writes 53 vs baseline 48 = the +5 mandated absorb x2 + resType x3 folds of previously-RAW writes now COUNTED+elided. Mobile NOT capturable (known P0 #mobile-preflight portrait breakage, deferred to P17a; renderer-independent skip-rate covered by desktop). REVIEWERS: qa-checklist 0 BLOCKING/0 MAJOR (28 PASS, byte-faithfulness traced; 2 MINOR-informational: safe write reorder across independent elements, ~2 short-lived objects/frame within budget) + adversarial coverage 0 BLOCKING/0 MAJOR (byte-faithful across 8 categories; MINOR: call-site formatting not unit-pinned ACCEPTED - extracting a player-only builder for one use violates no-abstraction-for-one-use + both reviewers hand-verified; the import-order nit fixed; the 'bar'-base invariant benign+documented). green-perf-gate commit c35f42b2 TAGGED frontend-v016-p10b-perf-gate. commits local. The unit_frame FAMILY is now the seam P11a/P11b/P11c REUSE with no core change. RE-AUDIT 2026-06-25 (quality re-run, 5-dimension adversarial Workflow: 22 raised -> 7 in-scope fixed, 15 rejected as spec-intended/pre-existing/self-rejecting): (1) MAJOR a11y entry-parity gap FIXED - the original a11y change landed on play.html ONLY, leaving index.html (the / entry; both boot main.ts + ship the same HUD) a bare unlabelled div, so the acceptance criterion was false on the primary entry; mirrored role=group + data-i18n-aria + aria-label onto index.html (same key, NO i18n regen). (2) NEW client_shell guard asserts the player-frame group label in BOTH entries (mirrors the map-canvas parity test) so it cannot recur. (3) painter no-raw-write guard now also forbids .className (the exact write this phase folded away) AND a px literal (decision 12 names hex/px/color). (4) painter levelText null->'' branch now pinned (the P11 level-hide path). (5) NIT hud.ts updateLowResource reads this.pfResourceEl instead of re-querying $('#pf-resource') each frame (cached ref used 3 lines up). Re-validated: tsc clean; biome clean on all changed .ts; full vitest green (+2 tests); npm run build 4 entries green; perf_tour desktop frameP95 250ms HELD + hudHotDomSkipRate 0.97 >= baseline. NEXT: phase-11a-perframe-cast-bars.md) |
| P11a | Per-frame: cast bars (eat/drink discriminator, i18n-free core) | high | port+extend | done (BLOCKING design 1: the eat/drink overlay rides an i18n-free CORE discriminator the PAINTER localizes, NOT a t() call in src/render/cast_bar.ts. NEW consumeBarState(eating: Consuming\|null, drinking: Consuming\|null) -> {visible, fill(remaining/CONSUME_DURATION, clamped), mode: 'eat'\|'drink'\|'eatdrink', remaining}: the longer-remaining consumable drives the bar (>= ties to food, matching the inline block); castBarState BYTE-UNCHANGED. BLOCKING design 2: eat/drink is PLAYER-ONLY - it is a SEPARATE pure helper (option b) layered on the player instance, so the generic-Entity castBarState path the TARGET also uses stays cast/channel and the target can never render eat/drink. NEW src/ui/cast_bar_painter.ts: ONE instance-parameterized painter (decision 9, no hardcoded ids). CastBarOptions {resolveCastLabel(state)->string, clearOnHide?}: PLAYER passes resolveCastLabel = (s)=>castDisplayName(s.label) + clearOnHide:true + a consume in its paint input; TARGET passes (s)=>s.label (RAW id, byte-faithful: the old target block set the raw tcb.label, NOT castDisplayName - preserved as an instance difference, NOT unified, to keep zero behavior change) + no clearOnHide + NO consume. paint() routes setDisplay(block/none) + toggleClass('channel') + setWidth('${(fill*100).toFixed(1)}%') + setText(label) + setText(formatNumber(max(0,remaining),{1,1})) through the PainterHostWriters facet in the EXACT inline order so the hotWriteCache keys line up byte-for-byte; the .channel class (4 formerly-RAW classList.toggle/add/remove writes) now goes through the multi-slot toggleClass (separate hotClassCache, no collision with hotWriteCache; grep-confirmed only the painter touches `channel` on #castbar/#tf-castbar - renderer.ts toggles it on the DIFFERENT .np-castbar nameplate). Both inline Hud.update blocks deleted; the player cast UNIFIED onto castBarState (the inline cast branch did NOT use the core - it inlined frac with Math.max(0.01,castTotal); the core's castTotal<=0/dead guards + [0,1] clamp differ only on non-occurring, strictly-safer edges, byte-faithful for every reachable state). The orphaned private Hud.setWidth was REMOVED (the cast bars were its last caller; the facet setWidth shares the same hotWriteCache + 'width:' key, so no skip-rate/parity regression - biome noUnusedPrivateClassMembers would otherwise red it); painter_host.ts 6-writers doc refreshed (Hud's private mirror is now 5; the facet interface still exposes all 6). A11Y (D10, WINDOW/CONTROL row): #castbar/#tf-castbar role=progressbar + aria-valuemin=0/aria-valuemax=100 + data-i18n-aria (hydrated to a t() aria-label by main.ts:3966 translatePage, NO concat/?? fallback) in BOTH index.html AND play.html (the recurring play.html/index.html parity miss; client_shell.test.ts loops [html,playHtml] and pins both bars in both entries); ONE English-only catalog group hudChrome.castBar.{playerAria='Your Cast Bar', targetAria='Unit Cast Bar'} (NON-WORDY: longest lowercase run 3, so M16 does not flag English-fill in zh/ja/ko/ru; 'Target' would be wordy, 'Unit' is accurate for friendly+enemy targets and matches the unitframe class); aria-valuenow OMITTED (indeterminate-rate bar: a per-frame valuenow is SR-announcement spam AND the elided-writer facet has no attribute writer, so a raw setAttribute would break decision 5a; min/max satisfy the aria-value* acceptance); the eat/drink LABEL reuses hud.core.eating|drinking|eatingDrinking (NO new label keys); forced-colors fill shading (the .fill gradient is stripped) DEFERRED to P15b which owns the browser-a11y/CSS pass (no CSS-rule changes here; the bar border+outline + the visible label/timer text survive). Architecture guard HARDENED (decision 6/8): forbiddenRenderCoreImport now also bans the i18n runtime (/(?:^\|\/)[a-z_]*i18n$/) so a t()/tEntity call in the i18n-free core fails the guard, not just the file header (the painter is correctly NOT in UI_PURE_CORES so it may import i18n) + a teeth unit test mirroring the ui-core one; cast_bar stays in RENDER_PURE_CORES. TESTS: cast_bar.test.ts (+consume hidden/eat/drink/eatdrink/longer-driver/tie-to-food/clamp/determinism + decision-15 Sim-vs-ClientWorld parity feeding the client Consuming records DIVERGENT non-remaining fields (itemId/hpPer2s/manaPer2s zeroed) so it proves the core reads ONLY .remaining, not a tautology) + NEW cast_bar_painter.test.ts (recording-facet byte-order routing for BOTH instances; cast-wins-over-consume; player clearOnHide full clear vs target display-only hide; target-never-eats; no-raw style/classList/textContent/className/setAttribute/setProperty scan; no-magic hex/rgb/px scan) + client_shell both-entries progressbar guard + the forbiddenRenderCoreImport teeth assertions. VALIDATION: tsc clean; biome 0 new debt on hud.ts (NET -1: the (p.eating??p.drinking)! moved to the core then refactored away, + the orphaned setWidth removed); full vitest 4327 pass/8 skip (410 files); architecture TEETH PROVEN (injected a REAL `import { t } from '../ui/i18n'` + `document.title` -> RED on BOTH the i18n-ban and the DOM-global guards -> green after revert). PERF GATE (perf_tour desktop, same M4 Max as P0 - identical bootMiB 68.779 confirms same machine): frameP95 250ms == baseline 250ms HELD; hudHotDomSkipRate 0.963 >= baseline 0.962 (HELD/improved 54 writes/1424 skipped/1478 total - the 4 formerly-RAW .channel classList writes are now COUNTED + steady-state SKIPPED through toggleClass, the same honest accounting nudge P10a/P10b saw). Mobile NOT capturable (P0 #mobile-preflight portrait breakage, deferred to P17a; renderer-independent skip-rate covered by desktop). Presentation-only (D4): world_api/sim/server/net/headless/i18n.locales/src/styles diff EMPTY. REVIEWERS: qa-checklist 0 BLOCKING/0 MAJOR (24 PASS, 3 VERIFY = perf which WAS run 0.963 + full CI gate, 4 MINOR/NIT) + a 5-dimension adversarial Workflow (elision/i18n-free, byte-faithful, a11y/parity, decision-15/determinism) + a completeness critic that independently read painter_host/renderer/main = READY TO COMMIT, 0 BLOCKING/0 MAJOR. Folded 2 reviewer MINORs (the decision-15 consume parity made non-tautological; the forbiddenRenderCoreImport teeth test added) + 2 critic NITs (switched Foe->Unit for friendly-target accuracy; refreshed the painter_host 6-writers doc). The TARGET cast instance built here is CONSUMED by P11b's target frame (P11b does NOT rebuild the cast bar). commits local. NEXT: phase-11b-perframe-target-frame.md) |
| P11b | Per-frame: target frame (unit_frame instance) | high | port+extend | done (PROOF of the decision-9 family contract: the target is the FIRST richer instance and instantiates the P10b family with NO core (unit_frame.ts) change, forking nothing - the descriptor P10b validated against the FULL target field set already covered it. NEW targetFramePainter = new UnitFramePainter(writerFacet, {frame/name/level/hpFill/hpText/absorb=cached #tf-absorb}, {shownDisplay:'flex', repaintPortrait}); the inline target block -> targetFramePainter.paint(unitFrameView({present, hpFrac, hpText=dead?Dead:`hp / maxHp`, levelText=boss?BOSS_SKULL_GLYPH:String(level), name=entityDisplayName, portraitKey=String(id), absorb=dead?null:target, resourceKind:'none', dead:false, outOfRange:false})) + the target-only sub-parts. PAINTER EXTENSIONS (instance-agnostic, NOT a fork): the portrait gate now RESETS on the !present (hide) path (lastPortraitKey=null) so re-acquiring/reused-id repaints - the old lastPortraitTarget=-999 reset - plus a NEW invalidatePortrait() the Hud calls on onPortraitsReady (replacing the -999 reset when the 3D assets load). The core is UNCHANGED (still in UI_PURE_CORES, teeth re-proven). TARGET-ONLY writes routed through the P10a extension: elite class -> toggleClass(targetFrameEl,'elite'); hostile/friendly name color -> setStyleProp(targetNameEl,'color',var(--color-hostile\|friendly)); combo 'on' -> toggleClass(pip,'on'); the boss/elite TAG stays setText(t('hud.core.boss'\|'elite')) in hud. DEEP-REVIEW CORRECTION: the absorb node is CACHED ONCE (private targetAbsorbEl=$('#tf-absorb')) driven by the family paintAbsorb, replacing the old per-frame updateAbsorb('#tf-absorb',e) hardcoded-selector document query (decision 9 + per-frame violation); the dead updateAbsorb method + the absorbBarView import REMOVED. drawTargetPortrait reads a transient targetPortraitSubject set immediately before the synchronous paint (the gate fires inside paint): player->drawClass(skin), else->drawCrest(crestIdForEntity). Combo pips lazy-built ONCE (children.length !== COMBO_PIP_COUNT) then only toggled; count = comboTargetId===target.id?comboPoints:0. Target debuffs CONSUME renderAuras('debuffs') (auras pool = P12); target cast bar CONSUMES P11a's cast_bar TARGET instance (no rebuild). ORPHANED-WRITER (P11a pattern): the target hp transform moving onto the family painter orphaned the private Hud.setTransform (target was its LAST caller); REMOVED (facet setTransform shares hotWriteCache + 'transform:' key, parity-neutral; biome noUnusedPrivateClassMembers would otherwise red it). NO MAGIC (D12): BOSS_SKULL_GLYPH (pre-existing U+2620 skull named, NOT a new emoji), COMBO_PIP_COUNT, ABSENT_TARGET_DESCRIPTOR named; name color is a var(--color-*) token. A11Y (D10): #target-frame role=group + data-i18n-aria=hudChrome.unitFrame.targetLabel + aria-label in BOTH index.html AND play.html (entry parity, client_shell loops [html,playHtml]); ONE English-only key targetLabel='Your Mark' (NON-WORDY so M16 does not flag English-fill in zh/ja/ko/ru; faction-neutral); frame is INFORMATIONAL (selection = 3D world click + a pre-existing right-click contextmenu unchanged), no new keyboard/target-size duty; live-region target-name announcement DEFERRED to P15a (the group keeps its static name); the hostile-vs-friendly NAME COLOR is the one color-alone cue (PRE-EXISTING, byte-faithful) - a forced-colors redundant cue DEFERRED to P15a (no CSS-rule changes here, matching P11a); elite/boss carry non-color cues (the ELITE/BOSS tag text + skull). TESTS: unit_frame_painter.test.ts (+reset-on-hide repaint + invalidatePortrait forced repaint); NEW tests/target_frame.test.ts (D15 Sim-vs-ClientWorld parity modeling the mirror FAITHFULLY: server WireAura omits the absorb value + online.ts forces aura.value=0, so the shield is OFFLINE-ONLY; asserts the wire-carried frame fields + castBarState + the combo selection MATCH and documents the absorb as the ONE intended divergence, NOT a false toEqual); client_shell.test.ts (role=group both-entries parity + hud source guards: #tf-absorb queried exactly once, elite/name-color/combo routed through the elided writers, combo lazy-build guarded); options_window.test.ts (hardened the fragile update()-slice anchor: hudTs.indexOf('update(') -> indexOf('\\n  update(): void {'), since a comment mentioning update() shifted the slice into a later method). VALIDATION: tsc clean; biome 0 new debt on hud.ts (noUnusedPrivateClassMembers held at the pre-existing 2 after the setTransform removal); full vitest 4338 pass/8 skip (411 files); architecture purity TEETH re-proven (injected document.title reds unit_frame, green after revert). PERF GATE (perf_tour desktop, same M4 Max as P0): frameP95 250ms == baseline 250ms HELD; hudHotDomSkipRate 0.962 == P0 baseline 0.962 HELD (the formerly-RAW elite/name-color/absorb/combo target writes now COUNTED + elided; the rate climbs 0.832->0.962 through the tour as steady-state writes skip). Mobile NOT capturable (P0 #mobile-preflight portrait breakage, deferred to P17a; renderer-independent skip-rate covered by desktop). Presentation-only (D4): world_api/sim/server/net/headless/i18n.locales/src/styles diff EMPTY. REVIEWERS: qa-checklist 30 PASS/0 FAIL/0 BLOCKING + a 5-lens adversarial coverage Workflow (byte-faithfulness CLEAN, write-elision/perf CLEAN, a11y 1 NIT pre-existing-deferred, parity 1 MAJOR + 1 MINOR on the TEST, no-magic/hygiene 1 NIT). FOLDED the parity MAJOR (the absorb cross-host equality was FALSE online - rewrote the test to model the mirror's value-zeroing + document the offline-only shield) + the MINOR (fixtures de-tautologized + hostile-color parity added) + the hygiene NIT (dangling setTransform-removal comment cleaned). The TARGET cast instance from P11a is CONSUMED here, not rebuilt. commits local. NEXT: phase-11c-perframe-party-pool.md) |
| P11c | Per-frame: party frames (innerHTML-wipe -> keyed pool) | high | port+extend | done (top-risk 3 retired. NEW src/ui/party_frame_row.ts = the one-time row BUILDER (createPartyRow builds the .party-frame.panel DOM + the dead/combat/oor badges + crest + hp/resource bars, attaches click/contextmenu/keydown ONCE, constructs the per-row unit_frame INSTANCE) + the PURE live-slot partyRowHandlers factory; NEW src/ui/party_frames_painter.ts = the HOT path (a pid-keyed Map pool + a free list; sync() reconciles create/reuse/recycle/detach + repaints in place; the per-frame no-raw-write guard scans THIS file - the builder is intentionally exempt). Each member is an INSTANCE of the P10b unit_frame family (decision 9, NO bespoke party core): I extended ONLY the family PAINTER src/ui/unit_frame_painter.ts (hpText + resource.text -> OPTIONAL elements; a formatScaleX option keeps the party bars' inline .toFixed(3) precision) - player/target omit formatScaleX + still pass the text elements -> raw scaleX -> BYTE-IDENTICAL; the pure core unit_frame.ts is UNCHANGED. LIVE-SLOT (top-risk 3): the listeners + the crest gate read row.slot.member (overwritten before paint), never a captured member, so a row recycled (free-list) to a reused pid acts on the NEW member; a test recycles a freed row to a new pid + asserts the click/contextmenu read it. HOIST: NEW pure partyFrameSignature in src/ui/party_frames.ts (selectPartyFrameMembers UNCHANGED) = a single string pass, NO selector-array alloc, called BEFORE the selector so an unchanged party short-circuits without allocating the sorted/filtered/mapped arrays (byte-faithful to the old inline sig incl the pre-existing mres omission). Per-frame writes all elided: --cls -> setStyleProp, combat + below-target -> toggleClass, dead/oor -> family stateClasses, the 3 badges -> setDisplay (persistent nodes, display-toggled = the forced-colors-safe icon cue), leave label -> setText. A11Y (D10): each row role="button" + tabIndex=0 (was a bare unkeyboardable div), Enter/Space -> target, the Menu key opens the ctx menu natively (row-box fallback when clientX/Y are 0); the member NAME is the accessible name (visible text, recycle-safe, NO per-frame setAttribute); the 3 badges are aria-hidden decorations (so '⤢' does not leak into the button name); #party-frames role="group" + data-i18n-aria="hudChrome.unitFrame.partyLabel" in BOTH index.html + play.html (client_shell parity guard) + ONE English-only key partyLabel='Your Band' (NON-WORDY, no overlay edit); focus ring = the existing global [tabindex="0"]:focus-visible !important (shell.css, overrides the .panel 1px outline). RELOCALIZE: pooled rows reuse their DOM so refreshLocalizedDynamicUi (woc:languagechange) calls partyFramesPainter.relocalize() to re-set badge tooltips + leave label in place (qa reviewer caught the staleness). DECISION 15: the oor flag is member x/z vs player pos; offline Sim sends full-precision x/z, server partyWire sends round2(x/z) - a parity test drives BOTH + asserts oor agrees away from the 100yd knife-edge. VALIDATION: tsc clean; biome clean (hud.ts 0 new debt, base==work 32); full vitest 4358 pass/8 skip (412 files); build 4 entries green; architecture purity guard PASSES + TEETH PROVEN (injected document.title reds party_frames.ts); i18n regen STAGED + M16 clean. PERF GATE (perf_tour desktop, same M4 Max as P0): frameP95 250ms == baseline HELD x4; hudHotDomSkipRate median 0.962 == P0 baseline (the +1 write 53->54 = the below-target toggle, RAW before now COUNTED+elided, the honest-accounting dip P10b/P11a documented; the relocalize/aria-hidden folds are creation-time/cold-path, hot path byte-unchanged). REVIEWERS: qa-checklist 0 BLOCKING / 1 SHOULD-FIX (badge-tooltip relocalize FOLDED) + adversarial coverage Workflow (6 dimensions) 0 BLOCKING / 2 MAJOR both REFUTED on verify (focus ring exists globally; the mres stale-fill is byte-faithful + cannot occur) / 4 MINOR / 7 NIT - FOLDED relocalize + oor aria-hidden + a comment; DEFERRED the raid-group screen-reader conveyance + mres-signature omission to P15a (pre-existing, byte-faithful, not P11c regressions). commits local. The keyed-pool discipline (listeners-once + live-mutable-slot + free-list recycle) de-risks the auras pool P12b + the FCT pool P13. NEXT: phase-12a-perframe-action-bar.md) |
| P12a | Per-frame: action bar (multi-bar descriptor) + the allocation-budget spike | high | port+extend | done |
| P12b | Per-frame: auras keyed pool + minimap markers (canvas) | high | port+extend | done (top-risk 3 retired for auras + the canvas no-magic surface landed. AURAS: NEW src/ui/auras_view.ts (UI_PURE_CORES) = createAurasView(mode, deps) -> tick(entity) returns a REUSED {slots,count} pool (grown to high-water, mutated in place; alloc proxy probes container + slots array); the 22-kind debuff ALLOWLIST + negative-value buff_* lives in-core (isAuraDebuff); mode-parameterized 'all'/'debuffs' (decision 9); name/stacks/iconId via injected deps. NEW src/ui/auras_painter.ts = keyed per-aura node pool replacing __sig + innerHTML wipe: free-list recycle, tooltip attached ONCE per node reading a LIVE mutable record (rec updated BEFORE writes, so recycle A->B renders B - stale-capture test pins it), all writes elided (setStyleProp icon-on-change / toggleClass debuff / setText dur+stacks / setDisplay stacks-badge), reconcileOrder minimal-move. hud: 2 instances (#buff-bar + #tf-debuffs), renderAuras + __sig DELETED. MINIMAP: NEW src/ui/minimap_markers.ts (UI_PURE_CORES) = createMinimapMarkers().build(world,S,pxPerYard) -> REUSED {markers,zoneId} wrapper, DISCRIMINATED Marker union (ally/npc/portal/object-loot/mob/mob-loot/party-disc/party-arrow/player) for the OVERWORLD branch ONLY (the in-delve schematic stays delegated to delve_map_painter, the already-extracted discriminated sibling); minimapMode = the delve/overworld source of truth wired into hud.updateMinimap; friend/guild/party classification in-core, Sets built ONCE per call (the recon 'Sets off the hot path / collapse the double-scan' claims were FALSE vs live source - the entity + party loops iterate DIFFERENT collections - and are dropped). NEW src/ui/minimap_painter.ts = thin CANVAS painter: 12 --color-minimap-* tokens via getComputedStyle ONCE/redraw (cached, never per-marker), every hex/font/size/radius named, cached terrain blit, #zone-label via the elided setText (EXEMPT from the routing test, gated on fastHud ~10Hz + cached bg + frameP95). NEW --color-minimap-* tokens in tokens.css. Alloc reuses the P12a proxy (tests/util/alloc_probe.ts), NO new proxy; the minimap is wrapper-floor only (per-marker variants rebuilt by design - a discriminated union cannot share one fat reused slot; 10Hz; perf_tour is the backstop). VALIDATION: tsc clean; biome clean; full vitest 4439 pass/8 skip (419 files); purity guard PASSES + TEETH PROVEN (injected document.title reds auras_view). PERF GATE (perf_tour desktop, same M4 Max as P0): frameP95 250ms == baseline; hudHotDomSkipRate cumulative 0.960 (steady-state incremental ~0.998; the 0.002 dip is the P12a-precedent boot-write-count artifact - previously-uncounted innerHTML writes now route through the COUNTED elided cache while actual DOM churn DROPPED; user-accepted). REVIEWERS: qa-checklist 0 BLOCKING (9 dims) - FOLDED F2 (the decision-15 parity test now MODELS the value-zeroing, not only the parity-safe stacks) + F3 (minimapMode wired into hud); F1 (negative-buff value-zeroing online/offline divergence, reachable via 7 mob stat-saps, the wire decodes value:0) DOCUMENTED pre-existing + byte-faithful + out-of-scope (a wire change: send value or a debuff flag); F4/F5/F6/F7 accepted NITs. commits local. The keyed-pool discipline + the canvas getComputedStyle-once policy de-risk the FCT pool (P13). RE-AUDIT 2026-06-25 (fresh adversarial Workflow, 4 dims x verify): 1 NEW MAJOR fixed (the keyed pool collapsed two SAME-ID auras from DIFFERENT sources onto one node - the sim dedups by id+sourceId sim.ts:4340, so a boss with the same DoT from two casters / two healers' same HoT on you carries duplicate-id auras; old renderAuras rendered one .buff each. Fix = painter disambiguates same-id duplicates by per-frame OCCURRENCE INDEX, NOT the composite id+sourceId which online.ts zeroes on the wire; core 'ids unique per entity' comment corrected; new painter+core regression tests) + perf folds (memoized Intl.NumberFormat per locale+options in i18n.ts mirroring pluralRulesCache, so every-frame formatNumber callers stop rebuilding a formatter; detach-sweep -> pool.values() drops a per-entry tuple alloc on the 60fps buff bar) + test teeth (minimap guard asserts resolveColors called once + never inside drawMarkers; auras .className pinned to 3). Gated metrics invariant-or-better by construction (no DOM-write-count change -> skip-rate held; strictly less per-frame work -> frameP95 cannot rise), gate holds without re-running the E2E. full vitest 4442 pass/8 skip. NEXT: phase-13a-perframe-fct-core-driver.md) |
| P13a | Per-frame: FCT core + per-frame driver (folded into hud.update) | high | new | done (the determinism-critical FCT half landed with ZERO behavior change; P13b only swaps the spawn impl onto a proven, already-ticking driver. NEW src/ui/fct_core.ts (UI_PURE_CORES) = describeFct(event, jitter01) -> pure spawn DESCRIPTOR {text, colorToken (keyed by kind+isSelf), crit, anchor (head-offset world pos), jitterOffset (screen px), riseDistance, ttlMs}. Jitter INJECTED jitter01*FCT_JITTER_RANGE - FCT_JITTER_RANGE/2 (0->-15, 1->+15) so the core stays Math.random/Date.now/performance.now/DOM-free AND clock-free (ttl is a duration; the spawn clock lives in the driver's bornAt). 9 FctKind -> 11 FctColorToken covering all 8 live spawn sites; crit flips ONLY the crit flag (never the token), matching the live fct() that keys color by call-site arg + crit by class. NO magic values (decision 12): FCT_JITTER_RANGE=30, FCT_TTL_MS=1250, FCT_ANCHOR_HEAD_OFFSET=2.2, FCT_RISE_PX=76 all mirror the live fct()/fct-rise CSS (CRUX: the crit rise is -86px via .fct.crit, NOT the descriptor's 76 -> P13b drives rise off the CSS class, documented at the constant). Anchor is structural FctAnchorSource {pos,scale} so it reads identically off a Sim entity and a ClientWorld mirror -> decision-15 parity test feeds BOTH shapes (the structural type is the real guard; the test documents intent since FCT carries no Sim-vs-mirror field divergence, unlike P11b absorb). NEW src/ui/fct_driver.ts = FctDriver(writerFacet) class folded into hud.update()'s EVERY-FRAME tier (this.fctDriver.step(now) at hud.ts:4224, ungated between the fastHud-close and the mediumHud gate; decision 8, NOT a second rAF). DORMANT: nothing calls spawn() (verified no caller), live[] is always empty, step() early-returns -> zero DOM writes + zero alloc; the per-event fct() (hud.ts:6725), showSelfNote (6721), and the 7 switch sites + heal2 (5567-5889) are byte-UNTOUCHED. The driver takes the PainterHost write-elision facet so P13b's pooled writes are elided by construction (no raw-write path; the placeholder step() body already routes only through setText/setDisplay). fct_core registered in UI_PURE_CORES; the driver/painter deliberately NOT registered (P13b's painter may use Math.random for jitter). VALIDATION: tsc clean; biome 0 NEW debt on hud.ts (base==work) + clean on the 4 new files; vitest 26 pass (fct_core+fct_driver+architecture); purity guard TEETH PROVEN (injected document.title reds fct_core, reverted). PERF GATE (perf_tour desktop, same M4 Max as P0): frameP95 250ms == baseline; hudHotDomSkipRate 0.96 == the pre-P13a P12b floor -- the no-op every-frame step adds zero writes so it holds skip-rate BY CONSTRUCTION (the 0.960-vs-0.962 gap is the user-accepted P12a/P12b boot-write artifact, NOT a P13a regression; cf. P12b 'gate holds by construction' when a change touches no DOM-write count). Mobile NOT capturable on the untouched tree (portrait + #mobile-preflight boot timeout, re-verified) -> P17a; skip-rate is renderer-independent so the DOM-write gate covers both via the desktop capture. REVIEWERS: qa-checklist SHIP/PASS 0 BLOCKING / 0 SHOULD-FIX (decision-10 combat-text live region recorded as NOT DUE in P13a -> carried to P13b); adversarial Workflow 3 lenses 0 BLOCKING / 0 MAJOR / 8 NIT (all forward-looking; 2 folded as doc clarifications). DEFERRED to P13b: pooled-div ring painter + worldToScreen projection + getUiScale divide + behind-cull + hex->class-token CSS migration + ttl/FIFO eviction (placeholder step() never splices live[]) + max-concurrent cap + the 8 spawn-site migration (showSelfNote main.ts:1765 caller + its unused color param) + fct() deletion + the bounded-AoE perf gate + the combat-text live region (decision 10). commits local. NEXT: phase-13b-perframe-fct-painter-migration.md) |
| P13b | Per-frame: FCT pooled painter + spawn-site migration + bounded-AoE gate | high | new | done (the per-event createElement+setTimeout fct() lifted onto a FIXED-SIZE pooled-div ring; all 8 spawn sites migrated; the dormant P13a driver realized and removed. NEW src/ui/fct_painter.ts = FctPainter over the PainterHost write-elision facet: FCT_POOL_CAP=64 PRE-ALLOCATED divs (no createElement after construction; free-list + live[] kept in spawn order, so live[0] is the FIFO eviction victim -- no separate sequence counter). spawn(event, now): describeFct (injected Math.random jitter, allowed on the painter NOT the core) -> project the head anchor ONCE via renderer.worldToScreen -> behind-cull at spawn (faithful `if (v.behind) return`) -> claim a free slot or EVICT live[0] -> setText + colour-token toggleClass + crit toggleClass -> position left/top ONCE in author space ((v.x+jitter)/getUiScale, v.y/getUiScale) -> setDisplay -> appendChild -> restart the CSS rise ONLY for a same-tick EVICTED still-attached node. step(now) on hud.update()'s every-frame tier (decision 8; the P13a call site, fctDriver renamed fctPainter) ONLY recycles TTL-expired slots (now-bornAt>=ttlMs). SCREEN-ANCHORED (user decision: match WoW/classic combat text): the number floats up in SCREEN space via the CSS @keyframes and does NOT re-project per frame (it does not slide with the camera) -- byte-faithful to the old fct(). An earlier build re-projected the world anchor each frame; both reviewers flagged the camera-drift as a non-faithful feel change, and the user directed matching WoW, so it was reverted to project-once-at-spawn (step() now does no per-frame position write at all -- strictly fewer hot writes than the re-projection variant). COLOURS (decision 12): the 6 hardcoded FCT hex colours moved off el.style.color onto .fct-<token> CSS class rules in hud.css (#bbb/#fff miss-dodge self/other, #ffe97a/#fff dmg ability/auto, #ff5544 taken, #3ce63c heal, #b974ff xp, #4a9eff rested, #ff8c66 self-note), toggled via the elided toggleClass; el.style.color is GONE and no hex survives in the painter. ANIMATION RESTART (the pool-reuse hazard): a free / TTL-recycled node was detached in an earlier frame so re-append restarts the rise naturally; only a same-tick EVICTED (still-attached) node needs the forced animation:none->offsetWidth reflow->restore, so normal play pays ZERO reflow and only an over-cap AoE burst does. 8 SITES in hud.ts: the 7 SimEvent-switch fct() calls share one `const now=performance.now()` per handleEvents batch; showSelfNote (kind self-note; its vestigial color param dropped -> showSelfNote(text), the sole caller main.ts:1765 passes one arg, tsc-verified). The old private fct() DELETED; src/ui/fct_driver.ts + tests/fct_driver.test.ts removed (the dormant scaffold is now the realized painter; fct_core unchanged + still in UI_PURE_CORES, the painter NOT registered since it draws Math.random). NEW tests/fct_painter.test.ts (no-raw-write + no-magic source guards; pooled-ring proof over a fake DOM: pre-alloc, cap never exceeded, FIFO eviction no-drop/no-dup, TTL recycle, empty-pool step no-op, getUiScale divide, jitter min/mid/max, spawn-time behind-cull, screen-anchored no-reposition, conditional animation restart, colour+crit class swap, full elided routing, decision-15 Sim-vs-ClientWorld parity). PERF GATE authored in scripts/perf_tour.mjs: a 3-wave x 400-event AoE burst through the real hud.handleEvents->spawn path asserts the live #ui .fct node count is >0, bounded <400, and stable across waves (wired into hardErrors). VALIDATION: tsc clean; biome 0 NEW debt on hud.ts (base==work) + clean on the new .ts; full vitest 4469 pass/8 skip (421 files). PERF GATE (perf_tour desktop, same M4 Max as P0, 5 runs): frameP95 250ms == baseline; hudHotDomWrites PINNED at 152 every run -- FCT is dormant during the no-combat tour (empty pool -> step early-returns -> zero added hot writes), so the skip-rate is byte-identical to P12b BY CONSTRUCTION; the 0.956-0.966 reading is the user-accepted P12a/P12b boot-write band (frame-count noise on the denominator), NOT a P13b regression. FCT burst BOUNDED [64,64,64]=FCT_POOL_CAP every run (the unbounded createElement churn is eliminated). Mobile uncapturable on the untouched tree -> P17a; the renderer-independent skip-rate covers both via the desktop capture. The pre-existing software-WebGL prewarm-budget timeout (identical every run) is environmental, not a P13b gate. COMBAT-TEXT LIVE REGION (decision 10): DEFERRED to P15a with a TODO(P15a) marker in fct_painter.ts recording what to build (a polite role=status/aria-live region fed a FCT_ANNOUNCE_CADENCE_MS-coalesced summary, never raw per-damage streaming, plus aria-hiding the raw .fct nodes); FCT divs are decorative pointer-events:none transient text, so no focus trap; the a11y row is tracked honestly, not skipped. REVIEWERS: qa-checklist 0 BLOCKING (24 PASS, 4 NICE-TO-HAVE) + a 4-lens adversarial Workflow (pool-lifecycle / write-routing / migration / positioning, finder->adversarial-verify) 0 BLOCKING / 0 MAJOR / 6 MINOR+NIT confirmed / 5 refuted. FOLDED: the screen-anchored revert (per the user's WoW directive); the conditional animation restart (reflow only on eviction); dropped the dead sequence field (FIFO is array order); made isSelf honest at the heal/heal2 sites (inert, removes a latent trap); the projection-model comment. NOT FOLDED (intentional): descriptor.riseDistance carried-but-unread (P13a documentary data, CSS owns the rise); FCT numbers bypass formatNumber (pre-existing byte-faithful); cap:0 throw (unreachable). P14a tiers the pool's cap/lifetime/drop-non-crit knobs off the static fxLevel. commits local; green-perf-gate tagged frontend-v016-p13b-perf-gate. RE-RUN SENIOR-QUALITY AUDIT 2026-06-26 (ultracode 4-lens adversarial Workflow over the COMMITTED phase: pool-lifecycle / write-perf / faithfulness-spec / arch-tests, finder -> per-finding adversarial verify; pool lifecycle confirmed SOUND, 0 BLOCKING / 0 real MAJOR). FIXED (1e986fa0 + 71abf547): (1) dropped the dead per-spawn setDisplay('') write -- a pooled node shows by attach / hides by remove(), never display:none, and setDisplay shared setText's single-slot elision cache so neither elided, one fewer hot write per spawn (the row's earlier '-> setDisplay -> appendChild' sequence is now just '-> appendChild'); (2) corrected 2 stale hud.ts comments (the fctPainter field + the step() call still claimed step() 're-projects/repositions' from the reverted re-projection build); (3) documented the spawn-before-step frame-ordering contract the natural animation restart relies on + the over-cap eviction reflow characteristic; (4) test teeth: pin the shipping FCT_POOL_CAP=64 at the bound, a TTL-recycle x over-cap-eviction interleave with staggered bornAt, a hud.css .fct-<token> colour-faithfulness guard, renamed the stale 'FIFO-by-sequence' wording (FIFO is array position, no counter). NOT CHANGED (intentional, with rationale): the over-cap reflow-per-eviction (MINOR; only >FCT_POOL_CAP floaters in one tick; the proven reflow trick is robust + self-healing; WAA/batch trade robustness or stay O(n); P14a drop-non-crit caps eviction pressure); riseDistance carried-but-unread (P13a documentary data); the margin-top rise animation (byte-faithful CSS, a P14 compositor-transform opportunity); the getElementById mount cast (matches the Hud field-init pattern, #ui always present). Re-validated: tsc clean; biome clean on the changed files + hud.ts base==work (2err/28warn); full vitest 4472 pass / 8 skip (+3). NEXT: phase-14a-graphics-tiering.md) |
| P14a | Per-element graphics tiering (tier knobs read the static preset) | medium | port+extend | done (every per-element hot knob is now a PURE function of the STATIC ui tier (data-fx-level), never the governor. NEW src/game/ui_tier_knobs.ts -- registered in UI_PURE_CORES (a render-importable game leaf, imports ONLY the UiEffectsTier type) -- holds the named per-tier constants + pure functions: fctMaxConcurrent/fctTtlScale/fctDropNonCrit, minimapRedrawIntervalMs, auraVisibleCap/auraRefreshIntervalMs, target/partyFrameNonSelfIntervalMs, the cadenceDue(lastAt,now,interval) predicate (interval<=0 => always due => the full-tier path is the unchanged path) and coerceFxTier (unknown/unset => ultra, never silently sheds). Four slices read the tier via Hud.fxTier()=coerceFxTier(document.documentElement.dataset.fxLevel): (A) fct_painter.spawn drops non-crit DAMAGE-NUMBER floaters on low (scoped to fct_core.isDamageFctKind so xp/rested-xp/self-note/heal/miss/dodge survive low -- only the combat-number spam sheds), tiers the live cap (full==pool cap, byte-identical evict) + ttlMs scale; (B) minimap redraw gated by minimapRedrawIntervalMs inside the fastHud band (10Hz full -> ~3-4Hz low); (C) auras_painter caps the render loop at auraVisibleCap (full=+Infinity, byte-identical) + the two strips' repaint gated by auraRefreshIntervalMs (target debuffs force-repaint on target swap); (D) party (mediumHud, low ~2Hz) + target-frame body (low ~10Hz, force-on-target-swap) non-self gates -- the SELF/player frame stays UNGATED. NO new IWorld/sim/server/net; no new raw write (elided-writer guards still pass); no new t() string; NO motion knob (the focus-outline-under-motion check is N/A). The TWO-CONTROLLER guard is an import-absence source scan (ui_tier_knobs imports only ./ui_effects_profile, no governor/.state()/.levels/render/ui/net/DOM/Math.random) + a behavioral "only the tier moves a knob" test; the gfx.ts `ui` band stays governable:false (untouched). TESTS: NEW tests/ui_tier_knobs.test.ts + P14a describes in fct_painter/auras_painter (drop-non-crit damage-scoped, cap 24-vs-64, TTL, decision-15 Sim-vs-ClientWorld parity at fixed fxLevel) + an isDamageFctKind taxonomy test in fct_core. tsc clean; biome clean on all new/changed .ts (perf_tour.mjs is pre-existing-non-biome, outside the .ts ratchet; my additions there are clean, its pre-existing formatting untouched); full vitest 4499 pass/8 skip (422 files). PERF GATE (perf_tour desktop, same machine as P0; added a PERF_PRESET seed of woc_settings.graphicsPreset + the FCT burst now spawns CRIT damage so it exercises the per-tier cap): frameP95 250ms==baseline on ultra/high/low; hudHotDomWrites PINNED at 152 on ALL THREE tiers (byte-equivalence invariant; the report is captured before the burst); skip-rate ultra 0.956 / high 0.967 / low 0.989 (ultra at the bottom of the accepted 0.956-0.966 band, hot-writes identical => no elision regression; throttling the hot-heavy auras/target raises low's rate); FCT burst BOUNDED [64,64,64] ultra/high and [24,24,24] low. The knob that moved the perf needle most on low: the FCT live-cap + drop-non-crit-damage (64->24 floaters and the bulk of combat-number spawns shed); the minimap/aura/non-self throttles are smaller wins (their writes were already elided -- the saving is the skipped tick/diff CPU, not skip-rate). REVIEWERS: qa-checklist 0 BLOCKING (19 PASS, all NICE-TO-HAVE) + an independent adversarial coverage reviewer 0 BLOCKING / 0 MAJOR (byte-equivalence of eviction, cadence-gate staleness, fxTier coercion, aura cap, the crit-burst side-effect safety, purity -- all CLEAN). FOLDED: scoped drop-non-crit to damage kinds (an unscoped low drop also killed xp/self-note/heal/miss/dodge; both reviewers flagged it). SENIOR RE-AUDIT (2026-06-26, fairness pass; 5-dim adversarial audit + qa reviewer) supersedes the Slice C/D drafts so a preset tiers COSMETIC richness only, never ACTIONABLE info: (C) the aura cap is now DEBUFF-PRIORITY (auras_painter sheds BUFF overflow only via `if (!isDebuff && rendered >= cap) continue`), so a player debuff on the 'all' bar is NEVER hidden on low (the old "cap 8 can hide a debuff" tradeoff is RESOLVED, not accepted); full tiers byte-identical. (D) the PARTY throttle is REMOVED (partyFrameNonSelfIntervalMs + its constant deleted; hud reverted to unconditional updatePartyFrames) because party-member HP is a healer's only actionable signal -> party stays 4Hz mediumHud all tiers; Slice D is now TARGET-ONLY (coarse HP + full-rate cast bar), and the target swap-bypass is extracted to the unit-tested pure nonSelfRepaintDue(). PERF: minimap_painter caches the static --color-minimap-* tokens (resolved once, not getComputedStyle per ~10Hz redraw). TEST TEETH: LOW constants literal-pinned; a Hud.fxTier() source-scan pinning the static-stamp wiring (the previously-untested two-controller seam); debuff-past-cap-renders + all-debuff-exceeds-cap; 3-damage-kind drop uniformity; decision-15 parity strengthened to compare painted output by value; cadenceDue rewound-clock edge. RE-VERIFIED: tsc clean; biome clean on changed .ts (hud.ts monolith untouched by --write); full vitest 4510 pass/8 skip; perf_tour desktop all tiers frameP95 250ms + hudHotDomWrites PINNED 152 + FCT cap [64,64,64] full / [24,24,24] low; skipRate ultra 0.966 / high 0.96 / low 0.988. commits local, not pushed. NEXT: phase-14b-nameplate-extraction.md) |
| P14b | Nameplate extraction: nameplate_view core + painter + tier-driven interval | medium | port+extend | done (renderer.updateNameplates split into a pure view-core + a thin painter; the mobile-vs-desktop nameplate interval converted to a static-preset tier read. NEW src/render/nameplate_view.ts -- registered in RENDER_PURE_CORES alongside cast_bar (the phase doc's "UI_PURE_CORES" was loose; a src/render core belongs in RENDER_PURE_CORES) -- is DOM/Three/i18n-free: nameplatePlanInto(out, e, player, viewHeight, showNameplates) writes per-entity {hidden, anchorYOffset, urgent, hasOverheadEmote, threat, comboPips} into a CALLER-OWNED plan (allocation-light: one plan reused across every view per frame, the speedStreaksInto/cameraSpace out-param idiom), reproduces the old hidden/urgent/anchor logic byte-for-byte (named consts NAMEPLATE_RANGE 55/_SQ 3025, NAMEPLATE_URGENT_RANGE 14, NAMEPLATE_ANCHOR_LIFT 0.8 / SELF_EMOTE 0.2, UNLABELED_DOOR_DUNGEON_ID 'nythraxis_boss_arena'), DELEGATES to comboPipsFor (nameplate_combo) + isMobThreateningViewer (nameplate_threat), and imports no three/i18n/governor. NEW src/render/nameplate_painter.ts = class NameplatePainter (the Humble Object) owns the Three projection + every DOM write (relocated setNameplateStatic/Tier/Hp/Combo + updateCastBar + the object/player/npc/mob kind switch) + i18n, consuming plan.threat/plan.comboPips, with its own scratch tmpV/tmpV2 + the single reused plan; constructed once with {views, camera, world:IWorld, getViewport(), showNameplates(), isHostilePlayer()} and composed by the renderer at BOTH updateNameplates call sites. NEW src/render/entity_labels.ts = the mob/npc/object display-name i18n helpers lifted out of renderer.ts (objectDisplayName is also the renderer's build-time nameplate label, so a shared home avoids a renderer<->painter cycle; EntityView is exported, painter imports it TYPE-ONLY -> no runtime cycle). INTERVAL (two-controller, decision 6): nameplateIntervalSec(tier) + NAMEPLATE_INTERVAL_LOW_SEC=1/15 / _FULL_SEC=1/24 added to ui_tier_knobs.ts; the renderer reads nameplateIntervalSec(coerceFxTier(document.documentElement.dataset.fxLevel)), NEVER the governor, replacing the old isMobileRuntime() ? 1/15 : 1/24 device fork. Unlike the shed-on-low knobs (0 = always due) this is a BASE cadence that always throttles: low holds the 1/15s mobile FLOOR, medium/high/ultra the 1/24s desktop cadence. ACCEPTED CONSEQUENCE: the cadence axis is now PRESET, not DEVICE -- a mobile device on a non-low preset now refreshes at 1/24 (faster) instead of the old 1/15; the mobile-floor test guards the SLOW direction (<= 1/15s, no lag) per the phase's own definition; surfaced for conscious acceptance, mobile perf gate is P17a. TESTS: NEW tests/nameplate_view.test.ts (visibility/urgent/anchor/threat/combo, allocation-light reuse, same-input-same-output, decision-15 Sim-vs-ClientWorld parity with both stub shapes, the no-governor import-absence scan [the arch guard ALLOWS render siblings so it would NOT catch a render_budget import -- this pins it], the renderer interval WIRING scan); ui_tier_knobs.test.ts gained the nameplate-interval describe (mapping, always-positive, the <= 1/15 floor on every tier, literal pins); architecture.test.ts registers nameplate_view in RENDER_PURE_CORES; localization_coverage.test.ts corpse-name guard now reads nameplate_painter.ts (relocated, like the P12b minimap_painter move). VALIDATION: tsc clean; biome clean on every new/changed .ts (warning-NEUTRAL; removed the now-dead `t` import); full vitest 4545 pass/8 skip (423 files). PERF GATE (perf_tour desktop ultra, same swiftshader machine as P0): frameP95 250ms == P0 baseline (3/3 runs); hudHotDomSkipRate 0.958/0.96/0.958 inside the P14a-established user-accepted 0.956-0.966 ultra band AND structurally independent (nameplates are renderer-owned DOM, not hud.ts's writer facet, which is untouched); no new per-frame alloc; renderer -362 net lines. WCAG (decision 10): nameplates are the 3D-world surface, OUT of screen-reader scope, recorded honestly not skipped. NO-MAGIC-VALUES (decision 12): the painter is positioned DOM not a 2D canvas, so the getComputedStyle-once rule is N/A; the requirement met is the named per-tier interval constants; pre-existing color hexes moved verbatim (tokenizing out of scope). REVIEWERS: qa-checklist NO BLOCKING (byte-faithful preservation confirmed both call sites; one major non-blocking = the device->preset cadence change above; one minor em-dash in the moved cast-bar comment FIXED). commits local, not pushed. NEXT: phase-15a-a11y-infra.md) |
| P15a | Accessibility infra: focus manager + skip links + live regions + forced-colors + print | medium | new | done (the five GLOBAL a11y slices no single window owns, shipped as the infra P15b will exercise. ONE shared focus manager NEW src/ui/focus_manager.ts (WIRING, NOT in UI_PURE_CORES): FOCUSABLE_SELECTOR lifted VERBATIM to one constant; a stack-based Tab/Shift+Tab trap that intercepts Tab ONLY when focus is already inside the trapped window -- the load-bearing adaptation, because Tab is the game's target-nearest key (main.ts onTab), so an unconditional trap would hijack tab-targeting -- plus focus-first + return-to-opener, self-healing on the next Tab. KEY DECISION: Esc is deliberately NOT owned by the manager; the HUD already has ONE Esc dispatcher (main.ts game input + gamepad -> hud.closeAll()), and adding a second Esc listener is the exact duplication the phase forbids, so the manager is trap+return only and Esc stays unified through closeAll (a keyboard user still escapes any trap). NEW src/ui/focus_order.ts (PURE, registered) = nextFocusIndex wraparound. The FULL hud.ts caller inventory was LARGER than the draft's 6: the 4 ad-hoc helpers (canRestoreFocusTo/currentFocusableElement/restoreFocus/focusFirstInteractive) DELETED; 10 painter windows routed through a new windowFocus(rootSel) pair (NO painter-file edits); 7 HUD-resident modals (delve, lockpick ante+board, questDialog, playerCard, skinEvent, confirm, input) migrated to focusManager.open()+handle with FocusTrapHandle fields replacing the *ReturnFocus fields. DOCUMENTED non-window exceptions (surfaced per the stopping rule): the dropdown return-to-trigger btn.focus() stays SYNCHRONOUS + outside the manager (WAI-ARIA listbox; the manager's restore() defers a tick and would drop focus to body before the native Tab handoff) + its arrow roving; the two chat-input focuses + the quest-tracker header refocus (not window-close paths). Skip links .hud-skip "Skip to Main HUD" / "Skip to Chat" first focusable in BOTH entries (reveal-on-:focus-visible mirroring src/guide .guide-skip; targets #ui + #chatlog tabindex=-1; hidden on the start screen); labels English-only hud_chrome.skipLinks.* (TITLE CASE so they stay non-wordy and the M16 leak guard passes English-filled into non-Latin -- the natural "Skip to main HUD" / "Skip to chat" had wordy "main"/"chat" and FAILED the completeness guard until capitalised). Live regions: #chatlog -> role=log aria-live=polite; NEW off-screen #combat-live (role=status polite) fed by NEW src/ui/combat_announcer.ts (WIRING, DOM-free via injected sink+clock, NOT registered) wired into the single combatLog() funnel + drained on hud.update fastHud; politeness PER TYPE via NEW src/ui/live_region_politeness.ts (PURE, registered) + COMBAT_ANNOUNCE_INTERVAL_MS=1500 + combatAnnounceDue gate, so a routine-damage burst collapses to <=1 polite announcement/interval (never assertive; assertive reserved for the existing role=alert urgents); #combat-live is a SEPARATE node so nothing double-announces. forced-colors + minimal print + skip-link CSS = 3 NEW 10-dash css_corpus sections in base.css (manifest 59->62 / 57->60); @media (forced-colors: active) is the ONLY contrast adaptation (decision 11: NO light theme / NO prefers-color-scheme), borders+focus survive via system-color keywords; @media print hides #game-canvas+#ui+#nameplates (hide, not reflow). user-scalable=no / maximum-scale viewport scale-lock DROPPED here (not P4b) from index.html:5 + play.html:5; the 16px input floor is the real anti-zoom guard, mobile_input_zoom_check 28/0 with the lock gone. 3D world/canvas stated OUT of a11y scope in comments, never faked aria. PRESENTATION-ONLY held; i18n resolved table + sha256 + status.summary regenerated and STAGED (git-freshness guards). VALIDATION: tsc clean; biome new files clean + hud.ts ZERO new debt (noNonNull 19->18) + main.ts comment-only; full vitest 4572 pass/8 skip (426 files); build 4 entries + backdrop survival OK; bare vitest launches no browser. REVIEWER qa-checklist NO BLOCKING; 2 MAJOR handled: (#1 FIXED) char_window keyboard-unequip refocus used the close restoreFocus dep and silently released the trap -> windowFocus.restoreFocus now releases only when the target is OUTSIDE the window root; (#2 DOCUMENTED+DEFERRED) the $WOC wallet-picker's parallel ad-hoc focus system is module-level main.ts outside the Hud windowFocus seam + STEP 1's hud.ts inventory -> TODO(P15b) for the chrome-wide per-window unification. DEFERRED to P15b: chrome-wide axe + keyboard E2E + per-window roles/aria + :focus-visible-never-animated guard + mobile target-size + wallet-picker unification + chatlog-flood tuning + #combat-live identical-text re-announce + decouple chat's live region from pane visibility (#chatlog aria-live goes display:none on the combat tab so chat is not announced there; combat stays announced via the tab-independent #combat-live). RE-AUDIT 2026-06-26 (ultracode adversarial Workflow over the COMMITTED phase, 5 lenses + per-finding verify): FIXED a trap-introduced regression -- the Tab cycle reused the focus-first selector excluding [data-close], so the visible window X (Tab-reachable before the trap) fell out of the keyboard order; FOCUSABLE_SELECTOR is now the cycle set INCLUDING [data-close] and focusFirst() skips the X via a JS filter (one canonical selector, not two), so the X is Tab-reachable again -- fixed HERE (the infra that caused it), so "X-in-Tab-cycle" leaves the deferral list. NEW tests/focus_manager.test.ts gives the centerpiece trap wiring faithful fake-DOM coverage (open/release stack, return-to-opener, focus-first-skip-close, the cycle incl close, the do-not-trap-outside gameplay guard, self-heal, listener lifecycle; previously only nextFocusIndex was tested). De-tautologized the decision-15 parity test (decorative event objects -> pins combatLineKind parameterlessness + never-assertive). Added a defensive stale-handle release in windowFocus.captureFocus (no orphaned trap on double-capture). CSS: skip-link slide gated by prefers-reduced-motion, the z-index recorded as an intentional literal, the "topmost" comment softened. Re-validated tsc + biome (new/edited files clean, hud.ts 2 err/27 warn base==work) + full vitest + build green. CLAUDE.md a11y contract = P16. commits local, not pushed. NEXT: phase-15b-a11y-audit-tooling.md) |
| P15b | Accessibility audit: chrome-wide axe + keyboard E2E + per-window fixes | medium | new | done (the chrome-wide WCAG 2.2 AA AUDIT-and-FIX over the P7-P14 inventory on the P15a infra + the OPT-IN browser suite. AUDIT = ultracode Workflow, 6 read-only auditors A-E + mobile, each gap adversarially verified: 38 confirmed (13 serious/12 moderate/13 minor), 7 refuted. PRESENTATION-ONLY held (no IWorld/sim/server/net/headless/world_api; action_bar_view.ts UNCHANGED so the P12a aria-label-on-t() is intact; NO new i18n key -- reused game.talents.title, hud.social.title, hud.options.returnToGame, itemUi.market.title, itemUi.equipment.levelClass -- so M16 + release-tier untouched, no regen). CSS FIXES (src/styles): restored steady tokenized :focus-visible rings where rules used outline:none/0 (.lb-page-btn/.mkt-page-btn split out, .mkt-select-option/.ui-dd-item gain a real outline -> 2px var(--color-border-focus)); HARDENED the forced-colors net (:focus-visible{outline:Highlight !important} in @layer base beats the later @layer components normal overrides); D12 focus-hex -> token (.bag-chip/.bag-search #d9c27a, .mkt-search #cc9a3c, #delve-board var(--gold)); REMOVED #perf-overlay `transition: outline-color` (the FB-lesson violation the new guard CAUGHT and the audit Workflow had MISSED -- the ring must snap, not fade); mobile target-size raised >=40x40 on party-frame/#party-leave (base+landscape), #mobile-more-close, .map-zoom-btn; .ql-item min-height 24; .mkt-collect-item class. PAINTER FIXES (src/ui): dialog role + name on talents/social/options/market/char/arena roots; talents close span->button + tablist/radiogroup roving arrows + a guarded choice-popup focus-return; social ARIA-1.2 combobox; leaderboard async focus-return on error/empty page swaps; spellbook in-sync toggle aria-label; cast_bar aria-valuenow via elided setAttr (both progressbars); fct_painter build-time aria-hidden (the deferred-from-P13b combat-text boundary); char preview HOST role=img + honest level/class summary; bags ad-hoc prompts role=dialog+aria-modal+local Tab trap+Escape+return; market async role=status live region + focus-restore on rebuilds. STATIC dual-entry: #swingbar/#nameplates/#minimap canvas aria-hidden; #rotate-device role=alert + .rotate-icon aria-hidden. main.ts: wallet-picker kept standalone (already accessible; the TODO reworded -- shared-manager unification is consistency-only, decision-9 instance model, not an a11y gap). TOOLING: NEW vitest.browser.config.ts (FB Vitest-4 Browser-Mode: @vitest/browser-playwright playwright() + browser.instances chromium; OPT-IN npm run test:browser; vite.config Node test.exclude adds tests/browser/** + **/*.browser.test.ts so a bare `vitest run` launches NO browser; deps axe-core 4.12.1 + @vitest/browser(-playwright) 4.1.8 + playwright 1.61.1; vitest PINNED exact 4.1.8). NEW tests/browser/a11y.browser.test.ts (axe over 9 windows w/ the real style barrel, 0 serious/critical; leaderboard under BOTH Sim + ClientWorld shapes + the rejecting-error state = the decision-15 centerpiece), keyboard_nav.browser.test.ts (the REAL FocusManager: focus-first-skip-close, Tab/Shift+Tab cycle incl close, release returns to opener, NOT trapped when focus is outside), target_size.browser.test.ts (rendered getBoundingClientRect under page.viewport(844x390) landscape >=40x40, real geometry not CSS-text). NEW tests/focus_visible_guard.test.ts (Node, always-on: the FB-lesson guard over src/styles/*.css). 24-vs-40 resolved EXPLICITLY: 24px SC 2.5.8 absolute floor everywhere, 40x40 the stronger mobile-touch floor, never weakened. Canvas/3D OUT (decision 10): map/arena/char-preview/minimap/nameplate/FCT hosts get a label + honest summary, pixels no faked aria. VALIDATION: tsc clean; biome clean (new/changed .ts); full Node vitest 4589 pass/8 skip (428 files); bare vitest = NO browser; test:browser 19 pass (chromium); build 4 entries + backdrop survival OK; mobile_input_zoom_check 28/0 + mobile_minimap_safe_area clears notch. REVIEWER qa-checklist NO BLOCKING (9/9 criteria PASS); 2 SHOULD-FIX FOLDED (talents stray-listener guard, social close-label reverted to hud.options.returnToGame) + 1 NICE folded (#minimap aria-hidden). SENIOR RE-AUDIT 2026-06-26 (fresh adversarial Workflow, 6 lenses verified + a coverage review of the diff, NO BLOCKING): the axe gate had only covered 7 DISTINCT DOM windows ("9" double-counted leaderboard's 3 states) and MISSED bags/char/market, the windows this phase changed, so two serious-tier gaps slipped. FIXES (presentation-only, still no new i18n key, reused hudChrome.perf.title): (1) OPTIONS Performance sub-view left aria-labelledby="options-title" DANGLING (perf panel emits no such id) = a nameless dialog + serious axe violation in a reachable state -> render() names PER VIEW (aria-label for performance, aria-labelledby else, never both/neither); (2) the BAGS discard prompt's quantity input had NO name (real WCAG 1.3.1/4.1.2 the new axe case caught) -> installPromptDialog labels an UNLABELED .prompt-number (gated so the already-labeled sell input is not shadowed); (3) SOCIAL combobox options focusable <button> -> non-focusable <div role=option aria-selected> (aria-activedescendant model + off the trap order); (4) the TALENTS choice flyout (role=menu on document.body, no roving/arrows) could Tab-ESCAPE the dialog -> completed roving tabindex + Arrow/Home/End + a focusout-return-to-anchor + a contains(pop) stale-listener guard; (5) PERF spellbook refreshHotbarControls (runs every frame) elided to on-bar FLIPS only (disabled stays per-frame); (6) bags prompt id -> explicit counter + canonical FOCUSABLE_SELECTOR. NEW TESTS: axe over char/market(both shapes)/bags/options-Performance/online-social; options main strengthened to assert #options-title resolves; a real-window->FocusManager integration test + 4 talents-flyout behavioral tests (the riskiest change, previously untested) + a .map-zoom-btn case + a spellbook-elision guard; over-claim comments corrected. ACCEPTED-not-fixed: dialog-root micro-pattern NOT extracted (audit-only phase; P16 is docs), arena idempotent per-tick setAttribute, focus_visible_guard hardening, Esc->closeAll E2E (out-of-manager by design). RE-VALIDATED: tsc clean; biome clean (changed .ts); Node vitest 4590 pass/8 skip (428 files), bare vitest = NO browser; test:browser 31 pass (chromium); build 4 entries + backdrop survival. THIRD-PASS SENIOR AUDIT 2026-06-26 (fresh ultracode adversarial Workflow, 6 quality lenses verified + a full gate re-run, NO blocking/major): invariants RE-CONFIRMED and all gates re-run GREEN; folded 5 residual gaps the two prior passes missed (presentation-only, no new i18n key): (1) bags discard/sell CONFIRM dropped focus to <body> (cancel/Escape returned it) = WCAG 2.4.3 on the common path -> discard returns to the bags close button post-rebuild, sell to the surviving vendor cell; (2) PERF (reverses the re-audit's accept of arena per-tick setAttribute): arena dialog role/aria-modal/aria-labelledby/tabindex set ONCE in toggle() not every render() (the 250ms band); (3) the talents roving choice-opt drew :focus-visible via outline:none+faint border -> steady tokenized outline ring var(--color-border-focus), now under the guard; (4) the market axe fixture rendered EMPTY (vacuous pass) -> one populated listing so the row buy/pager controls are axed under both shapes; (5) HONESTY: focus_visible_guard header no longer claims a nonexistent live-computed-style browser backstop (guard is outline-scoped; box-shadow/border indicators are forced-colors-covered) + a minimap canvas honest-boundary comment matching fct/nameplate. NOTED-not-done (correct): roving-index triplication folds into the planned roving_tabindex.ts packet; the windowFocus copy in the keyboard E2E is a later monolith-extract; the Sim-vs-ClientWorld axe parity stays redundant coverage (pure-core view tests prove the real parity). RE-VALIDATED GREEN: tsc, biome (5 pre-existing noNonNull warnings), vitest 4590/8 skip, test:browser 31, build 4 entries, worktree clean. Cross-engine WebKit/Firefox-in-CI = P17b; CLAUDE.md a11y contract = P16. commits local, not pushed. NEXT: phase-16-standards-codification.md) |
| P16 | Standards codification into CLAUDE.md (component/token/a11y/perf/browser/bundle) | low | new | done (docs-only; CREATED src/styles/CLAUDE.md + added its row to the root Repo map; root CLAUDE.md gained the COMPONENT CONTRACT (Modularity) + the GAMEPLAY-NEUTRAL GRAPHICS invariant; src/ui/CLAUDE.md bumped WCAG 2.1->2.2 AA KEEPING the >=40x40 touch floor and adding 24px SC 2.5.8 only as the absolute minimum, added the HUD-chrome a11y checklist + the per-frame performance contract, and FOLDED the window-extraction recipe into one "Authoring a new HUD component" recipe. Six contracts codified, each citing its REAL guard. FAITHFULNESS CORRECTIONS to the phase's own draft citations: no-magic-values is enforced by PER-PAINTER source guards, NOT a central architecture.test.ts guard; tests/hud_perf_budget.test.ts does NOT exist (standing budget pending P17a; scripts/perf_tour.mjs is the live per-phase gate); perf_tour gates frameP95 + the bounded AoE-burst but NOT skip-rate -- the always-on write-elision guard is tests/painter_host.test.ts (cited), skip-rate is reported + per-phase-checked; alloc_probe is a separate always-on vitest gate; ui_effects_profile is in src/game not src/ui; the unit_frame core is unit_frame.ts not unit_frame_view.ts; the graphics stat-sap wire-parity gap is RESOLVED (commit a15c910c, guarded by snapshots/auras_view/auras_painter) so the invariant is codified airtight+enforced, NOT an open gap. PENDING-P17b: the bundle-budget CI gate + cross-engine WebKit-in-CI. No earned component-dir CLAUDE.md (the unit_frame family is flat files, no barrel). Faithfulness subagent (over-claim + under-cite, every cited guard read): 5 must-fix (2 MAJOR perf-gate, 3 MINOR), ALL FOLDED; final verdict no rule over-claimed AND no shipped guard left uncited. RE-AUDIT 2026-06-26 (ultracode workflow + web research + fresh reviewer) hardened it: FIXED guard-contradicted src/styles lines (the @layer table was in the WRONG order -- real is tokens,base,layout,components,hud[reserved-empty],shell,hud-mobile,extras; "ten-dash" banner showed a four-dash example) and OVER-CLAIMS (theme.ts is NOT dark-only, it ships parchment[light]+highContrast presets, reworded + theme.test.ts cited; canvas no-magic list narrowed to map/minimap/delve, nameplate=hex-exception, arena=DOM; windowFocus is a Hud wrapper not a focus_manager export; :focus-visible scoped to OUTLINE rings; painter_host.test.ts = elision mechanism, no-direct-write = per-painter scans). FILLED 6 uncited packet guards (per_entry_css_wiring, css_value_validity, backdrop_filter_survival, browserslist_targets, ui_effects_profile, ui_effects_wiring). ADDED a src/ui Canvas/DOM hot-path browser-perf section (offscreen cache, node-pool+reconcile, elide-the-resolve, refs-once, loop-invariant ctx.font, DPR, idle prewarm, transform-vs-layout). Trimmed root bloat (209->204). NEXT: phase-17a-harness-floor.md) |
| P17a | Harness floor (test-only): client_shell re-author + standing perf budget + purity sweep + first all-together perf run | low | port+extend | done (commits 00673eaf client_shell pooled-family guards / d9cf8cb4 architecture completeness / b37e85f2 NEW tests/hud_perf_budget.test.ts / 839fc584 perf_tour mobile boot; local, not pushed). KEY FILE PATHS now: `tests/client_shell.test.ts` greps the PAINTER that owns each moved id, not hud.ts (the per-frame/cold-window phases already repointed as they extracted; the static-template ids never moved); P17a added new-shape guards for the 3 dissolved keyed-pool families (party_frame_row.ts / auras_painter.ts createNode / fct_painter.ts pool). `tests/architecture.test.ts` is COMPLETENESS-checked: `onDiskCores()` scans every `*_view`/`*_core` under src/ui (-> UI_PURE_CORES) + src/render (-> RENDER_PURE_CORES) and asserts each is registered (src/guide excluded); forbiddenUiCoreImport teeth re-proven by a REAL injected import. `tests/hud_perf_budget.test.ts` is the STANDING per-frame floor, host-split ARM 1 (Node static raw-write) / ARM 2 (Node fake-DOM steady-loop skip-rate >= floor READ from `perf-baseline-v016.md`, never defaulted, Sim+ClientWorld d15, + alloc_probe) / ARM 3 (env `HUD_PERF_BUDGET_TOUR=1`, perf row: perf_tour artifact vs the baseline + FCT cap). `scripts/perf_tour.mjs` mobile profile boots in LANDSCAPE 844x390 + #mobile-preflight dismissal; `perf-baseline-v016.md` mobile section now CAPTURED. First all-together run: desktop frameP95 250 == baseline + skip 0.962 == floor + FCT [64,64,64]; mobile frameP95 250 + hotWrites 152 (== desktop, elision intact) + skip 0.961 (boot-write noise) + FCT [64,64,64]. No per-frame regression. tsc/biome/vitest 4606 pass green |
| P17b | Bundle-budget gate + selective lazy-load + cross-engine E2E + axe CI + packet close | low | measure-only | done (MEASURE-AND-DECLINE; no code committed, tree clean). Measured the play-entry eager JS (3,327,191 B / 3.173 MiB raw, ~952 KB gzip; ~1.5 MiB is non-deferrable i18n DATA) via a temporary Rolldown generateBundle plugin (per-module renderedLength). A fully-proven options+market lazy-load prototype (a11y loading state + 140-test cross-engine/forced-colors/parity suite + green bundle gate) shrank it by only 50,688 B (~14 KiB gzip, ~1.5%, ZERO runtime/FPS impact). Maintainer DECLINED decision 13 (lazy-load + bundle gate) as not worth the complexity -> reverted in full. Decision 14 (cross-engine + axe CI: Chromium/Firefox/WebKit + mobile WebKit first-class) was prototyped + verified green but reverted with it; OPEN/optional standalone re-land. See decisions 13/14 OUTCOME notes + progress.md P17b row + [[frontend-v016-phase17b-bundle-lazy-declined]]. |

P18 cleanup wave (the leftover nice-to-have / should-have / nit items, see `phase-18-cleanup-overview.md`;
all PRESENTATION-FIRST: src/ui / src/render / src/styles / tests / docs only, no sim/server/net/IWorld/wire):

| Phase | Title | Risk | Kind | Status |
|---|---|---|---|---|
| P18a | Shared keyboard-nav + dialog-root a11y primitives (roving_index core + dialog_root helper + market-filter kbd nav + purity-guard widen) | low | extract+adopt | done (NEW src/ui/roving_index.ts [pure, UI_PURE_CORES] folds the 3 triplicated talents roving handlers via the normalized wrap (((x%n)+n)%n), byte-faithful; NEW src/ui/dialog_root.ts markDialogRoot adopted at 13 roots, 2 left inline [hud confirm-dialog + bags prompt: no tabindex today]; dropdownKeyNav wired into the market filter listboxes [the one behavior ADD]; forbiddenUiCore/RenderCoreImport regex widened to flag a *_window import with teeth, no registered core trips it. roving_index = visible-siblings primitive, dropdown_nav = open/collapse primitive. tests: roving_index/dialog_root units + 3 market keyboard_nav.browser cases + the widened guard teeth + 3 re-pointed window guards. reviewers qa-checklist + adversarial coverage + completeness, all no-blocking; tsc/biome clean, full vitest 5068 pass/11 skip, test:browser 34. talents_window -42, hud.ts -7. commits local) |
| P18b | Per-window interactive ARIA + structure polish (social tablist, bags/char/party a11y, axe coverage) | low | a11y polish | done (9 items: social role=tablist + roving nav; bags non-modal focus-return [no trap] + inert prompt; char preview name; party leader-star split / raid-group / focus-opacity; + the social-tablist-keyboard / char-close / expanded-combobox test pins). The party leader-star + raid-group rode the PER-FRAME perf gate (perf_tour desktop+mobile: hudHotDomWrites=152 == P0, frameP95=250 == P0, fail 0; tagged frontend-v016-p18b-perf-gate); social/bags/char were cold. 2 review-found bag bugs fixed in-session (close() guard on display==='none' since bags shows 'flex' OR 'block'; inert cleared on EVERY prompt teardown incl confirm, not just cancel/Escape) + tests added. The new EXPANDED-combobox axe case surfaced + fixed a real WCAG 1.4.3 contrast bug (scoped .soc-sugg-item .soc-meta color). i18n DEVIATION from the packet's "English-only / never edit overlays": both new keys are WORDY so the M16 non-Latin completeness guard required real fills -> the 5 non-Latin overlays (zh_CN/zh_TW/ja_JP/ko_KR/ru_RU) were translated; Latin stays pending. SENIOR RE-VERIFICATION 2026-06-27 (ultracode Workflow: 7 lenses + critic + qa, no BLOCKING): remediated a 3rd bag bug (a BLOCKING inert leak -- close()/closeVendor left #bags inert when force-closed under an open prompt, since the bags keybind fires on the prompt's focused confirm BUTTON and bypasses dismiss() -> dead grid on reopen; both paths now clear inert, and close() also tears down the orphaned aria-modal prompt), a SOCIAL roving gap (the shared window Tab trap cycled all 4 tabs because FOCUSABLE_SELECTOR matched <button> regardless of tabindex -> excluded tabindex="-1" from EVERY clause so a trapped roving widget is one Tab stop, also corrects talents/market, validated across all ~17 trap sites), and a PARTY item-7 gap (.party-frame transitions opacity 0.2s and opacity applies to the outline, so the focus reset faded the ring in over 200ms -> transition:none, never animated). Two catalog comments + a components.css contrast comment corrected (the keys ARE wordy; #998d6a is a hex not a token). +2 browser tests + 2 source guards; test:browser now 40; perf gate unaffected (no hot-path file touched). |
| P18c | Visual a11y: forced-colors cues + residual-hex tokenize + target-size + focus-indicator check | low | a11y/css | planned (6 items) |
| P18d | Live regions + FocusManager unification (target-name / combat / chat live regions + wallet-picker fold) | medium | a11y/new | planned (6 items) |
| P18e | Render-tier, content-i18n, perf nits + structural dedup (nameplate ceiling, aura i18n, arena perf, entity dedup, FCT internals) | medium | port+extend | planned (9 items) |
| P18f | Tooling, test-harness + docs hygiene (mobile E2E, biome INFOs, windowFocus seam, completeness gate, docs fixes, optional cross-engine CI) | low | test+docs | planned (14 items) |

### New `IWorld` members / `SimEvent`s / wire fields / endpoints / DB tables
None. This packet adds none. It CONSUMES V16's already-landed IWorld (delve/lockpick/raid + the
paged `leaderboard()`); the only change vs FB is one painter consuming the paged leaderboard (P9b).

### New i18n keys
None expected beyond English-only `hud_chrome.ts` control labels: a unit-frame group aria-name (P10b
LANDED: `hudChrome.unitFrame.playerLabel` = `'Your Hero'`, kept NON-WORDY so the M16 completeness gate
does not flag it as untranslated-English in non-Latin locales; registered `pending`, maintainer fills at
release), the cast eat/drink label resolved in the painter (P11a LANDED: reused the existing
`hud.core.eating|drinking|eatingDrinking` keys, NO new label keys; the cast-bar progressbar aria-names are
the only additions: `hudChrome.castBar.playerAria` = `'Your Cast Bar'` + `targetAria` = `'Unit Cast Bar'`,
both NON-WORDY for M16, registered `pending`), skip-link + live-region-prefix labels (P15a),
async-failure copy for leaderboard/market (P9b/P8b), and a lazy-window loading label (P17b).
P18b LANDED two English-only `hud_chrome.ts` keys: `hudChrome.character.modelPreview` = `'Character Model Preview'`
(the char role=img preview host name) and `hudChrome.unitFrame.partyGroup` = `'Group {n}'` (the visually-hidden raid
party-group cue, `{n}` via formatNumber). UNLIKE every prior unit-frame label, BOTH are WORDY by the M16 rule
(`/[a-z]{4,}/` after stripping `{...}`: "Group" -> "roup", "Character"/"Model"/"Preview" all match), so an
English-filled non-Latin locale WOULD trip the completeness leak guard. The packet's "Title Case keeps it non-wordy"
premise is therefore wrong for these two; the M16 mechanism is per-WORD consecutive-lowercase, not per-word-count.
So the 5 non-Latin overlays (zh_CN/zh_TW/ja_JP/ko_KR/ru_RU) were filled with real translations (the documented
non-Latin-completeness exception, consistent with prior feature PRs); the Latin overlays stay `pending` (release-tier
maintainer fill). This is the one i18n case in the P18 wave where English-only does NOT suffice.

### Key file paths (V16 line numbers from the recon; the deep-review corrections are inline)
- Per-frame entry: `Hud.update()` at `src/ui/hud.ts:3627` (frame-divider: every-frame +
  fast >=100ms + medium >=250ms + slow >=500ms). Write-elision: `hud.ts:1322-1372` (the four writers
  are `private` on `Hud`; P6 bound them as closures via `makeWriterFacet` in `src/ui/painter_host.ts`,
  sharing the same `hotWriteCache`) + `perfStats()`.
- Hot elements: player frame 3656-3667, buff bar 3670 (renderAuras 4186-4245), target frame
  3672-3749 (lastPortraitTarget gate 3692-3708, combo pips), player cast bar 3752-3798, swing timer
  3800-3827 (the `#swingbar` per-frame `$()` + raw style LEAK), action bar 3829-3931 (per-frame
  aria-label via t()), xp bar 3933-3952; minimap 5022-5258 (3-branch canvas: delve schematic / NPC
  glyphs / proximity-scaled party discs+arrows; Sets already built once per 10Hz call, NOT a
  double-scan to collapse); party frames CALL SITE 11508-11562 (the pure selector lives in
  `src/ui/party_frames.ts`, NOT inline at 11520); FCT `fct()` 7258-7276 + 7 SimEvent spawn sites in
  6100-6422 PLUS `showSelfNote` (7255, caller at `main.ts:1727`) = the 8th site; `getUiScale`
  (`hud.ts:288`/7270) is load-bearing for FCT positioning under zoom; nameplates `renderer.ts`
  updateNameplates 4413 (mobile interval at renderer.ts:4113, 1/15 vs 1/24).
- Cold windows (inline unless noted): renderVendor 8126 (ALREADY delegates to vendor_window),
  renderMarket 8343, renderBags 8839, renderChar 9116 (skin-event Math.random 9596; Three.js preview),
  renderLeaderboard 10673 (async), renderSpellbook 10766, renderTalents 10909 (mutable `talentStage`
  edit buffer, NOT IWorld-derived), renderQuestLog 11398, renderSocial 12025 (repaints on the 500ms
  slowHud divider with listener churn), renderOptions 12783 (~1180 lines, 9 sub-panels behind a
  9-member OptionsHooks), updateMapWindow 5561 + renderArenaWindow 5300 (BOTH called from
  `hud.update()`'s mediumHud band, NOT purely cold).
- Existing pure cores to REUSE: `xp_bar`, `cast_bar` (in `src/render/cast_bar.ts`, i18n-free),
  `absorb_bar`, `party_frames` (selector, `src/ui/party_frames.ts`), `rest_indicator`, `low_health`,
  `low_resource`, `clock`, `compass`, `coords`, `quest_tracker`, `delve_map`, `raid_lockout_view`,
  `vendor_view`. Plus the cold-window cores extracted in P7-P8: `talents_view` (P7a),
  `social_view` + `bags_view` (P7b), `options_view` (P8a, the declarative options model: control
  descriptors + pure dispatch-value fns + per-panel builders + buildBugReportInfo), `market_view` +
  `char_view` (P8b: `market_view` = the snapshot state-union builder {no-data|browse|sell|collect}
  reusing `market_filters`; `char_view` = `buildPaperdollView` the two equip-slot columns). Plus the
  per-frame cores P10-P12b: `swing_timer` (P10a), `unit_frame` (P10b family), `action_bar_view` (P12a),
  and NEW `auras_view` + `minimap_markers` (P12b, both UI_PURE_CORES; auras_view is mode-parameterized
  'all'/'debuffs'; minimap_markers owns the OVERWORLD branch + exports minimapMode, the delve branch
  stays delve_map). The minimap canvas painter keeps the existing 10Hz cadence (P14a will tier it
  10Hz->4Hz) and the aura strip keeps the existing visible-count (P14a tiers it). The allocation-budget
  proxy is the P12a one (tests/util/alloc_probe.ts); P12b introduced no new proxy. Nameplate
  `src/render` has only narrow helpers (`nameplate_combo`/`_projection`/`_threat`); a real
  `nameplate_view` core is NEW (P14b).
- PainterHost (NEW, P6): `src/ui/painter_host.ts` is the THIN two-facet host. FACET 1
  `PainterHostPresentation` (`itemIcon`/`moneyHtml`/`itemTooltip`/`attachTooltip`) is the shared
  icon/money/tooltip dep-bag the cold windows COMPOSE into (P7-P9 reuse it); today ONLY the vendor
  window composes it (`VendorWindowDeps extends PainterHostPresentation`, Hud builds one
  `presentationBag` and spreads it). lockpick + raid_lockout compose NOTHING (callbacks-only / pure
  string builder) -- do NOT "fix" them onto the host. FACET 2 `PainterHostWriters` +
  `makeWriterFacet(cache, stylePropCache, classCache, onWrite, onSkip)` binds SIX elided writers as
  closures over Hud's SAME caches + counters (one skip-rate; Hud's `private` writers keep their
  visibility). P10a EXTENDED it (D5a LANDED): the four single-slot writers (setText/setDisplay/
  setTransform/setWidth, keyed one string per element via `hotWriteCache`) plus `setStyleProp(el, prop,
  value)` + `toggleClass(el, cls, on)`, which are MULTI-SLOT (keyed per (element, prop) / (element,
  class) via `hotStylePropCache` / `hotClassCache`, a per-element inner map) so one element can hold
  many custom properties / toggled classes without clobbering each other or weakening the four
  single-slot writers (the Top-risk-1 cache-key guard). P10b-P13 route setProperty / classList writes
  through these two. The host is host-agnostic (no `window`), Node-importable.
- Delve painter (NEW, P6): `src/ui/delve_map_painter.ts` is the Canvas-2D pilot: pure
  `delveDrawModel(IWorld, size, pad, names)` (the parity-tested core path) + `DelveMapPainter`
  (`paintMinimapDelve` writes `#zone-label` via the writer facet; `paintWorldMapDelve` paints its title
  on-canvas), consuming `src/ui/delve_map.ts` (which now also exports `delveLocalToCanvas`). It resolves
  the `--color-delve-*` tokens (`src/styles/tokens.css`) via `getComputedStyle` once/redraw (D12).
  Template for the per-frame painter halves: pure core from `IWorld` + thin painter, DOM via the facet,
  Canvas NOT routed through the facet (a 2D context cannot be elided, D12).
- Per-frame painters (NEW, P10a, the first per-frame slices): `src/ui/xp_bar_painter.ts` (consumes the
  existing `xp_bar.ts` xpBarView core; caches #xpbar / .fill / .rested / .label / #player-frame once and
  routes --xp-fill x2 + rested left/width + label + overflow/rested toggles through the six writers) and
  `src/ui/swing_timer.ts` (NEW pure core in UI_PURE_CORES: i18n-free, parameter-in/next-state-out edge
  tracking, returns a ready-vs-seconds label discriminator + the seconds value) + `src/ui/
  swing_timer_painter.ts` (caches #swingbar / .fill / .label once, resolves the discriminator through
  t()). Both replace inline `Hud.update()` blocks that re-queried elements + wrote raw style every frame
  (the two write-elision LEAKS this phase fixed). `updateLowHealthVignette` was also rerouted (cached
  ref + setStyleProp/toggleClass) to exercise Hud's new private writers (an unclaimed in-scope leak).
- The unit_frame FAMILY (NEW, P10b, decision 9, registered in UI_PURE_CORES): `src/ui/unit_frame.ts`
  (pure core: `UnitFrameDescriptor` -> `UnitFrameView`; present/hidden gate + shared `absorbBarView`
  resolution + `unitResourceClass` discriminator incl a `none` case; NO hardcoded id / single-instance
  assumption) + `src/ui/unit_frame_painter.ts` (thin write-elided painter; REQUIRED frame/level/hpFill/
  hpText + OPTIONAL name/absorb/resource-group element refs; options `shownDisplay` / `repaintPortrait`
  [the portrait-repaint gate lives in the PAINTER, so P11b target's lastPortraitTarget gating is the same
  code path] / `stateClasses` [gate dead+oor, party-only]). The PLAYER is the first instance: the inline
  `Hud.update()` player block -> `playerFramePainter.paint(unitFrameView({...}))`, folding the raw
  `updateAbsorb('#pf-absorb')` (transform + overshield) and the resource className swap (-> per-type
  `toggleClass`, never clobbering the `low`/`bar` classes) into the elided writers. The player painter
  omits name/shownDisplay/stateClasses/repaintPortrait so the path is byte-faithful (name stays at the
  login set; the frame is always CSS `display:flex`). `updateLowHealthVignette` + `updateLowResource`
  stay at the Hud call site (player-only, OUT of the family). P11a/P11b/P11c REUSE this seam with NO core
  change (the descriptor was validated against the FULL target/party field set, not a stub).
- The action bar FAMILY (NEW, P12a, decision 9, registered in UI_PURE_CORES): `src/ui/action_bar_view.ts`
  (pure core: `createActionBarView(descriptor, deps)` -> `tick(world)` returning a REUSED, preallocated
  per-slot `ActionBarState` mutated in place; instance-parameterized by a bar descriptor of per-slot
  `slotIndex`/`isAttack` + host accessors `hasAction`/`ability`/`item`/`keybindLabel`, NO element refs;
  four slot kinds attack/empty/item/ability; the aria-label is resolved IN the core via an INJECTED
  `t()` every tick - Top risk 4 - so the i18n key keeps firing while the painter elides the DOM write)
  + `src/ui/action_bar_painter.ts` (`new ActionBarPainter(writers, paintDescriptor, resolveBgImage)`,
  multiplicity is a constructor arg; per-slot `lastIcon` cache skips the expensive icon resolve;
  named-const attr/prop/class names, no literal hex/px/color). This phase builds ONLY the one existing
  bar through the seam; adding a 2nd/3rd bar is a follow-on FEATURE. The PainterHost facet GAINED an
  elided `setAttr` writer (decision 5a, the way P10a added setStyleProp/toggleClass): the action-bar
  aria-label is the first per-frame ATTRIBUTE write, keyed per (element, attr) in a new `hotAttrCache`;
  the "per-button cache keyed on the rendered string" IS that attr cache. ALLOCATION-BUDGET PROXY
  (open-decision 4) RESOLVED here: in-process reference-stability (`tests/util/alloc_probe.ts`), which
  P12b/P13b/P17 reuse; heapUsed-delta rejected as noise; full-HUD fallback = perf_tour frameP95 +
  longtasks. Perf gate: frameP95 250 == baseline; skip-rate 0.973 steady-state clears the 0.962 anchor
  (the 0.960 standard-tour reading is the action bar's previously-raw cooldown writes now counted, not a
  cache-key regression; actual DOM writes dropped via the aria elision).
- Build: `vite.config.ts` (Lightning flip in P1; reconcile the now-dead `css.postcss` Tailwind-defeat
  at 134-139; derive Lightning targets via `browserslistToTargets`), `package.json`, `.browserslistrc`
  (new P1), `biome.json`, `tsconfig.json`. V16 has NO existing CSS-import seam (unlike FB): P1 defines
  it (a barrel imported once from the game entries' TS) and which entries load it. CSS (new):
  `src/styles/{tokens,base,layout,components,hud,shell,hud-mobile,index-extra,play-extra}.css` (the file
  is `hud.mobile.css` but its LAYER is `hud-mobile`; likewise `index.extra.css`->`@layer index-extra`,
  `play.extra.css`->`@layer play-extra` - flat hyphenated names, NOT dotted, because a dot in a @layer name
  is a SUBLAYER, see P4b).
  ALL FILLED as of P4b: tokens/base (P1), hud (P2, `@layer components`), layout.css (`@layer layout`, the
  `.window` shell only) + components.css (`@layer components`, every feature-window body + the P4b paperdoll/
  bags chrome) (P3/P4b), shell.css (`@layer shell`, pre-game shell + char-select) (P4a), hud.mobile.css
  (`@layer hud-mobile`, the in-game mobile-touch block) + index.extra.css/play.extra.css (the per-entry
  #rotate-device gate) (P4b). FLAT @layer order: tokens, base, layout, components, hud, shell, hud-mobile,
  index-extra, play-extra (hud-mobile AFTER shell so mobile overrides of shell elements win; index/play extra
  last). Barrel import order tokens,base,layout,hud,components,shell,hud.mobile (components.css before-but-
  same-layer as hud.css; per-entry .extra load via a `<link>` in each entry's `<head>`, NOT the barrel).
  Entries: `index.html`, `play.html` (BOTH inline `<style>` blocks now EMPTY; play.html reconciled to the
  canonical modules in P4b), `admin.html`, `guide.html` (survival-only, decision 18).
  `--range-fill` is NOT a `:root` token: it is the inline `var(--range-fill, 0%)` fallback on the
  slider track at `index.html:356`, written per-element at `hud.ts:12899`; it rides into `base.css`
  inside the slider rule, do NOT promote it to `:root`.
  P1 LANDED (commits b9fe99b2/0a120e9f/0892c250, local): the Lightning flip (`css.transformer` +
  `cssMinify` = lightningcss, targets via `browserslistToTargets` fed by a zero-dep `.browserslistrc`
  parser at `scripts/browserslist_targets.mjs`, NO `browserslist` npm dep), `.browserslistrc`, and the
  `src/styles/index.css` barrel seam are in. The dead `css.postcss` block is removed (no
  `tsconfig.json` change was needed; the CSS import type-checks via the existing `vite/client` types).
  The single `@layer` order is declared once; as of P4b it is the FLAT-named
  (`tokens, base, layout, components, hud, shell, hud-mobile, index-extra, play-extra`) order (P1 declared a
  dotted variant with hud.mobile before shell; P4b corrected both the dot-as-sublayer bug and the order so
  hud-mobile lands after shell); later CSS phases only fill layers. The barrel is imported once from `src/main.ts`, the SHARED bootstrap for
  both game entries (V16 has no separate per-entry TS, unlike the recon's assumption), so one import
  styles `index.html` AND `play.html`. `--range-fill` stayed in the slider rule (not `:root`). GOTCHA
  for P2-P4: the global Lightning flip hard-errors on a relative `url()` inside a custom property, so
  any inline CSS a later phase touches must use root-absolute `url('/...')`; P1 had to absolutize
  `play.html`'s 3 inline cursor `url()`s as a build-survival fix (its token/base extraction itself is
  still deferred to P4, its block diverges 327 vs 433 lines). A `biome.json` override scoped to
  `src/styles/**` disables `noImportantStyles` + `noDescendingSpecificity` (they fire on verbatim
  load-bearing `!important` and source order; reuse it as later phases extract more legacy CSS).
- Tier: `src/render/gfx.ts` (`graphicsPresetLabel` at 245, 5 labels; `GFX_BUCKET_BANDS`; the `ui`
  band `governable:false`; `gfx.ts:308` bare `0.5` -> import `EFFECTS_QUALITY_LOW_CUTOFF`),
  `src/game/settings.ts`, `src/game/ui_effects_profile.ts` (new P5, render-importable),
  `src/ui/theme.ts` (applier shape).
- Focus a11y (P15a): the ad-hoc helpers `hud.ts:2570-2604` (canRestoreFocusTo / currentFocusableElement
  / restoreFocus / focusFirstInteractive, canonical focusable selector at 2598). The full restoreFocus /
  focusFirstInteractive / dropdown-focus-return caller set is ~15 sites (grep the FULL set; the 6 listed
  in the draft were incomplete), plus the `src/guide/chrome.ts:85` skip-link precedent to reuse.
- A11y primitives (P18a): `src/ui/roving_index.ts` (pure core, registered in UI_PURE_CORES) =
  `rovingTarget(key,current,count,orientation)`, the always-visible roving-siblings keyboard
  primitive (tablist / radiogroup / flyout), folded out of the three triplicated talents handlers;
  `src/ui/dialog_root.ts` = `markDialogRoot(el,{label|labelledBy,modal})`, the cold-window dialog-root
  helper (raw setAttribute, NOT the elision facet; NOT a registered pure core), adopted across 13
  window roots. `dropdown_nav.ts` stays the open/collapse-listbox primitive (the market filter wires
  onto it) -- do NOT merge the two keyboard-nav cores. The UI-purity guard
  (architecture.test.ts `forbiddenUiCoreImport` / `forbiddenRenderCoreImport`) now also flags a
  `*_window` import, not just `*_painter` / `painter_host`.
- Guards: `tests/client_shell.test.ts`, `tests/architecture.test.ts`, `tests/css_corpus.test.ts`
  (new, 10-dash markers), `tests/hud_perf_budget.test.ts` (new, STANDING in P17a),
  `scripts/perf_tour.mjs`, `scripts/*_shot.mjs` (visual baseline), the V16 `mobile_*` E2E scripts,
  `vitest.browser.config.ts` (opt-in axe/keyboard, P15b; cross-engine CI on in P17b).

## Top risks
1. Per-frame write-elision regression (non-byte-identical cache keys / raw writes silently collapse
   the skip-rate). Mitigation: the PainterHost elided-writer facet + the writer EXTENSION (decision 5a)
   + a unit test rejecting raw writes + the skip-rate perf gate every per-frame phase. A failed gate
   blocks completion and bisects to the offending phase (the first all-together run is P17a).
2. FCT extraction (P13a/P13b): net-new pool + per-frame driver; pool lifecycle errors drop/duplicate
   text; AoE worst-case is the perf-gate scenario; `getUiScale` positioning + the 6 hex->class-token
   migration + the `showSelfNote`/`main.ts:1727` precondition are easy to miss. Isolated last.
3. innerHTML-wipe -> keyed-pool rewrites (auras P12b, party P11c) silently dropping listeners/tooltips;
   the tooltip closure must read a live MUTABLE slot record, not capture-by-value (stale after recycle).
4. Action-bar aria-label (P12a): per-frame i18n+a11y+allocation triple-hazard; elide WITHOUT dropping
   `t()` or adding a fallback.
5. Two-controller hazard (P5/P14a): tier knobs read the static preset, never the governor.
6. CSS cascade/rule-drop (P2-P4b): mitigated by the css_corpus section-by-section guard every CSS
   phase + the backdrop -webkit-first gotcha + JS-written custom props kept in `:root` + a
   screenshot-diff against the P0 visual baseline. Orphan band now CLOSED: P2 took Fiesta HUD (2303);
   P3 took arena (1846) / market (1900) / options (1973) / theme (2040) / emote (2108) into
   components.css, so the 1846-2161 window band is fully assigned. P3 had NO backdrop-filter in its
   moved content, so the -webkit-first gotcha was a no-op there (gate still runs).
7. Canvas painters vs no-magic-values (P9a, P12b, P14b): a 2D context cannot read CSS vars; resolve
   tokens via `getComputedStyle` once per redraw (decision 12), do not weaken the guard.
8. ClientWorld-vs-Sim drift (decision 15): an offline-only-shape assumption ships broken online; the
   per-core parity test is the mitigation.
9. Spec-vs-live drift in the phase docs (the FB-carryover root cause): the deep review corrected the
   known set; if a phase finds a NEW stale ref, trust live source and surface it.
10. Scope creep into sim/server/net: the only IWorld interaction is consuming V16's landed members.
