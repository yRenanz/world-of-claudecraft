# Frontend Modernization v0.16.0: Progress

Live status of the 30-phase restart packet on `feature/frontend-modernization-v016`
(off `release/v0.16.0`, `e31eb05d`). Update at the end of every phase (state.md STEP 6).
The packet was DEEP-REVIEWED and RESTRUCTURED 2026-06-24 (18 -> 30 phases via sub-letter splits);
the OLD -> NEW map, locked decisions, and validation matrix are in `state.md`.
Full per-phase scope/acceptance: each `phase-NN-*.md`; line numbers in `v016-recon-and-packet.md`.

## Status table

| Phase | Title | Layer | Status | Notes / deferrals |
|---|---|---|---|---|
| P0 | Foundation gates (CSS-corpus + UI-purity guard + perf/visual/mobile baseline) | gates | done | css_corpus.test.ts (47 index + 45 play 10-dash sections, corpus = inline UNION src/styles, teeth-proven) + architecture.test.ts UI_PURE_CORES (15 src/ui cores) + RENDER_PURE_CORES (cast_bar); tsc clean, npm test 3899 pass, biome clean, all 3 negative proofs fired+reverted. Baselines: perf-baseline-v016.md (DESKTOP frameP95 250ms / inputIntentToFrameP95 652.7ms / hudHotDomSkipRate 0.962 on M4 Max swiftshader; MOBILE deferred to P17a - portrait 390x844 + undismissed preflight, landscape-only d16a), visual-baseline-v016.md (theme/tab_target/loot_roll/mmo_controls; no diff harness exists), mobile-baseline-v016.md (4 PASS / 2 RED: button_size+joystick_size don't dismiss #mobile-preflight - pre-existing harness gap; only 2 of 6 truly assert; no portrait #rotate-device assertion - all for P4b) |
| P1 | CSS A: Lightning flip + tokens + base + CSS-import seam | css | done | Lightning flip (css.transformer + cssMinify = lightningcss) with targets via browserslistToTargets fed by a zero-dep .browserslistrc parser (Chrome>=120/FF>=121/Safari>=17.2/iOS>=17.2), NO browserslist npm dep (vanilla-only); dead css.postcss removed. src/styles/index.css barrel declares one @layer order (tokens,base,layout,components,hud,hud.mobile,shell,index.extra,play.extra), imported ONCE from src/main.ts (covers BOTH game entries; not admin/guide). tokens.css (index 186-269) + base.css (270-618) verbatim (tokens byte-identical, base differs only by biome cosmetic leading-zero); --range-fill stays the slider inline var(--range-fill,0%) fallback (NOT :root); cursor url()s absolutized to /ui/cursors/. GOTCHA: global Lightning flip hard-errors on play.html's inline relative cursor url() (custom-property ambiguity) -> 3-line absolutize survival fix (NOT the deferred P4 extraction); admin/guide had none. biome.json src/styles/** override (noImportantStyles + noDescendingSpecificity off, verbatim legacy CSS). client_shell: 3 relocated-rule assertions repointed to base.css. 3 commits b9fe99b2/0a120e9f/0892c250 (local, not pushed). tsc clean, full vitest 3906 pass / 8 skip, build x4 + manifest + i18n modulepreload green, qa-checklist no BLOCKING |
| P2 | CSS B1: in-world HUD chrome | css | done | Relocated the in-world HUD chrome from index.html inline `<style>` into NEW `src/styles/hud.css` under `@layer components` (barrel `index.css` now `@import`s it, after tokens+base), in source order: run A (nameplates, chat bubbles, new-adventurer tutorial, unit frames, buff/cast/swing bars, bottom cluster, chat+report frames, quest/delve trackers) + run B (combat meters, minimap+compass, community HUD) + tooltip + FCT + Interface&Comfort/adaptive-fx/perf-overlay + 2v2 Fiesta HUD + center/vignette/death. ORPHANS CLOSED: Fiesta HUD + the lowercase `/* tooltip */` block (upgraded to a 10-dash marker, now counted by css_corpus -> 48 index / 46 play). CASCADE-FLIP fixes (the unlayered-beats-@layer hazard at the intermediate state, FB's "layer beats specificity"): (1) moved the UI-chrome-icon glyph sizing (.ui-icon / .x-btn .ui-icon / .micro-btn .ui-icon / .pfm-crest) into `base.css` `@layer base` - the phase note's "P1/base-tier glyph styling" gap P1 had left inline-unlayered - so `#mm-music .ui-icon` (component override) wins again; (2) DEFERRED 5 fiesta/arena integration rules (`.btn.fiesta-practice`(+:hover), `.arena-bracket.fiesta`(.active), `.arena-bracket-tag.fiesta`, index.html ~1153) inline beside their `.btn`/`.arena-bracket` bases (windows+arena, still unlayered) -> they relocate in P3 WITH those bases. Other generic-class cross-overs (.bar, .portrait, .community-link, .action-btn, .panel-title, .x-btn, .window) are safe: the staying rule is more-specific (already wins) or its base is in `@layer base` (component override correctly wins). backdrop `-webkit`-first on the 2 real rules (frosted panels, #perf-overlay) + PORTED `scripts/check_backdrop_survival.mjs` (a P0 gap; first phase with teeth) wired into `npm run build` + its 12-case unit test. Lossless proven (selector+declaration multiset identical before/after). Validation: css_corpus + client_shell (native-app block repointed to hudCss) + backdrop_filter_survival green; tsc clean; full vitest 3931 pass / 8 skip; build x4 + survival OK (both twins in dist main CSS); biome clean (hud.css+base.css); live HUD screenshot renders identical. Commits local, not pushed. |
| P3 | CSS B2: modal + feature windows | css | done | Relocated the windows block + the out-of-block window sections out of index.html inline `<style>` (lines 187-1158 emptied: delve, lockpick, the windows block, arena, market, options, theme picker, emote, and the 5 deferred Fiesta glue rules) into TWO NEW modules: `src/styles/layout.css` (`@layer layout`, holds ONLY the generic `.window` centering/shell rule) + `src/styles/components.css` (`@layer components`, all feature-window bodies). Section map -> components.css in source order: delve board, lockpicking minigame, windows (lead shared chrome = quest dialog/.btn/loot/item-quality), character window, spellbook, quest log, leaderboard, talents, modals and dropdown, vendor, bags, social, map, Ashen Coliseum (arena), The World Market, options/game menu, UI theme picker, emote wheel, then the 5 Fiesta glue rules (`.btn.fiesta-practice`(+:hover), `.arena-bracket.fiesta`(.active), `.arena-bracket-tag.fiesta`) now co-located with their `.btn`/`.arena-bracket` bases in the same layer. Barrel `src/styles/index.css` now imports both new modules; import order tokens,base,layout,hud,components: hud.css and components.css are BOTH `@layer components`, components.css imported LAST so window bodies win equal-specificity ties over HUD chrome (matches the pre-extraction unlayered precedence; in practice they share no selector). Both modules load from the ONE barrel via src/main.ts, so BOTH index.html and play.html get them (play.html inline windows untouched -> deferred to P4b; its unlayered inline still wins, no visual change). CASCADE verified safe: the only bare `.window` outside layout.css is hud.css `.panel,.window{opacity}` (disjoint property) + `body.frosted-panels .window` (more specific); base.css `.window .panel-title` / `.window.window-dragging` / `#start-screen :is(.btn,...)` are more specific or disjoint; no equal-specificity same-property tie flips. NO backdrop-filter in P3 content (survival gate still runs, OK). css_corpus EXTENDED: split the coarse single "windows" banner into per-window 10-dash banners, +11 names (window shell, character window, spellbook, quest log, leaderboard, talents, modals and dropdown, vendor, bags, social, map) -> 59 index / 57 play, so one window body can no longer be silently dropped. 6 em dashes in moved comments normalized to commas. 3 tests repointed to the new modules (client_shell, mobile_window_transform via inline-UNION-modules effective stylesheet, social_status_dots). Lossless proven (625 top-level rules identical old-vs-new after absorbing biome leading-zero/whitespace). Validation: tsc clean; css_corpus + client_shell green; full vitest 3931 pass / 8 skip; build x4 + i18n modulepreload + backdrop survival (5 webkit twins) OK, moved selectors present in dist CSS loaded by both entries; biome clean on new .css. Commits local, not pushed. |
| P4a | CSS C-1: shell + char-select -> shell.css | css | done | Relocated the desktop pre-game shell + char-select out of index.html inline `<style>` into NEW `src/styles/shell.css` (`@layer shell`, NOT an FB port; barrel `index.css` now `@import`s it after components.css), verbatim, source order preserved. Moved 12 of the 19 inline banners (+1 internal char-select sub-banner): start screen, loading, play console, Skin picker (`.skin-*` rows only), Premium Accessible Login Form, Animated Backdrop, looping cinematic backdrop, Collapsible Controls Drawer, Clean up styles (char-select rows `#char-list`/`.char-list-*`/`.text-center-lg`/`.fatal-overlay`/`.modal-backdrop`/`.confirm-modal`/`.confirm-actions`), Premium Class Details Panel, Unified Character Select Layout (whole, incl its `@media (max-width:860px)` block + interspersed `body.mobile-touch` shell rules), Cosmetic skin-select event overlay (whole). DISCOVERY (index.html NOT cleanly partitioned after P2/P3): 6 chrome banners (chat `#chat-input`, party frames `#party-frames`, context menu `#ctx-menu`, prompts `#prompt-stack`, trade window `#trade-window`, elite target frame `#target-frame.elite`) plus `#tf-debuffs` and the paperdoll (`.equip-slot .equip-unequip-btn`) + bags (`#bags.drop-target`) rules were P2/P3 chrome NEVER extracted (confirmed absent from src/styles) sitting under shell-range / grab-bag banners. Per the stop-rules they are HELD inline + SURFACED as a P2/P3-extraction-cleanup follow-up (not forced into shell.css); the two grab-bag banners (Skin picker, Clean up styles) were SPLIT so their char-select/shell rows moved (base+override kept together) and only the genuine in-game chrome stayed inline. CASCADE: moved whole sections into `@layer shell`, source order preserved, so every base+override pair stays co-layered; the held mobile-touch SECTION + chrome cluster stay inline-unlayered. ONE BLOCKING flip caught + fixed: `#hero-view #login-panel,#realm-panel { margin-top: 10px }` (spec 2,0,0) moved to `@layer shell` lost to the held `body.mobile-touch[data-start-panel] #X-panel { margin-top: 0 }` (spec 1,2,1, inline-unlayered, whose `margin-top:0` was DEAD in the original); FIX relocated that one held panel rule into `@layer shell` too so `#hero-view` keeps winning (10px preserved). Backdrop `-webkit`-first: 3 std-first pairs reordered (start screen blur12, play console blur6, login none), 3 std-only Lightning twins; survival OK 11 twins. css_corpus needs NO change (union NAME-presence guard; all 59 banners present: 12 moved -> shell.css, 7 held inline). 2 tests repointed (client_shell + charselect_sort_parity -> shellCss). NOTE for P4b: 2 bare HUD rules (`#community-hud`/`#quest-tracker`) rode along inside the char-select 860 block (verified cascade-neutral, re-home in P4b); the shell-mobile overrides + the per-entry .extra split + emptying the inline blocks remain P4b. Lossless PROVEN (4526 selector+declaration pairs identical, biome cosmetics + the backdrop reorder absorbed) + a puppeteer COMPUTED-STYLE diff across 6 viewport/body-class states = 0 cascade diffs. Validation: tsc clean; css_corpus + client_shell + charselect_sort_parity green; full vitest 3931 pass / 8 skip; build x4 + i18n modulepreload + backdrop survival OK; biome clean on shell.css. Adversarial coverage Workflow (cascade / lossless / classification / qa + synthesis): the 1 BLOCKING it found is the margin-top flip, now fixed + harness-verified, all else cleared. Commits local, not pushed. |
| P4b | CSS C-2: mobile-touch + per-entry .extra; empty both inline blocks | css | done | Moved the in-game mobile-touch block from index.html inline into NEW src/styles/hud.mobile.css. Homed the orphaned P2/P3 chrome P4a surfaced: chat/party frames/context menu/prompts/trade window/elite target frame + #tf-debuffs -> hud.css (@layer components); paperdoll `.equip-slot .equip-unequip-btn` + `#bags.drop-target` -> components.css; pre-start `body:has(#start-screen) #ui` hide -> base.css. Re-homed the 2 rode-along HUD rules (#community-hud/#quest-tracker) from shell.css's char-select 860 block into hud.mobile.css. EMPTIED both inline `<style>` blocks (index + play now comment-only). play.html turned out to be PURE stale drift of index (237 rules missing, 41 drifted values, 0 intentional unique CSS) so it was RECONCILED to the canonical modules (emptying its inline makes it fall back to the shared @layer modules both entries already load via the src/main.ts barrel); only per-entry difference preserved is the #rotate-device orientation gate (decision 16a): index suppresses the overlay in-game (src/styles/index.extra.css), play shows it in portrait (src/styles/play.extra.css), each loaded via a per-entry `<link>`. LAYER-NAME BUG FOUND + FIXED by the computed-style diff: the packet's `hud.mobile`/`index.extra`/`play.extra` names contain a DOT = a SUBLAYER, so `hud.mobile` was the `mobile` sublayer of the early `hud` layer and LOST to shell; renamed to FLAT `hud-mobile`/`index-extra`/`play-extra`, ordered `tokens,base,layout,components,hud,shell,hud-mobile,index-extra,play-extra` (hud-mobile AFTER shell so mobile overrides of pre-game shell elements win as they did inline). Lossless multiset proof: index OLD==NEW (byte-identical rule set), play differs only by #rotate-device. Cascade gate: negative-control-validated puppeteer computed-style diff = ZERO real flips on index (the mobile #mode-select/.play-console/#btn-play flip was the bug, now fixed); index #rotate-device=none + play #rotate-device=flex confirmed. Validation: tsc clean; full vitest 3931 pass / 8 skip; build x4 + i18n modulepreload + backdrop survival (11 webkit twins) OK; biome clean on hud.mobile.css/index.extra.css/play.extra.css; per-entry <link>s resolve under the flat @layer order in dist. Mobile E2E responsive gate (decision 16): mobile_input_zoom_check 28/0, mobile_minimap_safe_area + mobile_community_hud_safe_area (real CDP) + mobile_chat_safe_area pass; mobile_button_size/joystick_size are pre-existing RED (stale harness clicks the now-hidden #btn-offline, not a P4b layout regression). 5 tests updated (client_shell 41/41 repoint to hudMobileCss/componentsCss, charselect_sort_parity reconcile, styles_extraction flat @layer order). The CSS extraction P1->P4b is COMPLETE: both inline blocks empty, all game CSS under the single flat @layer order. commits local, not pushed |
| P5 | ui_effects_profile resolver (src/game, 5-axis) + applier | effects | done | NEW src/game/ui_effects_profile.ts (pure leaf, imports nothing) DEFINES EFFECTS_QUALITY_LOW_CUTOFF=0.5, gfx.ts:308 imports it (render->game leaf, sibling 0.5s untouched); full 5-axis FB contract (resolve/tokens flat --fx-* map/equal/allowFctCrit); NEW src/ui/ui_effects_applier.ts (matchMedia + 180ms debounce + diff-guard, mirrors theme.ts); main.ts wires boot+reduceMotion(immediate)+graphicsPreset(immediate)+effectsQuality(debounced), reduceMotion single-source (OS MQL OR setting), body.reduce-motion stays the CSS hook; registered in UI_PURE_CORES; CSS B1 glass low-drop + B2 ambient/vignette-3-axis + B3 motion-scale + B4 glow + B6 fct-crit gate. Default Ultra byte-identical (built-bundle computed-style proof). Deferred: per-element JS tiering consuming uiEffectsAllowFctCrit -> P14a/b. qa-checklist no BLOCKING |
| P6 | PainterHost (two facets) seam + pilot | seam | done | NEW src/ui/painter_host.ts: PainterHostPresentation (itemIcon/moneyHtml/itemTooltip/attachTooltip) + PainterHostWriters + makeWriterFacet(cache,onWrite,onSkip) binding the 4 elided writers as closures over a SUPPLIED cache (host-agnostic, no window). NEW src/ui/delve_map_painter.ts (Canvas-2D): pure delveDrawModel(IWorld -> schematic+mobs+party+arrow+label) + DelveMapPainter.paintMinimapDelve (writes #zone-label via the facet) / paintWorldMapDelve (on-canvas title); absorbed the orphaned drawSchematicPrimitives; resolves 6 --color-delve-* tokens via getComputedStyle ONCE/redraw (D12). delve_map.ts exports delveLocalToCanvas (the former private toCanvas, dedupes the marker math + drops the painter's magic numbers). ONLY vendor composes the presentation bag (VendorWindowDeps extends PainterHostPresentation, call site spreads ...presentationBag); lockpick (callbacks-only) + raid_lockout (pure string builder) UNTOUCHED (D8). hud.ts: built writerFacet+delvePainter+presentationBag fields, both delve branches -> painter calls, overworld #zone-label now this.setText (cache-coherent), deleted 4 bg-cache fields + the orphaned drawSchematicPrimitives + dead delve_map/DELVE_MODULE_LAYOUTS imports (-211 net lines). DISCOVERY: the phase file + recon were WRONG that the world-map is "static schematic only" -- BOTH sites paint mobs+party+arrow; the painter preserves both via parameterized entry points (size/pad/clip/marker-size/lw/label-sink). Reviewers: fork PARITY HELD 0 visible deviations vs the deleted inline code; qa-checklist 0 BLOCKING / 3 NICE-TO-HAVE (grandfathered getContext! warn matching the repo pattern, canvas-draw parity by code-read + the adversarial fork, the outline token short-hex correctly in CSS not TS). Validation: tsc clean; biome clean on every new/changed .ts (vendor_window got a bounded biome pass: import sort + 2 concat->template, behavior-identical); full vitest 4008 pass/8 skip; architecture purity (delve_map.ts pure, no painter import) + no-magic-values canvas guard + write-elision-facet fake-cache unit test + Sim/ClientWorld parity + both-sites-same-output all green; one source-text guard repointed (#zone-label now this.setText); no sim/server/net/world_api touched (D4). Two-facet contract for later phases: P7-P9 reuse presentationBag; P10a EXTENDS the writer facet with setStyleProp/toggleClass (D5a). Commits local, not pushed. NEXT: phase-06-qa? (none authored) -> phase-07a-coldwindow-talents.md |
| P7a | Cold-window: talents | windows | done | NEW src/ui/talents_view.ts (pure core in UI_PURE_CORES) derives header/tab-pip counts + spec list + class/spec tree layout (per-node state + prereq arrows) from (talentStage, cls, total); NEW src/ui/talents_window.ts (thin TalentsWindow painter) composes PainterHostPresentation + Hud deps, owns tab state + mutation callbacks that mutate Hud's single talentStage buffer in place. hud.ts -518 (14172->13654). +WCAG focus-return (captureFocus/restoreFocus on ALL close paths incl Escape) +no-magic (8 inline hexes -> var(--color-*) tokens, CW/CH/NS/TOP named in core) +ClientWorld-vs-Sim seed-parity test. roleLabel lifted to talent_i18n (shared w/ char sheet). Reviewers: qa-checklist 0 BLOCKING (1 SHOULD-FIX folded); independent parity PARITY HELD. tsc + full vitest 4022/8 + biome(new) green |
| P7b | Cold-window: social + bags | windows | done | NEW src/ui/social_view.ts (pure core in UI_PURE_CORES): socialStructSig + socialDot + friendRows/ignoreRows/guildView/raidView, incl raidView.canUnconvert (the V16 convert-party footer the older FB port had dropped). NEW src/ui/bags_view.ts (pure core): bagItemAction + bagTooltipHintKey (6-way click/tooltip) + bagQualityKey + buildBagGrid (reuses bag_filter.ts, no re-derive). NEW src/ui/social_window.ts (SocialWindow painter): owns tab/notice/typeahead state; CRUX = social is NOT purely cold (repaints on the 500ms slow-HUD divider) so the painter uses ONE delegated click listener on .soc-body (onBodyClick) wired once per render(); refreshList() only swaps innerHTML so a cadence tick never churns per-row handlers; +WCAG focus-return on every close path (capture/restoreFocus, the inline had none). NEW src/ui/bags_window.ts (BagsWindow painter): composes PainterHostPresentation + bags deps; owns the bag-filter state (moved off hud, same localStorage key woc_bag_filter); .bag-grid scroll-offset captured + reapplied. hud.ts -889 (13654->12765): renderBags/renderSocial thin delegators, cadence -> socialWindow.refreshIfChanged(), chat convert -> selectRaidTab(), closeManagedWindow social -> socialWindow.close(); ~900 inline lines removed via an anchored, asserted string-splice (not sed). +no-magic (D12): bags '#fff' -> var(--color-quality-default) token, social cadence stays the named shared slow-HUD divider, typeahead timings named consts. tokens.css +--color-quality-default. social_status_dots source guard repointed hud.ts -> social_window.ts. NEW tests: social_view/bags_view (struct-sig stable + tab/permission models + filtered render + same-input-same-output + ClientWorld-vs-Sim parity) + social_window/bags_window (no-magic + delegation/scroll source guards). REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 3 NICE-TO-HAVE (deferred: tablist semantics=P15 a11y pass, bags closeManagedWindow symmetry, live-axe row); adversarial parity Workflow (2 slices + per-deviation re-verify) PARITY HELD = 5 claimed deviations, 0 confirmed regressions. tsc clean + full vitest 4056/8 (387 files) + biome clean on the 4 new files (4 grandfathered noNonNullAssertion warns, same idiom as delve_map_painter) + architecture purity + client_shell green. Presentation-only: world_api/sim/server/net/headless/i18n.locales untouched. No perf gate (cold + 500ms-divider diff-gated). commits local |
| P8a | Cold-window: options | windows | done | NEW src/ui/options_view.ts (pure core in UI_PURE_CORES): the densest control surface modelled declaratively. 4 control-descriptor primitives (slider/toggle/boolToggle/choice) + pure dispatch-value fns (sliderDispatchValue=Number(), toggleNextValue=>=0.5?0:1, toggleIsOn, boolToggleNextValue=!x) + per-panel builders (buildGraphicsControls w/ the advanced-preset=5 reveal + useTouchInterface + !isNativeAppShell gating, buildAudioControls incl a musicToggle position marker, buildControllerControls, buildInterfaceControls) + buildOptionsMenu + buildBugReportInfo (the ONE IWorld slice = parity surface). DOM/Three/game-free; label keys typed via type-only TranslationKey from ./i18n.catalog (game-key types are forbidden in a core). NEW src/ui/options_window.ts (class OptionsWindow, STANDALONE OptionsWindowDeps -- composes NO PainterHostPresentation, renders no item rows, per the social precedent/decision 6): owns #options-menu DOM + the view/capturingKey/keybindNote/perfSettings state + lifecycle (toggle/close/isOpen/onPerfOverlayMoved) + the 4 control builders (driven by the core descriptors) + the bespoke controls the 4 primitives do not cover -- language picker (PR#730 hardening verbatim), theme, chat-timestamps, keybind capture, gamepad remap, bug-report form, performance delegate. hud.ts -1237 (12765->11528): thin delegators (toggleOptionsMenu/closeOptions/optionsOpen getter/onPerfOverlayMoved) + a lazy-closure deps bag by the other window fields; ~1237 inline lines + the LANGUAGE_ENDONYMS/BIND_CATEGORY_LABEL_KEYS/BIND_ACTION_LABEL_KEYS consts + the optionsView/capturingKey/keybindNote/perfSettings fields removed via an anchored asserted string-splice (mustContain/mustNotContain + no-dangling-ref guards, not sed); 16 now-dead imports pruned. +WCAG focus-return (captureFocus before closeOthers in toggle(), restoreFocus on every close incl closeManagedWindow->closeOptions->close(); the inline had NONE). +no-magic (D12): the painter carries ZERO hex (the gold log tint '#ffd100' stays Hud-side via the log dep closure), BUG_DESC_MAX_LEN/RANGE_FILL_FULL_PCT named. Tier boundary HELD (decisions 6/8): graphics reads the static graphicsPreset as a plain rounded setting value; NO governor read, NO EFFECTS_QUALITY_LOW_CUTOFF, NO ui_effects_profile import. NEW tests: options_view.test.ts (full per-sub-panel dispatch matrix: 4 primitives + graphics/audio/controller/interface ordering+gating + main-menu routing + bug-report info; same-input-same-output; ClientWorld-vs-Sim bug-report parity via two DISTINCT world shapes (offline Sim prototyped instance + '' realm vs online ClientWorld plain wire object + populated realm, decision 15, not a JSON self-clone)) + options_window.test.ts (no-color hex+rgb/hsl / named-const 2000-pinned-to-its-def-line / no-dash + tier-boundary + WCAG focus-return/roles + gamepad-listbox-name + slider-aria-valuetext + changeLanguage hardening + bug-report async states + keybind dispatch + per-primitive dispatch-expression + applyControls-routing + cold-window source guards). tests/architecture.test.ts registered options_view; client_shell menu guard repointed (inline add() -> buildOptionsMenu labelKey) + S3/R2 localizeBugReportError guard repointed hud.ts->options_window.ts. REVIEWERS: adversarial parity Workflow (6 sub-panel clusters + per-finding adversarial verify) PARITY HELD = 6/6 clusters, 0 material findings, 0 confirmed regressions; qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 1 NICE-TO-HAVE. RE-RUN AUDIT (2026-06-25, ultracode 6-finder adversarial Workflow over the committed extraction, 18/43 confirmed, 0 BLOCKING) added 2 a11y fixes (gamepad listbox aria-label per WCAG 4.1.2; slider aria-valuetext) + made the decision-15 parity test two real shapes + tightened the dispatch + no-magic source guards. HONEST WCAG SCOPE: per-window roles/aria/labels/focus-return proven by source guards; live axe / forced-colors / computed target-size / :focus-visible DEFERRED to P15b (opt-in vitest.browser.config.ts), per P6/P7a/P7b. tsc clean + full vitest 4087 pass/8 skip + biome clean(4 new files) + build(4 entries) + purity guard(teeth proven via an injected ../game import). Presentation-only (D4): world_api/sim/server/net/headless/i18n.locales diff EMPTY. No perf gate (cold; not in Hud.update). commits local |
| P8b | Cold-window: market + char | windows | done | NEW market_view + market_window (snapshot state-union {no-data/browse/sell/collect} reusing market_filters; the "error" variant is OMITTED -- the market is snapshot-driven, NOT promise-based, so there is no error channel; both reviewers confirmed faithful, leaderboard is the only Promise window, P9b) + char_view (paperdoll-only core) + char_window (the shared 3D preview + skin picker stay HUD-owned via renderPreview/renderSkinPicker callbacks; the skin-event Math.random lives in a separate overlay, never in the core). hud.ts -712 net; +2 empty-slot tokens; WCAG focus-return added to both (incl a new closeManagedWindow char case). qa-checklist PASS 0 BLOCKING/0 SHOULD-FIX; adversarial per-window parity HELD/HELD. tsc/biome(4 new files)/vitest 4127 pass+8 skip/build(4 entries)/purity-guard(teeth proven)/localization all green; presentation-only diff EMPTY. Detail below + state.md. commits 83f129e0 + c7545779 local. RE-RUN SENIOR-QUALITY AUDIT 2026-06-25 (ultracode 6-finder adversarial Workflow, 16 agents, over the COMMITTED extraction): 10 findings -> 3 refuted + 7 survived (1 minor + 6 nits), 0 BLOCKING / 0 MAJOR = extraction confirmed faithful + clean. HARDENED 2 test gaps: (1) market_view.test.ts coin-split now exercises the buyValue ?? LEFT arm (healing_potion 170c -> 1s70c) AND the nonzero-gold sellValue*4 RIGHT arm (deathlord_warplate 36000c -> 3g60s); the prior copper-only 40c case left the gold floor-division, the silver modulo, and the ?? left arm untested. (2) architecture.test.ts adds a forbiddenUiCoreImport negative test (flags three/render/game/net/painter, allows sim+sibling cores) so the UI-purity guard's teeth survive a future matcher weakening: the per-phase "guard must FAIL on an injected forbidden import" is now a durable regression test, not a manual ritual. vitest 4130 pass/8 skip. NICE-TO-HAVE tracked for P15b (none a regression): market filter listbox roving-tabindex/arrow-key nav (byte-faithful carry-over of the inline original; advertises listbox semantics it does not fully deliver to AT), an Escape/closeManagedWindow focus-return source-grep, and the live axe / forced-colors / computed target-size rows; the decision-10 "per-window target-size NOT deferred to P15" wording vs the 5-phase live-check-deferral practice is a doc tension to reconcile in P15 (not a P8b defect). |
| P9a | Cold-window canvas: map + arena | windows | done | map = canvas painter; arena = DOM painter (the phase doc framed both as canvas; renderArenaWindow is DOM/innerHTML -- only the map is a true canvas window, spec-vs-live drift, trusted source). NEW map_window_view.ts (pure core, UI_PURE_CORES): mapWindowMode + buildOverworldMapModel -> flat canvas-px draw model (blit fractions/view/cursor/zoneId/POI/portal/npc/player-arrow/ally + the >=MAP_DETAIL_ZOOM detail overlay). NEW map_window_painter.ts (Canvas-2D, not a registered core): resolves 22 --color-map-* tokens via getComputedStyle ONCE/redraw (D12), owns the decorations cache, faithful draw order; Hud routes the delve branch to P6's delvePainter (no duplicated marker drawing). NEW arena_window_view.ts (pure core, UI_PURE_CORES): buildArenaView -> {offline}|{live} union (bracket resolve+commit, canSwitchBracket, 3-branch queueDisabled, party/action sections, ladder/all-time rows, the verbatim raw-row sig). NEW arena_window.ts (DOM painter): owns bracket/all-time-cache/fetch-throttle/sig/openerFocus; +WCAG focus-return (inline had none). hud.ts: updateMapWindow thinned + drawMapDetail/renderArenaWindow/fetchArenaLeaderboard/toggleArena deleted -> delegators; mediumHud band -> arenaWindow.render() (same display guard + 250ms cadence); closeManagedWindow 'arena-window' case; the moved arena/map state fields removed; +22 --color-map-* tokens, play.html canvas role=img + sr-only #map-summary, hud.core.mapCanvasLabel/mapSummary (English-only, non-wordy so M16 passes). qa-checklist 0 BLOCKING / 0 SHOULD-FIX / 4 NICE-TO-HAVE (all faithful-to-inline or intentional). tsc/biome(8 new files)/vitest 4171 pass+8 skip/build(4 entries)/purity(both cores)/client_shell/i18n all green; presentation-only diff EMPTY. No perf gate (mediumHud cadence-gated like P6). Detail in the narrative below + state.md. commits local. |
| P9b | Cold-window DOM: questlog + spellbook + leaderboard | windows | done | CLOSES the cold-window seam (P6 pilot through P9b; no inline cold windows remain). NEW leaderboard_view.ts (pure core, UI_PURE_CORES; ASYNC-FREE: maps a resolved LeaderboardPage or a loading/error discriminator -> rows + off-page sticky standing + pager + clamped page) + leaderboard_window.ts (the packet's ONE consumed-new IWorld signature: awaits the paged leaderboard(page,size):Promise<LeaderboardPage>, consumed NEVER changed; owns the page index + focus opener; the painter owns the Promise/await/page-controls; NEW error state -> t('game.leaderboard.retry') when leaderboard() rejects/offline, where the inline collapsed rejection into the empty state). NEW spellbook_view.ts (class kit -> rows w/ known/rank/onBar/toggleDisabled) + spellbook_window.ts (ability icons via iconDataUrl not the item-icon bag; hotbar +/- toggle, drag-to-bar via a local writeDraggedAction mirroring bags_window, refreshHotbarControls is the one NOT-cold touch called from hud.update() while open). NEW questlog_view.ts (list rows + selection resolution + the selected-quest detail structure; this is the LOG window, NOT the quest_tracker core, so there are no tracker rows to re-derive) + questlog_window.ts (composes the presentation bag for the reward row; OWNS the selected quest id, read back by Hud's /share command via the selectedQuestId getter; abandon -> confirmDialog -> world.abandonQuest). hud.ts net -266 (-351/+85): 3 thin toggle delegators + 3 lazy-closure dep-bags + closeManagedWindow cases (spellbook/quest-log-window/leaderboard-window -> painter.close for focus-return) + call-site updates (refreshLocalizedDynamicUi -> questlogWindow.render, the per-frame band + resetActiveFormBarToDefault -> spellbookWindow.refreshHotbarControls, maybeHandleQuestShareCommand -> questlogWindow.selectedQuestId); the inline render*/toggle*/leaderboardPagerHtml/refreshSpellbookHotbarControls methods + the selectedQuestLogId/questLogReturnFocus/leaderboardPage fields removed (no dangling ref). +WCAG (D10): all three got focus-return (the inline leaderboard/spellbook had NONE), role=dialog/aria, leaderboard close upgraded <span>-><button> w/ aria-label, loading=role=status+aria-busy, error=role=alert; capture-opener-BEFORE-closeOthers (correct order so a sibling's restore cannot clobber the captured opener). +no-magic (D12): leaderboard subtitle inline #998d6a/11px -> .lb-subtitle class; questlog '#fff' -> QUALITY_DEFAULT_COLOR token; &mdash;/&starf; entities (no literal em-dash/star glyph -> passes the painter em-dash guard + the hex guard). i18n: GOTCHA M16 completeness flags "Close" as WORDY (>=4 lowercase) so an English-only hud_chrome key alone fails npm test -> FILLED the 5 non-Latin overlays for hudChrome.leaderboard.close (zh_CN 关闭/zh_TW 關閉/ja 閉じる/ko 닫기/ru Закрыть), Latin stays English-pending (allowed); the leaderboard ERROR copy reuses the existing fully-localized game.leaderboard.retry (NO new key). Player names esc()'d in the leaderboard painter (the inline had a latent unescaped-name; now matches the questlog sibling + the src/ui invariant). NEW tests: 3 view-core suites (per-state incl leaderboard loading/empty/error/clamp + decision-15 parity feeding BOTH a Sim-shaped and ClientWorld-mirror stub across every state incl a rejecting path -> error + determinism) + 3 painter source-guards (WCAG roles/aria/focus-return + no-magic hex/px + em-dash + named-const). architecture.test.ts +3 cores; client_shell spellbook assertions repointed hud.ts -> spellbook_window.ts. REVIEWERS: qa-checklist 0 BLOCKING (1 MAJOR = un-esc'd leaderboard names, FIXED); adversarial 5-finder coverage Workflow (faithfulness vs the deleted inline via git-show HEAD + a11y + wiring/parity, each finding adversarially verified) 2 raw -> 0 confirmed = faithful + clean. tsc/biome(6 new files)/vitest 4245 pass+8 skip/build(all entries)/purity guard(3 cores)/client_shell/localization/M16-completeness all green; presentation-only diff EMPTY (world_api/sim/server/net/headless untouched). commits local. NEXT: phase-10a-perframe-leakfix-host-writers.md |
| P10a | Per-frame: xp + swing leak-fix + elided-writer extension | per-frame | done | LANDS decision 5a: makeWriterFacet grew 4->6 writers (+setStyleProp/+toggleClass, MULTI-SLOT per (element,prop)/(element,class), separate hotStylePropCache/hotClassCache so they cannot collapse into or weaken the four single-slot writers). NEW swing_timer core (zero-import, i18n-free, param-in/next-state-out edge tracking) + swing_timer_painter + xp_bar_painter (over the existing xpBarView); both inline Hud.update() blocks replaced, the two write-elision LEAKS fixed (refs cached once, no per-frame $()/querySelector). Incidental: updateLowHealthVignette rerouted through the new private writers (cached ref, exercises them). PERF GATE (the first): frameP95 250ms==baseline, hudHotDomSkipRate 0.965>=0.962 IMPROVED (2 stable runs); mobile deferred to P17a (skip-rate renderer-independent). qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 5 NICE-TO-HAVE (all verified non-issues) / 1 VERIFY (mobile perf). tsc / biome (8 new files clean, hud.ts 0 new debt vs HEAD) / vitest 4278+8 / purity-teeth (injected render import reds swing_timer) all green; presentation-only diff EMPTY (no sim/server/net/world_api/CSS/i18n.locales; no new i18n keys). Detail below. commits local. |
| P10b | Per-frame: unit_frame FAMILY (player instance) | per-frame | done | The load-bearing reusable FAMILY (decision 9): NEW src/ui/unit_frame.ts (pure core in UI_PURE_CORES; UnitFrameDescriptor -> UnitFrameView; present/hidden gate + absorbBarView resolution + resourceKind->resClass discriminator incl a 'none' case; NO hardcoded id, NO single-instance assumption; DOM/i18n-free, deterministic) + NEW src/ui/unit_frame_painter.ts (thin write-elided painter over the six writers; optional absorb + resource element groups, optional shownDisplay + stateClasses + repaintPortrait gate so target/party reuse it with no core change). The PLAYER frame is the first instance: the inline Hud.update player block is replaced by playerFramePainter.paint(unitFrameView(playerDescriptor)); the raw updateAbsorb('#pf-absorb',p) transform + overshield toggle and the resource className swap FOLD into the elided writers (the className swap became per-type toggleClass(rage/energy/mana) so it no longer clobbers the 'low' class updateLowResource owns; the static 'bar' base class is never touched). BYTE-FAITHFUL: scaleX uses RAW fracs (no toFixed), level=String(p.level), hpText/resText formats unchanged, the player name stays at the login set (painter omits name for the player), the player frame display is never written (always CSS flex). updateLowHealthVignette + updateLowResource stay at the call site (player-only side effects, own cores, OUT of the family). a11y (decision 10): #player-frame gains role="group" + a localized accessible name via data-i18n-aria backed by ONE English-only key hudChrome.unitFrame.playerLabel='Your Hero' (kept NON-WORDY so the M16 completeness gate does not flag it as untranslated-English in non-Latin locales; user-chosen); hp/resource conveyed as text (#pf-hp-text/#pf-res-text, not aria-hidden); the frame carries no focusable control so target-size/focus-return N/A; 3D portrait/world out of a11y scope. PERF GATE (scripts/perf_tour.mjs desktop, same M4 Max as P0): frameP95 250ms == baseline 250ms (HELD); hudHotDomSkipRate 0.962-0.965 across runs, min 0.962 == P0 baseline (writes 48->53: the +5 are the mandated absorb x2 + resType x3 folds of previously-RAW writes now COUNTED+elided; folding name/dead/oor was REJECTED as unfaithful to the old player frame). Mobile NOT capturable (ConnectionClosedError = the known P0 #mobile-preflight portrait breakage, deferred to P17a; skip-rate is renderer-independent so the desktop capture covers the DOM-write gate). VALIDATION: tsc clean; biome clean on all new files (hud.ts 0 new debt); full vitest 4307 pass / 8 skip (409 files); architecture purity guard PASSES + TEETH PROVEN (injected document.title reds unit_frame, green after revert); css_corpus + client_shell green after the play.html attribute add; npm run build 4 entries green (backdrop-survival OK); localization_fixes green (the new key registered pending, PR-tier permits English-only). Two-descriptor + Sim-vs-ClientWorld parity core test pins decision 9 + decision 15 against the FULL P11 field set (not a stub). REVIEWERS: qa-checklist 0 BLOCKING / 0 MAJOR (28 PASS, byte-faithfulness traced end to end; 2 MINOR-informational: a safe write reorder across independent elements, ~2 short-lived objects/frame within budget) + adversarial coverage 0 BLOCKING / 0 MAJOR (player path byte-faithful across all 8 categories; MINOR: the call-site formatting is not unit-pinned, accepted - extracting a player-only builder for one use violates the no-abstraction-for-one-use rule and both reviewers hand-verified it; fixed the cosmetic import-order nit it raised). The green-perf-gate commit is TAGGED frontend-v016-p10b-perf-gate. commits local. Detail below. |
| P11a | Per-frame: cast bars | per-frame | done | NEW eat/drink MODE discriminator on the i18n-free render core (src/render/cast_bar.ts: consumeBarState(eating,drinking) -> {visible,fill,mode:'eat'\|'drink'\|'eatdrink',remaining}; castBarState byte-unchanged; the longer-remaining consumable drives the bar; the core emits the stable mode + the painter localizes it, the SAME precedent the `label` discriminator set). NEW src/ui/cast_bar_painter.ts: ONE instance-parameterized painter (decision 9), PLAYER instance (#castbar: localizes the cast id via a castDisplayName closure + the PLAYER-ONLY eat/drink overlay via consumeBarState + clearOnHide) + TARGET instance (#tf-castbar: RAW cast id byte-faithful to the old block, NO consume so it can never render eat/drink, display-only hide), both routing setDisplay/setWidth/setText + the .channel toggleClass through the elided writers in byte-identical order. Both inline Hud.update blocks deleted; the player cast unified onto castBarState (strictly-safer guards on non-occurring edges); orphaned private Hud.setWidth removed (the cast bars were its last caller; the facet setWidth is independent). A11Y (D10): #castbar/#tf-castbar role=progressbar + aria-valuemin/max + data-i18n-aria (hydrated to a t() aria-label by main.ts) in BOTH index.html AND play.html (client_shell parity guard); ONE English-only key hudChrome.castBar.{playerAria='Your Cast Bar',targetAria='Unit Cast Bar'} (NON-WORDY so M16 does not flag untranslated English); aria-valuenow OMITTED (indeterminate-rate bar, no SR announcement spam, the elided-writer facet has no attribute writer; min/max satisfy aria-value*); forced-colors fill shading deferred to P15b (no CSS-rule changes here; the bar border+outline survive). Architecture guard HARDENED: forbiddenRenderCoreImport now bans the i18n runtime so a t()/tEntity call in the core fails the guard (not just the file header) + a teeth unit test; cast_bar stays in RENDER_PURE_CORES. TESTS: cast_bar.test.ts (+consume branches incl tie-to-food + D15 Sim-vs-ClientWorld parity feeding DIVERGENT non-remaining Consuming fields, proving the core reads only .remaining) + cast_bar_painter.test.ts (recording-facet byte-order routing for BOTH instances, cast-wins-over-consume, clearOnHide vs display-only, target-never-eats, no-raw-write + no-magic scans) + client_shell both-entries progressbar guard + the forbiddenRenderCoreImport teeth test. VALIDATION: tsc clean; biome 0 new debt on hud.ts (-1: the `!` + orphaned setWidth gone); full vitest 4327 pass/8 skip (410 files); architecture TEETH PROVEN (injected real ../ui/i18n import + document.title -> RED on both -> green after revert). PERF GATE (perf_tour desktop, same M4 Max as P0, identical bootMiB 68.779): frameP95 250ms == baseline 250ms; hudHotDomSkipRate 0.963 >= baseline 0.962 (HELD/improved - the 4 formerly-RAW .channel classList writes are now COUNTED + elided through toggleClass). Mobile NOT capturable (P0 #mobile-preflight portrait breakage, deferred to P17a). REVIEWERS: qa-checklist 0 BLOCKING/0 MAJOR (24 PASS) + 5-dimension adversarial Workflow + completeness critic = READY TO COMMIT, 0 BLOCKING/0 MAJOR (folded 2 MINORs: the parity test made non-tautological + the guard teeth test; switched Foe->Unit for friendly-target accuracy; refreshed the painter_host 6-writers doc for the setWidth removal). The TARGET cast instance built here is the one P11b's target frame CONSUMES (no cast-bar rebuild). commits local. NEXT: phase-11b-perframe-target-frame.md |
| P11b | Per-frame: target frame (unit_frame instance) | per-frame | done | The target frame is now the SECOND instance of the unit_frame FAMILY (decision 9), NO bespoke target_frame core, NO core (unit_frame.ts) change: the P10b descriptor already covered the full target field set. NEW targetFramePainter = new UnitFramePainter(writerFacet, {frame/name/level/hpFill/hpText/absorb}, {shownDisplay:'flex', repaintPortrait}); the inline target block (hud.ts ~3672 old) is replaced by targetFramePainter.paint(unitFrameView({...target...})) + the target-only sub-parts. The descriptor maps: hpFrac=hp/max(1,maxHp), hpText=dead?Dead:`hp / maxHp`, levelText=boss?BOSS_SKULL_GLYPH:String(level), name=entityDisplayName, portraitKey=String(id), absorb=dead?null:target, resourceKind='none' (no power bar), dead:false/outOfRange:false (target omits stateClasses). The two raw writes the four original writers cannot express route through the P10a extension: the elite class -> toggleClass(targetFrameEl,'elite'), the hostile/friendly name color -> setStyleProp(targetNameEl,'color',var(--color-hostile\|friendly)); the combo-pip 'on' -> toggleClass(pip,'on'). The boss/elite TAG stays a t('hud.core.boss'\|'elite') setText in hud (never concat). DEEP-REVIEW CORRECTION LANDED: the absorb node is CACHED ONCE (private targetAbsorbEl=$('#tf-absorb')) and driven by the family painter's paintAbsorb, replacing the old per-frame updateAbsorb('#tf-absorb',...) document query (decision 9 hardcoded-id + per-frame-query violation); the now-dead private updateAbsorb + the absorbBarView import are removed. PORTRAIT CHANGE-GATE preserved in the PAINTER (not the core): lastPortraitTarget(-999) -> the painter's lastPortraitKey; redraws ONLY on a target-id change; RESET-ON-HIDE (paint(!present) clears lastPortraitKey, reached via ABSENT_TARGET_DESCRIPTOR on no-target/world-object) so re-acquiring or a reused id repaints; NEW invalidatePortrait() replaces the onPortraitsReady -999 reset (force-redraw when the 3D portrait assets load). drawTargetPortrait reads the transient targetPortraitSubject (set immediately before the synchronous paint): player target -> drawClass(skin), else -> drawCrest(crestIdForEntity). COMBO PIPS lazy-built ONCE (guarded by children.length !== COMBO_PIP_COUNT) then only toggled, never rebuilt per frame; count = comboTargetId===target.id ? comboPoints : 0. The target debuffs CONSUME the existing renderAuras('debuffs') (auras pool rewrite is P12); the target cast bar CONSUMES P11a's cast_bar TARGET instance (no rebuild). ORPHANED-WRITER GOTCHA (P11a pattern): moving the target hp transform onto the family painter made the private Hud.setTransform dead (the target was its LAST caller); REMOVED it (the facet setTransform shares the same hotWriteCache + 'transform:' key, parity-neutral). NO MAGIC VALUES (decision 12): BOSS_SKULL_GLYPH (the pre-existing U+2620 skull moved into a named const, not a new emoji), COMBO_PIP_COUNT, ABSENT_TARGET_DESCRIPTOR are named; name color is a var(--color-*) token. A11Y (decision 10): #target-frame gains role="group" + data-i18n-aria="hudChrome.unitFrame.targetLabel" + aria-label in BOTH index.html AND play.html (entry parity, hydrated to t() by main.ts), ONE English-only key targetLabel='Your Mark' (NON-WORDY so M16 does not flag English-fill in non-Latin locales; faction-neutral for friendly+hostile targets); the frame is informational (selection is the 3D world click + a pre-existing right-click contextmenu, unchanged), so no new keyboard/target-size duty; the live-region target-name announcement is DEFERRED to P15a (the group keeps its static accessible name); the hostile-vs-friendly NAME COLOR is the one color-alone cue (PRE-EXISTING, byte-faithful) and a forced-colors redundant cue is DEFERRED to P15a (no CSS-rule changes in this slice, matching P11a's forced-colors deferral); elite/boss have non-color cues (the ELITE/BOSS tag text + the skull glyph). TESTS: unit_frame_painter.test.ts (+reset-on-hide repaint, +invalidatePortrait forced repaint); NEW tests/target_frame.test.ts (decision-15 Sim-vs-ClientWorld parity: models the mirror FAITHFULLY - server WireAura omits the absorb value + online.ts forces aura.value=0, so the shield is OFFLINE-ONLY; asserts the wire-carried frame fields + cast + combo MATCH and documents the absorb as the one intended divergence, NOT a false toEqual); client_shell.test.ts (target-frame role=group in BOTH entries + hud source guards: cached absorb query exactly once, elite/name-color/combo routed through the elided writers, combo lazy-build guarded); options_window.test.ts (hardened the fragile update()-slice anchor from the first 'update(' literal -> the real '\n  update(): void {' method, since a comment mentioning update() was shifting the slice). VALIDATION: tsc clean; biome 0 new debt on hud.ts (the orphaned setTransform removal kept noUnusedPrivateClassMembers at the pre-existing 2); full vitest 4338 pass / 8 skip (411 files); architecture purity TEETH re-proven (injected document.title into unit_frame.ts reds the guard, green after revert; the core is unchanged). PERF GATE (perf_tour desktop, same M4 Max as P0): frameP95 250ms == baseline 250ms HELD; hudHotDomSkipRate 0.962 == P0 baseline 0.962 HELD (the rate climbs 0.832->0.962 through the tour as steady-state writes elide; the formerly-RAW elite/name-color/absorb/combo writes are now COUNTED + elided). Mobile NOT capturable (P0 #mobile-preflight portrait breakage, deferred to P17a; renderer-independent skip-rate covered by desktop). Presentation-only (decision 4): no IWorld/sim/server/net/headless/i18n.locales/src/styles change. REVIEWERS: qa-checklist 30 PASS / 0 FAIL / 0 BLOCKING + a 5-lens adversarial coverage Workflow (byte-faithfulness CLEAN, write-elision/perf CLEAN, a11y 1 NIT pre-existing, parity 1 MAJOR + 1 MINOR on the TEST, no-magic/hygiene 1 NIT). FOLDED the parity MAJOR (the absorb cross-host equality was false online - rewrote the test to model the mirror's value-zeroing + document the offline-only shield) + the MINOR (fixtures no longer tautological, hostile-color parity added) + the hygiene NIT (the dangling setTransform-removal comment). commits local. NEXT: phase-11c-perframe-party-pool.md |
| P11c | Per-frame: party frames (keyed pool) | per-frame | done | The high-churn innerHTML-wipe -> KEYED NODE POOL (top-risk 3 retired). NEW src/ui/party_frame_row.ts (one-time row builder: createPartyRow builds the .party-frame.panel DOM + dead/combat/oor badges + crest + hp/resource bars, attaches click/contextmenu/keydown ONCE, constructs the per-row unit_frame INSTANCE; partyRowHandlers is the pure live-slot handler factory) + NEW src/ui/party_frames_painter.ts (the HOT path: a pid-keyed Map pool + free list, sync() reconciles create/reuse/recycle/detach + repaints in place, every write through the elided writers, leave button kept last). Each member is an INSTANCE of the P10b unit_frame family (decision 9, NO bespoke party core, NO core change) - to fit it I extended ONLY the family PAINTER (src/ui/unit_frame_painter.ts): hpText + resource.text became OPTIONAL (party shows bars only, no readout) and a formatScaleX option preserves the party bars' inline .toFixed(3) precision; player/target omit it -> raw scaleX -> BYTE-IDENTICAL (the pure core unit_frame.ts is UNCHANGED). LIVE-SLOT (top-risk 3): the row's listeners read row.slot.member (the pool overwrites it before paint), NOT a captured member, so a row recycled to a reused pid acts on the NEW member (a test recycles a freed row to a new pid + asserts the closure reads it); the crest repaint runs through the family portrait gate reading the live slot. HOIST: NEW pure partyFrameSignature in src/ui/party_frames.ts (selectPartyFrameMembers UNCHANGED) computes the cheap signature in ONE string pass with NO selector-array allocation, called BEFORE the selector so an unchanged party short-circuits without allocating its sorted/filtered/mapped arrays (test pins the order; byte-faithful to the old inline sig incl the mres omission). Per-frame writes: --cls via setStyleProp, combat + below-target via toggleClass, dead/oor via the family stateClasses, the dead/combat/oor badges via setDisplay (persistent nodes, only display toggles - the forced-colors-safe icon cue), the leave label via setText. A11Y (decision 10): each row is role="button" + tabIndex=0 (was a bare unkeyboardable div), Enter/Space -> target, the Menu key opens the context menu natively with a row-box fallback when clientX/Y are 0; the member NAME is the accessible name (visible text, recycle-safe, NO per-frame setAttribute); the badges are aria-hidden decorations (so '⤢' does not leak into the button name); #party-frames is role="group" + data-i18n-aria="hudChrome.unitFrame.partyLabel" in BOTH index.html and play.html (client_shell parity guard) backed by ONE English-only key partyLabel='Your Band' (NON-WORDY, no overlay edit, parallels Your Hero/Your Mark); the focus ring is the existing global [tabindex="0"]:focus-visible !important (shell.css; overrides the .panel 1px outline). RELOCALIZE: the pooled rows reuse their DOM, so refreshLocalizedDynamicUi (the woc:languagechange hook) now calls partyFramesPainter.relocalize() to re-set the badge tooltips + leave label in place (a regression the qa reviewer caught). DECISION 15: the party oor flag is computed from member x/z vs player pos; the offline Sim sends full-precision x/z while the server partyWire sends round2(x/z) - a parity test drives BOTH shapes and asserts the oor flag agrees away from the 100yd knife-edge. VALIDATION: tsc clean; biome clean on all new/changed .ts (hud.ts 0 new debt, base==work 32); full vitest 4358 pass/8 skip (412 files); npm run build 4 entries green; architecture purity guard PASSES + TEETH PROVEN (injected document.title reds party_frames.ts, green after revert); i18n regen STAGED + M16 clean. PERF GATE (perf_tour desktop, same M4 Max as P0): frameP95 250ms == baseline HELD across 4 runs; hudHotDomSkipRate 0.961/0.962/0.963/0.961 (median 0.962 == P0 baseline; the +1 write 53->54 is the below-target toggle, RAW before, now COUNTED+elided = a real per-frame DOM-write reduction, the same honest-accounting dip P10b/P11a documented; the relocalize/aria-hidden folds are creation-time/cold-path only, hot path byte-unchanged). REVIEWERS: qa-checklist 0 BLOCKING / 1 SHOULD-FIX (the badge-tooltip relocalize - FOLDED) + adversarial coverage Workflow (6 dimensions) 0 BLOCKING / 2 MAJOR both REFUTED on adversarial verify (focus ring exists globally via shell.css; the mres-signature stale-fill is byte-faithful to base + cannot occur) / 4 MINOR / 7 NIT - FOLDED the relocalize + the oor aria-hidden + an imprecise comment; DEFERRED the raid-group screen-reader conveyance + the mres-signature omission to P15a as pre-existing (not P11c regressions). commits local, not pushed. This keyed-pool discipline (listeners-once + live-mutable-slot + free-list recycle) de-risks the auras pool (P12b) and the FCT pool (P13). NEXT: phase-12a-perframe-action-bar.md |
| P12a | Per-frame: action bar + allocation-budget spike | per-frame | pending | ULTRACODE; multi-bar descriptor; aria-label elision keeps t(); resolves the alloc proxy; PERF GATE |
| P12b | Per-frame: auras keyed pool + minimap canvas | per-frame | pending | ULTRACODE; tooltip closure reads live record; discriminated Marker union; canvas-token policy; PERF GATE |
| P13a | Per-frame: FCT core + per-frame driver | per-frame | pending | ULTRACODE; core determinism (injected jitter); driver folded into hud.update via adapter |
| P13b | Per-frame: FCT pooled painter + migration + gate | per-frame | pending | ULTRACODE; getUiScale; class-token colors; 7 sites + showSelfNote (main.ts:1727); bounded-AoE gate |
| P14a | Per-element graphics tiering | tiering | pending | tier knobs read static preset only; STEP 0 checks P13b exposed cap/TTL/crit knobs |
| P14b | Nameplate extraction | tiering | pending | nameplate_view core + painter (real Three/DOM extraction); tier-driven interval; mobile 1/15 floor |
| P15a | Accessibility infra | a11y | pending | NEW; ONE focus manager (full ~15-caller set), skip links, live regions (named cadence), forced-colors, print |
| P15b | Accessibility audit + tooling | a11y | pending | NEW; chrome-wide axe + keyboard E2E (opt-in browser suite); per-window fixes; mobile target-size pass |
| P16 | Standards codification into CLAUDE.md | standards | pending | NEW; docs-only; reconcile WCAG 2.1->2.2 + keep 40px floor; src/styles Repo-map row; faithfulness review |
| P17a | Harness floor (test-only) | close | pending | client_shell re-author + standing hud_perf_budget + UI_PURE_CORES completeness sweep + first all-together perf run |
| P17b | Bundle + lazy-load + cross-engine + close | close | pending | bundle-budget gate + selective lazy-load (a11y loading state) + mobile-Safari/WebKit E2E + axe CI + final exit |

Legend: pending / in-progress / complete / complete+QA.

## Layer gates (the de-risk ordering)

1. Gates + CSS + effects + cold-window seam (P0-P9b) restore the proven FB architecture on v0.16.0.
2. Per-frame extraction (P10a-P13b) only starts once the seam (P6) and gates (P0) are green; P10a
   lands the elided-writer extension and P10b the parameterized unit_frame family that P11a/b/c reuse.
3. Per-element tiering (P14a) only starts once every hot element is a core+painter (P10a-P13b);
   P14b formalizes nameplates.
4. Accessibility (P15a infra, P15b audit) consolidates + audits a11y once every component exists
   (per-window/per-control a11y is built in during P7a-P14b via the MANDATORY WINDOW/CONTROL gate).
5. Standards codification (P16) writes the proven, guard-backed contracts into the CLAUDE.md files.
6. Close (P17a harness floor, then P17b bundle + lazy-load + cross-engine E2E) re-authors the
   harness, makes the per-frame budget standing, and runs the final CI/perf exit gate.

## Per-phase deliverable checklists

Each phase's acceptance criteria live in its `phase-NN-*.md` STEP 5. This section accumulates the
ACTUAL outcome (files added, deferrals, commits, QA verdict) as phases complete. Empty until P0
starts.

- P0: _not started_
- P1: DONE (committed locally, not pushed: b9fe99b2 build flip / 0a120e9f barrel+seam / 0892c250 extract tokens+base). Files added: .browserslistrc, scripts/browserslist_targets.mjs (+ .d.mts + tests/browserslist_targets.test.ts), src/styles/{index,tokens,base}.css. Lightning targets via browserslistToTargets(zero-dep .browserslistrc parser) keep the build vanilla (no browserslist/caniuse-lite dep). @layer order declared once in index.css; barrel imported once from src/main.ts so both game entries (index.html + play.html, shared bootstrap) get tokens+base; admin/guide use their own entries (decision 18). tokens+base moved verbatim (tokens byte-identical; base differs only by biome cosmetic leading-zero on one cubic-bezier). --range-fill stays the slider inline var(--range-fill,0%) fallback (never :root); theme.ts --color-* and resizer --app-vw/--app-vh stay overridable :root defaults. SURVIVAL FIX: play.html's 3 inline cursor url()s absolutized './ui/cursors/' -> '/ui/cursors/' because Lightning rejects relative url() in a custom property (build-survival, NOT the deferred extraction). biome.json src/styles/** override disables noImportantStyles + noDescendingSpecificity (verbatim load-bearing !important + source order). client_shell: 3 relocated-rule assertions (base #ui, html/body touch-action, body.game-active) repointed to base.css. DEFERRED to P4: play.html token/base extraction + reconciliation (its block diverges, 327 vs 433 lines). QA: qa-checklist review = no BLOCKING (cascade-neutral ground-truthed: base sat entirely above all HUD CSS; the few real overlaps resolve to identical values). Per-phase QA Part 1 coverage trio (correctness + test-coverage + dead-code over the diff) = correctness/dead-code clean, but test-coverage found 2 SHOULD-FIX, both FIXED in a follow-up commit: (1) css_corpus keys on HUD banners so it gave NO rule-level protection for the banner-less tokens/base move (proven: dropping --app-vw stayed green) -> added tests/styles_extraction.test.ts (teeth-proven, pins runtime :root defaults / absolute cursor urls / range-fill-not-:root / @layer order / barrel import / Lightning transformer); (2) the parser's strip-comment-before-comma ordering was untested -> added that case + the ff/ios_saf alias cases, and dropped the dead edge id mapping. Validation: tsc clean, vitest 3906 pass / 8 skip, build all 4 entries + manifest + i18n modulepreload, biome ci green. backdrop-filter survival = N/A in P1 (0 in tokens/base/built chunk; HUD backdrop-filters move in P2).
- P2: _not started_
- P3: DONE (commits local, not pushed). Files added: src/styles/layout.css (`@layer layout`, the `.window` shell) + src/styles/components.css (`@layer components`, all feature-window bodies). Changed: src/styles/index.css (barrel imports both, order tokens,base,layout,hud,components), index.html (lines 187-1158 emptied), tests/css_corpus.test.ts (+11 window banners, 59/57), tests/client_shell.test.ts + tests/mobile_window_transform.test.ts + tests/social_status_dots.test.ts (repointed to the new modules). play.html NOT touched (its inline windows + the per-entry split are P4b; it loads the modules via the barrel and its unlayered inline still wins, so zero visual change). The single coarse "windows" 10-dash banner was split into per-window banners so css_corpus catches a single dropped window. The `.window` shell is `@layer layout` (below `@layer components`), cascade-verified safe because the only other bare `.window` rule (hud.css opacity) is disjoint-property and the rest are more specific. Lossless: 625 top-level rules identical old-vs-new (biome leading-zero/whitespace absorbed). Validation: tsc clean, css_corpus + client_shell green, full vitest 3931 pass / 8 skip, build x4 + survival OK (no backdrop in P3 content), biome clean on new .css. Adversarial coverage review (4 lenses: lossless / cascade / corpus-seam / qa-checklist + synthesis): no BLOCKING.
- P4a: DONE (commits local, not pushed). Files added: src/styles/shell.css (`@layer shell`, NEW, the desktop pre-game shell + char-select). Changed: src/styles/index.css (barrel `@import "./shell.css"` after components.css; @layer order UNCHANGED), index.html (12 shell banners + the char-select/shell rows from the 2 grab-bag banners removed from the inline `<style>`; one held panel rule relocated into shell.css for the margin-top fix), tests/client_shell.test.ts + tests/charselect_sort_parity.test.ts (repointed moved-rule assertions to a new shellCss source; auth-panel-premium backdrop expectation flipped -webkit-first). play.html NOT touched (its inline shell + the per-entry .extra split are P4b; it loads shell.css via the barrel but its unlayered inline still wins, zero visual change). KEY: index.html was NOT cleanly partitioned after P2/P3 - 6 chrome banners (chat/party frames/context menu/prompts/trade window/elite target frame) + `#tf-debuffs` + paperdoll + `#bags.drop-target` are P2/P3 chrome that was never extracted (held inline, SURFACED as a follow-up); the two grab-bag banners (Skin picker, Clean up styles) were split. shell sections moved WHOLE (incl interspersed body.mobile-touch shell rules + the two `@media(max-width:860px)` blocks) so base+override stay co-layered. The one cascade flip (`#hero-view #login/realm-panel` margin-top) was caught by a puppeteer computed-style diff (and independently by the Workflow cascade lens) and fixed by relocating the conflicting held panel rule into `@layer shell`. NOTE for P4b: 2 bare HUD rules (`#community-hud`/`#quest-tracker`) rode along in the char-select 860 block (cascade-neutral, re-home then); the shell-mobile overrides still in the held mobile-touch block, the per-entry `.extra` split, and emptying both inline blocks are P4b. Lossless: 4526 selector+declaration pairs identical (biome cosmetics + backdrop reorder absorbed). Validation: tsc clean, css_corpus + client_shell + charselect_sort_parity green, full vitest 3931 pass / 8 skip, build x4 + survival OK (11 twins), biome clean on shell.css, computed-style diff 0 across 6 states. Adversarial coverage Workflow (cascade / lossless / classification / qa + synthesis): 1 BLOCKING = the margin-top flip, now fixed + verified; all else cleared. NEXT: phase-04b-css-mobile-extra.md.
- P4b: DONE (commits local, not pushed). The CSS extraction P1->P4b is COMPLETE: both inline `<style>` blocks are EMPTY and all game CSS lives under the single flat @layer order. Files added: src/styles/hud.mobile.css (`@layer hud-mobile`, the in-game mobile-touch block), src/styles/index.extra.css (`@layer index-extra`), src/styles/play.extra.css (`@layer play-extra`). Changed: index.html + play.html (inline `<style>` emptied to a comment; each `<head>` now `<link>`s its per-entry .extra), src/styles/index.css (barrel: `@import "./hud.mobile.css"` + flat-name @layer order `tokens,base,layout,components,hud,shell,hud-mobile,index-extra,play-extra`), src/styles/hud.css (+orphan chrome), src/styles/components.css (+paperdoll/bags), src/styles/base.css (+pre-start #ui hide), src/styles/shell.css (the 2 rode-along HUD rules removed), tests/client_shell.test.ts + tests/charselect_sort_parity.test.ts + tests/styles_extraction.test.ts + the NEW tests/per_entry_css_wiring.test.ts (the per-entry `<link>` + #rotate-device gate guard, added in the final commit; the earlier-recorded "3931 / 5 tests" predates it). Actual per-entry split sizes: index.extra.css 25 lines (the in-game #rotate-device suppress + portrait none), play.extra.css 22 lines (the #rotate-device portrait show), tiny as expected from the reconcile (not the ~976/~60 preserve-both estimate).
- P4b RE-AUDIT (2026-06-24, ultracode): re-ran every gate green - tsc; full vitest 3939 pass / 8 skip (377 files); build x4 + i18n modulepreload + backdrop survival (11 webkit twins); biome clean - plus the mobile E2E responsive row (mobile_input_zoom_check 28/0, mobile_chat_safe_area + mobile_minimap_safe_area + mobile_community_hud_safe_area with real CDP insets all pass). A 6-finder adversarial Workflow plus an independent selector-set diff re-confirmed the highest-risk decision: the play.html RECONCILE is LOSSLESS (2034/2034 play selectors present in the shared modules, zero genuinely-unique play rules dropped; the only candidate was a benign `#perf-overlay .perf-graph` margin-top drift-twin), and the #rotate-device split is faithful and cascade-safe (index's `display:none !important` suppress competes only against normal declarations, so the later-layer-!important reversal never bites; play loads only play.extra so the suppress cannot leak). Verdict: PASS-WITH-FIXES, 0 BLOCKING. Fixes applied this pass: (1) corrected the index.html empty-block comment that still listed a dotted `hud.mobile` in the wrong order (the exact trap P4b eliminated, comment-only, no cascade impact); (2) hardened tests/styles_extraction.test.ts to assert the barrel `@import`s all 7 shared modules exactly once in cascade order (closes a silent-un-styling gap: every other CSS guard reads modules off disk, so a dropped `@import` left the suite green) - teeth-proven against drop / dup / hud-components swap; (3) extended the dotted-name guard in tests/per_entry_css_wiring.test.ts to both `.extra` files (they re-declare the @layer order, so a dotted regression there is load-bearing too); (4) tightened the barrel header comment in src/styles/index.css (the only intentionally-empty barrel layer is now the reserved `hud` slot). FOLLOW-UPS: (a) FIXED src/styles/shell.css `.se-preview-hint` invalid `color: var(--color-text-muted) b0` (a stray `b0` voided the declaration since the original feature commit; Lightning passes var() through so it built clean and only failed in the browser). Replaced with the codebase idiom `color-mix(in srgb, var(--color-text-muted) 69%, transparent)` (0xb0 = 176/255 ~= 69%) and added tests/css_value_validity.test.ts to scan every module for that bug class, teeth-proven (commit c7fd1364). (b) FIXED the cross-cutting mobile harness entry-flow drift (commit d8318b5e): the mobile_* screenshot/E2E scripts each re-implemented the offline pre-game entry with inconsistent robustness (several used page.click('#btn-offline'), which throws in the V16 single-Play-CTA UI where #btn-offline is a hidden legacy hook). Added scripts/enter_offline_game.mjs as the one canonical entry (wait + in-page click of #btn-offline, in-page name/class/Enter World, dismiss the mobile preflight gate, then wait for the window.__game world-boot hook instead of a blind delay) and routed all 23 mobile_* scripts through it, preserving each script's class/name/settle and post-entry logic. Verified against the dev server: 21/23 fully pass; mobile_touch_opacity captures all screenshots but exits 1 on the environmental 502 (no local game server / no .env Postgres), and mobile_joystick_deadzone has a separate pre-existing post-entry settings-navigation issue (the Graphics deadzone slider lookup), neither being an entry-flow problem.
  - SCOPE DECISION (Fernando: "do what is absolutely best for the project"): P4a surfaced that 6 chrome banners + #tf-debuffs + paperdoll + #bags.drop-target were P2/P3 chrome never extracted (orphaned). P4b can only empty both inline blocks if those are homed, so the orphan-cleanup was folded into P4b (chrome -> hud.css, paperdoll/bags -> components.css, pre-start -> base.css), completing the chrome/window extraction P2/P3 left open.
  - RECONCILE DECISION: play.html was pure stale drift of index (237 rules missing, 41 drifted values, 0 intentional unique CSS), so rather than the packet's preserve-both-exactly (which would banish index's ~1379 newer lines into index.extra to lock in play's rot), play was RECONCILED to the canonical modules. Emptying play's inline makes it fall back to the shared @layer modules (both entries already load the barrel via src/main.ts), so play now renders like index. The ONE intentional per-entry difference (decision 16a, in-game landscape-only) is preserved: #rotate-device suppressed in-game on index (index.extra.css), shown in portrait on play (play.extra.css); index's `!important` suppress lives in index.extra.css which play does not load, so it cannot leak. So index.extra/play.extra are TINY (only #rotate-device), not the ~976/~60 the recon estimated.
  - LAYER-NAME BUG (found by the computed-style cascade gate, fixed): the packet's `hud.mobile`/`index.extra`/`play.extra` layer names contain a DOT, which in CSS @layer means a SUBLAYER. So `hud.mobile` was the `mobile` sublayer of the EARLY `hud` layer and LOST to `shell` (a later top-level layer) -> the mobile #mode-select/.play-console/#btn-play overrides of pre-game shell elements rendered at desktop sizes. Renamed to FLAT hyphenated `hud-mobile`/`index-extra`/`play-extra` and ordered hud-mobile AFTER shell (matching the original inline-unlayered precedence where the mobile block beat every shell rule). index.extra/play.extra happened to work even dotted (their `index`/`play` top-level layers land after shell) but were flattened for consistency + to kill the latent trap.
  - VERIFICATION: lossless multiset proof (index OLD==NEW byte-identical rule set; play differs only by #rotate-device); negative-control-validated puppeteer computed-style diff across 8 viewport/body-class/panel states = ZERO real cascade flips on index after the layer fix (remaining diffs are dynamic noise: animations, hover, the joystick fade); per-entry probe confirms index #rotate-device=none and play #rotate-device=flex, and mobile #mode-select/.play-console/#btn-play now win on both entries. tsc clean; full vitest 3931 pass / 8 skip; build x4 + i18n modulepreload + backdrop survival (11 twins) OK; biome clean on the 3 new files. Mobile E2E (decision 16, targets index at :5173): input_zoom 28/0, minimap/community/chat safe-area pass (community uses real CDP insets); button_size/joystick_size are pre-existing RED (stale harness clicks the now-hidden #btn-offline, NOT a P4b layout regression - the start screen renders, proven by input_zoom). NEXT: phase-05-ui-effects-profile.md.
- P5: DONE (commits local, not pushed). Files added: src/game/ui_effects_profile.ts (pure resolver leaf, imports NOTHING), src/ui/ui_effects_applier.ts (the DOM applier host), tests/ui_effects_profile.test.ts (pure-core + import-absence), tests/ui_effects_wiring.test.ts (source guards for the load-bearing CSS rules + the applier/main.ts wiring, folded from QA). Changed: src/render/gfx.ts (2-line: import + use EFFECTS_QUALITY_LOW_CUTOFF at the PRESET_ADVANCED effectsQuality gate, the bare 0.5 -> the shared constant; sibling terrainDetail/foliageDensity/shadowQuality 0.5s untouched), src/main.ts (construct + boot applyNow; route reduceMotion/graphicsPreset/effectsQuality through the applier; em-dash normalized in the reduceMotion comment), tests/architecture.test.ts (registered src/game/ui_effects_profile.ts in UI_PURE_CORES per the phase; the forbiddenUiCoreImport guard forbids render/game/net/three/painter, fitting a zero-import leaf), src/styles/tokens.css (:root seeds --fx-shadow:1/--fx-ambient-anim:running/--motion-scale:1 = full-Ultra defaults, + the B1 glass low whole-rule drop), src/styles/hud.css (B4 glow calc(Npx*var(--fx-shadow,1)) x4; B2 ambient animation-play-state + animation-duration*motion-scale x5; the death-warning vignette full-tint hold on the body.reduce-motion + :root[data-fx-level=low] axes alongside the existing @media OS axis; B6 :root[data-fx-level=low] .fct.crit -> animation-name:fct-rise). KEY DECISIONS: (1) resolveUiEffectsProfile takes the phase's OBJECT input {presetLabel,effectsQuality,reduceMotion} and uiEffectsTokens returns a FLAT Record<string,string> --fx-* map (theme.ts-style) so the applier is a dumb loop, both adapting FB's positional/structured shapes per the phase. (2) uiEffectsAllowFctCrit keeps FB's STRING signature (the published data-fx-level value = profile.tier), not the phase's literal (profile), because the P14 consumer reads document.documentElement.dataset.fxLevel; flagged. (3) B6 FCT-crit is a minimal data-fx-level CSS gate (FB did it in JS; P5 must not touch hud.ts), inert at non-low; the per-element uiEffectsAllowFctCrit JS consumption is deferred to P14a/P14b. Two-controller hazard: the resolver imports nothing (no governor), proven by an import-absence test + the UI_PURE_CORES guard; gfx.ts GFX_BUCKET_BANDS `ui` band stays governable:false (untouched). reduce-motion single source: OS matchMedia OR settings('reduceMotion') feed the resolver; body.reduce-motion stays the CSS hook (no second flag). Validation: tsc clean; full vitest 3977 pass / 8 skip (379 files, +the new resolver test); tests/ui_effects_profile + architecture + css_corpus + client_shell + css_value_validity green; npm run build x4 + backdrop survival (12 webkit twins, +1 from the B1 -webkit-first glass drop); biome clean on the 3 new files; gfx.ts surgical (no new biome); main.ts 0 errors (11 pre-existing warnings, = HEAD); hud.css 1 pre-existing format error (P4b compact-chrome, unchanged). A computed-style proof on the BUILT dist bundle confirms DEFAULT(ultra)=byte-identical (every calc/var resolves to today's literal, the low-tier gates don't match), LOW sheds every bucket (glow->0, ambient->paused, glass->none, vignette full-tint, fct->fct-rise), and the reduce-motion vignette holds full tint while ultra keeps glass+glow. No WCAG/no-magic-values/perf rows apply (no window/control/painter/per-frame work this phase). qa-checklist verdict: 0 BLOCKING / 3 SHOULD-FIX (all doc/test-hardening, no code defect) / 3 NICE-TO-HAVE. FOLDED: the vignette 3-axis safety + B6 fct-rise + the applier/main.ts wiring are now pinned by NEW tests/ui_effects_wiring.test.ts (the tmp computed-style proof is gitignored, not durable); the flat-token-map reshape + the debounce trailing-edge latency are recorded as intended (per the phase). NOT folded (intentional, matches theme.ts): no applier dispose()/removeEventListener (startGame runs once per session). NEXT: phase-06-painterhost-seam-pilot.md.
- P6: DONE (commits local, not pushed). Files added: src/ui/painter_host.ts (the two-facet host: PainterHostPresentation dep-bag + PainterHostWriters + makeWriterFacet factory; host-agnostic, Node-importable), src/ui/delve_map_painter.ts (Canvas-2D painter: pure delveDrawModel + DelveMapPainter with two entry points + the absorbed schematic drawer), tests/delve_map_painter.test.ts (write-elision facet fake-cache unit test, no-magic-values canvas source scan + token-definition check, Sim/ClientWorld parity via two stubs, both-sites-same-output, vendor WCAG-chrome markup scan). Changed: src/ui/delve_map.ts (exported delveLocalToCanvas = the former private toCanvas, so the painter positions mob/party/player markers in the same space as the static schematic; zero behavior change to the existing exports/tests), src/styles/tokens.css (+6 --color-delve-* tokens with the EXACT pre-existing hex: room #0e0c0a, mob #e74c3c, mob-aggro #ff8800, party-dead #9a9a9a, label #ffe9a0, outline #000), src/ui/vendor_window.ts (VendorWindowDeps extends PainterHostPresentation + a bounded biome pass: import sort + 2 concat->template, behavior-identical), src/ui/hud.ts (writerFacet+delvePainter+presentationBag fields near the portraits field; vendor call site spreads ...presentationBag; updateMinimap + updateMapWindow delve branches -> painter calls; overworld #zone-label now this.setText for cache coherence; removed the 4 bg-cache fields + the orphaned drawSchematicPrimitives + the now-dead delve_map/DELVE_MODULE_LAYOUTS imports; net -211 lines), tests/localization_coverage.test.ts (repointed the #zone-label source-text guard to the new this.setText form). KEY DECISIONS: (1) ONLY vendor composes the presentation facet -- lockpick is callbacks-only and raid_lockout is a pure HTML-string builder, so both are left UNTOUCHED (the FB-carryover "3 windows migrate onto a unified bag" was wrong). (2) The delve pilot is Canvas-2D so it proves the CORE-TO-PAINTER split + the dep-bag wiring, NOT the DOM write-elision path at scale; the facet is bound here (the painter's ONLY DOM write, #zone-label, routes through it) and exercised against a fake cache by the unit test, the contract P10a EXTENDS (setStyleProp/toggleClass, D5a) and P10-P13 consume. (3) The minimap and world-map delve sites are NOT minimap-arrow-only-vs-static: BOTH paint mobs+party+arrow; they differ only by size/pad/circular-clip/marker-size/line-width/label-sink, all preserved by the two parameterized entry points. (4) Canvas colors resolved from --color-delve-* tokens via getComputedStyle once/redraw (D12), so the painter carries zero literal hex (the schematic-primitive + arrow colors flow through from the pure core; the alive-party color is classCss injected as a resolver). Validation: tsc clean; full vitest 4008 pass / 8 skip (381 files, +the new painter test); architecture purity (delve_map.ts stays a pure UI core, no painter import) + no-magic-values + write-elision facet + Sim/ClientWorld parity + both-sites + client_shell + S3 i18n guard green; biome clean on every new/changed .ts (delve_map_painter has the one grandfathered noNonNullAssertion warn-level getContext! matching icons.ts/hud.ts/player_card.ts/unit_portrait_painter.ts, non-gating); no sim/server/net/headless/world_api touched (D4). Reviewers: fork adversarial render-parity PARITY HELD (0 visible deviations, line-by-line vs the deleted inline code); qa-checklist 0 BLOCKING / 3 NICE-TO-HAVE (the getContext! warn, the canvas-draw parity being code-read-only since a 2D ctx + getComputedStyle are not in the Node suite, the outline token short-hex). WCAG row: the vendor change is purely compositional (byte-identical markup) so a11y posture is preserved; with no DOM/axe in this Node suite, the source-scan asserting the accessible vendor markup (focusable <button> rows + aria-labels) survives composition is the axe-equivalent. The delve schematic Canvas is the 3D-world-class surface OUT of a11y scope; the #zone-label it writes stays a real text node (in scope). NEXT: phase-07a-coldwindow-talents.md (no phase-06-qa.md exists; P6 QA ran through the in-phase qa-checklist + the adversarial fork).
- P7a: DONE (commits local, not pushed). Files added: src/ui/talents_view.ts (pure core: buildTalentsView(stage,cls,total) -> {hasTree,available,total,spent,classSpent,specSpent,valid,specs,selectedSpec,classTree,specTree}; named layout consts TALENT_CELL_W=86/CELL_H=70/NODE_SIZE=46/TOP_PAD=6; per-node shape/state/disabled/left/top/chosen + arrow x1,y1,x2,y2,filled; gating reuses cloneAllocation+validateAllocation+dormantNodes+pointsSpent byte-for-byte), src/ui/talents_window.ts (class TalentsWindow: open/close/render + paintSpecTab/paintTree/talentTooltip/openChoicePopup/setSpec/nodeClick/nodeRemove/footerHtml/wireFooter/keyboardActivate; composes PainterHostPresentation [only attachTooltip used] + a Hud deps bag), tests/talents_view.test.ts (layout consts, header/geometry, node gating == validateAllocation, filled/maxed/dormant, prereq-arrow geometry+filled-flip, same-input-same-output, ClientWorld-vs-Sim seed parity via two world stubs, hasTree=false defensive branch), tests/talents_window.test.ts (decision-12 source guards: no hex, references the var(--color-talent-*) tokens, tokens defined in tokens.css, no em/en dash). Changed: src/ui/hud.ts (removed inline toggleTalents body + renderTalents/renderSpecTab/stageSetSpec/renderTalentTree/talentNodeClick/talentNodeRemove/talentTooltip/talentFooterHtml/wireTalentFooter/openChoicePopup + private roleLabel + keyboardActivate + the talentTab field; added a thin toggleTalents delegate + a TalentsWindow field with a lazy-closure deps bag near presentationBag; KEPT applyLoadoutBar [called via deps]; pruned now-dead imports cloneAllocation/dormantNodes/exportBuild/importBuild/pointsSpent/Role/TalentNode/validateAllocation/talent_icons, kept FIRST_TALENT_LEVEL/talentsFor/TalentAllocation; talentSummaryHtml + closeManagedWindow 'talents-window' case updated; net -518 lines, 14172->13654), src/ui/talent_i18n.ts (exported roleLabel(role)=t('game.talents.role{Tank,Healer,Dps}'), the former private hud method, now shared with the char sheet), src/styles/tokens.css (+6 --color-talent-* tokens with the EXACT pre-existing hex: arrow #f5c843, arrow-dim #5a4a22, opt-dim #aaa, hint #8aa, req #caa, dormant #e0635a; classAccent reuses --color-text-muted #998d6a, signature reuses --gold #ffd100), tests/architecture.test.ts (registered src/ui/talents_view.ts in UI_PURE_CORES). KEY DECISIONS: (1) the core is NOT IWorld-derived: it takes the mutable talentStage edit buffer + cls + total as INPUTS; Hud OWNS the single buffer (talentStage field) and the painter reads/replaces it via getStage/setStage and mutates it in place (no second buffer cloned in the painter; clones only for the validate-probe + the saveLoadout commit, as inline). (2) The painter composes PainterHostPresentation but uses ONLY attachTooltip from it; all other coupling (dialogs/dropdown/loadout commit/focus) is talents-specific deps, lazy this.* closures in a field initializer (safe: this.sim set in ctor body, closures run later; presentationBag declared first so the spread is safe). (3) Decision 12: the painter carries ZERO hex; SVG arrow stroke moved from a presentation attribute to an inline style="stroke:var(--...)" (var() does not resolve in an SVG presentation attribute) -- the one DOM-attribute-shape delta beyond the literal hex->var swap, visually identical. (4) WCAG focus-return was ADDED (the inline talents had none): captureFocus/restoreFocus deps map to the proven currentFocusableElement/restoreFocus; wired on open() + ALL close paths (toggle, X button, coming-soon X, and the Escape/closeManagedWindow case now routes through talentsWindow.close()). (5) em-dash " — " separators (mastery tooltip, tal-mastery block, choice tooltip) -> ASCII " - ". Validation: tsc clean; full vitest 4022 pass/8 skip (383 files, +talents_view +talents_window tests); talents_view + talents_window + architecture purity (talents_view DOM/Three-free, no painter import) + client_shell green; biome clean on both new files (bounded safe-format pass, never touched the monolith); no-hex + no-em-dash source guards green; no sim/server/net/headless/world_api touched (D4); no i18n.locales/catalog edits (roleLabel reuses existing keys). REVIEWERS: qa-checklist APPROVE 0 BLOCKING / 1 SHOULD-FIX (the Escape close path bypassed the new focus-return -- FOLDED: case 'talents-window' now calls talentsWindow.close()) / 3 NICE-TO-HAVE (the SVG attr->style delta documented here; the pre-existing 'eyJ2Ijox…' ellipsis placeholder, out of scope; the signatureName tEntity-vs-abilityDisplayName equivalence). Independent parity reviewer: PARITY HELD (line-by-line vs the deleted inline code; only the 3 intended changes + the required SVG attr->style form; no correctness bug, no dangling ref, no wiring hazard). WCAG row: the painter preserves all role/aria/tabindex/keyboard wiring byte-faithfully (tablist/tab/tabpanel, radiogroup/radio roving tabindex, tree role=button + Enter/Space/Backspace/Delete, choice menu Escape) PLUS adds focus-return; the talents CSS (P3-owned, untouched) carries node-state ::after rings + the text rank badge + :focus-visible, so node state is not color-alone; the live axe/forced-colors/target-size pixel snapshot needs a browser and is deferred to manual per the P6/P7 precedent (the byte-faithful DOM == the inline baseline, so the AA posture is unchanged + improved by focus-return). No perf gate (talents is cold/open-on-demand, not in hud.update()'s frame divider). NEXT: phase-07b-coldwindow-social-bags.md.
- P7b: _not started_
- P8a: DONE (commits local, not pushed). The densest single cold-window surface (the inline renderOptions was ~1237 lines across nine sub-panels). Files added: src/ui/options_view.ts (pure core, UI_PURE_CORES) + src/ui/options_window.ts (painter) + tests/options_view.test.ts + tests/options_window.test.ts. Changed: src/ui/hud.ts (-1237, 12765->11528), tests/architecture.test.ts (register options_view), tests/client_shell.test.ts + tests/localization_fixes.test.ts (repoint two source-text guards). KEY DECISIONS / surprising rules: (1) The core is GAME-FREE by guard: forbiddenUiCoreImport blocks `../game/*` even type-only (the IMPORT_RE scan does not distinguish), so setting keys are typed as plain `string` in the descriptors and the painter narrows them (`key as NumericSettingKey`/`as BoolSettingKey`) per the control's discriminator; label keys use a type-only `TranslationKey` from `./i18n.catalog` (a pure types barrel, allowed). (2) STANDALONE deps (no PainterHostPresentation) -- options renders no item rows, like social (decision 6); the painter imports `type { OptionsHooks, BugReportHooks } from './hud'` type-only (isolatedModules erases it -> no runtime cycle; runtime edge is hud->options_window only). (3) The 4 primitives became descriptors; the per-kind value coercion is pinned as pure fns the painter calls, so the dispatch matrix proves no drift without a DOM. The descriptor's `value` is for the test; the painter still reads the LIVE `hooks.settings.get(key)` for the slider value+readout (byte-faithful to inline). (4) Bespoke controls that do NOT reduce to the 4 primitives stay painter-side (music toggle reads `music.enabled`; language/theme/chat-ts/keybind-capture/gamepad-remap), each tested via descriptor markers + source guards. (5) Graphics body controls + interleaved notes come from the core and append to the `.set-rows` BODY; the trailing graphicsNote/reloadNote/reload button + footer are painter chrome appended to the WINDOW ROOT (preserve the DOM-parent split). (6) The gold log color '#ffd100' is routed through a Hud-side `log(message)` dep so the painter carries zero hex (D12). (7) `slotActionName(slot)` is a Hud dep resolving abilityForSlot->abilityDisplayName (a hud-private fn) / itemForSlot->itemDisplayName; `actionDisplayName`+`BIND_*_LABEL_KEYS` moved to the painter. (8) WCAG focus-return ADDED (inline had none): captureFocus() runs BEFORE closeOthers() in toggle() so the opener is captured before sibling teardown; restoreFocus on every close path. (9) chatTimestamps/chatClock stay Hud fields (the chat renderer reads them); the painter mutates via getChatTimestamps/setChatTimestamps + getChatClock/setChatClock deps that do the field + localStorage write Hud-side. (10) A 14th endonym `ru_RU: 'Русский'` is required by `Record<SupportedLanguage,string>` exhaustiveness -- missing it fails tsc (caught both the painter copy + the splice anchor). (11) Two repointed source-text guards: client_shell's menu-dedup guard (was `add(t('hud.options.interface'))` -> now `labelKey: 'hud.options.interface'` in options_view), and the S3/R2 `localizeBugReportError` guard (hud.ts -> options_window.ts). REVIEWERS: adversarial parity Workflow PARITY HELD 6/6 clusters 0 confirmed regressions; qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 1 NICE-TO-HAVE (mobile a11y computed-style rows deferred to manual per P6/P7). (12) RE-RUN SENIOR-QUALITY AUDIT (2026-06-25, ultracode 6-finder adversarial Workflow over the COMMITTED extraction: parity / architecture / spec+tests / i18n+a11y / bug-hunt / qa-checklist, then per-finding adversarial verify; 18/43 confirmed, 0 BLOCKING -- the extraction is byte-faithful, all hard invariants hold). Remediated: S1 the one real user-facing defect = gamepad remap listboxes had no accessible name (WCAG 4.1.2), now pass the physical-button aria-label like the language picker; N1 sliders set aria-valuetext to the formatted readout (50%/90 degrees); S3 the decision-15 parity test was a JSON self-clone -> now two genuinely different shapes (Sim prototyped instance + '' realm vs ClientWorld plain wire object + populated realm); S2 tightened the source guards to pin each primitive's exact dispatch expression + applyControls routing; N5 broadened the no-magic guard to rgb()/hsl() and pinned the 2000 literal to its const line. S4 HONESTY: full live-axe-over-the-built-window / forced-colors / computed target-size / :focus-visible (and full live-DOM event-driving of the dispatch matrix) need a running browser/DOM and are DEFERRED to P15b (the opt-in vitest.browser.config.ts), per P6/P7a/P7b; the per-window roles/aria/labels/focus-return ARE proven here by source guards + the 2 audit fixes. NOT done (deliberate): no jsdom/axe-core devDependency added (tiny-dependency invariant + the packet schedules that suite for P15b). Validation: tsc clean; full vitest 4087 pass/8 skip; biome clean on the 4 new files; build all 4 entries; purity guard green + teeth proven (injected ../game import reds options_view, green after revert); presentation-only empty diff. No perf gate (cold). NEXT: phase-08b-coldwindow-market-char.md.
- P8b: DONE (commits local, not pushed: 83f129e0 market, c7545779 char). Two cold windows extracted from the hud.ts monolith. NEW src/ui/market_view.ts (pure core, UI_PURE_CORES): buildMarketView -> a discriminated MarketView union {no-data | browse | sell | collect}. KEY: the market is SNAPSHOT-DRIVEN (sim.marketInfo: MarketInfo|null), NOT promise/fetch-based, so the data-absent case (info===null) IS the loading/no-merchant state and there is genuinely NO error channel to surface; the packet asked for a loading/empty/ERROR triad, but an error variant would be never-rendered dead code (the only Promise-returning market-class surface is leaderboard(): Promise<LeaderboardPage>, decision 4 / P9b), so it is DELIBERATELY OMITTED -- an honest deviation both reviewers confirmed faithful, not a gap. browse models empty(browse|search|filtered)/list; sell models pick-empty|cannot-market|form (suggested ask = max(1, buyValue ?? max(1,sellValue)*4) split via COPPER_PER_GOLD/COPPER_PER_SILVER named consts); collect models empty|items; filtering reuses the already-extracted market_filters (filterMarketListings/paginateMarketListings) in the CORE -- NOT re-derived in the painter (guarded). NEW src/ui/market_window.ts (class MarketWindow): owns the view-state (tab/itemType/subtype/rarity filters/browsePage/sellItemId/searchQuery/lastSig) + lifecycle (open/close/isOpen/isSellTab/stageSell/refreshIfChanged/render); composes ...presentationBag (it renders item rows); the tabs + filter dropdowns + the three bodies + buy/list/cancel/collect dispatch + money formatting all preserved byte-faithfully. NEW src/ui/char_view.ts (pure core, UI_PURE_CORES): buildPaperdollView(equipment, items) -> {left,right} equip-slot columns (helmet/shoulder/chest/mainhand | gloves/waist/legs/feet) + the equipped ItemDef; PAPERDOLL DATA ONLY, DOM/Three/RNG-free. NEW src/ui/char_window.ts (class CharWindow): renders the paperdoll + the HUD-built stat/talent/progression fragments + share/prestige + the unequip/drag/contextmenu/keep-focus wiring; the shared 3D turntable preview + the cosmetic skin picker STAY HUD-owned (the single WebGL CharacterPreview is borrowed by the skin-event overlay + the player card), triggered via renderPreview()/renderSkinPicker() deps callbacks, so no Three type crosses into the core. CRUX: the char skin-event Math.random is in randomSkinEventLandingAngle (the skin-event gacha OVERLAY), a SEPARATE window that was never inside renderChar's scope -- so it is trivially out of the core (renderCharPreview/mountCharPreview/renderCharSkinPicker stay byte-identical in hud, invoked by callback). hud.ts: the renderMarket cluster + renderChar deleted (~712 net deletions across the diff) via two anchored sed line-deletes after asserting NO foreign methods interleaved (the leaderboardPage field sat INSIDE the market field block -- the Phase-7 interleaved-keep hazard -- preserved); thin delegators (openMarket/closeMarket/marketWindowOpen/toggleChar/renderCharIfOpen) + two lazy-closure deps fields by the other window painters; cross-window hooks repointed (bags isMarketSell->marketWindow.isSellTab, stageMarketSell->marketWindow.stageSell); the slow-divider proximity check -> marketWindow.refreshIfChanged()/close(); the locale-repaint -> marketWindow.render() / charWindow.renderIfOpen(). +WCAG focus-return ADDED to BOTH windows (the inline had none): captureFocus before closeOthers, restoreFocus on every close incl a NEW closeManagedWindow 'char-window' case (the inline char-window fell through to a bare display:none default). +no-magic (D12): both painters carry ZERO hex -- QUALITY_COLOR[quality] ?? var(--color-quality-default), and TWO new tokens --color-slot-empty-text:#666 / --color-slot-empty-border:#444 (exact prior values, provenance-noted in tokens.css) for the empty paperdoll slot; coin math via named consts. NEW tests: market_view.test.ts (full state-union: no-data + the 3 browse-empty reasons + pagination clamp + unknown-item drop + sell pick-empty/cannot-market/form coin-split + collect empty/items/badge; same-input-same-output; ClientWorld-vs-Sim parity via two stub shapes -- a prototyped Sim snapshot + a JSON-mirror, decision 15) + char_view.test.ts (full/partial/unknown-id paperdoll + column order + determinism + two-shape parity + an explicit no-Three/no-RNG source assertion) + market_window.test.ts + char_window.test.ts (no-hex / named-coin-const / no-dash + WCAG focus-return/aria/roles + delegates-to-core-not-re-derived source guards). tests/architecture.test.ts registered market_view + char_view; tests/client_shell.test.ts market guards repointed hud.ts->market_window.ts (this.marketBrowsePage->this.browsePage; the renderMarketFilterMenu + data-market-* + pageRange + mkt-filters guards; MARKET_PAGE_SIZE -> the buildMarketView delegation), char has NO hud-bound client_shell guard (its only client_shell reference is the #char-window CSS guard, unaffected). Validation: tsc clean; full vitest 4127 pass/8 skip (393 files); biome clean on the 4 new files (hud.ts biome is pre-existing-RED on whole-file organizeImports+format = deferred per project policy, confirmed identical 2-error count at base 0a6d1e4f; the 3 pre-existing dead options imports BoolSettingKey/NumericSettingKey/SETTING_RANGES were already dead at base = left as-is, out of scope; P8b introduced ZERO new dead imports); build all 4 entries green with no tracked-file side-effects; architecture purity green + TEETH PROVEN (injected ../render/characters/assets into char_view.ts reds the forbidden-import guard with the exact path, green after git checkout); localization_fixes green (NO new player-visible string -- every t() key reuses existing itemUi/hudChrome/hud/auth/playerCard keys). Presentation-only (D4): git diff of world_api.ts/sim/server/net/headless 0a6d1e4f..HEAD EMPTY. REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 3 NICE-TO-HAVE (painter tests are source-text guards per the P6/P7/P8a precedent; one stale "renderChar" comment in hud's mountCharPreview FIXED; the pre-existing #bags block-vs-flex display inconsistency reproduced faithfully, out of scope); adversarial per-window parity Workflow (2 parallel default agents, branch-by-branch vs the inline original at 0a6d1e4f) PARITY HELD = HELD/HELD, 0 BLOCKING, every divergence NICE-TO-HAVE render-neutral or additive: (1) focus-return added; (2) market locale-repaint drops the lastSig='' reset = render-neutral since render() always full-rebuilds (stale sig can only force an EXTRA repaint, never skip one); (3) market pager/list-button read a fresher world().marketInfo snapshot = equal-or-more-correct; (4) char stat-grid whitespace removed = render-neutral per CSS grid; (5) char unknown-item ?? null vs undefined = both falsy, same empty-slot path; (6) char drag-handler op reorder = 4 independent side-effects, no ordering dependency. The first parity Workflow FAILED (I wrongly set agentType cross-platform-sync, which emits its own report not StructuredOutput) -- re-ran with the default agent. HONEST WCAG SCOPE: the per-window roles/aria/labels/focus-return ARE proven here by source guards + the added focus-return; the live-axe-over-the-built-window / forced-colors snapshot / computed target-size (>=24px, 40x40 mobile) / :focus-visible rows need a running browser/DOM and are DEFERRED to P15b (the opt-in vitest.browser.config.ts), consistent with P6/P7a/P7b/P8a; the char 3D preview canvas is OUT of a11y scope (not screen-readable), stated honestly. No perf gate (cold; neither window is wired into Hud.update). RE-RUN SENIOR-QUALITY AUDIT 2026-06-25 (ultracode 6-finder adversarial Workflow, 16 agents, over the COMMITTED extraction): 10 findings -> 3 refuted + 7 survived (1 minor + 6 nits), 0 BLOCKING / 0 MAJOR = extraction confirmed faithful + clean. HARDENED 2 test gaps: (1) market_view.test.ts coin-split now exercises the buyValue ?? LEFT arm (healing_potion 170c -> 1s70c) AND the nonzero-gold sellValue*4 RIGHT arm (deathlord_warplate 36000c -> 3g60s); the prior copper-only 40c case left the gold floor-division, the silver modulo, and the ?? left arm untested. (2) architecture.test.ts adds a forbiddenUiCoreImport negative test (flags three/render/game/net/painter, allows sim+sibling cores) so the UI-purity guard's teeth survive a future matcher weakening: the per-phase "guard must FAIL on an injected forbidden import" is now a durable regression test, not a manual ritual. vitest 4130 pass/8 skip. NICE-TO-HAVE tracked for P15b (none a regression): market filter listbox roving-tabindex/arrow-key nav (byte-faithful carry-over of the inline original; advertises listbox semantics it does not fully deliver to AT), an Escape/closeManagedWindow focus-return source-grep, and the live axe / forced-colors / computed target-size rows; the decision-10 "per-window target-size NOT deferred to P15" wording vs the 5-phase live-check-deferral practice is a doc tension to reconcile in P15 (not a P8b defect). NEXT: phase-09a-coldwindow-canvas-map-arena.md.
- P9a: DONE (commits local, not pushed). The two mediumHud-band windows extracted from the hud.ts monolith. CRITICAL FINDING up front: the phase doc + recon framed BOTH map and arena as "canvas" painters with the getComputedStyle-once token policy, but the LIVE source shows renderArenaWindow is DOM/innerHTML (no 2D context) -- only updateMapWindow is a true canvas window. So this phase split into TWO disciplines: map -> a Canvas-2D painter (D12 getComputedStyle-once --color-* token policy applies), arena -> a DOM Humble-Object painter (token policy N/A; colors stay in CSS classes, like vendor/market). Spec-vs-live drift (risk 9), trusted live source, surfaced in the final report. NEW src/ui/map_window_view.ts (pure core, UI_PURE_CORES): mapWindowMode(world) -> 'delve'|'overworld' (the discriminator) + buildOverworldMapModel({world, zone, zoom, center, canvasSize, decorations}) -> an OverworldMapModel that maps IWorld+content to a FLAT canvas-pixel draw model: the cached-bg blit sub-rect as fractions [0,1] (so the painter multiplies by the actual non-square bg dims), the view rect (Hud assigns to this.mapView), grab/default cursor, zoneId (painter localizes the on-canvas title), and every POI(zoneId+poiIndex)/portal(dungeonId)/npc(ready:bool ?/!)/player-arrow(angle=-facing)/ally(friend|guild, deduped-by-id friends-first) marker already projected to (mx,my), plus the >=MAP_DETAIL_ZOOM detail overlay (decorations rock/tree(pine)/oak(tree2) + rotated building footprints + prop dots), all radii computed from ppu=S/spanX with named min/scale consts. DOM/Three/2D-ctx/i18n/color-free. NEW src/ui/map_window_painter.ts (Canvas-2D, NOT a registered core, lives in src/ui like delve_map_painter): class MapWindowPainter.paintOverworld(ctx, world, {zone, bg, canvasSize, zoom, center}) -> {view, cursor}; resolves 22 --color-map-* tokens via getComputedStyle ONCE per redraw (cached, never per-marker), owns the cached whole-world decorations (lazy generateDecorations(seed), matching the inline this.mapDecorations), draws in the EXACT inline order (blit -> detail -> title -> POIs -> portals -> npcs -> player arrow -> allies), zero inline hex (every font/radius/offset/line-width a named const). The delve branch is NOT in the painter: Hud routes by mapWindowMode -> P6's delvePainter.paintWorldMapDelve, so the delve marker drawing is never duplicated. NEW src/ui/arena_window_view.ts (pure core, UI_PURE_CORES): buildArenaView(input) -> {kind:'offline'} | {kind:'live', ...} discriminated union -- arena is an online ranked feature, so info===null IS the offline/unavailable note (the decision-15 online-only-shape trap). The live branch derives the resolved bracket (a.match?.format ?? queuedFmt ?? selected) + commitBracket (queued||match pins the selection), canSwitchBracket, the 3-branch queueDisabled ladder, party members/warn/none, in-match/queued/idle action, ladder + all-time rows (rank + me-flag + knownClass=!!CLASSES[cls] so the painter localizes only known ids), the practice gate, and the render-skip sig (JSON.stringify of the SAME raw rows the inline used, byte-identical). DOM/i18n-free; CLASSES is read only for the knownClass flag. NEW src/ui/arena_window.ts (class ArenaWindow, DOM painter): owns the selected bracket + the all-time-ladder cache + the per-bracket lbFetchedAt fetch throttle (named LEADERBOARD_REFETCH_MS) + lastSig + openerFocus + practiceHook; toggle/close/render/setPracticeHook; fetchLeaderboard (network + performance.now, fine in a painter); the offline note + the live panel (title/bracket tabs/rank/party/action/practice/online ladder/all-time) HTML byte-faithful; +WCAG focus-return that the inline renderArenaWindow LACKED (captureFocus on open, restoreFocus on toggle-off / X / Escape). hud.ts: updateMapWindow thinned to the delve delegate + mapPainter.paintOverworld + this.mapView/cursor from the returned result + the sr-only #map-summary via setText (delve summary reuses mapSummary with the delve name); drawMapDetail DELETED; toggleArena/fetchArenaLeaderboard/renderArenaWindow DELETED -> toggleArena delegates to arenaWindow.toggle(), the mediumHud band's `if ($('#arena-window').style.display==='block')` guard now drives arenaWindow.render() (same call site + 250ms cadence), setFiestaPracticeHook forwards to the painter, a NEW closeManagedWindow 'arena-window' case routes Escape through close() for focus-return; the moved arena state fields (lastArenaSig/arenaBracket/arenaAllTime/arenaLbFetchedAt/fiestaPracticeHook) + the mapDecorations field removed; MAP_MAX_ZOOM/MAP_DETAIL_ZOOM moved into the core; dead imports pruned (Decoration/generateDecorations/overworldDungeonPortals/PROPS from sim, ArenaFormat/FriendInfo from world_api). +tokens: 22 --color-map-* in tokens.css (exact inline hex byte-for-byte: label #ffe9a0, outline #000, portal #c084ff/#e0c0ff, npc #ffd100, player #fff, allies #4ade80/#60a5fa, rock/tree/oak, building outline/chapel/inn/house, 7 props). play.html: #map-canvas role=img + data-i18n-aria=hud.core.mapCanvasLabel + a sr-only role=status #map-summary live region (matching the lang-select-status precedent). i18n: hud.core.mapCanvasLabel 'Map' + mapSummary 'Map of {zone}.' added to the en catalog -- deliberately NON-WORDY (the M16 completeness gate flags only >=4-lowercase-letter English leaking into non-Latin; 'Map'/'Map of {zone}.' are short, so they pass English-only with no per-locale fills, and the delve summary reuses mapSummary), then the full i18n.resolved.generated/ + pending + sha256 + status.summary.json regenerated (i18n:build/scan + hash --write) and staged so the completeness/equivalence/freshness gates pass. NEW tests: map_window_view.test.ts (mode discriminator both shapes + the data-absent delve-band trap + Sim/ClientWorld parity via two structurally-distinct stubs + determinism + zoom-1 full-blit {0,0,1,1}+default-cursor + zoom>1 grab + detail-threshold + player-arrow/npc-glyph/ally-dedup/player-off-edge) + map_window_painter.test.ts (no hex/rgb + getComputedStyle EXACTLY once + all 22 tokens read AND defined in tokens.css + decorations-cache-once + the mediumHud call-site + cached-bg-not-rebuilt source guards) + arena_window_view.test.ts (offline/live + two-shape parity + bracket resolution/commit + queue gating 3 branches + ladder/all-time rows + practice gate + sig stability/change) + arena_window.test.ts (WCAG focusable controls + close()-routes-all-paths focus-return + no hex + named cadence). tests/architecture.test.ts registered map_window_view + arena_window_view (both pass the UI-purity scan). client_shell unaffected (it never greped the map/arena ids; 41 pass). Validation: tsc clean; full vitest 4171 pass/8 skip (397 files); biome clean on all 8 new files (the hud.ts splice introduces NO new violation class -- the single getContext('2d')! is the grandfathered inline pattern, the rest of the file's noNonNullAssertion/noExplicitAny warnings are pre-existing); build all 4 entries green with no tracked-file side-effects; architecture purity (both cores) + client_shell + i18n completeness/resolved-hash/status-registry green after regen+stage. Presentation-only (D4): git diff of world_api.ts/sim/server/net/headless EMPTY (the painters consume pre-existing IWorld members only). WCAG: the map canvas now carries an accessible name (role=img + t() aria-label) + an sr-only live summary (current zone / delve name); the 3D-style canvas PIXEL content stays OUT of SR scope (decision 10, stated honestly), only the chrome + the text summary are in scope. Arena focus-return added on toggle/X/Escape. Live axe / forced-colors / computed target-size / :focus-visible DEFERRED to P15b per the P6/P7/P8 precedent. No perf gate (both windows ride the mediumHud cadence, like P6's delve pilot; neither is in the every-frame hot path). REVIEWERS: qa-checklist 0 BLOCKING / 0 SHOULD-FIX / 4 NICE-TO-HAVE, all faithful-to-inline or intentional and NOT regressions: (1) the in-match auto-close (4112) raw-hides #arena-window without focus-return -- faithful to the inline (which had no focus-return at all) and arguably better mid-fight UX, the one dismissal path bypassing the WCAG contract, leaves openerFocus stale until the next toggle (harmless, restoreFocus guards it); (2) the arena offline branch rebuilds innerHTML each 250ms with no sig-skip -- faithful, offline-arena rarely open; (3) the painter's 3 zoneDisplayName/zonePoiLabel/dungeonDisplayName tEntity wrappers duplicate hud.ts module helpers -- intentional seam (the painter must not import hud.ts), not rule-of-three; (4) #map-summary carries both role=status and aria-live=polite -- redundant but matches the existing lang-select-status precedent, and setText elides identical writes so no per-tick re-announce. commits local, not pushed (explicit-path commits per the shared-worktree rule; map+arena committed as ONE impl unit because hud.ts carries both splices and the painter source-guard tests assert against hud.ts -- splitting would leave a non-compiling intermediate). NEXT: phase-09b-coldwindow-dom-questlog-spellbook-leaderboard.md.
- P9b: _not started_
- P10a: DONE (commits local, not pushed). The FIRST per-frame phase: it lands the load-bearing PainterHost write-elision EXTENSION (decision 5a) and proves the per-frame Humble-Object pattern on the two safest hot elements, fixing two real write-elision leaks as a measurable skip-rate WIN, behind the packet's first perf gate. THE EXTENSION: src/ui/painter_host.ts grew PainterHostWriters from 4 to 6 and makeWriterFacet from (cache,onWrite,onSkip) to (cache,stylePropCache,classCache,onWrite,onSkip). The two new writers are setStyleProp(el,prop,value) (drives a custom --var or any standard property via el.style.setProperty) and toggleClass(el,cls,on); BOTH are MULTI-SLOT, keyed per (element,prop)/(element,class) through a per-element inner Map (the shared writeSlot helper), because one element legitimately holds many custom properties / toggled classes -- collapsing them into the single-slot hotWriteCache the four originals use would silently break elision (Top risk 1). hud.ts mirrors them: +hotStylePropCache/+hotClassCache fields, +private setStyleProp/toggleClass (byte-identical key scheme to the facet: raw value for props, 'on'/'off' for classes), and writerFacet rebuilt over the same two caches + the same counters, so HUD-direct writes and painter writes share ONE skip-rate (exactly as P6 did for the four single-slot writers). SLICE A (xp bar): NEW src/ui/xp_bar_painter.ts consumes the EXISTING xpBarView core (not re-derived); constructor caches #xpbar / .fill (reuses xpFillEl) / .rested / .label (reuses xpLabelEl) / #player-frame ONCE; paint() routes setWidth(fill) + setStyleProp(--xp-fill) on BOTH #xpbar and #player-frame + setStyleProp(rested left/width) + setText(label) + toggleClass(overflow/rested). The inline hud.ts block did the --xp-fill x2 + rested.style.left/width + 2 class toggles RAW (uncounted, the leak) and re-queried #xpbar/.rested/#player-frame every frame; now one painter.paint(bar) call. SLICE B (swing timer): NEW src/ui/swing_timer.ts pure core (UI_PURE_CORES, ZERO imports, i18n-free): swingTimerState(player, target, prevPeriod, prevTimer) -> {visible,frac,ready,labelKind('ready'|'seconds'),seconds,nextPeriod,nextTimer}. The interval recovery (swingTimer counts down; the full period is recovered on the reset edge and carried while counting down) is PARAMETER-IN / NEXT-STATE-OUT: the core takes prevPeriod/prevTimer and returns the new ones, holding no hidden mutable state, so it is deterministic; Hud holds the two scalars and feeds them back next frame. It is i18n-free (returns a discriminator, like cast_bar); NEW src/ui/swing_timer_painter.ts caches #swingbar/.fill/.label ONCE (killing the per-frame $()+querySelector('.fill'|'.label') leak), routes setDisplay/setWidth/toggleClass/setText, and resolves the discriminator through t(hudChrome.swing.ready|seconds) + formatNumber. INCIDENTAL in-scope leak-fix: updateLowHealthVignette (a per-frame getElementById + raw setProperty('--lhv-*') + classList add/remove, unclaimed by any later phase) now caches the overlay ref and routes through the new private setStyleProp/toggleClass, exercising the Hud-private writers (the painters reach the writers via the facet, so this is the in-tree use that keeps them from being dead code under biome noUnusedPrivateClassMembers). BYTE-IDENTICAL keys preserved end-to-end (--xp-fill .toFixed(4); .toFixed(1) percent widths; swing seconds via formatNumber 1-digit) so the skip-rate cannot collapse on a non-byte-identical key. swing_timer registered in UI_PURE_CORES; the relocated swing comment's em dash normalized to a colon. NEW tests: painter_host.test.ts (the six-writer facet with the MULTI-SLOT cache-key regression guard: setStyleProp writes-then-skips per (el,prop,val), does NOT collapse two props on one element, keys per element; toggleClass tracks on AND off; two facets over shared caches keep one skip-rate); swing_timer.test.ts (visibility gating, edge-recovery + carry + frac clamp, ready/seconds discriminator, same-input-same-output, decision-15 Sim-vs-ClientWorld-mirror parity feeding two stubs that carry host-specific extras the core must ignore, + an i18n-free-core source guard); swing_timer_painter.test.ts + xp_bar_painter.test.ts (a recording facet asserts the painters drive the six writers with byte-identical values + a source scan rejecting any raw .style/.textContent/.classList/.setAttribute/.setProperty + the no-magic hex/rgb scan). The makeWriterFacet facet tests MOVED out of delve_map_painter.test.ts (now a six-writer concern) into painter_host.test.ts; architecture.test.ts += swing_timer; client_shell.test.ts's mobile-XP-ring guard repointed from the inline hud.ts --xp-fill writes to xp_bar_painter.ts (asserting it drives --xp-fill on both #xpbar and #player-frame, and Hud wires the player-frame ref). VALIDATION: tsc clean; biome clean on all 8 new files (hud.ts shows 2 errors / 31 warnings IDENTICAL to HEAD -- proven by running biome on a git-show HEAD copy -- so ZERO new debt, all grandfathered organizeImports/format); full vitest 4278 pass / 8 skip (407 files); architecture purity guard PASSES and TEETH PROVEN (an injected `import {Renderer} from '../render/renderer'` line reds swing_timer's UI-purity scan, green after revert). PERF GATE (the packet's first; scripts/perf_tour.mjs desktop on the same M4 Max as the P0 baseline, TWO stable runs): frameP95 250ms == baseline 250ms; hudHotDomSkipRate 0.965 >= baseline 0.962 -- IMPROVED, exactly because the formerly-raw xp (--xp-fill/.rested) + swing + low-health-vignette writes are now COUNTED through the elided helpers and skipped at steady state; 0 errors. The mobile profile is NOT capturable on the untouched tree (deferred to P17a per perf-baseline-v016.md: portrait 390x844 + undismissed #mobile-preflight); the skip-rate is renderer-independent so the DOM-write gate still holds for both profiles via the desktop capture. inputIntentToVisibleP95 ~6.4s in both runs tracks a prewarm-budget overrun (15.7s vs 12s, asset-loading boot noise on a busy machine), NOT a per-frame regression (the change only constructs 2 painters + caches a few refs once at boot). A11Y (decision 10): the xp bar + swing timer are NON-INTERACTIVE indicators conveying state via WIDTH + a TEXT label (swing 'Swing'/'{s}s'; xp via its hover label), never color alone -- forced-colors + SR get meaning; they carry NO focusable control, so target-size / focus-return DO NOT apply, and that boundary is stated honestly in the painter headers (not claimed as audited); the 3D canvas stays out of a11y scope. NO MAGIC VALUES (decision 12): the painters drive named-constant prop/class names + CSS custom properties; percent VALUE strings (.toFixed) are excepted; zero literal hex/rgb (source-scan guarded). i18n: the swing label reuses the existing hudChrome.swing.ready/.seconds keys (NO new keys, no concat, no `?? 'English'` fallback); the xp label is already localized by xpBarView. Presentation-only (decision 4): git diff of world_api.ts / src/sim / server / src/net / headless / src/styles / i18n.locales is EMPTY (the painters write CSS custom props/classes that already exist). REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 SHOULD-FIX / 5 NICE-TO-HAVE (all verified non-issues: the vignette write-order flip is paint-frame-identical; the first-frame active=false no-op; one small SwingTimerState alloc per visible frame at HUD cadence; the pre-existing #player-frame raw combat-class toggle at hud.ts:4176 touches a DIFFERENT class so it cannot stale the painter's multi-slot cache, left for P10b; hand-built parity stubs per the established pure-core pattern) / 1 VERIFY (mobile perf deferred to P17a, honestly disclosed). The green-perf-gate commit is TAGGED so a later cumulative regression (first surfaced at P17a) bisects to this phase. NEXT: phase-10b-perframe-unit-frame-family.md.
- P10b: DONE (commits local, not pushed; 178c8626 core+painter / 3cf6f815 player integration / d4097183 a11y+i18n / c35f42b2 tests). Stands up the load-bearing reusable unit_frame FAMILY (decision 9) with the player as its first instance. THE FAMILY: src/ui/unit_frame.ts is a pure, allocation-light core (registered in UI_PURE_CORES) mapping a UnitFrameDescriptor (present, hpFrac, hpText, resourceKind, resFrac, resText, levelText|null, name, portraitKey, absorb|null, dead, outOfRange) to a UnitFrameView; it does three real jobs (the present/hidden gate returning a shared HIDDEN constant, the absorb-shield resolution via the shared absorbBarView so player/target/party never re-derive it, and unitResourceClass mapping resourceType|'none'|null -> rage/energy/mana/none which FOLDS the old inline rage:energy:mana ternary and adds the 'none' case a resource-less target needs) and passes the preformatted fracs/text through verbatim. NO hardcoded #player-frame id, NO single-instance assumption, DOM/i18n-free, deterministic. src/ui/unit_frame_painter.ts is the thin write-elided painter: the element set has REQUIRED frame/level/hpFill/hpText + OPTIONAL name (a frame whose name is static omits it), OPTIONAL absorb (party has none), OPTIONAL resource group (target has none); options are shownDisplay (omit for an always-visible frame), repaintPortrait (the lastPortraitTarget gate lives HERE, not the core, so P11b target reuse is the same code path), and stateClasses (gate the dead/oor frame classes; party-only). paint() routes EVERY write through the six PainterHostWriters; absorb folds updateAbsorb (setTransform scaleX + toggleClass overshield) and the resource type folds the className swap into 3 toggleClass(rage/energy/mana). THE PLAYER INSTANCE: the inline Hud.update block (was setText level / setTransform hp / updateAbsorb / setText hpText / setTransform res / setText resText / className swap) becomes one playerFramePainter.paint(unitFrameView({...})) call; the player painter is constructed with NO name (static, set once at login $('#pf-name')), NO shownDisplay (the frame is always CSS display:flex, never toggled, so it gains no inline style), NO stateClasses (the player frame never carries dead/oor), NO repaintPortrait (its portrait is drawn at character setup) -> the player path is byte-faithful (raw scaleX fracs, String(level), `${hp} / ${maxHp}`, `${Math.round(resource)} / ${maxResource}`, same absorbBarView result; the 'low'/'bar' classes on #pf-resource survive the className->toggle fold because updateLowResource owns 'low' on a separate raw path and 'bar' is the static HTML base). updateLowHealthVignette + updateLowResource stay at the Hud call site (player-only, own cores, deliberately OUT of the shared family so target/party do not inherit them); pfNameEl ref removed (now unused), pfAbsorbEl cached (kills the per-frame $('#pf-absorb') query inside the old updateAbsorb). A11Y (decision 10): play.html AND index.html (BOTH game entries; see RE-AUDIT) #player-frame gains role="group" + data-i18n-aria="hudChrome.unitFrame.playerLabel" + aria-label fallback, hydrated to t() by main.ts translatePage; ONE new English-only key hudChrome.unitFrame.playerLabel = 'Your Hero' (the phase's suggested 'Player frame'/'Your character' are both WORDY and tripped the M16 completeness gate as untranslated-English in zh/ja/ko/ru; the user chose the non-wordy 'Your Hero' over filling overlays - it stays English-only/no-overlay-edit per the phase and the maintainer translates at release); hp/resource conveyed as TEXT (#pf-hp-text/#pf-res-text not aria-hidden), no focusable control in the frame so target-size/focus-return N/A, 3D portrait/world out of a11y scope. TESTS: tests/unit_frame.test.ts (the TWO-DESCRIPTOR contract drives the FULL P11 field set - resClass 'none', levelText null, dead boss target, absent target present:false, out-of-range party, null absorb - proving P11a/b/c reuse needs no core change, NOT a token stub; + the discriminator mapping incl null->mana and 'none'; the present/hidden gate; absorb resolution + overshield; determinism; the decision-15 Sim-vs-ClientWorld-mirror parity feeding the entity-shaped absorb with host-specific extras the core must ignore; DOM/i18n/id-free source guards) + tests/unit_frame_painter.test.ts (recording-facet routing: the player instance writes the byte-exact level/hp/hpText/absorb x2/resType x3/res/resText sequence and NOTHING else - no display/name/dead/oor; the fuller instance with stateClasses+shownDisplay+name writes display/name/dead/oor; the target-like no-resource and party-like no-absorb instances skip those groups; the portrait gate repaints only on key change; no-raw-write + no-magic-color source scans); architecture.test.ts += unit_frame in UI_PURE_CORES. VALIDATION: tsc clean; biome clean on all new files (hud.ts shows only pre-existing organizeImports debt, 0 new); full vitest 4307 pass / 8 skip (409 files); architecture purity guard PASSES + TEETH PROVEN (an injected `export const _domProbe = document.title;` reds unit_frame's DOM-globals scan, green after revert); css_corpus + client_shell green after the play.html attribute add; npm run build 4 entries green (backdrop-survival OK, i18n regen byte-stable); localization_fixes 27 pass (the new key registered pending, PR-tier permits English-only). PERF GATE (scripts/perf_tour.mjs desktop, same M4 Max as the P0 baseline, 3 runs after the apples-to-apples baseline check via git stash): frameP95 250ms == baseline 250ms HELD; hudHotDomSkipRate 0.962/0.965/0.962 (min 0.962 == P0 baseline, touches P10a's 0.965); deterministic real writes 53 vs baseline 48 (the +5 are exactly the absorb-transform + absorb-overshield + 3 resType toggles - previously RAW/uncounted writes the phase mandates folding into the COUNTED elided path; an earlier draft that also painted name/dead/oor measured 56 writes / 0.961 and was rejected as both a tiny regression AND unfaithful to the old player frame). Mobile NOT capturable (the known P0 #mobile-preflight portrait breakage closes the puppeteer connection; deferred to P17a; the renderer-independent skip-rate is covered by the desktop capture). REVIEWERS: qa-checklist PASS 0 BLOCKING / 0 MAJOR (28 PASS / 0 FAIL / 5 routine VERIFY [perf done; M16 release-tier + maintainer-fill by design; SR/CI browser checks] / 2 MINOR-informational: the safe write reorder crosses independent elements so the DOM is identical, and ~2 short-lived objects/frame at HUD cadence consistent with the swing/xp view pattern and within the perf budget) + adversarial coverage PASS 0 BLOCKING / 0 MAJOR (byte-faithful across all 8 categories; MINOR: the hud.ts call-site formatting is not unit-pinned - ACCEPTED, extracting a player-only descriptor builder for one use violates the no-abstraction-for-one-use doctrine and P11 builds its own descriptors, and both reviewers hand-verified the formatting matches; the import-order nit it raised was fixed; the 'bar'-base-class invariant is benign and documented in the painter). The green-perf-gate commit (c35f42b2, vitest-4307 / skip-min-0.962) is TAGGED frontend-v016-p10b-perf-gate so a later cumulative regression bisects here. The unit_frame FAMILY is now the seam P11a (cast bars adjacency), P11b (target frame), P11c (party frames) REUSE with no core change. RE-AUDIT 2026-06-25 (quality re-run via a 5-dimension adversarial Workflow: 22 findings raised -> 7 in-scope fixed, 15 rejected as spec-intended/pre-existing/self-rejecting): (1) MAJOR a11y entry-parity gap FIXED - the d4097183 a11y change landed on play.html ONLY, so index.html (the / entry; both boot main.ts + ship the HUD) shipped a bare unlabelled #player-frame and the acceptance criterion was false on the primary entry; mirrored role=group + data-i18n-aria + aria-label onto index.html (same key, NO i18n regen needed). (2) NEW client_shell parity guard asserts the group label in BOTH entries (mirrors the map-canvas test) so the gap cannot recur. (3) painter no-raw-write guard now also forbids .className (the write this phase folded away) AND a px literal (decision 12 names hex/px/color). (4) painter levelText null->'' branch now pinned (the P11 level-hide path). (5) NIT updateLowResource reads this.pfResourceEl instead of re-querying $('#pf-resource') each frame. Re-validated: tsc clean; biome clean on all changed .ts; full vitest green (+2 tests); npm run build 4 entries green; perf_tour desktop frameP95 250ms HELD + hudHotDomSkipRate 0.97 >= baseline. NEXT: phase-11a-perframe-cast-bars.md.
- P11a: _not started_
- P11b: _not started_
- P11c: DONE (commits local, not pushed). Party frames -> keyed node pool: NEW party_frame_row.ts (row builder + pure live-slot partyRowHandlers) + party_frames_painter.ts (pid-keyed pool + free-list recycle + elided per-frame writes), each member a unit_frame INSTANCE (family painter extended with optional hp/resource text + a formatScaleX precision option; pure core unchanged; player/target byte-identical). Hoisted the cheap partyFrameSignature ahead of selectPartyFrameMembers so an unchanged party allocates no selector arrays. A11y: role=button rows (Enter/Space/Menu-key), member-name accessible name (recycle-safe, no per-frame aria), #party-frames role=group + partyLabel='Your Band' in both entries, aria-hidden badges, global tabindex focus ring. relocalize() re-localizes pooled badge tooltips + leave label on woc:languagechange. Decision-15 oor round2 parity test. PERF GATE frameP95 250ms == baseline, hudHotDomSkipRate median 0.962 == baseline. Reviewers: qa-checklist 0 BLOCKING (1 SHOULD-FIX folded = relocalize) + adversarial Workflow 0 BLOCKING (2 MAJOR both refuted on verify). See the P11c table row. NEXT: phase-12a-perframe-action-bar.md.
- P12a: _not started_
- P12b: _not started_
- P13a: _not started_
- P13b: _not started_
- P14a: _not started_
- P14b: _not started_
- P15a: _not started_
- P15b: _not started_
- P16: _not started_
- P17a: _not started_
- P17b: _not started_

## Open items / carry-forward
- The allocation-budget Node-proxy spike (recon open-decision 4) is pulled forward into P12a so the
  downstream per-frame phases inherit a decided proxy (fallback = perf_tour frameP95 + longtasks).
- iOS real-device pass for the mobile-landscape CSS (carried from FB's Phase 5) remains a manual
  deliverable that cannot run in CI; P4b wires the automatable mobile E2E scripts as the CI proxy.
- The first all-together perf_tour run lands in P17a; if it surfaces cumulative drift, the recovery
  is re-opening the offending per-frame phase (tagged at its green gate), never relaxing the budget.
- admin.html / guide.html CSS is SURVIVAL-only this packet (decision 18); not extracted.
