# SESSION Z1 - Finale: integrate, regenerate once, scanner-zero, release-locale-fill handoff, doc pass
> Model: Fable 5 / Opus 4.8, xhigh effort. Harness: Claude Code. Branch: feature/ip-pivot. Mode: plain.
> READ FIRST: 00-SHARED-CONVENTIONS.md (in this folder) + the LOCKED NAME-MAP.md + README.md + 05-INTEGRATION.md - the prime directive (RENAME DISPLAY, FREEZE IDS), the two English source layers, the regen sequence, the six contracts/gates, the standard session loop, and the validation command list. Do not re-derive them.

## What we are doing
This is THE FINALE of the IP-pivot job (the Z1 analog of the sim refactor's finale and the world-api W11). The gate (G0), the NAME-MAP lock (G1), and all four rename tracks (Vocab V1->V2, Creatures C1->C2, World W1->W2, Text T1) have landed and merged back into `feature/ip-pivot` per 05-INTEGRATION. Z1 does NO new rename: it confirms the whole job is complete and green, regenerates the deterministic artifacts ONCE cleanly on the integrated tree, proves `tests/ip_scrub.test.ts` is fully green (zero residual verbatim-WoW / Blizzard-coined name in any player-visible field), runs the full pre-merge suite, writes the RELEASE-tier locale-fill handoff note (the reword-staleness trap), and does the documentation / memory pass. This is a confirm + regenerate + docs + handoff slice, not a rename.

## Goal
Confirm the whole IP pivot is complete and green - all four tracks merged, `ip_scrub` fully green with zero residual, the full suite + build green, every existing `tests/parity` golden byte-identical to the base EXCEPT the enumerated C1/C2 coined-id token swaps - then regenerate the i18n + guide artifacts ONCE cleanly, write the release-locale-fill handoff note for the maintainer, and mark the program done in the working memory + maintainer memory (noting the deferred tracks).

## Scope (verified)
Z1 runs on the END state (post-merge), not the base. Confirm the integrated reality with ONE Explore agent before editing: the numbers/ids below are audit-captured on v0.18.0 and may drift; re-confirm current lines/ids in the session (say so). All four tracks edited almost-disjoint source (`classes.ts` vs `talents_classic.ts` vs `zone*/types/manifest/warlock_pets` vs `items.ts` vs docs); their ONLY shared conflict surface is the regenerated i18n / guide artifacts, which Z1 reconciles by RE-RUNNING the generators.

### (1) Confirm all tracks merged, then regenerate ONCE (the only edit Z1 makes)
- Confirm per 05-INTEGRATION that V1, V2, C1, C2, W1, W2, T1 are all merged into `feature/ip-pivot` (check the 02-WORKING-MEMORY status board: every slice `merged (<sha>)`). `git status` clean; `npm ci` if `node_modules` is missing in this worktree.
- Regenerate the three deterministic artifacts ONCE, cleanly, on the integrated tree, in this exact order:
  ```
  npm run i18n:gen            # rebuild src/ui/i18n.resolved.generated/* + the status registry
  npm run i18n:hash -- --write # rewrite src/ui/i18n.resolved.sha256
  npm run wiki:content        # regenerate src/guide/content.generated.ts
  ```
- Regen is deterministic and idempotent: run `i18n:gen` a SECOND time and confirm the tree is clean (no further delta). Commit any delta with EXPLICIT paths (never `git add -A`): `src/ui/i18n.resolved.generated/*`, `src/ui/i18n.resolved.sha256`, `src/guide/content.generated.ts`. Log the touch in 02-WORKING-MEMORY's generated-artifact registry (Z1 = last regenerated by).

### (2) Require the scanner fully green (zero residual)
- `tests/ip_scrub.test.ts` must be FULLY GREEN: every denylist entry / scanner-worklist row (02-WORKING-MEMORY registry) cleared - zero residual verbatim-WoW / Blizzard-coined name in any player-visible field (resolved English tables + sim content).
- If ANY entry still flags: identify the owning slice from the worklist (Heroic Strike/Fireball/... = V1; talent + spec/tree names = V2; Murloc/Bristleback/Drakonid = C1; Voidwalker/Felguard/... = C2; Shadowmeld/Lightwell = W1; Mortal Strike/War Stomp mob auras = W2) and STOP - a track missed one. Do NOT rename it here (that is the owning track's job) and NEVER loosen or `.skip` the scanner to pass. Flag it for the operator and hand back to the owning track.

### (3) Confirm behavior unchanged: goldens byte-identical except the enumerated C1/C2 token swaps
- Full pre-merge suite green, mirroring CI: `npm test && npx tsc --noEmit && npm run build`.
- `tests/parity` goldens must be byte-identical to the base EXCEPT the C1/C2 coined-id token swaps (the ONE sanctioned golden delta in the whole job). Enumerate EXACTLY which goldens changed and confirm the delta is nothing but the renamed tokens: the `MobFamily` ids `murloc`->`mudfin` and `kobold`->`tunnelrat` (C1), and the warlock demon-pet ids (`warlock_voidwalker`->..., `warlock_felhunter`->..., `warlock_felguard`->..., `warlock_doomguard`->..., ... per the LOCKED NAME-MAP) (C2). Any golden that shifted in any OTHER way means behavior or a frozen id moved: STOP and triage, do not re-baseline a golden to make it pass.

### (4) Write the release-tier locale-fill handoff note (the reword-staleness trap)
Write a dated maintainer note (create `docs/refactor/ip-pivot-release-locale-fill.md`; create `docs/refactor/` if absent) AND add a one-paragraph pointer near the top of 02-WORKING-MEMORY's Decisions & gotchas. The note states, for the maintainer, at release:
- Rewording an EXISTING English key does NOT flip its 20 non-English locale rows to `pending`. So every non-English overlay still renders the OLD WoW name and BOTH CI tiers (PR-tier and release-tier `I18N_RELEASE_TIER=1`) pass green - the staleness is invisible to the gates.
- `npm run i18n:worklist` will NOT list these keys (they are not `pending`), so the standard release-fill misses them entirely.
- The maintainer must, at release, diff `src/ui/i18n.resolved.generated/en.ts` merge-base (pre-`feature/ip-pivot`) vs HEAD for keys that EXISTED-AND-CHANGED (the renames), and re-fill every locale whose value did not ALSO change in this job to the new original name. This is a contributor-out-of-scope, release-tier obligation (contributors add English only; overlays are the maintainer's job).
- Reference the maintainer memory `release-i18n-fill` for the mechanics (admin copied-English allowlist + dialect shadows, SHA rebaseline via `i18n:hash --write`, and the whole-repo biome footgun to avoid).

### (5) Documentation + memory pass (prose only)
- **02-WORKING-MEMORY.md:** mark the program DONE - flip every slice on the status board to `merged`, tick every scanner-worklist row (scanner fully green), and record Z1's artifact regeneration in the touch log. Append-only.
- **Maintainer memory `ip-pivot-project`:** update to the completion state (all slices merged, `ip_scrub` green, goldens byte-identical except the enumerated C1/C2 token swaps, release-locale-fill handed off). Note the DEFERRED tracks for the future: the brand rename ("World of ClaudeCraft" -> new product name, the lawyer's P0, a separate business track) and the HUD trade-dress redesign (out of scope this pass), plus the residual nine-class "copied compilation" risk the operator accepted.

## The mapping (apply NAME-MAP verbatim)
Z1 applies NO mapping row - it introduces no new rename (every old -> new string is owned by V/C/W/T and already applied on the merged tree). Its only relationship to the NAME-MAP is CONFIRMATION: `tests/ip_scrub.test.ts` (keyed to the NAME-MAP `old` column) must be fully green, which proves every mapped `old` name is gone from every player-visible field. If a needed string were somehow off-map, that is a track defect, not a Z1 edit: STOP and surface it. The LOCKED NAME-MAP stays append-only and untouched.

## Slice-specific hazards
- **Z1 runs on the END state, not the base.** All four tracks are merged; abilities/items carry NEW names in BOTH English copies (sim record + catalog), the coined ids are swept in C1/C2. Confirm the post-merge reality with the Explore agent; do not trust pre-refactor line numbers here as an edit target.
- **Regenerate ONCE, cleanly, never hand-merge a generated file.** The i18n + guide artifacts are the only surface four parallel tracks collide on. Resolve ANY conflict by RE-RUNNING the generators in order (`i18n:gen` -> `i18n:hash --write` -> `wiki:content`), never by editing a generated file. A second `i18n:gen` MUST leave the tree clean; if it does not, a source edit is non-deterministic - STOP.
- **The scanner is the gate you are here to satisfy - never weaken it.** If `ip_scrub` still flags a name, the fix is in the OWNING TRACK, not here. Do NOT rename in Z1, do NOT `.skip`/loosen the denylist, do NOT edit the scanner to pass. Hand back to the owning track and flag the operator.
- **A golden that shifts by anything but the C1/C2 token swap = behavior or a frozen id moved.** Never re-baseline a `tests/parity` golden to make it pass. Enumerate the exact changed goldens and confirm the diff is ONLY the renamed coined tokens; anything else is a STOP.
- **The reword-staleness trap is INVISIBLE to CI - it is a release obligation, not a gate.** Do NOT try to fix the 20 locale overlays in this job (contributor rule: English only; overlays are the maintainer's job at release). Z1's job is to WRITE the reconciliation note, not to touch `src/ui/i18n.locales/<lang>.ts`. Both CI tiers passing does NOT mean the overlays are correct.
- **Commit with explicit paths on a shared checkout.** The regen writes many generated files; stage only the three artifacts Z1 owns plus the two doc/memory files. Never `git add -A`.
- **Doc/memory edits are prose only.** `tests/architecture.test.ts` / `localization_fixes.test.ts` scan literals; keep any example name/id in fenced/inline code so it is never misread as a live emit or a forbidden import.

## Gate / Parity (do this BEFORE editing)
Z1 mints no new fixture. BEFORE the single regen edit or any doc write:
1. Confirm all tracks merged: 02-WORKING-MEMORY status board shows V1/V2/C1/C2/W1/W2/T1 all `merged`; `git status` clean; `npm ci` if `node_modules` is missing.
2. Capture a green baseline of the behavior-unchanged gates on the untouched merged tree:
   ```
   npx vitest run tests/parity                            # goldens byte-identical except the C1/C2 coined-id token swaps
   npx vitest run tests/ip_scrub.test.ts                  # the verbatim-name scanner (must trend to fully green)
   npx vitest run tests/i18n_resolved_equivalence.test.ts # SHA gate over the 21-locale resolved table
   npx tsc --noEmit
   ```
   A red gate here (other than an `ip_scrub` residual that names an owning track, or the equivalence SHA needing the `i18n:hash --write` re-baseline) means a prior slice drifted - STOP and triage, do not start the finale.
3. Run the regen ONCE (section 1), re-run the equivalence gate + `ip_scrub` + `tests/parity`, and confirm: equivalence green (post `i18n:hash --write`), `ip_scrub` fully green, goldens byte-identical except the enumerated C1/C2 token swaps.

## Invariants in play
From 00-SHARED-CONVENTIONS, the ones that bite the finale:
- **RENAME DISPLAY, FREEZE IDS (PRIME DIRECTIVE).** Z1 renames nothing and freezes every id; it only CONFIRMS the tracks obeyed it. The ONLY id change in the whole job is the C1/C2 coined-id sweep, already landed.
- **Parity goldens stay byte-identical** except the enumerated C1/C2 coined-id token swaps; a display rename changes no sim state. Any other golden shift is a STOP.
- **i18n resolved-equivalence.** The SHA-256 gate over the 21-locale resolved table reds unless `i18n:hash -- --write` ran after the (re)generation; the duplicated ability/item English (sim record vs catalog) must match byte for byte.
- **The verbatim-name scanner.** `tests/ip_scrub.test.ts` fully green, zero residual; never loosen or `.skip` it.
- **Guide freshness.** `tests/guide.test.ts` reds if `src/guide/content.generated.ts` is stale; the Z1 regen + commit keeps it fresh.
- **Contributors add English only.** Never edit a `src/ui/i18n.locales/<lang>.ts` overlay; the release-tier re-fill is HANDED OFF to the maintainer, not done here.
- **src/ purity.** `tests/architecture.test.ts` stays green; Z1 touches no `src/sim/` source (the C1 coined-id sweep already landed under its own slice).
- **Determinism.** Nothing Z1 adds uses `Math.random`/`Date.now`/`performance.now`; the doc note is dated by the operator, not a wall-clock call in `src/`.

## Out of scope
- **Any new rename.** Every old -> new string is owned by V/C/W/T and already applied. Z1 introduces NO new mapped name; a missing/off-map string is an owning-track defect to flag, not a Z1 edit.
- **Re-opening a track's edit.** Z1 does not re-rename `classes.ts`, `talents_classic.ts`, `warlock_pets.ts`, `items.ts`, the creature families, or the mob-mechanic auras; it confirms their aftermath.
- **Fixing the locale overlays.** The 20 non-English overlays are the maintainer's release-tier job; Z1 WRITES the reconciliation note, it does not edit an overlay.
- **The deferred tracks.** The brand rename ("World of ClaudeCraft" -> new product name) and the HUD trade-dress redesign are separate future tracks, noted in the handoff, NOT done here.
- **Any id change beyond the already-landed C1/C2 sweep, any wire/save/RL-action-space change, any golden re-baseline** (other than confirming the C1/C2 token swap), and **any loosening/`.skip` of a gate.**

## Verify
Run from the worktree root (`npm ci` first if `node_modules` is missing). The behavior-unchanged gates + `ip_scrub` + the full pre-merge suite are mandatory.
```
npx vitest run tests/parity                            # goldens byte-identical EXCEPT the enumerated C1/C2 coined-id token swaps
npx vitest run tests/ip_scrub.test.ts                  # FULLY GREEN: zero residual verbatim-WoW / Blizzard-coined name
npx vitest run tests/i18n_resolved_equivalence.test.ts # SHA gate green (after npm run i18n:hash -- --write)
npx vitest run tests/guide.test.ts                     # guide content fresh (after npm run wiki:content)
npx vitest run tests/localization_fixes.test.ts        # S3 (confirm W2/C1 emit-literal matchers still green post-merge)
npx vitest run tests/architecture.test.ts              # src/sim purity intact
npx tsc --noEmit
# FULL PRE-MERGE GATE (REQUIRED for this finale, mirrors CI):
npm test && npx tsc --noEmit && npm run build
```
THEN a manual playtest spot-check (see QA handoff): boot the client and confirm the renamed surfaces render the NEW original names in-game.

## Review
Run a COVERAGE reviewer agent on the full diff (a fresh subagent, not the implementer); prompt it for COVERAGE (report every correctness / requirement gap with confidence + severity), NOT filtering. Have it confirm: (a) all four tracks are merged and the regen was run ONCE cleanly (a second `i18n:gen` leaves the tree clean); (b) `tests/ip_scrub.test.ts` is fully green with zero residual and no denylist entry was loosened/`.skip`ped; (c) the `tests/parity` golden delta is EXACTLY the enumerated C1/C2 coined-id token swaps and nothing else; (d) the full pre-merge suite + build are green; (e) the release-locale-fill handoff note accurately describes the reword-staleness trap (existing-key reword does not flip rows to `pending`, `i18n:worklist` misses them, maintainer diffs `en.ts` merge-base vs HEAD and re-fills unchanged-value locales) and references `release-i18n-fill`; (f) 02-WORKING-MEMORY + the `ip-pivot-project` memory are updated to done with the deferred tracks noted. Mode is plain: address BLOCKING + SHOULD-FIX before declaring the job complete.

## Acceptance criteria
- [ ] All four tracks (V1, V2, C1, C2, W1, W2, T1) confirmed merged into `feature/ip-pivot` per 05-INTEGRATION; 02-WORKING-MEMORY status board all `merged`.
- [ ] Artifacts regenerated ONCE cleanly (`i18n:gen` -> `i18n:hash -- --write` -> `wiki:content`); a second `i18n:gen` leaves the tree clean; the delta committed with explicit paths.
- [ ] `tests/ip_scrub.test.ts` FULLY GREEN: every denylist / scanner-worklist entry cleared, zero residual verbatim-WoW / Blizzard-coined name in any player-visible field; scanner never loosened or `.skip`ped.
- [ ] `tests/i18n_resolved_equivalence.test.ts` green (post `i18n:hash -- --write`); `tests/guide.test.ts` fresh; `tests/localization_fixes.test.ts` (S3) green; `tests/architecture.test.ts` green; `npx tsc --noEmit` clean.
- [ ] `tests/parity` goldens byte-identical to the base EXCEPT the enumerated C1/C2 coined-id token swaps; the exact changed goldens listed and the delta confirmed to be only the renamed tokens.
- [ ] Full pre-merge gate green: `npm test && npx tsc --noEmit && npm run build`.
- [ ] Release-tier locale-fill handoff note written (`docs/refactor/ip-pivot-release-locale-fill.md` + a pointer in 02-WORKING-MEMORY): reword-staleness trap explained, `i18n:worklist` gap called out, the `en.ts` merge-base-vs-HEAD diff + re-fill procedure spelled out, `release-i18n-fill` referenced; NO locale overlay edited.
- [ ] 02-WORKING-MEMORY marked program-done (all slices `merged`, scanner green, artifact touch logged); maintainer memory `ip-pivot-project` updated to the completion state with the deferred tracks (brand rename, HUD trade dress, residual nine-class risk) noted.
- [ ] No new rename introduced; no id changed beyond the already-landed C1/C2 sweep; no gate loosened; no golden re-baselined.

## QA handoff
Feed these into 00-QA-TEMPLATE.md for the paired QA session on this finale:
- Re-run the behavior-unchanged gates + `ip_scrub` + the full pre-merge suite independently and confirm green; open `tests/ip_scrub.test.ts` and confirm it is fully green with zero residual and that no denylist entry was loosened, `.skip`ped, or removed to pass.
- Confirm the `tests/parity` golden delta is EXACTLY the enumerated C1/C2 coined-id token swaps (murloc/kobold family ids + the warlock demon-pet ids per the LOCKED NAME-MAP) and nothing else; no other golden moved; no golden was re-baselined.
- Read `docs/refactor/ip-pivot-release-locale-fill.md` and confirm it correctly states the reword-staleness trap (existing-key reword does not flip its 20 locale rows to `pending`, both CI tiers pass while overlays render the OLD name), that `npm run i18n:worklist` will NOT list these keys, and the maintainer procedure (diff `src/ui/i18n.resolved.generated/en.ts` merge-base vs HEAD, re-fill every locale whose value did not also change, rebaseline via `i18n:hash -- --write`), referencing `release-i18n-fill`; confirm NO `src/ui/i18n.locales/<lang>.ts` overlay was edited.
- Confirm 02-WORKING-MEMORY and the `ip-pivot-project` memory are updated to the done state with the deferred tracks noted.
- **Final manual playtest checklist** (boot the client, spot-check that the renamed surfaces render the NEW original names in-game, not the old WoW names):
  - Spell / ability tooltips on 2-3 classes (e.g. the renamed warrior + mage + paladin abilities) show the new names on the action bar and in the tooltip.
  - Talent trees: spec/tree tab names and node titles show the new names; a talent that mirrors an ability shows the ability's NEW paired name.
  - A murloc/kobold-family mob nameplate + its associated quest text render the new original creature vocabulary (no "murloc"/"kobold"/candle flavor).
  - A summoned warlock pet shows its new demon-pet display name (and its portrait/nameplate).
  - An item (e.g. the renamed Shadowmeld Tunic / Slimy Murloc Scale) shows the new name in the tooltip, bag, and vendor list.
